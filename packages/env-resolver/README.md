# @luckystack/env-resolver (reserved placeholder)

This directory is an intentionally-reserved package slot. It has **no implementation
yet** — no `package.json`, no `src/`, no `dist/` — and is therefore **not built and
not published** (it is absent from the `WAVES` in `scripts/buildPackages.mjs` /
`scripts/publishPackages.mjs`).

The name is parked so the future env-pointer resolution surface can land here without
a rename. Until then there is nothing to install.

If you arrived here debugging a build/publish skip: this is expected — the slot is
empty on purpose. Do not add a `package.json` until the implementation is ready.
