# Branch Log — feat/ai-docs-layers-2026-06-29

## 2026-06-29 — Seven new AI-context layers (lessons, examples, gates, code→ADR, budget, eval) + consumer propagation

**User prompt:** After a docs-system audit, implement all the high-value gaps beyond RAG: doc-coverage gate, doc-staleness lint, eval-harness, code→ADR (bidirectional) link, retrieval token-budgeting, snippet/example corpus (filled carefully with multiple opus agents + checked), and cross-session lessons memory. Implement for THIS codebase, update CLAUDE.md so the layers stay maintained + backfill on empty (decision-memory style), and ship them under the framework's AI-docs install step. One autonomous pass; gates warn-only/opt-in block; all 4 example patterns.

**What I did:**
- **Doc-coverage gate** — new `doc-coverage` rule in `scripts/lintInvariants.mjs`: a NEWLY-ADDED `_api`/`_sync` route needs a top-of-file summary + `@docs owner`; a new `page.tsx` needs a `//? intent:` line. Diff-scoped to added files (git `--diff-filter=A`), WARN by default, opt-in block via `luckystack.invariants.json`, `// luckystack-allow doc-coverage` escape hatch. 6 new selftest cases (16/16 pass).
- **Doc-staleness** — new `scripts/checkDocStaleness.mjs` (`ai:doc-staleness`, report-only): a hand-written doc opts in with `<!-- @covers <glob> -->`; reports when covered code is > `docs.stalenessThreshold` (new config, default 5) commits ahead. Wired `ARCHITECTURE_API.md`/`ARCHITECTURE_SYNC.md` as demos.
- **Lessons layer** — `scripts/generateLessonsIndex.mjs` (`ai:lessons`) → `docs/AI_LESSONS_INDEX.md` from `docs/lessons/NNNN-*.md`; `docs/LESSONS_PROTOCOL.md`; template seed; seeded real lesson `0001` (node:* leak into client bundle). MCP `find_lesson`/`get_lesson`.
- **Example corpus** — `scripts/generateExamplesIndex.mjs` (`ai:examples`) → `docs/AI_EXAMPLES_INDEX.md` from `docs/examples/`. 4 canonical examples (auth-api-route, sync-pair, trycatch, page-component-middleware) **written + adversarially verified by parallel opus agents** (background Workflow) against real signatures — they fixed real issues (`@docs owner` must be JSDoc not `//?`, `i18nNotify({key})` signature) and surfaced a real pre-existing bug (`common.500` missing from en.json — reported, not fixed). MCP `list_examples`/`get_example`.
- **Code→ADR reverse link** — `//? @adr NNNN` convention; extended `generateDecisionsIndex.mjs` to scan src for tags + emit a "Code governed by decisions" reverse map; MCP `decision_for_file`. Tagged `packages/mcp/src/index.ts` with `@adr 0016` (verified: 1 entry, no false positives).
- **Context budget** — `scripts/generateContextBudget.mjs` (`ai:context-budget`) → `docs/AI_CONTEXT_BUDGET.md`: measured artifact token sizes + 5 per-task retrieval profiles (new-route/new-page/security-audit/debug/doc-fix); overridable via `luckystack.ai.json context.profiles`.
- **Eval harness** — `eval/` (`ai:eval`): deterministic with/without scorer (`scoreEval.mjs`, selftest 4/4) + 4 golden scenarios + README. The measurement ADR 0003 requires before RAG.
- **CLAUDE.md** — Rule 12 pre-commit list extended; Rule 28 session-start list + new indexes; new "Lessons Protocol", "Canonical Example Corpus", and "Doc-coverage/staleness/code→ADR/context budget/eval" sections (incl. backfill-on-empty etiquette mirroring Decision Memory §8); Documentation Reference table rows.
- **Consumer propagation** — mirrored 5 generators + `eval/` + 4 examples + seeds byte-for-byte into `template/`; added `doc-coverage` gate to the slimmed `template/scripts/lintInvariants.mjs` (kept transport-parity framework-only); updated `template/package.json` ai:* scripts, both `luckystack.*.json` configs, the `AI_INDEX_HOOK` constant in `create-luckystack-app/src/index.ts` + root `.githooks/pre-commit`. New `aiScriptParity.test.ts` (13/13) guards the mirror against drift. MCP tools ship via the `@luckystack/mcp` npm package (no template change).
- **ADR 0016** — `ai-context-layers-before-rag` (relates 0001/0003/0007): records the warn-only-gates + RAG-stays-deferred-until-eval-measures decision.
- **AI_BOOST_OVERVIEW.md** + `packages/mcp/CLAUDE.md` updated with the new surfaces/tools.

**Verification:** MCP `tsc --noEmit` clean, eslint clean, MCP tests 5/5, parity test 13/13, lint selftest 16/16, eval selftest 4/4, full `ai:lint` (transport-parity) clean, all generators run green, staleness + reverse-map produce correct real output. NOT committed (awaiting user review).

**Files touched:** `scripts/{lintInvariants,checkDocStaleness,generateLessonsIndex,generateExamplesIndex,generateContextBudget,generateDecisionsIndex}.mjs`, `eval/**`, `docs/{lessons,examples}/**`, `docs/{LESSONS_PROTOCOL,AI_LESSONS_INDEX,AI_EXAMPLES_INDEX,AI_CONTEXT_BUDGET,AI_DECISIONS_INDEX,AI_BOOST_OVERVIEW,ARCHITECTURE_API,ARCHITECTURE_SYNC}.md`, `docs/decisions/0016-*.md`, `CLAUDE.md`, `luckystack.{ai,invariants}.json`, `packages/mcp/src/index.ts`, `packages/mcp/CLAUDE.md`, `packages/create-luckystack-app/{src/index.ts,src/aiScriptParity.test.ts,template/**}`, `.githooks/pre-commit`, `package.json`.

**Notes / follow-ups for user:** real bug surfaced by corpus verifier — `common.500` missing from `src/_locales/en.json` (route `updatePreferences_v1.ts` returns it; renders raw key). Not fixed (out of scope). Next: run the eval harness with/without for a real artifact-value baseline; wire more `@covers` markers + `//? @adr` tags as you go.
