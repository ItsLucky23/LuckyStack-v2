# Reconciled area report: login (wave-2 → FINAL)

Branch `chore/package-split-prep`, HEAD 302cbf1 + ~248 uncommitted fixes applied this week.

Owned paths: `packages/login/src/**`, `packages/cli/assets/login/src/**`, `luckystack/login/**`.

Wave-2 inputs reconciled: `codebase-scan--wave2-14-06--1`, `--3`, `--4` (run-2 ignored — input digest only) + top-level SECURITY/BUGS/CODE_QUALITY/FEATURES.
Wave-1 baseline: `codebase-scan-14-06-MERGED/per-area/login.md` (40 findings) + `LOW_ANALYSIS/login.md`.

**Verdict.** Every wave-2 "fixed" / "now-fixed" claim was re-verified against the CURRENT tree (file:line cited). The login surface is genuinely hardened: ~18 prior MERGED items are confirmed FIXED in code. No critical. The most serious STILL-OPEN items are MERGED-8 (full password policy on the login path, now AMPLIFIED by the lockout into LOGIN-03) and two genuine **NEW regressions introduced by this week's refactor**: `register.ts` module-load config capture (LOGIN-01) and `logout` raw-`srem` adapter bypass (LOGIN-02). The shipped CLI template still has the GDPR self-delete break (UI omits the password the server requires) and an email-change with no current-password re-auth.

---

## FIXED (verified in current tree)

| # | Finding | Severity | Status | Wave1 | Wave2 | Location | Note |
|---|---------|----------|--------|-------|-------|----------|------|
| F1 | Unverified OAuth email → account-takeover under `unified`; built-ins shipped `emailVerifiedKey` OFF | high | FIXED | 4/6 | 3/3 | `login.ts:570`,`:846`(guard); `oauthProviders.ts:183`,`:258` | Verified: `findOrCreateOAuthUser` fail-closed cross-provider link guard (`if isCrossProviderLink && !emailVerified return null`); google `emailVerifiedKey:'email_verified'`+v3 userinfo, discord `'verified'`. |
| F3 | `extraSessionFields` got stripped `{email,name,avatar}` not raw provider claims | high | FIXED | 1/6 | 3/3 | `login.ts:1012` + `OAuthProfile.rawUserData` `:698`,`:799` | Verified: `extraSessionFields({ userData: profile.rawUserData, ... })`. |
| F6 | Session sanitizer fails OPEN — leaks raw record on throw | medium | FIXED | 1/6 | 3/3 | `session.ts:130-138` | Verified: on `sanitizeErr` falls back to password-stripped projection (`const {password:_p, ...safe}=data`), not raw. |
| F10 | `extraSessionFields` merge clobbers framework keys (id/token/csrf/password/admin) | medium | FIXED | 1/6 | 2/3 | `login.ts:1022` | Verified: destructures `{id,token,csrfToken,password,...safeExtra}` before merge + warns. |
| F11 | Microsoft `getEmail` falls back to UPN as account email | medium | FIXED | 1/6 | 3/3 | `oauthProviders.ts:404`,`:339` | Verified: UPN fallback gated behind `allowUpnFallback`, default `false`. |
| F12 | Backslash-relative path treated same-origin (open redirect) | medium | FIXED | 2/6 | 3/3 | `login.ts:570` | Verified: `if (url.includes('\\')) return false;` before same-origin shortcut. |
| F13 | `revokeUserSessions` over-counts attempted (not actual) revokes | medium | FIXED | 2/6 | 3/3 | `session.ts:464-465` | Verified: `results.filter(Boolean).length`. |
| F17 | Password reset / email-change consumer routes don't revoke sessions | medium | FIXED | 1/6 | 1/3 | `confirmEmailChange_v1.ts:63`; (confirmReset asset) | Verified: `revokeUserSessions(userId)` after the write. (Core `updatePasswordHash` still doesn't revoke — but the SHIPPED route does; see F17-residual below.) |
| F21 | OAuth nonce `!==` not constant-time | low(quality) | FIXED | 4/6 | 3/3 | `login.ts:213-216` | Verified: `timingSafeEqual` on SHA-256 digests + length guard; misleading comment gone. |
| F23 | Provider `getAvatar` interpolates id without `encodeURIComponent` | low | FIXED | 1/6 | 3/3 | `oauthProviders.ts:270`(discord),`:367`(microsoft) | Verified: both `encodeURIComponent`-wrapped. |
| F24 | Google legacy `oauth2/v1/userinfo` (deprecated) | low(quality) | FIXED | 1/6 | 2/3 | `oauthProviders.ts:173`,`:183` | Verified: v3 userinfo + `email_verified`. |
| F30 | Empty `data.id` mints untracked session | low | FIXED | 1/6 | 3/3 | `session.ts:55-58` | Verified: `if (!data.id) return { ok:false, ... }`. |
| F37 | Duplicated lazy `@luckystack/email` import block | low(refactor) | FIXED | 3/6 | 1/3 | `emailModuleLoader.ts` (`loadEmailModule`) | Verified: extracted; both callers use it (catch-vs-throw behavior preserved per LOW_ANALYSIS Bucket-3). |
| F38 | `asOAuthUserData` not barrel-exported | low(quality) | FIXED | 5/6 | 2/3 | `index.ts:95`,`:107` | Verified: `asOAuthUserData` + `OAuthUserData` exported. |
| F9-sub | Active-set TTL drift on sliding read (sub-issue of F9) | — | FIXED | (r3) | 3/3 | `session.ts:295-297` | Verified: `touchActive` refreshes active-set TTL in lock-step. NOTE: the broader F9 sliding-amplification remains OPEN (below). |
| A1 | Malformed i18n key `common/.404` in `LoginForm.tsx` | low | FIXED | (LOW) | — | `LoginForm.tsx:123` | Per LOW_ANALYSIS: corrected to `common.404`. |
| A18 | Settings sessions UI token/handle contract mismatch | medium | FIXED | (LOW) | — | `settings/page.tsx` (ActiveSession.handle) | Per LOW_ANALYSIS Safe-sweep: renamed `token`→`handle`; verified `listSessions_v1`/`revokeSession_v1` both use `handle`. |
| F36-sub | `emailChange` used raw try/catch not `tryCatchSync` | — | FIXED | (r) | 2/3 | `emailChange.ts:53` | Verified: now `tryCatchSync`. (Shared `consumeSingleUseToken` extract still OPEN — F36 below.) |

---

## OPEN (real & still present)

| # | Finding | Severity | Status | Wave1 | Wave2 | Location | Note/Recommendation |
|---|---------|----------|--------|-------|-------|----------|------|
| LOGIN-01 | **NEW REGRESSION** — `register.ts` captures `getProjectConfig()` + `env()` at MODULE LOAD then `registerOAuthProviders()` at import time; re-introduces the stale-config footgun every other file deliberately avoids | medium | OPEN (new) | new | 1/3 | `register.ts:54-58,65,110` | VERIFIED: top-level `const projectConfig = getProjectConfig()` (:54), `callbackBase` from it (:58), `if (projectConfig.auth.credentials)` (:65), `registerOAuthProviders(providers)` at :110 runs at import. File header itself says a consumer overlay "runs AFTER this import" → if config is registered/overridden after this import, providers bake a stale/empty `callbackBase` or omit credentials. Fix: defer all config/env reads into provider factories or a first-request assembler (mirror `login.ts:528` `isDevMode`/`passwordReset.ts` call-time pattern). |
| LOGIN-02 | **NEW REGRESSION** — `logout` bypasses `SessionAdapter` with raw `redisClient.srem(activeUsersKeyFor(...))`; breaks the storage abstraction for non-Redis adapters | medium | OPEN (new) | new | 1/3 | `logout.ts:34` | VERIFIED: `deleteSession`→`adapter.untrackActive` already cleans the active-set (`session.ts` path); `logout` then does a redundant raw `srem`. On a custom (Dynamo/Postgres/JWT) adapter this hits a Redis the adapter doesn't use → on the `skipSessionDelete` branch the active-set is NEVER cleaned, leaking dead tokens (sign-out-everywhere gap). Fix: drop the raw `srem`; for the skip path call `getSessionAdapter().untrackActive(userId, token)`. |
| F8 / LOGIN-03 | Full password-policy validation runs on the LOGIN path (leaks exact policy to unauth probers; locks out pre-policy-tightening passwords) — AND policy-failure reasons trip the per-account LOCKOUT counter | medium→high | OPEN (known, amplified) | 1/6 | 3/3 | `login.ts:316`(`validateCredentialsShape`→`validatePassword`) called `:532` BEFORE login/register split `:535`; `authLockout.ts:84-89` | VERIFIED both legs: `validateCredentialsShape` runs `validatePassword` for the login branch; `NON_COUNTING_REASONS` lists only `accountLocked/empty/emailCharacterLimit/invalidEmailFormat` — NO `login.password*`, so policy-failed logins increment lockout. Combined: an attacker locks any victim by POSTing policy-violating passwords for their email, no real-password guess needed. Fix: cheap shape-checks only on login (empty/length cap), full policy on register; collapse policy fails to `login.wrongPassword`; add `login.password*` to `NON_COUNTING_REASONS`. |
| F4 | TOCTOU on `rejectNew` concurrent-session cap (count then non-atomic persist) | medium | OPEN (known) | 4/6 | 3/3 | `session.ts:92-94`(check)→`:141`/`:152`(persist) | VERIFIED: `listActive` count at :92, `setRaw`+`trackActive` ~50 lines later, no lock/Lua/INCR. Two concurrent logins both pass `:94`. `revokeOld` branch (`:188`) shares the race. Fix: atomic reserve (Lua/SETNX lock) or document soft-cap. |
| LOGIN-03-emailchange | Email-change initiated with NO current-password re-auth (session-hijack → account takeover via email rotation; victim locked out) | medium | OPEN (new) | new | 1/3 | `requestEmailChange_v1.ts:26-65` | VERIFIED: route requires only `auth.login:true`, never re-verifies password (contrast `deleteAccount_v1.ts:30`, changePassword). Confirm link goes to NEW address → legit owner never sees it; on confirm all sessions revoked → real owner locked out. Fix: require+verify current password for credentials accounts (mirror deleteAccount), add password field to the UI; optionally notify OLD address. |
| LOGIN-01-gdpr | Delete-account UI never sends the password the server requires → credentials users CANNOT self-delete (GDPR right-to-erasure broken) | high | OPEN (known) | 4/6 | 1/3 | `settings/page.tsx:284`; `deleteAccount_v1.ts:29-33` | VERIFIED: `handleDeleteAccount` sends only `data:{confirmation:'DELETE'}` (page.tsx:284); server requires `data.password` verify for any account with a password (deleteAccount_v1.ts:30) → always fails for credentials users. OAuth-only delete fine. Fails-closed (over-restrictive, not a security hole) → HIGH not CRIT. Fix: prompt current password, send `data:{confirmation:'DELETE',password}` (route already accepts `password?`). |
| F9 | `getSession` extends TTL on EVERY read (sliding-expiration amplification; side-effects in a getter; no read-without-refresh flag) | medium | OPEN (known) | 1/6 | 2/3 | `session.ts:264-317` | VERIFIED: unconditional `adapter.expire`+`touchActive` on every read; only a `preSessionRefresh` stop-hook disables; no `{refresh?}` param. A stolen token survives as long as anything (presence/sync/poll) touches it. Fix: `getSession(token,{refresh=true})` (false on internal reads) or a dedicated once-per-request `touchSession`. |
| M9 / LOGIN-05 | Session token stored in PLAINTEXT inside the Redis session VALUE (redundant with the key) and surfaced by `getAllSessions()` | medium | OPEN (new) | new | 1/3 | `login.ts:396,490,998,1048,1068`(token set on record); `session.ts:141`(persist),`:308`,`:414-436`(getAllSessions) | VERIFIED: `token:newToken` set on the session record before `saveSession`; `JSON.stringify(persisted)` persists it; `getAllSessions` returns parsed sessions WITH `token`. Shipped `listSessions_v1` asset uses `sessionHandle()` (raw token never leaves server) so the UI surface is safe, but the `getAllSessions` PRIMITIVE + any admin/log of a SessionLayout carries a live credential. Fix: strip `token` before `setRaw` (recoverable from the key) or at least in `getAllSessions`. |
| M7 | Redis session write is non-atomic SET-then-EXPIRE → a crash/EXPIRE-fail between leaves an immortal (TTL-less) session | medium | OPEN (new) | new | 1/3 | `sessionAdapter.ts:90-94` (also `trackActive` `:111-115`) | VERIFIED: `setRaw` does `await redis.set(key,value)` then SEPARATE `await redis.expire(key,ttl)`. A stolen token in a TTL-less key survives policy expiry. Fix: single atomic `redis.set(key,value,'EX',ttl)` — same atomicity OAuth-state/pwreset already use. |
| F22 | Dummy bcrypt hash hard-pinned to cost 10 → reopens enumeration timing channel when `bcryptRounds` raised >10 | medium | OPEN (known) | 3/6 | 2/3 | `login.ts:61` | VERIFIED: literal `$2b$10$...`. NOTE: the inline comment (`:57-60`) argues the early-return-with-zero-bcrypt-work is the dominant signal and a small cost mismatch "does not reopen the channel meaningfully" — a documented tradeoff, so this is borderline DEFERRED-DECISION, but the timing delta is real on `bcryptRounds:12+`. Fix: derive dummy hash lazily at configured rounds (cache per-rounds). |
| run4-7 | `confirmEmailChange` collision re-check is non-atomic AND scoped to `provider:'credentials'` (misses OAuth-row collisions under `unified`) | low | OPEN (new) | new | 1/3 | `confirmEmailChange_v1.ts:50-59` | VERIFIED: `findFirst({email:newEmail, provider:'credentials', NOT:{id}})` then separate `update` (:59), non-atomic; provider scope drops OAuth rows. Fix: rely on DB unique constraint (treat unique-violation as collision); drop `provider:'credentials'` scope under `unified`. |
| run4-6 | `signOutEverywhere` revokes the caller's OWN current session (no `exceptToken`) → logs the user out of the device they are using | low | OPEN (new) | new | 1/3 | `signOutEverywhere_v1.ts:20` | VERIFIED: `revokeUserSessions(user.id)` with no `exceptToken` (contrast changePassword which passes `user.token`). Not a security hole (revokes too much). Fix: decide semantics — pass `user.token` to keep current device, or document the full-revoke + graceful re-auth. |
| run4-8 | `deleteAccount` wipes sessions + raw `redis.del(activeUsersKeyFor)` BEFORE the `adapter.delete` that can throw; raw del bypasses the adapter | low | OPEN (new) | new | 1/3 | `deleteAccount_v1.ts:59-62` | VERIFIED: `revokeUserSessions` + `redis.del(activeUsersKeyFor)` at :59-60, then `adapter.delete` at :62. If delete throws, user is signed out of a still-existing account; raw `redis.del` leaks the active-set on a non-Redis adapter. Guards `if(!adapter.delete)` but not a runtime throw. Fix: delete first (or wrap), route active-set cleanup through the adapter. |
| F19 | Password-reset / email-change tokens stored UN-hashed in Redis (raw token as key) while OAuth state hashes its nonce | low | OPEN (known) | 1/6 | 3/3 | `passwordReset.ts:34`; `emailChange.ts:32` | VERIFIED: raw 64-char token is the key. (LOW_ANALYSIS A7 DEFERRED it — coordinated issue+consume+callsites change, recommend a shared `oneTimeToken` core primitive.) Fix: store `sha256(token)` as key, hash on lookup. |
| F14 | Unbounded `listAll` SCAN fan-out (O(all-sessions) SCAN + N×GET in memory) | low→medium | OPEN (known) | 2/6 | 1/3 | `sessionAdapter.ts:134-163` | VERIFIED: still O(all-sessions), no cursor. Fix: paginated cursor; document dev/admin-only. |
| F15 | Register path skips the per-account brute-force lockout | medium | OPEN (known) | 1/6 | 1/3 | `login.ts:321`(`registerWithCredentials` — no `isAccountLocked`); `:436`(login checks) | VERIFIED: register branch has no lockout check; unbounded per-account `resolveUserByEmail` via register. Fix: apply the lockout check on the register email-exists lookup. |
| F39 / run4-5 | Legacy `sessionKeyFor`/`activeUsersKeyFor` exports drive consumer routes to bypass the `SessionAdapter` (raw Redis) | low | OPEN (known) | 2/6 | 2/3 | `session.ts:471-472`; used by `listSessions_v1.ts:32,38`, `revokeSession_v1`, `deleteAccount_v1.ts:60`, `logout.ts:34` | VERIFIED: still exported and actively used for raw `redis.smembers/get/del`. A custom adapter silently breaks session-list/revoke/delete-cleanup. Fix: adapter-level primitives (`listActiveWithMeta`, handle resolver) so assets go through `getSessionAdapter()`; or document the Redis-adapter assumption. |
| F2 | Raw session token logged via the non-redacting default logger on error/debug paths | high(if core logger doesn't redact) | OPEN (known, partial) | 1/6 | 2/3 | `session.ts:56,133,313`(full `{token}`); trace paths use `token.slice(0,8)` | VERIFIED: `saveSession`/`getSession` error contexts pass full `{ token }` (`:56`,`:133`,`:313`); `logout`/`deleteSession` tracing use `slice(0,8)`. Whether this leaks depends on the CORE default logger running `sanitizeForLog` — cross-area (core) concern. Fix (framework-wide): route `getLogger()` context through `sanitizeForLog`, or `slice(0,8)` consistently here. |
| F16 | No invalidation of prior reset/email-change tokens on issue (multiple live redeemable links until TTL) | medium | OPEN (known) | 1/6 | 0/3 | `passwordReset.ts:31-36`; `emailChange.ts:28-34` | Wave-2 didn't re-flag but UNCHANGED in current code. Fix: track active token per user, delete prior on new issue, or document the window. |
| F18 | Avatar resolution `existsSync(${id}.webp)` sync FS on the login hot path (blocking; couples session to local disk) | medium | OPEN (known) | 1/6 | 0/3 | `login.ts:496`,`:855` | VERIFIED still `existsSync`. Fix: `fs.promises.stat` or an avatar adapter. |
| F36 | Duplicated single-use-token MULTI get+del across `consumeOAuthState`/`consumePasswordResetToken`/`consumeEmailChangeToken` (subtly different validation) | medium(refactor) | OPEN (known) | 5/6 | 1/3 | `login.ts:188`; `passwordReset.ts:46`; `emailChange.ts:44` | VERIFIED: three near-identical blocks remain (raw try/catch sub-fix landed). Shared extract belongs in core (LOW_ANALYSIS A9 DEFERRED). |
| F33/34/35 | God-file `login.ts` (~1077 lines) + god-functions `saveSession` (~213) / `loginCallback` (~138); duplicated `allowMultiple?'multiple'` BC shim at `session.ts:85`,`:173` | medium(refactor) | OPEN (known, partial) | 6/6 | 3/3 | `login.ts` whole; `session.ts:43-256`,`:85`,`:173` | VERIFIED: split started (register/redirect/accountStrategy/authLockout/emailModuleLoader extracted) but login.ts GREW to ~1077; saveSession/loginCallback still large; BC shim duplicated. Fix: continue the extraction; dedupe the shim. |
| LOGIN-04 | Unified-account resolution non-deterministic on duplicate-email rows (`findFirst` no orderBy) — also makes the cross-provider link SECURITY guard non-deterministic | medium | OPEN (new) | new | 1/3 | `userAdapter.ts:98-99` | VERIFIED via run-3; `findByEmailAnyProvider` `findFirst({where:{email}})` no ordering. Under a project enabling `unified` over existing duplicate rows, the same login resolves to different User rows across requests, and the `findResponse.provider`-based link guard flips non-deterministically. Fix: deterministic tiebreak (`orderBy:{createdAt:'asc'}`) + detect/warn on multiple matches; document `unified` needs unique email. |
| F7 | Register-time TOCTOU duplicate accounts (`resolveUserByEmail`→`create` not atomic; no `email @unique` by design) | medium | OPEN (known) | 1/6 | 1/3 | `login.ts:347`,`:364` | VERIFIED: non-atomic, no unique-violation re-resolve. Fix: require `email @unique` for `unified`; treat unique-violation as re-resolve-and-link. |
| run3-LOGIN-06 | `loginCallback` verifies no OIDC `id_token` nonce/code-binding beyond single-use state for non-PKCE providers | low | OPEN (new) | new | 1/3 | `login.ts` callback (userinfo path) | Real but marginal — cookie-nonce binding already closes login-CSRF; residual is userinfo-trust vs signed-id_token-trust. Fix (optional hardening): verify id_token signature+nonce for OIDC providers; document the trust model. |
| F20 | bcrypt 72-byte silent truncation documented but not enforced (default `maxLength` 191) | low(quality) | OPEN (known) | 5/6 | 0/3 | `passwordPolicy.ts:30-37` | UNCHANGED. Fix: default `maxLength` 72 or pre-hash (SHA-256→base64). |
| LOGIN-04(run1) | Microsoft `getAvatar` inlines unbounded provider photo as base64 data-URL into the session record (bloats Redis + socket) | low | OPEN (new) | new | 1/3 | `oauthProviders.ts:371-373` | VERIFIED: `data:${ct};base64,${buf}` no size cap; lives on the session JSON. Fix: cap bytes (e.g. 256KB) or store a reference. |
| LOGIN-05(run1) | All three `consume*` don't assert the MULTI `del` leg ran → single-use defeated if `DEL` silently fails | low | OPEN (new) | new | 1/3 | `login.ts:188-219`; `passwordReset.ts:46-52`; `emailChange.ts:44-50` | VERIFIED: read `txResult[0]` (GET), never `[1]` (DEL). Theoretical replay window. Folds into the F36 `consumeSingleUseToken` extract (assert both legs). |
| LOGIN-06(run1)/L7 | `sendPasswordResetEmail` logs the raw email + `found` at `info` on every request (PII + enumeration oracle in logs; response is anti-enum) | low | OPEN (known) | 2/6 | 2/3 | `forgotPassword.ts:31,55,64` | VERIFIED: `getLogger().info(...,{email,found})`. Defeats anti-enum for log readers. Fix: mask/hash email, drop `found`, gate detail behind `logging.devLogs`. |
| F25 | Raw attacker email carried verbatim to `loginFailed` subscribers + used as lockout Redis key (PII at rest) | low(quality) | OPEN (known/deferred) | 2/6 | 0/3 | `login.ts:255-263,528-529`; `authLockout.ts` | LOW_ANALYSIS A16 DEFERRED — changing the `loginFailed` payload is a hook-contract change SIEM subscribers depend on. Document email-is-untrusted-PII; optionally hash the lockout key. |

---

## DEFERRED-DECISION (real but intentionally deferred pending policy/contract/ADR)

| # | Finding | Severity | Status | Wave1 | Wave2 | Location | Note |
|---|---------|----------|--------|-------|-------|----------|------|
| A12 | `requestEmailChange` returns `auth.emailTaken` — authenticated account-enumeration oracle | low | DEFERRED | (LOW) | 2/3 | `requestEmailChange_v1.ts:46` | LOW_ANALYSIS A12 WONT-FIX-by-design: authenticated, 5/window rate-limited, the inline comment documents the tradeoff (constant-time confirm leaks the same bit); collapsing it removes useful "email taken" feedback. Policy decision. |
| F5 | Account-lockout as a remote DoS lever (email-only keyed, fires on non-existent accounts; distinct `accountLocked` reason re-opens enum channel) | medium | DEFERRED | 3/6 | 1/3 | `login.ts:432-437`; `authLockout.ts:29,104-108` | Opt-in (default off). Real tradeoff at the `rateLimiting.auth` seam — IP+account composite key / progressive delay vs hard refuse is a policy/ADR call. (LOGIN-03 above is the password-policy AMPLIFICATION of this, which is a clear bug regardless.) |
| F17-residual | Core `updatePasswordHash` doesn't revoke sessions (shipped routes do) | medium | DEFERRED | 1/6 | 0/3 | `passwordReset.ts:63-72` | Documented as the consumer route's responsibility; shipped confirmReset/confirmEmailChange DO revoke. Whether to move revoke INTO the primitive (a `completePasswordReset` orchestrator) is a feature/contract decision. |
| A19 | `common.500` errorCode referenced but absent from template locale JSON | low | DEFERRED | (LOW) | — | `listSessions/revokeSession/signOutEverywhere/updatePreferences _v1.ts` | LOW_ANALYSIS A19: the locale JSON lives in `create-luckystack-app/**` — OUTSIDE login's owned paths. Owner: create-luckystack-app cluster (add `common.500`). Rewriting keys to `common.404` is semantically wrong. |

---

## FALSE-POSITIVE / refuted-for-current-code

| # | Finding | Severity | Status | Wave1 | Wave2 | Location | Note |
|---|---------|----------|--------|-------|-------|----------|------|
| run3-LOGIN-08 | "unified links UNVERIFIED OAuth email → takeover" claimed as still-HIGH | — | FALSE-POSITIVE (now FIXED) | 4/6 | — | `login.ts:832-852` | Run-3 itself reclassifies this prior-consensus HIGH to a verified refutation: the fail-closed cross-provider guard is present. Recorded as FIXED (=F1) not open. |
| run3-LOGIN-09 | "stale ActiveSession interface (token vs handle) breaks build" | — | FALSE-POSITIVE (RESOLVED) | (prior HIGH) | — | `listSessions_v1.ts`,`revokeSession_v1.ts`,`settings/page.tsx:36-40` | Run-3 verified: all three consistently use `{handle, expiresInSeconds, isCurrent}`; raw token never leaves the server. (Aligned by LOW_ANALYSIS A18.) Refuted for current code. |
| run4-31 | "Template `updateUser_v1` ships without theme/language validation" framed as security | low | FALSE-POSITIVE (security framing) | confirms-w1 | — | `create-luckystack-app/template/.../updateUser_v1.ts` | Run-4 own verifier marked real:false — Prisma enum validation blocks the named exploit (arbitrary theme/language). A low consistency/hardening gap remains (asset vs template drift) but NOT the asserted security bug; and it's in the create-luckystack-app cluster, not login. |
| MERGED sqlite-schema (n/a here) | — | — | — | — | — | (Listed in task as the canonical wave-1 false positive; not a login finding.) |

---

## Notable / cross-area handoffs

- **F2 (token in logs)** and **A19 (`common.500`)** depend on CORE / create-luckystack-app respectively — flagged here, owned elsewhere.
- **Feature ideas** recurring across all three wave-2 runs: a hashed-at-rest single-use `oneTimeToken` core primitive (collapses F19 + F36 + LOGIN-05), an atomic `SessionAdapter.reserveSlot(userId,cap)` (closes F4 by construction), a `call-time-config` ai:lint invariant (would have caught LOGIN-01), and `revokeUserSessions` inside `updatePasswordHash` (closes F17-residual at the primitive).
- **NEW criticals/highs:** none NEW-critical. NEW-high: none confirmed (LOGIN-03's amplification raises F8 toward high but it's the known F8 made worse, not a new high). The two NEW regressions (LOGIN-01, LOGIN-02) are medium and are the items most worth fixing before release because they were introduced THIS week.
