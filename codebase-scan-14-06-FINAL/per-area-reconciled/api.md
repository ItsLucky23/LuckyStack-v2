# Reconciled area report: api

Working tree: branch `chore/package-split-prep`, HEAD `302cbf1` + ~248 uncommitted changes (this week's fixes).
Wave-2 sources: `codebase-scan--wave2-14-06--1/per-area/api.md`, `--3/per-area/api.md`, `--4/per-area/api.md` (run 2 was input-digest only, ignored).
Wave-1 baseline: `codebase-scan-14-06-MERGED/per-area/api.md` (6-scan merge, 28 items). No `LOW_ANALYSIS/api.md` exists.

Every kept finding was re-verified by OPENING the cited file in the CURRENT tree. The wave-2 reports' fixed/present claims were NOT trusted.

## Verdict headline

- **FIXED** since the wave-1 baseline: validateType fail-open + unbounded recursion (now in core, fail-closed + depth-guard), the `responseIndex:0` fragile guard, the socket array-payload guard, the socket handler having zero tests, and the prior PII claim about dev logs (now sharper — see OPEN API-TOKEN-KEY).
- **DOMINANT NEW THEME (HIGH)**: `packages/api/CLAUDE.md` is a **shadow API** — it documents an `apiAuthRejected` hook, a `preSocketMessage` hook, an `applyGlobalIpRateLimit` helper, and the `rateLimiting.identity` + `rateLimiting.skipLoopbackInDev` config knobs that **do not exist in the package's `src`** (verified: grep of `packages/api/src` for all five returns ZERO matches). An AI reading the package contract wires against no-ops.
- **NEW HIGH (security)**: HTTP loopback rate-limit skip is gated on spoofable `NODE_ENV` + `requesterIp.startsWith('127.')`, ignores the documented `skipLoopbackInDev` flag + `isLoopbackIp` (`handleHttpApiRequest.ts:359-362`).
- **NEW HIGH (security)**: per-route rate-limit bucket is keyed on the **RAW SESSION TOKEN** (`handleApiRequest.ts:141-143`, `handleHttpApiRequest.ts:318-320`) — directly contradicting the CLAUDE.md "never the raw token" invariant; token lands in Redis keys + dev logs.
- All the wave-1 structural/honesty OPENs (method-gate socket gap, logout hook-bypass, failed-auth no-bucket, twin drift, HTTP god-function) are STILL OPEN.

---

## FIXED

| # | Finding | Severity | Wave1 | Wave2 | Location | Note / Recommendation |
|---|---|---|---|---|---|---|
| F1 | `validateType` fails OPEN on unrecognized/terminal type shapes (type-confusion / NoSQL-operator smuggling) | medium (was) | 4/6 (#3) | 3/3 (r1 out-of-area, r4 fixed, r1-run flagged then refuted) | `packages/core/src/runtimeTypeValidation.ts:363-373` | FIXED in core: terminal branch now `{status:'error', '... unvalidatable type ... model it explicitly or set validation:relaxed'}` (:373); unresolved utility/alias error (:357,:360); array element fail-closed (:233). Lives in core, not api — api faithfully relays. Wave-2 run-1's "still fail-open" claim is a stale read; run-4 verifier confirms fixed. |
| F2 | Unbounded recursion in `validateType` (cheap DoS via attacker-nested payload) | medium (was) | 1/6 (#11) | 1/3 (r4 fixed) | `packages/core/src/runtimeTypeValidation.ts:184-188` | FIXED: `MAX_VALIDATION_DEPTH=64` guard returns error past the cap. Core, not api. |
| F3 | `responseIndex` guard brittle double-negative; no `0`-case test | low | 5/6 (#14) | 3/3 (r1,r4 now-fixed) | `handleApiRequest.ts:58`; `transportParity.test.ts:188` | FIXED: rewritten to `if (typeof responseIndex !== 'number')` with explanatory comment; parity test drives `responseIndex: 0` and asserts success. |
| F4 | Socket array-payload accepted where handler expects object | medium | — | 1/3 (r1 API-06, socket half) | `handleApiRequest.ts:69` | FIXED on socket: `validateApiMessage` rejects `Array.isArray(data)`. (HTTP half is still OPEN — see API-HTTP-ARRAY below.) |
| F5 | Socket handler had ZERO unit tests | medium | 1/6 (#24) | 3/3 (now-fixed) | `transportParity.test.ts` | FIXED (partly): `transportParity.test.ts` now drives the socket handler through auth/404/400/validation/stop-signal/hook-order parity cases. Socket-ONLY paths (logout, abort/cleanup, flushPressure) remain untested — residual is the LOW API-SOCKET-TESTS below. |
| F6 | Prior baseline claim "rate-limit keys never contain the raw token" | (verification) | r5 provenance | refuted by r1+r4 | `handleApiRequest.ts:141-143` | The baseline's positive is REFUTED in current code — promoted to the OPEN API-TOKEN-KEY finding below. Listed here only to record the baseline correction. |

---

## OPEN

| # | Finding | Severity | Status | Wave1 | Wave2 | Location | Note / Recommendation |
|---|---|---|---|---|---|---|---|
| O1 | **Shadow-API: `packages/api/CLAUDE.md` documents a hook + helper + config surface that does NOT exist in code** — `apiAuthRejected` hook, `preSocketMessage` hook, `applyGlobalIpRateLimit` helper, `rateLimiting.identity`, `rateLimiting.skipLoopbackInDev` are all documented as wired but grep of `packages/api/src` returns ZERO matches. | high | open (new) | — | 3/3 (r1 API-01, r3 API-03/04, r4 #3) | `packages/api/CLAUDE.md` ("Hooks consumed" / "Internal pipeline helpers" / "Config keys") vs `packages/api/src/*` | VERIFIED: grep for `apiAuthRejected\|preSocketMessage\|applyGlobalIpRateLimit\|skipLoopbackInDev\|rateLimiting.identity` over `packages/api/src` = 0 hits. An AI building abuse-detection wires `apiAuthRejected` (never fires); a consumer sets `skipLoopbackInDev`/`identity` (both dead). Fix: either implement the surface on both transports OR strike the phantom entries from CLAUDE.md now; add an `ai:lint` rule that every hook/config named in a package CLAUDE.md is actually dispatched/read in that package. |
| O2 | **HTTP loopback rate-limit skip is spoofable + ignores `skipLoopbackInDev`/`isLoopbackIp`** | high (r1) / medium (r3 downgrade) | open (new) | — | 3/3 (r1 API-04, r3 API-01, r4 #2) | `handleHttpApiRequest.ts:359-362` | VERIFIED: `requesterIsLoopback = process.env.NODE_ENV !== 'production' && (requesterIp === '127.0.0.1' \|\| ... \|\| requesterIp.startsWith('127.'))`. Reads neither `getProjectConfig().rateLimiting.skipLoopbackInDev` (default `false`, `projectConfig.ts:672`) nor `isLoopbackIp`. In any non-prod deploy a 127.x-looking IP defeats the cross-route `:api:all` abuse cap; remote-spoof path requires `trustProxy:true` + leaky XFF (per-route limits still apply). Always-true half: the documented config knob is a no-op. **Net severity: HIGH** (config-contract break is unconditional). Fix: gate on `skipLoopbackInDev && isLoopbackIp(resolvedIp)` using the `resolveClientIp`-derived IP. |
| O3 | **Per-route rate-limit bucket keyed on the RAW SESSION TOKEN** — contradicts CLAUDE.md "keyed on validated `user.id` ... never the raw token"; token lands in Redis keys + dev logs | high (r4) / medium (r1 API-05) | open (known, sharper) | 2/6 (#16, framed token-safe) | 3/3 (r1 API-05, r3 API-05, r4 #1) | `handleApiRequest.ts:141-143,162`; `handleHttpApiRequest.ts:318-320` | VERIFIED: `requesterIdentity = token ?? resolvedIp; keyPrefix = token ? 'token' : 'ip'; key = \`${keyPrefix}:${requesterIdentity}:api:${name}\``. Authenticated callers embed the raw bearer token verbatim in the rate-limit key (→ Redis key names) and in the dev warn log `{ key: rateLimitKey }` (:162). The wave-1 baseline's "key correctly never contains the token" is REFUTED. Fix: key on `user.id` when authenticated (else resolved IP), route through the documented `rateLimiting.identity`; add a test asserting no key contains the token. |
| O4 | **HTTP-method gate enforced only on HTTP transport** — a `GET`/read-only (or `get*`/`list*`/`fetch*`-named) route is fully callable + mutating over the websocket; docs claim "both transports execute the same sequence" | high | open (known) | 1/6 (#1) | 3/3 (r1 prior-confirm, r4 #4) | `handleHttpApiRequest.ts:393-407` (gate) vs `handleApiRequest.ts` (no method check anywhere) | VERIFIED: socket pipeline is auth→rate-limit→validate→execute with no method consideration; client `apiRequest` sends no method over sockets. Pairs with the GET-route CSRF/origin-exemption (server-side). Fix: enforce method on socket (client sends intended method, same `declaredMethod ?? inferHttpMethod` check) OR loudly document httpMethod as HTTP-only in `apiTypes.ts` JSDoc + CLAUDE.md. |
| O5 | **HTTP `data` shape guard does not reject ARRAY payloads** (transport asymmetry vs socket) | medium | open (new) | — | 1/3 (r1 API-06) | `handleHttpApiRequest.ts:221` | VERIFIED: HTTP guard is `if (typeof data !== 'object' \|\| data === null)` — `typeof [] === 'object'`, `[] !== null`, so an array passes and `requestData = data` hands an array to a handler that destructured `data: {...}`. Socket rejects it (:69). A `validation:'relaxed'` HTTP route receives the array → handler crash (→500) the socket would have 400'd. Fix: mirror socket: `... || Array.isArray(data)`. Add a parity case. |
| O6 | **`system/logout` socket shortcut bypasses the respond-hook chain, is not rate-limited, and always reports `success:true`** | medium | open (known) | 5/6 (#2) | 3/3 (r1 API-07, r4 #3) | `handleApiRequest.ts:452-459` | VERIFIED: emits hand-built `{status:'success',httpStatus:200,result:true}` directly via `socket.emit`, skipping `emitApiResult`/`preApiRespond`/`transformApiResponse`/`applyErrorFormatter`/`postApiRespond`; `performLogout`'s return is discarded; no rate-limit precedes it (the documented `applyGlobalIpRateLimit` cap does not exist — see O1). Response signing / PII redaction / audit subscribers miss the logout response; logout is spammable. Fix: route through `emitApiResult`, reflect real status, apply the global-IP bucket — or document the exception and fix the CLAUDE.md claim. |
| O7 | **Failed-auth requests consume no rate-limit bucket on either transport** (auth precedes rate-limit, early-returns) | medium | open (known) | 2/6 (#8) | 2/3 (r1 prior-confirm, r4 #5) | `handleApiRequest.ts:495-498`; `handleHttpApiRequest.ts:274-309` then `:311-391` | VERIFIED: both run auth and `return` before `applyApiRateLimits`. An attacker hammers an auth-required route from one IP with unlimited rejected requests; only observational hooks (would) fire. Conscious no-enumeration tradeoff but UNDOCUMENTED in `packages/api/CLAUDE.md`. Fix: increment the global-IP bucket on the auth-fail path before returning, OR document that failed-auth isn't rate-limited here (lockout lives in `@luckystack/login`). |
| O8 | **Rate-limit + auth logic hand-duplicated across both transports; parity test deliberately does NOT assert rate-limit/method/logout parity** (twin-drift hazard, already diverged on loopback skip) | medium | open (known) | 6/6 (#5) | 3/3 (r1 API-03, r3 API-08, r4 #7) | `handleApiRequest.ts:117-211` vs `handleHttpApiRequest.ts:311-391`; `transportParity.test.ts:22-24` | VERIFIED: validation/lifecycle/envelope ARE now shared in `_shared/`, but rate-limit + auth blocks remain inline twins and ALREADY diverge — socket resolves IP via `resolveClientIp` and has NO loopback skip; HTTP uses raw `requesterIp` and HAS a loopback skip. Parity test header (:22-24) explicitly excludes rate-limit keying + envelope shape, and `defaultSeam` disables both limits so the divergent stage is never exercised. Fix: extract `applyApiRateLimits({transport,...})` shared, resolve IP once on both, extend parity test to assert rate-limit key + loopback decision. |
| O9 | **HTTP global + per-route buckets fall back to literal `'unknown'`/`'anonymous'`, collapsing distinct callers into one shared bucket** (per-IP limiter → global limiter; socket twin resolves IP and diverges) | medium | open (known) | 5/6 (#4) | 2/3 (r1 prior-confirm, r4 #6) | `handleHttpApiRequest.ts:318,363` | VERIFIED: per-route `token ?? requesterIp ?? 'anonymous'` (:318); global `requesterIp ?? 'unknown'` (:363). When `requesterIp` is absent (proxy not passing it / `trustProxy:false` + proxy front), all such callers share `ip:unknown:api:all` → self-DoS + weakened per-attacker control. Socket now resolves via `resolveClientIp` (:134) and never substitutes a literal. Fix: resolve IP via `resolveClientIp` on HTTP too; consider fail-closed deny on the UNKNOWN sentinel for state-changing routes. |
| O10 | **`api.requestTimeoutMs` config has no enforcement on either transport** — a hung handler never times out server-side | medium | open (new) | — | 1/3 (r3 API-06) | `handleApiRequest.ts:213-238` (execute); `handleHttpApiRequest.ts:476-492` | VERIFIED: `api.requestTimeoutMs` exists in core config (`projectConfig.ts:463,759`) but grep of `packages/api/src` for `requestTimeoutMs` = 0 hits. Neither `executeApiHandler` nor the HTTP `tryCatch(main(...))` races a timeout; the only abort source is client disconnect / `apiCancel`. A hung upstream ties up the request indefinitely. Fix: race `main()` against `setTimeout(api.requestTimeoutMs)` that aborts the AbortController + resolves a localized `api.requestTimeout` envelope, when value > 0; both transports. |
| O11 | **`runHandleHttpApiRequestInner` is a ~345-line god-function** (HTTP side never decomposed; socket side was) | medium | open (known) | 6/6 (#6) | 3/3 | `handleHttpApiRequest.ts:168-512` | VERIFIED: still inlines abort/flush setup, shape guards, route parse, map lookup, auth, both rate-limit buckets, method check, validation call, stream closure, pre/post-execute hooks, tryCatch, envelope (~345 lines). It is the structural source of the O8 twin-drift. Fix: extract `applyHttpApiRateLimits`, `runHttpApiAuth`, `runHttpApiExecution`; reuse shared identity/rate-limit helpers. |
| O12 | `handleApiRequestInner` orchestrator threads mutable `let currentRouteName` / `let currentPerRouteFormatter` captured by the `emitApiError` closure before assignment | medium | open (known, improved) | 5/6 (#7) | 1/3 (r1 prior-confirm) | `handleApiRequest.ts:410-428` | VERIFIED: improved (now ~160 lines, stages extracted) but still uses mutable closure refs read by `emitApiError`. Lower-risk residual of #7. Fix: pass formatter/route into `emitApiError` explicitly. |
| O13 | `rateLimitExceeded` hook `scope` mislabeled — anonymous per-route bucket is IP-keyed but reported `scope:'route'` (and `token`-keyed reported `'user'` though it holds the token) | low | open (known) | 2/6 (#13) | 1/3 (r1 prior-confirm) | `handleApiRequest.ts:153`; `handleHttpApiRequest.ts:330` | VERIFIED: `scope: token ? 'user' : 'route'` on both. Abuse/audit subscribers misclassify the signal (compounds O3 — labeled `'user'` while the key is the raw token). Fix: map anon default to `'ip'`; label correctly relative to the actual key basis. |
| O14 | File-level `eslint-disable` on both handlers (socket file disables ALL rules; HTTP disables 3) hides future `\|\|`-vs-`??` / unsafe-cast bugs on security-critical code | low | open (known, worse on socket) | 3/6 (#15) | 2/3 (r1 API-09, prior-confirm) | `handleApiRequest.ts:1-2`; `handleHttpApiRequest.ts:1` | VERIFIED: socket file line 2 is a bare `/* eslint-disable */` (whole file, all rules); HTTP file disables `no-unnecessary-condition`, `restrict-template-expressions`, `prefer-nullish-coalescing`. Fix: replace with line-level disables only where deliberate; the HTTP targeted form is the template. |
| O15 | engine.io backpressure reads untyped internals via `(socket as unknown as {conn})` with `?? 0` / `?? true` fallbacks — an engine.io rename silently makes `flushPressure` a no-op (no warn) | low | open (known) | 2/6 (#17) | 1/3 (r1 prior-confirm) | `_shared/backpressure.ts:29-31` | VERIFIED: `conn?.writeBuffer?.length ?? 0`, `conn?.transport?.writable ?? true` — a shape change degrades to no-op rather than erroring. Fix: one-shot dev warn when neither `conn.writeBuffer` nor `conn.transport` is present; version-pinned comment. |
| O16 | Success envelope spreads raw handler result (`{...result, status, httpStatus}`) — extra fields (e.g. a Prisma row w/ passwordHash) serialize to the wire; `RuntimeSuccessResponse` index signature gives no guardrail | low | open (known) | 1/6 (#18) | 1/3 (r1 API-10) | `_shared/responseEnvelope.ts:86-93` | VERIFIED: by-design passthrough; `preApiRespond`/`transformApiResponse` are the only (opt-in) redaction seam. Consumer-facing behavioral change — surface before enforcing. Fix: document the leak risk prominently; consider an optional response-shape allowlist. |
| O17 | Error-leak asymmetry — a thrown handler error → generic `api.internalServerError` (safe), but a handler that `return`s `{status:'error', message:<db error/PII>}` flows that message verbatim | low | open (known) | 1/6 (#19) | 1/3 (r1 prior-confirm) | `_shared/responseEnvelope.ts:72-84` (vs `:26-38` throw path) | VERIFIED: returned error envelope's `message` passes straight into `normalizeErrorResponse`; thrown path is replaced with the generic code. Undocumented; an AI-authored handler won't know one path is redacted and the other isn't. Fix: document in `ARCHITECTURE_API.md` and/or redact returned error envelopes. |
| O18 | `handleHttpApiRequest.test.ts` mocks `@luckystack/login` `getSession`, which the handler no longer imports (dead/orphaned mock from pre-0.2.0 login-coupled era) | low | open (new) | — | 2/3 (r1 API-10, r3 API-09) | `handleHttpApiRequest.test.ts:57` | VERIFIED: `vi.mock('@luckystack/login', () => ({...}))` at :57; handler resolves sessions via core `readSession` (:4) which is separately mocked. The login mock is never exercised; misrepresents the module boundary. Fix: delete the `@luckystack/login` mock block. |
| O19 | Socket-ONLY paths remain untested — `system/logout` shortcut, `createApiRequestLifecycle` cleanup-on-every-exit, `flushPressure` poller, `emitStream` no-op-after-abort | low | open (new, residual of #24) | 1/6 (#24) | 1/3 (r1 API-08) | `handleApiRequest.ts:449-459,486-490`; `_shared/requestLifecycle.ts`; `_shared/backpressure.ts` | VERIFIED: `transportParity.test.ts` covers shared gates but no asserting test exists for logout/cleanup-idempotency/flushPressure/abort-gated emitStream. No regression net for fixing O6. Fix: add socket-only cases (logout emits + reflects status, cleanup runs each exit, emitStream no-ops after abort, flushPressure resolves on drain). |

---

## DEFERRED-DECISION

| # | Finding | Severity | Wave1 | Wave2 | Location | Note |
|---|---|---|---|---|---|---|
| D1 | Rate-limit fail-OPEN by default on Redis outage (`onStoreError:'memory'` → per-process counting multiplies the effective global limit by instance count under outage) | medium | 1/6 (#9) | (out-of-area, r1) | `packages/core/src/rateLimiter.ts`; config `rateLimiting.onStoreError` | Lives in core, not api (default still `'memory'`, `projectConfig.ts`). Documented availability tradeoff. Deferred to a policy/ADR decision: keep `'memory'` default or default `'deny'` when `store:'redis'` AND `NODE_ENV==='production'`. Tracked in the core area. |

---

## FALSE-POSITIVE

| # | Finding | Severity | Wave1 | Wave2 | Location | Why not a problem |
|---|---|---|---|---|---|---|
| FP1 | "`validateType` still fails OPEN" (wave-2 run-1 carried it as out-of-area / one run asserted still-open) | — | 4/6 (#3) | r1 (deferred), r4 verifier refuted | `packages/core/src/runtimeTypeValidation.ts` | Stale read of a pre-`7576c88` version. Current terminal + unresolved branches fail CLOSED (`:357,:360,:373`); depth guard at `:184`. Recorded as FIXED (F1/F2). The api package is a correct relay. |

---

## Wave-1 items not re-raised by wave-2 (status carried from baseline, spot-verified)

These wave-1 LOW/quality items were not surfaced by any wave-2 run; left as OPEN-LOW in the baseline, unchanged in current code (not individually re-tabled above to avoid noise; flagged here for completeness):

- #10 rate-limit-before-method (HTTP) — STILL present: rate-limit blocks (`:311-391`) precede the method gate (`:393-407`). Cheap denial primitive to burn budget with 405 probes. (LOW/medium; subsumed by O4+O8 territory.)
- #16 dev logs include rate-limit key / validator message (PII) — superseded by O3 (the key contains the RAW TOKEN — worse than #16 framed).
- #20 `inputType==='any'` silently disables validation with no prod-visible signal; #21 empty/invalid-status returns reported as 500; #22 `getProjectConfig()` called many times per request; #23 `flushPressure` no-op on HTTP undocumented; #25 streaming/cancellation contract undocumented in CLAUDE.md; #26 four spellings for `validation`; #27 `parseObjectFields` copy-paste (core); #28 stale `defaultRateLimitStrategy` JSDoc (core). All LOW, unchanged; not re-raised by wave-2. Recommend rolling into a single "api docs + minor-quality" cleanup pass.
