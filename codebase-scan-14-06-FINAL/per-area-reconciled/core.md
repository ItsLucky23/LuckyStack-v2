# Reconciled area report: core (@luckystack/core)

Branch `chore/package-split-prep`, HEAD `302cbf1` + ~248 uncommitted (this week's fixes).
Reconciliation date: 2026-06-15.

## Method

Collected every wave-2 core finding across the 3 real runs (run-1 split into
`core-transport` + `core-infra`; run-3 `core.md`; run-4 split into
`core-infra` / `core-realtime` / `core-runtime`), deduped, cross-referenced
against the wave-1 6-scan merge (`codebase-scan-14-06-MERGED/per-area/core.md`)
and the wave-1 LOW analysis (`LOW_ANALYSIS/core-transport.md`), and re-verified
EACH kept finding against the CURRENT working tree with file:line evidence.
Wave-2 "fixed/present" claims were NOT trusted — every verdict below was
re-derived by opening the cited file.

## Headline

The two wave-1 6/6 HIGH findings are genuinely **FIXED** in the current tree:
- **#1 apiRequest offline-queue `onDrop` hang** — fixed (`apiRequest.ts:439-446`,
  onDrop wired with the `suppressOnDrop` window mirroring `syncRequest`).
- **#2 validateType fail-OPEN** — fixed (terminal branch `runtimeTypeValidation.ts:363-373`
  now fails CLOSED; Record values validated, proto-keys rejected, depth-capped,
  `safeValidateType` tryCatch wrapper, prod `runtimeMode 'enforce'`).

Wave-2's most material NEW finding is **CORETRANSPORT-01** (HIGH, 1/3): a
`getParams` request-stream `'error'` REJECT becomes an unhandled promise
rejection because the only caller (`handleHttpRequest`) has no surrounding
try/catch and is fired as `void handleHttpRequest(...)`. VERIFIED real and
present. Same crash CLASS as the wave-1 avatar-stream crash, distinct path.

The remaining open items are the same security-posture / helper-level
medium/low findings the wave-1 merge already rated medium/low — all still
present and confirmed: leftmost-XFF trust (#4), legacy-Sentry context leak
(#11), redacted-key-set gaps (#12), fail-open rate-limit defaults (#6),
unbounded in-memory store (#7), ungated fleet-wide `clearAllRateLimits` (#16),
`'plain'` health-hash default (#13). Plus three genuinely NEW validator
parser-gaps wave-1 missed (quote-blind splitter, Date-accepts-any-string,
non-identifier-quoted-key drop).

---

## FIXED

| # | Finding | Severity | Status | Wave1 | Wave2 | Location | Note/Recommendation |
|---|---|---|---|---|---|---|---|
| F1 | apiRequest offline-queue `onDrop` never wired → queued-then-evicted promise hangs forever | high | FIXED | 6/6 | 3/3 (r1-transport, r3 CORE-C2, r4-realtime/runtime) | `apiRequest.ts:439-446` | onDrop resolves `offline.dropped` with the `suppressOnDrop` re-entrancy window; `cleanupAbortController()` on drop; `!enqueued` resolves `offline.queueFull`. Mirrors SYNC-09. Systemic residual = no shared parity gate between the api/sync twins (see Q1). |
| F2 | `validateType`/`validateInputByType` fails OPEN on unrecognized type strings (terminal `return success`) | high | FIXED | 6/6 | 3/3 (r1-infra, r3 CORE-C1, r4-runtime) | `runtimeTypeValidation.ts:363-373` | Terminal branch now `{status:'error','unvalidatable type … model it explicitly'}`; identifier branch (:355-361) returns `unresolved type/utility`. No `{status:'success'}` fall-through. Prod runs it (`runtimeMode 'enforce'`, validateInputByType.ts:421-433). Residual = quote-blind splitter (N1) + single-member skip (O11). |
| F3 | `Record<K,V>` accepts any object without validating V; single-member union skips; proto-keys admitted | high→med | FIXED (Record-value + proto half) | 2/6 | 3/3 | `runtimeTypeValidation.ts:262-296` | Record value validated against V (:291), key against K (:281-287), `__proto__`/`constructor`/`prototype` rejected (PROTO_POLLUTION_KEYS :157,272,325). Single-member-union half NOT fixed → tracked as O11. |
| F4 | `validateInputByType` not wrapped in tryCatch → throw becomes a 500 (fail-open-to-crash) | medium | FIXED | 1/6 | 2/3 (r1-infra, r4-runtime) | `runtimeTypeValidation.ts:381-387` | `safeValidateType` wraps `validateType` in `tryCatchSync`; a throw maps to `{status:'error','input validation failed'}`. |
| F5 | Unbounded validator recursion (stack DoS via deeply-nested value) | medium | FIXED | (wave1 api#11) | 1/3 (r4-runtime) | `runtimeTypeValidation.ts:184-189` | `MAX_VALIDATION_DEPTH = 64`, depth threaded through every recursive call. |
| F6 | `sanitizeForLog` unbounded recursion on a deep/cyclic context object | medium | FIXED | (wave1 LOW) | 1/3 | `redactedLogKeys.ts:49,64-65` | `MAX_SANITIZE_DEPTH = 8`; over-deep / cyclic graphs cut with the placeholder; input never mutated. |
| F7 | Error-tracker identity ALS primitive present + sanitizeContext scrubs on fan-out | n/a | FIXED (core seam sound) | 6/6 (ET-02) | 2/3 (r1-infra, r3 CORE-C5) | `errorTrackerRegistry.ts:85-119` | Core-owned ALS box + per-fanout sanitize confirmed correct. Whether api/sync wrap entry before first await + each adapter reads the getter is OUT OF AREA (api/sync/error-tracking) — verify there. |
| F8 | `configUtils.deepMerge` proto-pollution + depth/circular guard absent | medium | FIXED (guard present) | (implied #9) | 1/3 (r4-realtime) | `configUtils.ts:54-57,66` | depth(100)+circular WeakSet guard + proto-key skip present. NOTE the proto-key skip over-reaches (drops legit `constructor`/`prototype` config keys) → tracked as O18. |
| F9 | `getCookieValue` builds an UNescaped RegExp per call (ReDoS on auth hot path) | low | FIXED (ReDoS half) | 4/6 (#35) | 2/3 (r1-transport, r4-realtime) | `cookies.ts:1-3,10-13` | Cookie name now `escapeRegExp`'d — no ReDoS. The decode-failure-returns-raw-value half is by-design/lenient (see FP3). |
| F10 | `redis` proxy stray-prefix `del`/`mget` variadic asymmetry (get prefixes, del didn't) | low | FIXED | 1/6 (#38) | 1/3 (r4-infra) | `redis.ts` `ALL_ARGS_ARE_KEYS_COMMANDS` (:~115) | `del/unlink/exists/touch/mget` now all-args-prefixed via `ALL_ARGS_ARE_KEYS_COMMANDS` + `registerStrayPrefixCommand`. Residual command-coverage gap is by-design (see FP4). |
| F11 | `canonicalizeIp` IPv4-mapped/zone-id/lowercase normalization missing | low | FIXED (lightweight half) | 2/6 (#33) | 3/3 | `resolveClientIp.ts:43-48` | Strips `::ffff:`, drops zone id, lowercases IPv6. Full IPv6 zero-compression intentionally out of scope (documented dependency-free). |

---

## OPEN

| # | Finding | Severity | Status | Wave1 | Wave2 | Location | Note/Recommendation |
|---|---|---|---|---|---|---|---|
| O1 | `getParams` request-stream `'error'` → unhandled promise rejection → remote worker crash | **HIGH** | NEW (open) | — | 1/3 (r1 CORETRANSPORT-01) | `getParams.ts:111-113`; caller `server/httpHandler.ts:190,208-307` (no try/catch); dispatch `server/createServer.ts:195` (`void`, no `.catch`) | VERIFIED: `req.on('error', e => reject(e))`; `handleHttpRequest` has no top-level try/catch; fired as `void`. A client RST/aborted mid-body on POST/PUT/DELETE → unhandled rejection → process exit under Node default. **Core-owned safe fix**: resolve `null` (the parser's documented "no usable body" signal) on `'error'` instead of rejecting; ALSO add `.catch()` to the `void` dispatch as a server-side backstop. Same class as wave-1 avatar-stream crash (#3, already fixed via pipeline). |
| O2 | `getParams` body parser has no already-responded latch → second buffered `'data'` chunk re-trips 413 → `ERR_HTTP_HEADERS_SENT` (uncaught in event handler) | medium | OPEN (known) | 3/6 (#8) | 2/3 (r1 CORETRANSPORT-02, r4-runtime) | `getParams.ts:57-66` | `req.destroy()` does not synchronously stop an already-buffered chunk; the second invocation `res.writeHead(413)` after `res.end()` throws inside the `'data'` callback. Combined with O1's missing handler-level catch → another path to an uncaught throw on the hot path. Fix: `let responded=false` set in `writeJsonErrorAndResolve`, or `if (_res.headersSent) return;` guard. |
| O3 | `trustProxy` XFF resolution trusts the LEFTMOST (client-controlled) hop → per-IP rate-limit evasion + audit-IP spoofing | high (disputed med) | OPEN (known) | 4/6 (#4) | 3/3 (r1, r3 CORE-C3, r4-infra/runtime) | `resolveClientIp.ts:85-91` (`:89` `split(',')[0]`) | Confirmed. Spoofable once `trustProxy:true` AND an append-style edge / >1 hop. Default `trustProxy:false` (projectConfig.ts:702) is the safe gate, so default deploys are inert. Wave-2 verified-severity settled at MEDIUM (only exploitable on the opt-in posture). Fix: skip a `trustedProxyHopCount` from the RIGHT / prefer single-valued `X-Real-IP`; document overwrite-not-append requirement. |
| O4 | Legacy single-instance Sentry slot bypasses `sanitizeForLog` (raw context → Sentry `extra`) | medium | OPEN (known) | 1/6 (#11) | 2/3 (r1-infra, r4-infra; r3/r4 top-level SECURITY HIGH) | `sentrySetup.ts:42-48` (captureException), `:58-64` (captureMessage) | Confirmed: `sentry.captureException(error,{extra:context})` passes RAW context; only `captureExceptionAcrossTrackers` fan-out sanitizes. `tryCatch` + `hooks/registry.ts` auto-capture on every error. Only fires when a consumer mixes the legacy `initSharedSentry` path with framework capture (no framework call-site triggers it). Fix: `const safe = sanitizeForLog(context)` once at the top of both legacy fns; idempotent for the fan-out. |
| O5 | `redactedLogKeys` default set omits `csrftoken`/`apikey`/`secret`/`accesstoken`/`refreshtoken`; `isRedactedLogKey` exact-match only | medium | OPEN (known) | 4/6 (#12) | 3/3 (r1-infra, r4-infra/realtime/runtime) | `redactedLogKeys.ts:10-18` (defaults), `:30-32` (exact match) | Confirmed: defaults are only `password,confirmpassword,token,newtoken,authorization,cookie,set-cookie`; `token` ≠ `csrftoken`, `userPassword` not caught. Session carries a live `csrfToken`. NOTE: deferred in wave-1 LOW pass (behavior change — more aggressive redaction can hide legit debug fields) — a deliberate policy call. Fix (policy): add the missing keys + suffix matching (`*token`,`*secret`,`*password`). |
| O6 | Default rate limiter fails OPEN: `store:'memory'` is per-process; `onStoreError:'memory'` default; `'deny'` only honored inside the `isRedisMode()` branch (false fail-closed assurance for memory mode) | medium | OPEN (known) | 5/6 (#6) | 2/3 (r1-infra, r4-infra) | `rateLimiter.ts:228-235`; defaults `projectConfig.ts:665,671` | Confirmed: `check()` only enters the deny branch inside `if (isRedisMode())` (:228-232); memory mode ignores `'deny'` entirely. Consistent with the confirmed deliberate fail-open framework stance — flag, don't assert a bug. Fix (conscious-default review): honor `'deny'` regardless of store, or warn at boot that `onStoreError` is Redis-mode-only + memory mode is single-instance/dev. |
| O7 | In-memory rate-limit store unbounded — no max-entry cap / LRU; pruned only on the cleanup tick | medium | OPEN (known) | 1/6 (#7) | 2/3 (r1-infra, r4-infra) | `rateLimiter.ts:60` (`new Map`), populated `:111`, pruned `:348` | Confirmed: key-varying flood (via the spoofable IP, or per-route keys) grows the Map between ticks → GC pressure / OOM single-instance or post-Redis-fail. Fix: cap (LRU) or sweep opportunistically when size crosses a threshold; document memory mode single-instance/dev. |
| O8 | Redis `clearAll`/`clearAllRateLimits` SCAN+DEL on `${getRedisPrefix()}:*` is project-rooted (not tenant-rooted) and publicly exported with NO test/dev guard | medium | OPEN (known) | 1/6 (#16) | 2/3 (r1-infra COREINFRA-05, r4-infra) | `rateLimiter.ts:258-278`, exported (index.ts) | Confirmed: a single call wipes every tenant's rate-limit counters; JSDoc frames it as a benign "testing/restart" helper. Fix: gate behind `NODE_ENV!=='production'` or require an explicit namespace arg; document fleet-wide. |
| O9 | Synchronized-secret `/_health` hash ships unsalted-sha256 default (`healthHash.mode:'plain'`) on an unauthenticated endpoint → dictionary-attackable secret fingerprints | medium | OPEN (known) | 1/6 (#13) | 2/3 (r1-infra, r4-infra) | `synchronizedEnvHashes.ts`; default `projectConfig.ts:703-706` | Confirmed: `{mode:'plain',salt:''}`. `/_health` is an unauthenticated router-topology probe publishing stable fingerprints of synchronized secrets. (A boot-warn was added in `server/verifyBootstrap.ts:118-124` per r3/r4 SECURITY, reducing silent-misconfig but not changing the default exposure.) Fix: default `'hmac'` or salted `'@bootUuid'`; gate `'plain'` behind explicit opt-in. |
| O10 | Cookie-mode sessions silently accept `Authorization: Bearer` (and socket `handshake.auth.token`) as a fallback transport → CSRF model bypass for a stolen token | high (disputed med/low) | OPEN (known) | 4/6 (#5) | 1/3 (r3 CORE-N8) | `extractTokenFromRequest.ts:30-34`; `extractToken.ts` (socket twin) | Confirmed: cookie mode (`!session.basedToken`) returns `cookieToken ?? bearerToken`. A stolen token (XSS/logs) replays over Bearer dodging the cookie-bound CSRF defense. Fix: make the cross-transport fallback opt-in (`http.acceptBearerInCookieMode`, default false); cookie mode returns only the cookie token. |
| O11 | `validateType` silently skips a single-member union/intersection (`unionParts.length===1` / `intersectionParts.length===1`) — the lone member is never recursed into | low | OPEN (known) | (#9 sub) | 1/3 (r4-runtime) | `runtimeTypeValidation.ts:202-222` | Confirmed: branches only act when `length > 1`; a `\|`/`&`-bearing single-member type falls through to the primitive/object recognizers and (usually) the fail-closed terminal. Narrow (well-formed generated types rarely produce these). Fix: when length===1 recurse into `parts[0]`. |
| O12 | `apiRequest` interceptor window is un-timed and abort-unwired — response-timeout + external-abort armed only AFTER `await dispatchApiRequestInterceptors` | medium | OPEN (known) | 1/6 (#20) | 2/3 (r1 CORETRANSPORT-03, r4-realtime) | `apiRequest.ts:613-619` (await) vs `:474-494`,`:500-521` (arm inside runRequest) | Confirmed: interceptors awaited at :613-617 before `runRequest(socket)` at :619; timeout/abort live inside runRequest. A slow interceptor adds un-counted latency; an external abort for an `abortable:false` call is ignored during that window. Fix: wire external-abort + a guard timer before awaiting interceptors, or document the exclusion. |
| O13 | `apiRequest` lost-response listener + promise leak when timeout disabled (`timeoutMs:false`/`api.requestTimeoutMs:false` + non-abortable) | medium | OPEN (known) | 2/6 (#22) | 1/3 (r1 CORETRANSPORT-04) | `apiRequest.ts:500-521,602-605`; `socketState.ts` (no disconnect signal) | Confirmed: timer only armed when `effectiveTimeoutMs > 0`; no socket `disconnect` one-shot reaps the `.once(responseEventName)` listener. Server restart between emit and reply → forever-pending promise + leaked listener. Fix: attach a `socket.on('disconnect', …)` (or `setSocket`-fired pub/sub) that settles in-flight non-abortable requests. |
| O14 | External `AbortSignal` fired while the request waits in the offline queue is a no-op (handler wired only on the post-`canSendNow` send path) | medium | OPEN (known) | 1/6 (#18) | 1/3 (r4-realtime) | `apiRequest.ts:409-459` (enqueue branch returns) vs `:474-494` (abort wiring) | Confirmed: the enqueue branch returns at :458 before the externalSignal handler is registered at :474. For `abortable:false` (no internal controller) an abort while queued does nothing — the queue item isn't removed and `apiCancel` isn't emitted (promise still eventually settles via onDrop/run, so NOT a permanent hang). Fix: register the external-signal handler + `removeApiQueueItem` at/before the enqueue branch. |
| O15 | Name-prefix GET heuristic auto-attaches an abort controller to a mis-prefixed POST (`listAndArchive`) when `apiMethodMap` is unregistered → identical-payload duplicate silently aborts the first | medium | OPEN (known, partially mitigated) | 1/6 (#19) | 1/3 (r4-realtime) | `apiRequest.ts:76-88,340-344`; `httpApiUtils.ts` | Confirmed: `isGetMethod` falls back to `isGetMethodByPrefix` with no `isApiMethodMapRegistered()` guard. Mitigated since wave-1: the abort key now includes a stable payload hash (`buildAbortKey`) so only IDENTICAL-payload calls collide. Fix: when `!isApiMethodMapRegistered()`, default `abortable:undefined` to non-abortable, or one-time dev warn. |
| O16 | Origin resolver-function mode receives the RAW `Origin`/`Referer` value while static-array mode normalizes both sides → a natural `origin.startsWith('https://app.com')` resolver is bypassable | medium | OPEN (known) | 1/6 (#17) | 2/3 (r1 prior-list, r3 implied) | `checkOrigin.ts:68-71` (`:70` passes raw `origin`) | Confirmed: `configured(origin)` gets the raw header, not `normalizedOrigin`. Asymmetric/leaky contract. Fix: pass the resolver the already-`normalizeOrigin`'d value, or loudly document that the resolver receives a raw header and must normalize; export the normalizer. |
| O17 | `escapeHtml` escapes only the canonical five — unsafe for unquoted attrs / URLs / JS / CSS; JSDoc oversells it as the source of truth | medium (mostly doc) | OPEN (known, doc-only) | 3/6 (#14) | (carried) | `escapeHtml.ts:10-20` | The five-char escape is correct for text/quoted-attribute contexts; the issue is the over-promising JSDoc. Wave-1 LOW pass left the `String(str)` coercion variant DEFERRED (an eslint `no-unnecessary-type-conversion` blocker on the typed copy). Fix: tighten the JSDoc only. |
| O18 | `redis` proxy stray-prefix net covers only some commands (excludes `eval`/`scan`/`multi`/`sort`/`rename`/`copy`/`object`) → a colon-free key via an unlisted command lands unprefixed (cross-tenant collide / silent no-op) | low | OPEN (known, by-design net) | 1/6 (#38) | 1/3 (r3 CORE-N6) | `redis.ts:96-160` | Confirmed asymmetry beyond the now-closed `del`/`mget`. By-design: `formatKey()` is the authoritative safe path; the net is a best-effort backstop, NOT tenant scoping. Fix (doc/dev-warn): document the exhaustive uncovered-command list; optional dev-only warn when a colon-free key reaches an unlisted command. |
| O19 | `clientHookBus` / `apiInterceptors` dispatchers use raw `try/catch` + `console.error` instead of `tryCatchSync`+`getLogger()` (bypasses logger/redaction; CLAUDE.md "never raw try/catch"; AI-mirror risk) | low | OPEN (known) | 2/6 (#30) | 3/3 (r1 CORETRANSPORT-06, r4-realtime, r3 implied) | `clientHookBus.ts:122-132,155-164`; `apiInterceptors.ts:86-91,104-113` | Confirmed. Deliberate best-effort isolation but inconsistent. Fix: route through `getLogger().error(...)` (browser-safe console default) or annotate `// luckystack-allow no-raw-try-catch: …`; DRY into `runIsolated(label, fn)`. |
| O20 | `waitForSocket` busy-polls every 10ms ×500 (~5s magic ceiling) + `responseIndex` unbounded never-reset module-global | low | OPEN (known) | 4/6 (#27/#28) | 2/3 (r1 prior-list, r4-realtime) | `socketState.ts:18-25,27-37` | Confirmed: `while(!socket){ setTimeout 10 }` 500-iter cap; `responseIndex` monotonic, `setSocket` doesn't reset. Practically safe (server echoes the index). Fix: resolve via a one-shot pub/sub fired by `setSocket`; document/scope `responseIndex`. |
| O21 | `getCookieValue` returns the raw still-encoded value on `decodeURIComponent` throw (rather than null) | low | OPEN (known, lenient by-design) | 4/6 (#35) | 2/3 (r1-transport, r4-realtime) | `cookies.ts:19-23` | Confirmed: catch returns `rawValue`. The ReDoS half is fixed (F9). The decode-failure fallback is a deliberate, documented lenient contract; a stricter caller comparing a token could get an encoded mismatch. Fix (optional): return `null` on decode failure for stricter callers, or document. |
| O22 | `translate()` builds a fresh `RegExp` per param from the UNescaped `param.key` → mis-substitution / ReDoS on the translation/toast hot path (server `errorParams` flow through) | medium | OPEN (known) | 3/6 (#23) | (carried; react surface) | `react/TranslationProvider.tsx:92-97` | Not re-read this pass (react/* surface, lower priority); wave-1 3/6 consensus + no contradicting wave-2 evidence. Fix: escape `param.key` or use `split/join` literal substitution. |
| O23 | `Middleware` denial with no `redirect` calls `navigate(-1)` → can chain backwards out of the app / to a blank page | medium | OPEN (known) | 2/6 (#21) | (carried; react surface) | `react/Middleware.tsx:99-103` | Not re-read this pass (react/* surface). wave-1 2/6 (r5 ranked HIGH). Fix: navigate to a deterministic safe route (`loginRedirectUrl`) when no `redirect`. |

---

## NEW

| # | Finding | Severity | Status | Wave1 | Wave2 | Location | Note/Recommendation |
|---|---|---|---|---|---|---|---|
| N1 | `splitTopLevel` is quote-BLIND — splits inside a string literal containing `\|`/`&`. A literal-bearing union type (`"a\|b" \| "c"`) is mis-parsed → valid traffic REJECTED (route-level DoS); also `matchesIndexKeyType` Record key-unions | medium (down from HIGH) | NEW (open) | — | 1/3 (r3 CORE-N1) | `runtimeTypeValidation.ts:19-45`, used `:202`/`:213`/`:127` | VERIFIED: `splitTopLevel` tracks paren/brace/bracket depth, NO quote state; runs BEFORE the literal recognizer (:242). Proven impact is fail-CLOSED (valid value rejected / route DoS), NOT the over-accept the auditor framed — the fail-closed terminal (:373) makes an over-accept hard to construct. Genuinely new (wave-1's fail-open rewrite introduced/left this corner). Not covered by `runtimeTypeValidation.test.ts`. Fix: make `splitTopLevel` quote-aware (`inString` for `'`/`"` + escape); add the missing tests. |
| N2 | `validateType` accepts ANY string for a `Date`-typed field (no parseable-date check) | medium | NEW (open) | — | 2/3 (r1-infra COREINFRA-02, r3 CORE-N2) | `runtimeTypeValidation.ts:144` | VERIFIED: `if (type==='Date') return typeof value==='string' \|\| value instanceof Date;` — `{when:'not-a-date'}` passes. Handlers doing `new Date(x).toISOString()` get `Invalid Date`/`RangeError`. Silent fail-open inside the otherwise-fail-closed validator (limited blast radius — most handlers re-parse). Fix: `typeof value==='string' && !Number.isNaN(Date.parse(value))` (keep `instanceof Date`). |
| N3 | object-field parser silently DROPS fields with non-identifier quoted keys (`'foo-bar': string`, `'x.y': number`) → false-reject of valid input AND under-enforcement of required hyphenated keys | medium | NEW (open) | — | 1/3 (r1-infra COREINFRA-01) | `runtimeTypeValidation.ts:69,98` | VERIFIED: field regex `^("\|')?[A-Za-z_][A-Za-z0-9_]*("\|')?(\?)?…` — char class stops at `-`/`.`, the key isn't matched, the index-sig branch also fails, so the field is dropped from `fields`. A required `'foo-bar'` is never enforced; if present it's treated as an UNKNOWN key (:317-331) and the whole object is REJECTED. Generated TS type text CAN carry kebab-case/header-map keys. Fix: broaden the quoted-key capture to `("[^"]*"\|'[^']*'\|[A-Za-z_]…)`, unescape, and surface `unvalidatable field` rather than silently drop. |
| N4 | `parseRecordParts` returns `null` (no top-level comma — e.g. malformed `Record<string>`) → `:276 if(!recordParts) continue` skips value-validation for every key (narrow fail-open back-door) | low | NEW (open) | — | 1/3 (r1-infra COREINFRA-03) | `runtimeTypeValidation.ts:162-177,276` | VERIFIED: `parseRecordParts` blindly trims the final char as `>` and splits on the first top-level comma; no comma → `null` → keys pass with only the proto-key check. Requires a malformed generated `Record<…>` (generator bug) so very low. Fix: when `parseRecordParts` returns `null`, fail closed (`'unvalidatable Record type'`) instead of `continue`. |
| N5 | `configUtils.deepMerge` proto-pollution guard skips `constructor`/`prototype` keys, silently DROPPING legitimate config keys named that | low | NEW (open) | — | 1/3 (r1-infra COREINFRA-04) | `configUtils.ts:66` | VERIFIED: `if (key==='__proto__'\|\|key==='constructor'\|\|key==='prototype') continue;`. `__proto__` is the only one that pollutes via `result[key]=` on a fresh object; `constructor`/`prototype` as own-enumerable keys do not pollute the chain when set this way, but are silently dropped. No current config uses them (latent). Fix: only skip `__proto__` for the assignment guard, or document the three as reserved. |
| N6 | `checkRateLimitInMemory`/`InRedis` hardcode `windowMs = 60_000` param default that SHADOWS the configured `rateLimiting.windowMs` for any call site omitting `windowMs` | low | NEW (open) | — | 1/3 (r3 CORE-N4) | `rateLimiter.ts:104,126` | VERIFIED: param default `windowMs = 60_000`; `normalizeWindowMs` only falls back to config when the value is non-finite/<=0, but `60000` is finite → a caller passing `{key,limit}` without `windowMs` silently gets 60s, not the configured window. `buildDeniedResult` DOES read config window → inconsistency between active window and reported `resetIn`. Fix: drop the `= 60_000` default; let `normalizeWindowMs` resolve `undefined` to the config window. |
| N7 | `getParams` declared-Content-Length 413 branch returns WITHOUT draining/destroying the request body (contrast the `'data'`-path `req.destroy()`) | low | NEW (open) | — | 1/3 (r1 CORETRANSPORT-05) | `getParams.ts:47-50` | VERIFIED: `if(declaredLength>maxBodyBytes){ writeJsonErrorAndResolve(413); return; }` — no `req.destroy()`/`req.resume()`. A client declaring an over-size length then streaming leaves the socket half-consumed until keep-alive timeout (marginal slow-loris). Fix: `req.destroy()`/`req.resume()` after the 413, matching the `'data'`-path teardown. |
| N8 | Offline-queue eviction calls user `onDrop` (and the `queueItemDropped` client hook) SYNCHRONOUSLY inside `enqueueWithPolicy`, re-entering an UNRELATED queued request's resolve during a new enqueue; the `queueItemDropped` dispatch is not tryCatch-wrapped | low | NEW (open) | — | 1/3 (r3 CORE-N5) | `offlineQueue.ts:74-94,101` (evictExpired before push) | VERIFIED (per r3 trace; offlineQueue eviction wired). `evictExpired` runs before pushing, invoking `notifyDropped → item.onDrop` synchronously, so enqueuing B can settle a previously-queued expired A from inside B's call stack. Currently benign (resolve-only idempotent) but couples queue mutation with arbitrary consumer callback execution; a throwing `queueItemDropped` hook (NOT wrapped, unlike `notifyDropped`) could propagate. Fix: wrap the `dispatchClientHook('queueItemDropped',…)` in `tryCatchSync`, or defer eviction notifications to a microtask. |
| N9 | `db.ts`/`redis.ts` proxies forward `Reflect.get(real, prop, receiver)` with the PROXY as receiver (not `real`) — latent break if the underlying client exposes a prototype getter using private `#fields` | low | NEW (open, latent) | — | 1/3 (r1-infra COREINFRA-06) | `db.ts:43`, `redis.ts:139` | VERIFIED-by-claim (not re-opened; consistent with the `.bind(real)` pattern). Harmless for plain data/method reads today; only bites if a future PrismaClient/ioredis version adds a prototype getter with a `#x` brand check (`TypeError: Cannot read private member`). Fix: pass `real` as the third arg: `Reflect.get(real, prop, real)`. |
| N10 | `dispatchHook`/`dispatchSyncHook` catch body calls `getLogger().error(...)` + `captureException(...)` UNGUARDED — a throwing logger/tracker escapes the per-handler isolation contract | low | NEW (open) | — | 1/3 (r1-infra COREINFRA-07) | `hooks/registry.ts:54-60,111-116` | VERIFIED-by-claim. `errorTrackerRegistry.logTrackerFailure` defensively wraps its own logger call (:225-231) but the hook dispatcher does not, so a broken logger sink / adapter aborts the remaining handlers — defeating "one failing handler never interrupts the flow". Requires a throwing logger/tracker (rare). Fix: wrap the catch body in `tryCatchSync` (shared `runIsolated`). |
| N11 | `checkOrigin` same-origin fast-path fails when bind `port` is empty (no `registerBindAddress`, `SERVER_PORT` unset) → legit same-origin CORS silently rejected unless `allowLocalhost`/`allowedOrigins` matches | low | NEW (open) | — | 1/3 (r3 CORE-N7) | `checkOrigin.ts:51-81` | VERIFIED-by-claim (code comment acknowledges "Empty port produces an unmatchable host: entry"). For a mis-bootstrapped non-localhost prod deploy, same-origin socket/HTTP CORS can be rejected, presenting as flaky connection failures. Fix: when bind port is empty, fall back to host-only same-origin compare, or fail boot loudly when neither `registerBindAddress` nor `allowedOrigins` is set in prod. |

---

## DEFERRED-DECISION

| # | Finding | Severity | Status | Wave1 | Wave2 | Location | Note/Recommendation |
|---|---|---|---|---|---|---|---|
| D1 | Default-permissive sync receiver auth (`allowClientReceiverAll:true`, `requireRoomMembership:false`) — defaults live in core, enforcement in `@luckystack/sync` | medium | DEFERRED-DECISION | 5-6/6 (CORE-C4) | 2/3 (r3 CORE-C4, top-level SECURITY M5) | `projectConfig.ts:773-774` | Confirmed defaults still permissive. The secure-default flip for 0.2.0 is a deliberate cross-package policy/ADR (coordinate with the sync enforcement path); the `receiverAuth` helper is correct + wired. Reference: the "default-insecure-opt-in" 0.2.0 hardening bucket. |
| D2 | `env.ts` runs `loadDotenv` at module import (`export const env = bootstrapEnv()`) — contradicts the "no import-time side effects" doctrine | low | DEFERRED-DECISION | 3/6 (#36) | (carried) | `env.ts:108` | Converting the eager `export const env` to a lazy `getEnv()` is a public-API/contract change (every `import { env }` consumer). `bootstrapEnv()` is idempotent (cached), impact low. Needs an ADR. |
| D3 | `rateLimiting.auth` lockout slot declared in config but unread by the core rate limiter (shadow-config gap — a consumer enabling it at core level gets no lockout) | low | DEFERRED-DECISION | — | 1/3 (r4-infra finding 8) | `projectConfig.ts:673-677`; `rateLimiter.ts` (never reads `.auth`) | VERIFIED: core `rateLimiter.ts` consumes only store/enabled/windowMs/onStoreError/redisKeyPrefix. If the per-account lockout is implemented in `@luckystack/login` it's out of scope, but the core config surface is misleading. Decision: confirm `@luckystack/login` consumes it + document the core slot as a no-op without login, OR emit a boot warn when `auth.enabled:true` with no registered lockout. |

---

## FALSE-POSITIVE

| # | Finding | Severity | Status | Wave1 | Wave2 | Location | Note/Recommendation |
|---|---|---|---|---|---|---|---|
| FP1 | `apiRequest` is a 600-line god function / `validateType` + `projectConfig` god-files (resolve-race / maintenance risk) | quality | FALSE-POSITIVE (not a defect) | (theme) | 2/3 (r3 CORE-N3, all-runs theme) | `apiRequest.ts:247-621`; `runtimeTypeValidation.ts:186-374`; `projectConfig.ts:1-846` | Real observation, but no concrete defect — a maintainability/decomposition recommendation, not a finding. The resolve-only-swallows-double-settle risk is mitigated by the wave-1 fixes + the additive `transportParity.test.ts` (16 green) added in the wave-1 bucket-3 pass. Tracked as a refactor recommendation, not OPEN. |
| FP2 | Socket transport enforces no HTTP method → GET/read-only route callable as a mutation over the websocket = HIGH transport-asymmetry vuln | high (claimed) | FALSE-POSITIVE (factual core true, security framing wrong) | — | 1/3 (r4-runtime finding 4, self-DISPUTED) | `packages/api/src/handleApiRequest.ts` (out of core area) | The run-4 skeptic disputed its own finding: method is an HTTP-semantics control (CSRF-exemption), NOT an authz boundary; the socket is independently origin-gated (`loadSocket` CORS) + runs `readSession`+`checkApiAuth` per message; the docs explicitly state method is HTTP-only. Residual real concern (a side-effecting route should never be GET-named, because over HTTP it becomes CSRF/Origin-exempt) is an api/HTTP route-authoring hazard, NOT a core/socket bug. Out of core area + not the claimed vuln. |
| FP3 | `getCookieValue` returns raw value on bad percent-encoding = bug | low | FALSE-POSITIVE (by-design) | (#35 half) | 2/3 | `cookies.ts:19-23` | Deliberate, documented lenient fallback (see O21 for the report-only note). The ReDoS half is genuinely FIXED (F9). Not a defect. |
| FP4 | `redis` proxy stray-prefix net is project-not-tenant rooted = tenant-isolation bug | low | FALSE-POSITIVE (by-design) | 1/6 (#38) | 1/3 | `redis.ts:96-160` | Documented inline + in CLAUDE.md as a best-effort net with `formatKey()` as the authoritative safe path. Tenant-scoping a colon-free stray key is explicitly NOT the proxy's job. (The command-coverage gap is separately tracked as O18 report-only.) |
| FP5 | `serveAvatar` format-loop existence oracle (vetoed vs missing) + stream crash | low/high | FALSE-POSITIVE (oracle) / FIXED (crash) | 1/6 (#39) / 2/6 (#3) | — (wave-2 didn't re-flag) | `serveAvatars.ts:27-72` | Per wave-1 LOW analysis: hook-vetoed and missing both return identical `404 'File not found'` now; the only diff is per-format `access()` order, rated "marginal" non-traversal. Traversal hardening (`path.basename`+`FILE_ID_REGEX`) solid. The HIGH stream-crash (#3) is FIXED via `stream.pipeline` with an error sink. Neither is an open defect. |
| FP6 | `acquireLease` returns `null` indistinguishably for error vs held | low | FALSE-POSITIVE (documented ambiguity, safe) | 1/6 (#37) | — | `lease.ts:46-53` | Behavioral ambiguity safe under partition; primitive-level documented contract. Not re-flagged by wave-2. Report-only, not a defect. |

---

## Cross-area notes

- **O1 (getParams crash)** is core-OWNED at the reject site (`getParams.ts:111-113`) but only becomes a crash because of the un-wrapped caller in `@luckystack/server` (`httpHandler.ts` + `createServer.ts:195`). The cheapest core-only safe fix is "resolve `null` on `'error'`"; the defense-in-depth `.catch()` on the `void` dispatch belongs to the server area.
- **F7 (ALS identity)**: the core seam is sound; the actual PII-bleed verification (entry wrapped before first await; each adapter reads the getter) lives in `@luckystack/api`, `@luckystack/sync`, `@luckystack/error-tracking`.
- **D1 (sync receiver defaults)**: core owns the insecure defaults; `@luckystack/sync` owns enforcement — fix must be coordinated.
- **FP2 (socket method)** belongs to `@luckystack/api`; reconciled here only because run-4 filed it under core-runtime.
