# Reconciled area report: scaffolder (`packages/create-luckystack-app`)

Reconciliation of wave-2 scan findings against the **current working tree** (branch `chore/package-split-prep`, HEAD `302cbf1` + ~248 uncommitted changes). Every kept finding was re-verified by opening the cited file in the live tree; no wave-2 "fixed/present" claim was trusted blindly.

Sources reconciled:
- Wave-2: `codebase-scan--wave2-14-06--1/per-area/scaffolder.md`, `…--3/per-area/scaffolder (create-luckystack-app).md`, `…--4/per-area/scaffolder-packages-create-luckystack-app.md` (run 2 = input digest, ignored).
- Wave-1 baseline: `codebase-scan-14-06-MERGED/per-area/scaffolder.md` (6-scan merge; no `LOW_ANALYSIS/scaffolder.md` exists — this session never recorded scaffolder fix/defer decisions).

**Headline:** `src/index.ts` is 1639 lines (down from the 1746 in the wave-1 baseline; refactored in `302cbf1`, so wave-1 line numbers no longer align — all locations below re-confirmed against the current file). No critical findings. **Two genuinely NEW HIGH findings live in the SHIPPED `template/` tree** (not the copier): a credentials user can never self-delete their account, and `ErrorPage` leaks full stack traces in production. Both are drift/regressions where the framework's OWN copy was fixed but the template copy was not. **One NEW HIGH in the copier**: the framework-docs copy substitutes `{{…}}` example tokens inside the very docs that document the token mechanism. **Nothing from wave-1 was FIXED** in scaffolder — every wave-1 maintainability finding is still present.

## Status summary

| Status | Count |
|---|---|
| FIXED | 0 |
| OPEN | 21 |
| NEW (subset of OPEN, wave-2 only) | 9 |
| DEFERRED-DECISION | 0 |
| FALSE-POSITIVE | 1 |

---

## OPEN — NEW (wave-2 found, wave-1 missed, verified real in current code)

| # | Finding | Severity | Status | Wave1 | Wave2 | Location | Note/Recommendation |
|---|---------|----------|--------|-------|-------|----------|---------------------|
| N1 | **Credentials user can NEVER self-delete account (GDPR break).** `deleteAccount_v1` was hardened to require a password when `dbUser.password` is set, but `handleDeleteAccount` only sends `{ confirmation: 'DELETE' }` — no password field. Every credentials user → `ok=false` → always `login.wrongPassword`. OAuth-only users unaffected. Ships in every auth-enabled scaffold. | HIGH | open (new) | — (0/6) | 1/3 (r3) | `template/src/settings/page.tsx:281-285` (no password) vs `template/src/settings/_api/deleteAccount_v1.ts` | Collect current password (second `menuHandler.confirm` input or extend the confirm form) and pass as `data.password`; mirror the changePassword section. **Verified: page.tsx:281-285 body is exactly `data:{ confirmation:'DELETE' }`, no password collection anywhere in the handler.** |
| N2 | **ErrorPage leaks full `error.stack` in production (stack-trace disclosure, CWE-209).** `errorDetails = error.stack ?? null` with NO `import.meta.env.DEV` guard, rendered verbatim in a `<details><pre>`. The framework's OWN `src/_components/ErrorPage.tsx:37` already applies the `DEV ? … : null` fix — it was never propagated to the template. Genuine drift/regression. | HIGH | open (new) | — (0/6) | 1/3 (r3) | `template/src/_components/ErrorPage.tsx:34` (assign), `:67-76` (render) | Gate: `errorDetails = import.meta.env.DEV ? (error.stack ?? null) : null;` (match framework copy). **Verified line 34 has no guard; grep of file for DEV/PROD/import.meta.env = 0 hits; rendered at 67-76.** Default-on across all scaffolds; severity MEDIUM-leaning per r3 verifier (client bundle is shipped JS anyway) but recorded HIGH given default blast radius. |
| N3 | **Framework-docs copy substitutes the tokens the docs EXPLAIN — corrupts AI-facing copied docs.** When `aiInstructions` on, `docs/` is copied via `copyTree(src,dst,vars)` which runs `replacePlaceholders`. `docs/AI_QUICK_INDEX.md` and `docs/ARCHITECTURE_EXTENSION_POINTS.md` contain literal `{{PROJECT_NAME}}` / `{{DB_PROVIDER}}`-style tokens AS documentation → they get rewritten into concrete values in the consumer's `docs/luckystack/`. | HIGH | open (new) | — (0/6) | 1/3 (r1-wave2) | `src/index.ts:1543` (`copyTree(src,dst,vars)`); tokens confirmed in `docs/AI_QUICK_INDEX.md`, `docs/ARCHITECTURE_EXTENSION_POINTS.md` | Copy framework docs WITHOUT substitution (pass `{}` or an opt-in `substitute=false` flag). The template tree needs substitution; already-rendered docs do not. **Verified: 1543 passes `vars`; grep found both docs contain `{{PROJECT_NAME}}`/`{{DB_PROVIDER}}` literals.** |
| N4 | **No partial-scaffold cleanup; a prune/copy throw leaves a half-written dir the next run refuses.** `main().catch` (1635-1638) only `console.error`+`exit(1)`, no `rmSync(targetDir)`. `copyTree` writes the whole tree, THEN `pruneOptionalPackages`→`editScaffoldFile` can `throw`. Retry hits the `existsSync(targetDir)` guard (1430-1433) and refuses → manual `rm` required. **Refutes wave-1 prose claiming cleanup is "hardened and tested".** | MEDIUM | open (new) | refutes baseline prose | 2/3 (r1-w2, r4) | `src/index.ts:1635-1638` (catch), `1430-1433` (guard), `1504` (copy) | Track whether this run created `targetDir`; in catch `rmSync(targetDir,{recursive,force})` only if freshly created. **Verified: catch at 1635-1638 has no rmSync; only `removeScaffoldPath` (1029) does rmSync, unrelated.** |
| N5 | **`template/scripts/generateAiCapabilities.mjs` drifted from root — missing the `Tests` column + `hasTestFile`.** Root `scripts/generateAiCapabilities.mjs` has `hasTestFile` (3×) and the `\| Tests` column (2×); template copy has neither (0/0); 42 diff lines. Template file carries NO `KEEP IN SYNC` marker; no `checkTemplateSync` CI guard. Scaffolded consumers' `docs/AI_CAPABILITIES.md` never shows the per-route Tests indicator CLAUDE.md Rule 12 tells the AI to read. | MEDIUM | open (new) | confirms wave-1 #5 (partial) | 3/3 (r1-w2, r3, r4) | `packages/create-luckystack-app/template/scripts/generateAiCapabilities.mjs` vs root `scripts/generateAiCapabilities.mjs` | Re-sync + add `KEEP IN SYNC` marker + `scripts/checkTemplateSync.mjs` (CI + pre-commit) asserting byte-equality. Note: wave-1 #5's `lintInvariants.mjs` half is now RE-SYNCED (fixed); the `generateAiCapabilities` half is the live residual. **Verified via diff: 42 changed lines, template 0× Tests/hasTestFile.** |
| N6 | **`ls-np/` is a 115-file committed scaffold-output artifact carrying STALE overlay files.** Not in `package.json files`, referenced by no `src`/`scripts`. Still ships the OLD overlay layout `template/` has dropped: `ls-np/luckystack/login/oauthProviders.ts`, `userAdapter.ts`, `sentry/init.ts`, `posthog.ts`, `email/init.ts`. Repo bloat + an AI/dev trap (a second "canonical-looking" template surface that drifts). | MEDIUM | open (new) | — (0/6) | 2/3 (r1-w2, r3) | `packages/create-luckystack-app/ls-np/` (115 tracked files) | Delete `ls-np/` + add to `.gitignore`; regenerate in CI into a temp dir if a reference is wanted. **Verified: 115 tracked files; the 5 stale overlay files exist under `ls-np/luckystack/`.** |
| N7 | **Next-steps + README tell SQL users to run `npm run prisma:migrate:dev` — a script that does not exist.** `src/index.ts:1612` prints it for any non-mongodb provider; `template/README.md:13` hardcodes it; `template/package.json` defines only `prisma:generate` + `prisma:db:push`. 3 of 4 DB choices get a broken first command (`Missing script`). | MEDIUM | open (new) | — (0/6) | 1/3 (r3) | `src/index.ts:1612`; `template/README.md:13`; `template/package.json:28-29` | Add `"prisma:migrate:dev": "dotenv -e .env.local -- prisma migrate dev"` to template `package.json` (docs/post-scaffold-suggestions.md confirms `migrate dev` is the intended SQL command). **Verified all three.** |
| N8 | **CI calls `npm run test:e2e` (undefined) gated on `@vercel-labs/agent-browser` (never installed).** `template/.github/workflows/ci.yml:55-56` guards on `node_modules/@vercel-labs/agent-browser/package.json` then runs `npm run test:e2e`; `wireAiBrowserTooling('all')` installs `@playwright/test` (different marker), and `test:e2e` is NOT in `template/package.json`. Guard always false → generated Playwright spec never runs; if it passed it would crash on the missing script. | MEDIUM | open (new) | — (0/6) | 1/3 (r3) | `template/.github/workflows/ci.yml:55-56`; `test:e2e` absent from `template/package.json` | Add `"test:e2e":"playwright test"`; change CI guard to a real marker (`node_modules/@playwright/test` or `tests/e2e`); keep `.gitlab-ci.yml` in sync. **Verified: guard + missing script both confirmed.** |
| N9 | **Overlay-doc twin drift: docs point at `luckystack/login/oauthProviders.ts` + `userAdapter.ts` that no longer exist in `template/`.** OAuth/login wiring moved into `@luckystack/login/register` (env self-wiring); `template/luckystack/` now ships only `core/i18n/server`. But `_dot_env_dot_local_template:26`, `template/README.md:94-95`, and `prisma/schema.prisma:3` still breadcrumb to the dropped files. | MEDIUM | open (new) | — (0/6) | 1/3 (r3) | `template/_dot_env_dot_local_template:26`; `template/README.md:94-95`; `template/prisma/schema.prisma:3` | Update breadcrumbs to the `/register` self-wiring + env-enable path. **Verified: `template/luckystack/` = `core/i18n/server` (no `login`); all three doc refs present.** |

---

## OPEN — KNOWN (wave-1 already had it; still present in current tree)

| # | Finding | Severity | Status | Wave1 | Wave2 | Location | Note/Recommendation |
|---|---------|----------|--------|-------|-------|----------|---------------------|
| K1 | `pruneOptionalPackages` ~220-line god-function; three prunes driven by verbatim template-source literals; `editScaffoldFile` `throw`s on `occurrences !== 1` → any whitespace/format change in a template file silently breaks a non-default prune until a smoke scaffold. The #1 AI-maintainability hazard. | MEDIUM | open (known) | 5/6 | 3/3 | `src/index.ts:1177-1396` (current); `editScaffoldFile` 1146-1171 | Split `prunePresence`/`pruneAuth`/`pruneI18n`; sentinel-comment markers (`/*ls:prune-start auth*/`) instead of literal blocks; CI matrix over `--auth=none`/`--no-presence`/`--no-i18n`. |
| K2 | `main()` ~220-line god-function; three inline provider lookup tables (`USER_ID_ATTRS_BY_PROVIDER`, `DATABASE_URL_BY_PROVIDER`, `OAUTH_PROVIDER_ORIGINS`) rebuilt per call. | MEDIUM | open (known) | 4/6 | 3/3 | `src/index.ts:1398-1618`; tables ~1452/1458/1472 | Hoist tables to module scope; extract `validateAndResolveTarget`/`buildTemplateVars`/`copyFrameworkDocs`/`runPostInstall`. |
| K3 | Unguarded `JSON.parse(fs.readFileSync(...))` across 5 pkg.json/.mcp.json mutators — opaque `SyntaxError` with no file name. Not exploitable (files freshly written this run); diagnosability gap, compounded by N4 (no cleanup). | MEDIUM | open (known) | 5/6 | 3/3 | `src/index.ts:969, 999, 1014, 1078, 1131` (`readSelfVersion` 677 is the only guarded reader) | One `readJson(path)`/`mutatePackageJson(targetDir,fn)` helper wrapping parse with the path in the message. **Verified all 5 sites.** |
| K4 | All three MCP servers wired `@latest` (`@playwright/mcp@latest`, `chrome-devtools-mcp@latest`, **and now `@luckystack/mcp@latest`**) — unpinned supply chain. Opt-in + `permissions.ask`-gated. The wave-1 "pinned `@luckystack/mcp` counter-example" no longer exists — drift made it WORSE (uniform `@latest`). | MEDIUM | open (known) | 4/6 | 3/3 | `src/index.ts:1124-1125` (browser), `1572` (`@luckystack/mcp@latest`) | Pin to `^${luckystackVersion}` (already in hand for first-party) or document the deliberate exemption in a `//?`. **Verified 1572 = `@latest`.** |
| K5 | `set -e` pre-commit AI-index hook runs the full `ai:*` chain; first generator to exit non-zero (e.g. commit before `npm install` populates `node_modules`) hard-blocks the commit, contradicting the CLAUDE.md "backstop, not a gate" contract. Only guard is `command -v npm`. | MEDIUM | open (known) | 1/6 (r4 only, wave-1) | 3/3 | `src/index.ts:930-955` (`set -e` at 934, `ai:lint` 940, `ai:graph` 952) | Wrap each non-lint generator `… || echo "[pre-commit] skipping"`; keep only `ai:lint` blocking; add a "node_modules missing → skip + hint" guard. **Verified set -e + only `command -v npm` guard.** |
| K6 | `copyTree` follows symlinks: branches only on `entry.isDirectory()`, so a symlink dirent falls into the `copyFileSync`/text branch and is dereferenced. Latent (trees are package-controlled, symlink-free today). | LOW | open (known) | 4/6 | 3/3 | `src/index.ts:882-900` (888 `isDirectory()` only); docs-copy reuses it 1543 | One-line `if (entry.isSymbolicLink()) continue;`. wave-1 had a Medium/Low split; recorded LOW (latent, no symlinks today). **Verified 888.** |
| K7 | i18n boolean asymmetry: `i18n: answers.i18n === 'Yes'` (default OFF on garbled input) while `presence`/`aiInstructions` use `!== 'No'` (default ON) — contradicts `DEFAULT_CHOICES` (all true). Latent (normal flow always supplies `'Yes'`/`'No'`). | LOW | open (known) | 4/6 | 3/3 | `src/index.ts:555` (i18n) vs `554`/`556` | `i18n: answers.i18n !== 'No'` or comment the deliberate asymmetry. **Verified 554-556.** |
| K8 | Repeated read-parse-mutate-write package.json across `installAiIndexHook`/`injectOptionalDeps`/`dropDependency`/`wireAiBrowserTooling` — 4 uncoordinated full-file rewrites; a future parallel writer would clobber a sibling. | LOW | open (known) | 3/6 | 2/3 | `src/index.ts:967-972, 997-1007, 1012-1019, 1129-1134` | Consolidate into one `mutatePackageJson(targetDir,fn)` (also closes K3). |
| K9 | `pickFromList` non-TTY/piped fallback returns `match ?? defaultValue` SILENTLY on an unrecognized answer — pipes the DEFAULT db (e.g. `postgres`→mongodb) with no diagnostic. The `--db=` flag path correctly `exit(2)`. (wave-2 r3 notes the prior F-18 warning is GONE → now silent, no longer "warn-then-proceed".) | LOW | open (known) | 2/6 | 3/3 | `src/index.ts:247-249` (`return match ?? defaultValue`) | In non-TTY mode treat an unrecognized non-blank answer as `exit(2)` (mirror `parseValueFlag`); keep blank→default. **Verified 249.** |
| K10 | `editScaffoldFile` silent no-op when target file ABSENT (`if (!fs.existsSync(filePath)) return;`) — its own doc-comment promises it "throws loudly … so template drift surfaces"; a renamed/removed template file makes prune edits vanish silently. | LOW | open (known) | 1/6 | 1/3 (r1-w2) | `src/index.ts:1148` (current) | Thread an `optional` flag so deliberately-maybe-absent targets opt into the no-op; rest throw on missing. |
| K11 | dep injectors / hook installer silently no-op when `package.json` absent (`if (!fs.existsSync(pkgPath)) return;`) — a broken template copy means a selected dep is never written; user finds out at build time. | LOW | open (known) | 1/6 | 1/3 (r1-w2) | `src/index.ts:968, 998, 1013` | Log a warning instead of swallowing. |
| K12 | `isTextFile` treats any post-rename dotfile basename as text → a binary dotfile would be UTF-8-read + run through `replacePlaceholders`, corrupting it. Latent (no such file today). | LOW | open (known) | 1/6 | 1/3 (r1-w2) | `src/index.ts:873-880` | Keep the extension allow-list primary; only treat extensionless dotfiles as text. |
| K13 | TOCTOU between `existsSync(targetDir)` guard and first `mkdirSync`. Benign single-user CLI (no privilege boundary); racing scaffolds merge rather than refuse. | LOW | open (known) | 2/6 | 1/3 (r1-w2) | `src/index.ts:1430-1433` (guard), copy 1504 | Optional: `mkdirSync(dir,{recursive:false})` + treat `EEXIST` as "already exists". |
| K14 | Mutator helpers (`copyTree`/`pruneOptionalPackages`/`editScaffoldFile`/`injectOptionalDeps`/`wireAiBrowserTooling`/framework-docs copy) have ZERO unit coverage — exercised only via the full `scaffold:test` smoke run. Highest-leverage AI-safety gap: green unit run while a non-default scaffold is broken. | LOW | open (known) | 4/6 | 3/3 | `src/index.test.ts:9-19` (imports only pure helpers) | Temp-dir integration test scaffolding each major choice combo; assert no `editScaffoldFile` throw + expected files present. Turns the K1 prune-drift class into a caught failure. |
| K15 | `pickFromList`/`convertAnswersToChoices` vs `normalizeChoices` cross-field invariants (oauth-only-under-oauth, browser-rides-on-ai) are hand-synced across the prompt vs `--no-prompt` paths; a new rule must be added in both or they drift. | LOW | open (known) | 1/6 | 0/3 | `src/index.ts` (`convertAnswersToChoices` ~556-561, `normalizeChoices` ~607) | Extract a shared `applyChoiceInvariants` both paths call. (wave-1-only; no wave-2 corroboration but still present.) |
| K16 | Regex export extractors in `generateProjectIndex.mjs` miss valid TS forms (multi-line `export const httpMethod =`, `satisfies`, `export * from`) → the AI-facing index can mislabel a route's method. Lossy-by-design, invisible to an AI treating the table as authority. | LOW | open (known) | 1/6 | 0/3 | `scripts/generateProjectIndex.mjs:~133-200` | Document recognized forms in the generated header, or reuse `generateGraph.mjs`'s `ts.Program` TypeChecker. (wave-1-only; not re-checked line-exact this pass — carried forward.) |

### Additional wave-2-only LOW findings (real in current tree)

| # | Finding | Severity | Status | Wave1 | Wave2 | Location | Note/Recommendation |
|---|---------|----------|--------|-------|-------|----------|---------------------|
| K17 | **Five template vars are DEAD + CLAUDE.md documents them as substituted (doc lie).** `vars` sets `AUTH_MODE`, `OAUTH_PROVIDERS`, `EMAIL_PROVIDER`, `MONITORING_PROVIDER`, `I18N_ENABLED`, but `grep` of `template/` shows NONE of them appear as `{{…}}` tokens (used tokens: `DATABASE_URL, DB_PROVIDER, EMAIL_ENV_VARS, EXTERNAL_ORIGINS, LUCKYSTACK_VERSION, MONITORING_ENV_VARS, OAUTH_ENV_VARS, PROJECT_NAME, PROJECT_TITLE, USER_ID_ATTRS`). The package `CLAUDE.md` "Template variables" lists all five as substituted. | LOW | open (new) | — (0/6) | 1/3 (r1-w2) | `src/index.ts:1492-1500`; `packages/create-luckystack-app/CLAUDE.md` "Template variables" | Remove the 5 dead vars + doc rows, OR add the tokens to template files. Make code + doc agree. **Verified: grep of `template/` shows none of the 5; CLAUDE.md lists all 5.** |
| K18 | Prisma `enum LANGUAGE { en nl de fr }` left untouched by the `--no-i18n` prune (which strips nl/de/fr locales + collapses the picker to `['en']`). Cosmetic/consistency — a single-language scaffold ships a schema contradicting its own locale set. | LOW | open (new) | — (0/6) | 1/3 (r3) | `template/prisma/schema.prisma:16-21` | Add a `schema.prisma` edit to the `!i18n` prune branch reducing `LANGUAGE` to `{ en }`. **Verified enum body still `en nl de fr`.** |
| K19 | Template `src/` uses arbitrary `text-white` (Rule 14 / `luckystack.invariants` forbid non-token colors) in `reset-password/page.tsx`, `settings/page.tsx`, `settings/confirm-email/page.tsx`, `ErrorPage.tsx`, `LoginForm.tsx` (+ `Avatar.tsx`, `ConfirmMenu.tsx`). The scaffold installs `ai:lint`, so the first `npm run`/commit flags the code it just generated. | LOW | open (new) | — (0/6) | 1/3 (r3) | `template/src/...` (7 files, e.g. `settings/page.tsx`) | Replace `text-white` on accent backgrounds with `text-common-primary`; verify `ai:lint` clean on a fresh scaffold. Note: confirm whether these are inside `// luckystack-allow` blocks before bulk-editing. **Verified 7 template files contain `text-white`.** |
| K20 | CI uses `npm ci --prefer-offline` but the scaffold ships no `package-lock.json` (created by post-scaffold `npm install`). A user who pushes before `npm install` (or `--no-install`) gets a red CI on first push. `.gitignore` correctly does NOT ignore the lockfile (intent = commit it). | LOW | open (new) | — (0/6) | 1/3 (r3) | `template/.github/workflows/ci.yml:30` | README note that `npm install` (committing the lockfile) precedes first push, or CI fallback to `npm install` when no lockfile. **Verified `npm ci --prefer-offline` at line 30.** |
| K21 | `.env` is gitignored (`_dot_gitignore:6`) while `_dot_env_template:1` header says "Commit this file as `.env`." Following the instruction, the copied `.env` is silently untracked. Contradictory guidance (defaults exist in `config.ts`, so low impact). | LOW | open (new) | — (0/6) | 1/3 (r3) | `template/_dot_gitignore:6` vs `template/_dot_env_template:1` | Decide policy: un-ignore `.env` (keep only `.env.local` ignored) + keep the guidance, OR change the header. **Verified both: gitignore line 6 = `.env`; env_template line 1 = "Commit this file as `.env`".** |

> `runPrismaGenerate` running bare `npx prisma generate` without `dotenv -e .env.local` (wave-2 r3 SCAF-11, LOW, conf:low) is noted but not tabled separately — at scaffold time `.env.local` does not exist yet, `prisma generate` (client codegen) does not need a live URL, and the failure is caught + logged with a manual-fallback hint. Inconsistency only; carry as an observation, not a defect.

---

## FALSE-POSITIVE

| # | Finding | Why it is not a problem | Wave1 | Wave2 | Location |
|---|---------|------------------------|-------|-------|----------|
| FP1 | wave-1 H5 "stale `ActiveSession` interface in `settings/page.tsx`" | **Already fixed/non-drift in current tree.** wave-2 r4 verified `interface ActiveSession { handle; expiresInSeconds: number\|null; isCurrent }` (settings/page.tsx:36-40) exactly matches the `listSessions_v1` `main()` return shape (`handle=sessionHandle(token)`, `expiresInSeconds=ttl>=0?ttl:null`, `isCurrent=token===user.token`). No drift remains. Recorded as resolved/non-issue, not an open finding. | (wave-1 hint) | refuted by r4 | `template/src/settings/page.tsx:36-40` ↔ `…/_api/listSessions_v1.ts` |

---

## Notes for the parent reconciler

- **Theme:** every scaffolder weakness is hand-synced **drift** (template ⇄ root scripts, template src ⇄ framework src, docs ⇄ dropped overlay files, code vars ⇄ CLAUDE.md), with no machine parity gate. The copier core (path-traversal guard, prototype-pollution-safe `replacePlaceholders`, fail-loud `readSelfVersion`, throw-on-ambiguous `editScaffoldFile`, `PROVIDER_OPTIONS as const`) remains genuinely solid — no security regressions there.
- **The two template-src HIGHs (N1 deleteAccount, N2 ErrorPage) are the standout NEW items** and are exactly the regression class the task flags: the framework's own component was fixed but the verbatim-copied template twin was not. They ship to every consumer by default.
- **N3 (docs-token substitution)** is a NEW copier-side HIGH-impact regression specific to the AI-docs feature — it silently corrupts the very authority docs an AI consumer relies on.
- **Zero FIXED:** there was no `LOW_ANALYSIS/scaffolder.md`, i.e. this session never actioned scaffolder findings; the only wave-1 item that resolved (`lintInvariants.mjs` re-sync, FP1 ActiveSession) did so incidentally, and the headline drift hazards (K1–K5) all persist.
