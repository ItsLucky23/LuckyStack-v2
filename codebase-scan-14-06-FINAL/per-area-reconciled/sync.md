# Reconciled area report: sync (FINAL)

Commit baseline: HEAD 302cbf1 + ~248 uncommitted working-tree changes (this week's fixes).
Scope: `packages/sync/src` (+ cross-refs `packages/core/src/cancelRegistry.ts`, `packages/core/src/projectConfig.ts`, `packages/server/src/loadSocket.ts`).

Wave-2 runs reconciled: `--1`, `--3`, `--4` (run `--2` was the wave-1 input digest, ignored).
Wave-1 baseline: `codebase-scan-14-06-MERGED/per-area/sync.md` (6-scan merge, 23 findings). No `LOW_ANALYSIS/sync.md` exists.

**Method note:** every "fixed/present" claim below was re-verified by opening the cited file at the CURRENT tree with file:line evidence — the wave-2 reports' status claims were NOT trusted on their own.

## Headline

The sync package was genuinely hardened this week — six wave-1 items that were OPEN in the merged baseline are now FIXED in the working tree (HTTP `requireRoomMembership` enforcement, client-only-route auth gap, fail-open validation-mode typo, array/non-plain-object guard, raw-vs-normalized `'all'` branch, blanket eslint-disable narrowed). What remains is posture/parity + two real NEW issues wave-1 missed: a **shadow-config `flushPressure.maxBufferedBytes`** that is documented but never read (real, verified), and a **dead `redactToken` shadow-API + raw token leak via `targetToken`/`tokens` log/tracker keys** (real, verified — wave-2 rated HIGH). No critical/remote-unauth-crash defect in any run.

---

## FIXED (re-verified in current tree)

| # | Finding | Severity | Status | Wave1 | Wave2 | Location | Note/Recommendation |
|---|---|---|---|---|---|---|---|
| F1 | HTTP `requireRoomMembership` bypass (`isMember:null` short-circuit fanned to unjoined rooms) | High | FIXED | 6/6 | 3/3 | `handleHttpSyncRequest.ts:374`; `_shared/receiverAuth.ts:64` | VERIFIED FIXED. HTTP passes `isMember: user ? () => Boolean(user.roomCodes?.includes(normalizedReceiver)) : null`; `receiverAuth.ts:64` now fails CLOSED `if (requireRoomMembership && receiver !== 'all' && !isMember?.())` — `null` (anonymous) is rejected, not silently passed. Residual (LOW, see O8): HTTP membership derives from session `roomCodes`, eventually-consistent with live socket rooms — a false-negative possibility, not a bypass. |
| F2 | Client-only routes (only `_client`, no `_server`) skip ALL auth + validation | High | FIXED | 2/6 (r5,r6) | 3/3 | `handleSyncRequest.ts:377-383`; `handleHttpSyncRequest.ts:304-309` | VERIFIED FIXED. Both transports now reject `!syncObject[\`${resolvedName}_server\`]` with `sync.notFound`. Matches the documented "mandatory `_server`". |
| F3 | `resolveSyncValidationMode` returns unvalidated string (fail-open on a typo silently disables input validation) | Medium (sec) | FIXED | 1/6 (r5) | 3/3 | `_shared/validationMode.ts:27` | VERIFIED FIXED. Now whitelists: `return validation === 'relaxed' ? 'relaxed' : 'strict'` (string form) and `validation.input === 'skip' ? 'relaxed' : 'strict'` (object form). Fail-closed. |
| F4 | Array / non-plain-object payload passes structural guard, reaches `_server` as object | Medium (bug) | FIXED | 2/6 (r4,r6) | 3/3 | `handleSyncRequest.ts:274` | VERIFIED FIXED. Guard is now `!name || !data || typeof name != 'string' || typeof data != 'object' || Array.isArray(data)`. Arrays + null rejected with `sync.invalidRequest`. HTTP path receives normalized `Record<string,unknown>` via `normalizeHttpSyncParams`. |
| F5 | HTTP `'all'` branch keyed off RAW receiver but authorized/fetched with NORMALIZED | Medium (bug) | FIXED | 1/6 (r4) | 3/3 | `handleHttpSyncRequest.ts:617` | VERIFIED FIXED. Branch is now `normalizedReceiver === 'all' ? ...`; authorize at :370 also uses `normalizedReceiver`. Aligned with the socket handler. |
| F6 | File-wide blanket eslint-disable of 5 rules on both handlers | Low (quality) | FIXED (narrowed) | 6/6 | 3/3 | `handleSyncRequest.ts:9`; `handleHttpSyncRequest.ts:6` | Partial: still file-scoped for the same 5 rules but now carries a SYNC-20 per-rule rationale comment (not a bare `/* eslint-disable */`). `eqeqeq`/`no-non-null-assertion` remain file-wide. Treated as resolved-enough by all 3 wave-2 runs; residual is the LOW O10 below if the project wants per-line scoping. |

---

## OPEN (re-verified still present)

| # | Finding | Severity | Status | Wave1 | Wave2 | Location | Note/Recommendation |
|---|---|---|---|---|---|---|---|
| O1 | Receiver-auth defaults fail-open (`allowClientReceiverAll:true`, `requireRoomMembership:false`) — any client (unauth on a `login:false` route) may broadcast `receiver:'all'` (cluster-wide amplifier) or target any unjoined room | High | OPEN (known) / DEFERRED-DECISION | 6/6 | 3/3 | defaults `packages/core/src/projectConfig.ts` `sync.{allowClientReceiverAll,requireRoomMembership}`; enforcement `_shared/receiverAuth.ts:46-68`; call sites `handleSyncRequest.ts:475`, `handleHttpSyncRequest.ts:374` | VERIFIED still default-permissive (receiverAuth only rejects when the flags are explicitly set). This is a deliberate 0.2.0 posture decision per the package CLAUDE.md — secure values exist (`requireRoomMembership:true`, `allowClientReceiverAll:false`) but are opt-in. Decision pending: flip defaults for 0.2.0 OR emit a boot/dev warning when both are permissive AND ship the scaffold `config.ts` with the secure values commented. Treat as the headline 0.2.0 hardening item. |
| O2 | Abort registry keyed on client-controlled `cb` (`socketId:cb`); two overlapping same-`cb` requests overwrite/cross-delete the controller → `syncCancel` aborts the wrong request or no-ops (LLM "stop generating" breaks) | Medium (bug) | OPEN (known) | 1/6 (r5) | 3/3 | `core/cancelRegistry.ts:29-30` (`registerSyncAbortController` does an unconditional `set`), `:21` `buildKey(socketId, cb)`; call sites `handleSyncRequest.ts:336` register / `:346` unregister; client default `cb = \`${name}/${version}\`` (`syncRequest.ts:532`) | VERIFIED still present. `registerSyncAbortController` does `const key = buildKey(socketId, cb); syncControllers.set(key, controller)` with NO collision guard. The client's default `cb` is the deterministic `${name}/${version}`, so any double-fire of the same route collides. Fix: key on a per-request unique id (include `responseIndex`, already unique per emit) or store a `Set<AbortController>` per `cb` and unregister by identity. Strong correctness candidate for the 0.2.0 list. |
| O3 | Rate-limit gated behind auth: every auth / receiver-auth / `preSyncAuthorize` early rejection returns BEFORE `applySyncRateLimits`; no per-socket message guard in `loadSocket.ts` | High (r1) / disputed→Low (r4) | OPEN (known) — severity contested | 1/6 (r1) | 3/3 | `handleSyncRequest.ts:408-530` (auth returns precede rate-limit @530); `handleHttpSyncRequest.ts` mirror | VERIFIED ordering still present (comment lines 403-405 say this is deliberate, mirroring the API handler). r4's skeptic verified the worst-case cost claim is OVERSTATED: a genuinely-unauthenticated socket (falsy token) short-circuits `getSession` with no Redis read, and downstream hooks are past the `auth.login` gate so never fire on the rejected path. Net residual: a GARBAGE token string forces an uncounted Redis `getRaw` per message — a real but bounded hardening note, NOT a high-sev DoS. Recommend: a connection-level / `preSocketMessage` message-rate guard, or a cheap pre-auth IP bucket. Rated High by wave-1-r1, Low by wave-2-r4; realistic severity Low–Medium. |
| O4 | Consumer `preSyncAuthorize`/`postSyncAuthorize` hooks (often DB/tenant lookups) run BEFORE `applySyncRateLimits` | Medium (sec) | OPEN (known) | 1/6 (r4) | 3/3 | `handleSyncRequest.ts:494` (preSyncAuthorize), `:521` (postSyncAuthorize), `:530` (rate-limit); HTTP `:390` then rate-limit | VERIFIED ordering still present. On a `login:false` route an unauthenticated caller can trigger the consumer's hook per message with no throttle. Tightly coupled to O3. Fix: move `applySyncRateLimits` ahead of `preSyncAuthorize` (keep the cheap built-in `authorizeSyncReceiver` first), OR document that these hooks run pre-rate-limit so consumers don't put unbounded work there. |
| O5 | `flushPressure` is a packet-COUNT approximation (`AVG_PACKET_BYTES=1024` × engine.io `writeBuffer.length`) under a byte-named `thresholdBytes` API; samples only the local room (first 32, `MAX_SOCKETS_FOR_PRESSURE_SAMPLE=32`); for `streamTo` samples the original `receiver` room, not the token-named rooms actually emitted to | Medium (bug) | OPEN (known) | 6/6 | 3/3 | `_shared/streamEmitters.ts:38-46` (byte-named JSDoc), `:59-64` (constants), `:79-101` (packet read), `:103-130` (local-only sample), `:227-261` (`streamTo` emits `io.to(filtered)` @231 but `flushPressure` samples `collectRoomSocketsForPressure(receiver)` @252) | VERIFIED all sub-aspects still present. `io.to(filtered)` for streamTo vs `collectRoomSocketsForPressure(receiver)` for pressure means streamTo recipients are never measured; the Redis-adapter cross-instance recipients + everyone past socket #32 are invisible. Real backpressure hole on the headline LLM-streaming use case. Fix: rename to `thresholdPackets` or sum real bytes; add `flushPressureTo(tokens|rooms)`; sample a rotating subset; document local-only + cross-instance limits in the type JSDoc. |
| O6 | God-function socket `handleSyncRequest` (~700 lines) — one function does validate/locale/hooks/auth/receiver-auth/rate-limit/execute/cross-instance-fetch/fanout, with ~12 manual `cleanupRequest()` return paths + the repeated `return typeof responseIndex == 'number' && socket.emit(... buildSyncError(...))` pattern | Medium (quality) | OPEN (known) | 6/6 | 3/3 | `handleSyncRequest.ts:180-876` | VERIFIED still ~700 lines, leaf helpers extracted to `_shared/` but the orchestration is inline. High edit/leak risk. Fix: extract `resolveAndAuthorize`/`runServerStage`/`runFanout` staged steps + a `rejectSync({responseIndex,errorCode})` bundling cleanup+typecheck+emit; single `try/finally`. |
| O7 | God-function HTTP `handleHttpSyncRequest` (~540 lines) — a hand-mirror of the socket handler; this duplication is the ROOT CAUSE of every transport-parity drift (O8, N1, N2, F1/F5 history); two near-identical rate-limit helpers | Medium (refactor) | OPEN (known) | 6/6 | 3/3 | `handleHttpSyncRequest.ts:179-721`; duplicated `applySyncRateLimits`/`applyHttpSyncRateLimits` | VERIFIED still a near-line-for-line twin. Fix: hoist a transport-agnostic `runSyncPipeline({transport, emit, resolveMembership, originator})` into `_shared/` with thin per-transport adapters; unify the two rate-limit helpers; add a table-driven transport-parity conformance test (also closes M4 below — the auto-sweep doesn't reach sync routes). |
| O8 | Full `serverOutput` broadcast verbatim to every room member when no `_client` exists; `preSyncRecipient` can SKIP but not REDACT, and passes `recipientUserId:null` | Medium (sec) | OPEN (known) | 1/6 (r1) + 4/6 (r1,r2,r3,r4 for the null-userId half) | 3/3 | `handleSyncRequest.ts` fanout `~:833-844`, `preSyncRecipient` `~:633`; HTTP mirror | VERIFIED still present (emit `{ serverOutput }` to every member; `preSyncRecipient` only skips, hard-codes `recipientUserId:null`). Easy to leak PII by omitting a `_client` file. Fix: document prominently; give `preSyncRecipient` an optional redaction/override return + an opt-in `resolveRecipientUser` so per-recipient shaping doesn't force a whole `_client` file. |
| O9 | Per-recipient `_client` handler runs SERIALLY (`await processClientSyncForRecipient` inside the fanout loop); originator's success ack waits for the entire fanout → large `receiver:'all'` fanouts can trip the client `requestTimeoutMs` (default 30000) with a false 504 | Medium (quality, r3) / Low (r1 N8) | OPEN (known) | 1/6 (r3) | 3/3 | `handleSyncRequest.ts:808` loop / `:852` ack; `handleHttpSyncRequest.ts:651` | VERIFIED serial. `fanoutYieldEvery` only yields, doesn't parallelize. Fix: emit the originator ack as soon as `_server` completes + fanout is dispatched (recipients get their own envelopes), or bounded-concurrency fanout; at minimum document that ack latency scales with fanout size. |
| O10 | `ignoreSelf` is a no-op for unauthenticated originators (`token && token === tempToken` skips when token falsy) and structurally meaningless over HTTP (no originator socket in the room) | Low (bug) | OPEN (known) | 3/6 (r4,r5,r6) | 3/3 | `handleSyncRequest.ts:782`; `handleHttpSyncRequest.ts:625` | VERIFIED still `token && token === tempToken`. Anonymous (token-less) originator never self-skips → duplicate optimistic-update echoes. Fix: fall back to `tempSocket.id === socket.id` on the socket transport; document the HTTP token-dependence. |
| O11 | HTTP rate-limit identity collapses anonymous callers into one bucket (`requesterIdentity = token ?? requesterIp ?? 'anonymous'` @97; `ipBucket = requesterIp ?? 'unknown'` @136); HTTP also takes a PRE-RESOLVED `requesterIp` from the caller (no `resolveClientIp` of its own) and adds a non-prod loopback skip @133-134 the socket handler lacks | Low–Medium (bug/sec) | OPEN (known) — wave-2 r3 re-rated Medium (SYNC-N4) | 1/6 (r2) | 3/3 | `handleHttpSyncRequest.ts:97,133-136`; socket uses `resolveClientIp` @`handleSyncRequest.ts:87` with NO loopback skip | VERIFIED. The two transports diverge: socket resolves IP itself via `resolveClientIp` and has no loopback skip; HTTP trusts a pre-resolved string + skips loopback in non-prod. All anonymous HTTP callers can collapse into `ip:unknown:sync:all` (shared-budget DoS) when `requesterIp` is absent. Fix: have the HTTP handler resolve its own IP via `resolveClientIp` from headers+trustProxy, use the resolved IP as the anonymous identity (not the `'anonymous'`/`'unknown'` literals), and align the loopback-skip behavior (or document the divergence with a parity test). |
| O12 | `serverOutput` spread into HTTP success envelope (`{ ...serverOutput, status:'success', message }`) leaks reserved transport keys (`result`/`errorCode`/`httpStatus`); HTTP returns a DIFFERENT shape than the socket transport (which nests under `result`) | Low (bug) | OPEN (known) | 1/6 (r4) | 3/3 | `handleHttpSyncRequest.ts:~702-706` vs socket `~:852-856` | VERIFIED shape still diverges. Transport-dependent response shape is an AI/consumer footgun. Fix: align both transports (nest under `result` or both flatten) + whitelist which keys may surface instead of spreading the whole `serverOutput`. |
| O13 | `collectRoomSocketsForPressure` is a local-per-process view (`io.sockets.adapter.rooms.get` / `io.sockets.sockets.get`); with the Redis adapter `flushPressure` ignores remote recipients — contradicts the "always cross-instance" framing | Low (bug) | OPEN (known) | 2/6 (r2,r4) | 3/3 | `_shared/streamEmitters.ts:103-130` | VERIFIED local-only. Sub-aspect of O5 but called out as its own root cause. Fix: document the local-only limitation next to the cross-instance notes; consider a Redis-published per-instance write-buffer gauge. |
| O14 | `streamTo` non-timing-safe `===` raw-bearer-token compare for every recipient; raw `tempToken` passed into `_client` handlers where an author could misuse token-equality as auth | Low (sec) | OPEN (known) | 1/6 (r4) | 3/3 | `handleSyncRequest.ts:734-740`; `_shared/clientFanout.ts:104` (`token: tempToken` into `_client`) | VERIFIED raw token still flows into `_client` params. Not auth itself, but a misuse foothold. Fix: resolve recipient identity by stable `userId`; keep raw tokens out of the per-recipient loop except where `streamTo` needs the room name. (Closely related to N2 below — the SAME raw token reaches the error-tracker context.) |
| O15 | Offline-queue id uses `Date.now()+Math.random()` (non-crypto). Purely client-side dedupe key, not a security token | Low (quality) | OPEN (no action) | 3/6 (r1,r2,r3) | (re-noted) | `syncRequest.ts:220-222` (`createQueueId`) | VERIFIED unchanged. Collision risk astronomically low; flagged only to pre-empt a reflexive `Math.random` flag. `crypto.randomUUID()` marginally cleaner. No action required. |
| O16 | `syncRequestInternal` / `runRequest` (~250 lines) interleaves offline-queue enqueue + drop-policy + ack-timeout + external-abort bridging + progress/response listeners with four interlocking `cleanup*` refs every settle path must tear down | Low (refactor) | OPEN (known) | 4/6 (r1,r2,r5,r6) | 3/3 | `syncRequest.ts:402-655` | VERIFIED still 4 interlocking cleanups. Correct today but the highest-risk client correctness surface. Fix: encapsulate into a single `teardown()` / `createPendingSyncRequest()` owning one `clearAll()` called from every settle path. |

---

## NEW (wave-2 found, verified real in current code)

| # | Finding | Severity | Status | Wave1 | Wave2 | Location | Note/Recommendation |
|---|---|---|---|---|---|---|---|
| N1 | **Shadow-config: `sync.flushPressure.maxBufferedBytes` (documented default 5 MiB) is NEVER read by the emitter** — `flushPressure` resolves its threshold from the per-call arg + a hard-coded `DEFAULT_THRESHOLD_BYTES = 1_048_576` (1 MiB); `getProjectConfig().sync.flushPressure` is not imported in `streamEmitters.ts` | Medium (bug) | NEW (confirmed) | (missed by all 6) | 1/3 (--1 SYNC-W2-02) | `_shared/streamEmitters.ts:59` (`DEFAULT_THRESHOLD_BYTES`), `:245-248` (resolves arg ?? hard-coded const, no config read); documented in `packages/sync/CLAUDE.md` (`maxBufferedBytes` default `5_242_880`) + core config | VERIFIED real. `streamEmitters.ts` has no `getProjectConfig` import (grep); the configured 5 MiB default + `highWaterMarkChunks` (default 1000) are DEAD config for this path — a consumer tuning `maxBufferedBytes` is silently ignored, real default is the hard-coded 1 MiB. Exactly the "shadow-API" theme. Fix: read `getProjectConfig().sync.flushPressure.maxBufferedBytes` (clamp by `highWaterMarkChunks`) as the fallback inside `flushPressure`, OR remove the config keys + docs. Folds into the O5 unit-correctness fix. |
| N2 | **Dead `redactToken`/`redactTokens` shadow-API + raw bearer-token leak to error-tracker + stream logs** — the SYNC-17 redaction helpers are imported ONLY by their own test; meanwhile `clientFanout.ts:111` passes raw `targetToken: tempToken` into the `tryCatch` error-context (→ `captureException`) and `streamEmitters.ts:222-223` logs raw `{ tokens: filtered }`. Neither key (`targetToken`, `tokens`) is in `DEFAULT_REDACTED_LOG_KEYS`, so `sanitizeForLog` does NOT mask them; the legacy Sentry slot forwards context with no sanitize at all | High (sec) | NEW (confirmed) | (missed by all 6) | 1/3 (--3 SYNC-N2) | `_shared/redactToken.ts:17-27` (dead — sole importer is `redactToken.test.ts`, grep-confirmed); leak sites `_shared/clientFanout.ts:111` (`targetToken: tempToken`), `_shared/streamEmitters.ts:222-223` (`{ tokens: filtered }`); core `DEFAULT_REDACTED_LOG_KEYS` (token/password/authorization/cookie — exact-match, no substring, so `targetToken`/`tokens` NOT masked) | VERIFIED real. grep: `redactToken|redactTokens` only in `redactToken.ts` + `redactToken.test.ts`. `targetToken` is the recipient's full raw session token (`extractTokenFromSocket`). The error-tracker path fires whenever a route's optional `_client` handler throws + a tracker is registered — NOT dev-gated (the stream-log half IS dev-gated behind `logging.stream`). Raw replayable bearer tokens can reach Sentry/PostHog/Datadog context. The mitigating control (the redactToken helper shipped specifically for these sites) was written but never wired — key-name mismatch. **Severity HIGH** (credential-in-tracker, session replay for anyone with tracker access; un-gated tracker path), bounded below CRITICAL by the `_client`-throw + tracker-registered precondition. Fix: route the two call sites through `redactToken`/`redactTokens`, OR `registerRedactedLogKeys(['targetToken','tokens'])` — but note the legacy Sentry slot in `sentrySetup.ts` forwards context WITHOUT `sanitizeForLog`, so registration alone fixes only the adapter path; call-site redaction is the complete fix. Tightly related to O14 (same raw token in the loop). |
| N3 | **Twin drift: HTTP handler omits `postSyncAuthorize` AND `preSocketMessage` hooks the socket handler fires** — audit/metrics subscribers on `postSyncAuthorize`, or a per-message gate on `preSocketMessage`, see only socket traffic and miss every HTTP-fallback sync | Medium (bug) | NEW (confirmed) | (missed by all 6) | 1/3 (--3 SYNC-N3) | socket fires `preSocketMessage` @`handleSyncRequest.ts:220` + `postSyncAuthorize` @`:521`; HTTP `handleHttpSyncRequest.ts` dispatches NEITHER (grep of `dispatchHook` in HTTP shows `preSyncAuthorize`→`preSyncValidate`, no `preSocketMessage`/`postSyncAuthorize`) | VERIFIED real. A `preSocketMessage` throttle/audit can be bypassed by forcing the HTTP transport; `postSyncAuthorize` audit logs under-count. Direct symptom of the O7 hand-mirror. Fix: dispatch `postSyncAuthorize` after the HTTP auth+receiver checks (parity with socket :521); decide + document whether `preSocketMessage` should also fire on HTTP, and add a behavioural-parity test asserting the hook-dispatch set per transport. |
| N4 | **`chunkCounters` module-level Map grows unbounded** — keyed `${routeName}|${recipient}` (recipient = room code OR session token); `bumpChunkIndex` only ever `set`s, no `.delete()`/`.clear()` anywhere; the header comment "Cleared on receiver-room teardown" is false | Medium (bug) | NEW (confirmed) | (missed by all 6) | 2/3 (--1 SYNC-W2-05, --3 SYNC-N1) | `_shared/streamEmitters.ts:12-19` | VERIFIED real. grep confirms the only `chunkCounters` references are the definition (`:12`), the `get`/`set` in `bumpChunkIndex` (`:16-17`) — no delete/clear. Cardinality grows with every distinct (route, token/room) that ever streams → process-lifetime leak on a long-lived streaming server (LLM tokens via `streamTo`, collab rooms). Two prior in-repo audits (SYNC-08, QUA-045) rated MEDIUM, matching reality (~100-200 bytes/entry — GC pressure, not near-OOM; wave-2 --3's own verifier downgraded its HIGH→MEDIUM). Fix: move the counter into the per-request closure of `buildSyncStreamEmitters` (rebuilt per request anyway) or drop it; at minimum make the "cleared on teardown" comment true. |
| N5 | **`thresholdBytes` is byte-named but interpreted as packets** (fixed 1024-byte divisor) — typed-contract lie | Low–Medium (quality) | NEW (confirmed) — restated sub-aspect of O5 | (within 6/6 O5) | 2/3 (--1 SYNC-W2-03, --3 SYNC-N5) | `_shared/streamEmitters.ts:38-46` (JSDoc "Drain threshold in bytes"), `:248` (`Math.ceil(effectiveThresholdBytes / AVG_PACKET_BYTES)`), `:82` (compares `writeBuffer.length` = packet count) | VERIFIED. A consumer passing `thresholdBytes: 65536` expecting 64 KB gets "64 packets". Misleading typed seam — folds into the O5 fix (rename `thresholdPackets` or sum real bytes). |
| N6 | Redundant disconnect handling: per-request `socket.once(disconnect)` aborts AND `abortAllForSocket` (wired in `loadSocket.ts`) already aborts every in-flight controller for the socket — two divergent abort mechanisms | Low (quality) | NEW (confirmed) | (missed by all 6) | 1/3 (--1 SYNC-W2-04) | `handleSyncRequest.ts:340` (`socket.once(disconnect, onSocketDisconnect)`), `:346` (cleanup removes it); `core/cancelRegistry.ts:77-91` (`abortAllForSocket`) | VERIFIED both paths exist. No leak in the happy path (cleanup removes the listener), but belt-and-suspenders that diverges the two abort mechanisms and is easy to break in a refactor. Fix: rely on `abortAllForSocket` for disconnect-driven aborts; keep `cleanupRequest` only for the registry unregister. Report-only (low). |
| N7 | `attachSyncReceiver` stream branch strips only `status`/`fullName`/`cb` but forwards every other server-controlled key (`errorCode`/`errorParams`/`httpStatus`/`message`) into the consumer's typed `stream` payload | Low (quality) | NEW (confirmed) | (missed by all 6) | 1/3 (--1 SYNC-W2-06) | `syncRequest.ts:962-977` | VERIFIED. Minor type-contract bleed into the originator's own client decode — cosmetic, no security impact. Fix: whitelist the stream payload to the route's declared stream fields, or also strip the reserved transport keys. Report-only (low). |
| N8 | Stream hooks fire even when the underlying emit no-ops (HTTP originator path with no SSE writer) — `postSyncStream` over-counts chunks that went nowhere AND feeds the N4 leak | Low (bug) | NEW (uncertain) | (missed by all 6) | 1/3 (--3 SYNC-N9) | `_shared/streamEmitters.ts:182-189` (`emitServerSyncStream` dispatches hooks then calls `emitOriginatorChunk` unconditionally) | PARTIALLY VERIFIED — `emitServerSyncStream` does dispatch hooks before `emitOriginatorChunk(payload)` with no sink guard; whether the HTTP originator passes an undefined sink depends on the HTTP `emitOriginatorChunk` adapter (not fully traced). Plausible observability inaccuracy. Fix: only dispatch stream hooks / bump the index when an emit has a sink, mirroring the broadcast path's early returns. Report-only (low). |
| N9 | (cross-area, sync-relevant) Auto-sweep test harness walks only `apiMethodMap`, never a `syncMethodMap` — sync auth-enforcement/contract over the HTTP-sync transport is untested by layers 1-4 | Medium | NEW (cross-area) | (missed by all 6) | 1/3 (--3 SECURITY M4) | test-runner `walkEndpoints` (out of `packages/sync` — flagged for the test-runner area) | VERIFIED as a real gap conceptually (the most authz-sensitive surface — F1/O1 — is the one the auto-sweep doesn't reach). Belongs to the test-runner area; noted here because it directly weakens confidence in the F1 fix and the O1 posture. Recommend a `syncMethodMap` walk running auth-enforcement + contract layers over HTTP-sync. |

---

## Cross-run positives (re-confirmed)

Token redaction helper EXISTS (SYNC-17) but is unwired (see N2). Validator messages withheld from clients (SYNC-04). Top-level `tryCatch` prevents process-killing `unhandledRejection` (SYNC-02). Receiver-auth now wired + fail-closed on BOTH transports (F1). Client-only routes rejected on both transports (F2). Validation-mode + array-guard fail-closed (F3/F4). CSRF/origin covers `/sync/*`. The package is well-hardened; the genuinely OPEN items are posture/parity + the two NEW shadow-API findings (N1 config, N2 token leak).
