# Reconciled area report: root-src (consumer demo `src/`)

> Wave-2 inputs: run-1 (`per-area/root-src.md`), run-3 (`per-area/root-src (consumer app).md`), run-4 (`per-area/root-src.md`) + top-level SECURITY/BUGS/CODE_QUALITY/REFACTOR rows. Wave-1 baseline: the 6-scan merge `codebase-scan-14-06-MERGED/per-area/root-src.md` (no `LOW_ANALYSIS/root-src.md` exists). All FIXED/OPEN verdicts re-verified against the CURRENT working tree (HEAD 302cbf1 + ~248 uncommitted) with file:line evidence — no report's fixed/present claim was trusted.

Scope: consumer-facing `src/` of the LuckyStack repo (auth/reset/settings/admin/playground API + sync handlers, `_functions/`, `_providers/`, `_sockets/`, `_components/`, `main.tsx`, `page.tsx`) + the shipped `packages/cli/assets/login/src/` twin where drift is the finding.

Net picture: this week's pass genuinely closed most of the wave-1 client/template-edge Highs (Sentry masking, `?token=` query adoption, ErrorPage stack leak, the `common.404` i18n typo, `updateSession` raw-parse + avatar double-`?v=`, the playground unauth stream + arbitrary-recipient email, the `MiddlewareResult` union). What remains: the credentials self-delete is STILL broken end-to-end, the three session handlers still hand-build Redis keys (multi-tenant/`projectName`-config divergence), the consumer `src/` settings copies have drifted further behind the hardened CLI asset, and the long i18n/token-cosmetic tail.

---

## FIXED (re-verified in current tree)

| # | Finding | Severity | Status | Wave1 | Wave2 | Location | Note/Recommendation |
|---|---|---|---|---|---|---|---|
| F1 | Sentry Session Replay masking now ON (matches the comment) + `beforeSend` strips `token`/`code` URL params | High | FIXED | 6/6 (#1) | 3/3 | `src/_functions/sentry.ts:62-63` (`maskAllText:true`,`blockAllMedia:true`), `:80-89` beforeSend | Verified true. No PII bleed into replays from this copy. |
| F2 | Root `page.tsx` `?token=` query-string session adoption removed | High | FIXED | 4/6 (#2) | 3/3 | `src/page.tsx:13-26` — no token branch; a `//?` comment forbids re-adding it; handoff lives only in `main.tsx` | Fragment-only design is now the single source of truth. |
| F3 | `ErrorPage` gates `error.stack` behind DEV | High | FIXED | 3/6 (#4) | 3/3 | `src/_components/ErrorPage.tsx:37` `errorDetails = import.meta.env.DEV ? (error.stack ?? null) : null` | No prod path/stack disclosure from this component. |
| F4 | `LoginForm` malformed i18n key `common/.404` | Low | FIXED | 3/6 (#14) | 3/3 | `src/_components/LoginForm.tsx:122` now `key: 'common.404'` | Verified. |
| F5 | `updateSession` raw `JSON.parse` (no tryCatch) + stale-socket closure | Medium | FIXED | 4/6 (#11) | 3/3 | `SessionProvider.tsx:124` parse in `tryCatch`; effect deps `[socket]` (:115) | Both halves fixed. |
| F6 | Avatar `?v=` double-append on re-emit (`...?v=1?v=2`) | Low | FIXED | 1/6 (#17) | 3/3 | `SessionProvider.tsx:134` strips existing `/[?&]v=[^?&]*/g` before re-stamping | Display-side double-append closed. (Upstream upload-bytes `?v=` is a separate still-open item — see O11.) |
| F7 | Playground `streamToToken` unauthenticated + uncapped recipients | Medium | FIXED | 3/6 (#8) | 3/3 | `streamToToken_server_v1.ts:36-38` `login:true`; `:42` `MAX_TARGETS = 10` | Residual (caller may-address-targets) is a documented dev fixture. |
| F8 | `testEmail_v1` arbitrary-recipient real email send outside dev | Medium | FIXED | 1/6 (#9) | 3/3 | `testEmail_v1.ts:35-37` hard-refuses when `!dev` | Verified. |
| F9 | `MiddlewareResult` type forbids `return undefined` (navigate-back) | Medium (refactor) | FIXED | 1/6 (#26) | 3/3 | `packages/core/src/middlewareRegistry.ts:18-21` union now includes `| undefined` | `admin/page.tsx`'s `return;` is type-valid. |
| F10 | Null-deref class on attacker-controlled fields — mitigated by generated zod input schemas (per run-4) | Low | FIXED (mitigated) | 1/6 (#13 partial) | 1/3 | `src/_sockets/apiInputSchemas.generated.ts` enforces required `z.string()` under `validation.runtimeMode:'enforce'` (default) | The raw `.trim()`/`.length` reads remain (see O12) but the validation stage rejects malformed shapes first under default config. Listed FIXED for the realized-risk path; the defensive guard is still nominally open as Low. |
| F11 | `signOutEverywhere` CLI asset logged caller out of own device | Medium | FIXED | — (NEW in wave2 run-3 DRIFT-02) | 1/3 | `packages/cli/assets/login/src/settings/_api/signOutEverywhere_v1.ts:23` now `revokeUserSessions(user.id, user.token)` | The wave-2 run-3 DRIFT-02 claim is stale: the asset already passes `exceptToken`, matching the consumer copy (`src/settings/_api/signOutEverywhere_v1.ts:23`). |

---

## OPEN (real + still present)

| # | Finding | Severity | Status | Wave1 | Wave2 | Location | Note/Recommendation |
|---|---|---|---|---|---|---|---|
| O1 | **Credentials self-delete broken end-to-end** — delete-confirm dialog never collects/sends a password, but `deleteAccount_v1` requires one for password accounts → every credentials account gets `login.wrongPassword`. OAuth accounts delete fine (masks the bug). GDPR right-to-erasure failure. NO regression test for the real UI flow. | High | OPEN (known) | 4/6 (#7) | 3/3 | `src/settings/page.tsx:271-289` (sends only `{confirmation:'DELETE'}`), `deleteAccount_v1.ts:29-33` (requires `data.password`), `ConfirmMenu.tsx:11,27` (`resolve: (confirmed: boolean)`, `resolve(true)` discards typed value), `menuHandler.ts:56` (`Promise<boolean>`) | Highest-impact live defect in the area. Add a password field to the credentials delete flow (extend `ConfirmMenu`/`menuHandler.confirm` to return the typed value) + send `data.password`; add a `deleteAccount_v1` happy-path UI regression test. Present in the CLI asset too (asset has the hardened handler but the same passwordless page). |
| O2 | **Session-management handlers hand-build Redis keys with `process.env.PROJECT_NAME`**, bypassing `formatKey`/`sessionKeyFor`/`activeUsersKeyFor`. Byte-identical under default config; silently divergent when project name is set via `session.projectName` config (env unset → falls back to `'luckystack'`) OR a `registerRedisKeyFormatter` (multi-tenant) is installed → `listSessions` empty, `revokeSession` can't resolve, `deleteAccount` leaves a stale activeUsers set. | High (Medium per wave2) | NEW (confirmed real) | 1/6 (partial #6 "misses key-builder") | 2/3 (run-1 ROOTSRC-02, run-3 DRIFT-01) | `listSessions_v1.ts:20,23,30,32`; `revokeSession_v1.ts:21,32`; `deleteAccount_v1.ts:20,38` — all `const PROJECT_NAME = process.env.PROJECT_NAME ?? 'luckystack'` + template literals | Import `sessionKeyFor`/`activeUsersKeyFor` from `@luckystack/login` (the CLI asset `listSessions_v1.ts:3,32,38` already does); delete the local `PROJECT_NAME`. Rated High here (real multi-tenant-unsafe code + it's the example AI clones). Closes the key half of O3. |
| O3 | **Consumer `src/settings/_api/*` drifted behind the hardened CLI asset** — `deleteAccount_v1` reaches past `getUserAdapter()` to `prisma.user.delete`, hardcodes activeUsers key, skips pre/postAccountDelete hooks + avatar unlink; `listSessions`/`revokeSession` use `id` (64-char) while the asset uses `handle` (16-char) → incompatible API shapes. Consumer `src/` is the regressed branch (asset == template == correct). No cross-tree parity gate covers `src/`. | High | OPEN (known) | 1/6 (#6) | 2/3 (run-3 DRIFT-01, run-4 #2) | consumer `src/settings/_api/{deleteAccount,listSessions,revokeSession}_v1.ts` vs `packages/cli/assets/login/src/settings/_api/*` (asset `deleteAccount_v1.ts:52-66` uses adapter+hooks+unlink; `listSessions_v1.ts:43` returns `handle`) | Re-sync the consumer `src/` copies from the asset (asset is the corrected side); add `ai:check-template-drift` CI that diffs `packages/cli/assets/login` against repo `src/`. Realized end-user blast radius is limited (shipped asset+template are correct) but the in-repo dogfood app is genuinely insecure/regressed. |
| O4 | `LoginForm` gates success on `!response.status` instead of `=== true` — credentials endpoint returns a real boolean today, but a framework error envelope `{status:'error'}` (truthy string) would slip into the success branch (toast + redirect). Defense-in-depth gap. | Medium (Low per run-1/run-4) | OPEN (known) | 1/6 (#5) | 2/3 (run-1 downgraded, run-4 #3) | `src/_components/LoginForm.tsx:107,127` (`status: boolean`, `if (!response.status)`) | Type `status` as `boolean \| string`, gate on `=== true`, key the message off `response.reason`. Note: here `response` is the raw register/login HTTP `fetch` JSON (not `apiRequest`), so the envelope can only appear if the auth route itself short-circuits — narrower than the `apiRequest` path. |
| O5 | `src/ updateUser_v1` omits the `auth.nameMaxLength` bound the template enforces — generated zod schema types `name` only `z.string().optional()` (no max), so an unbounded display name reaches the DB. | Low | OPEN (known) | 1/6 (#6 sub) | 1/3 (run-4 #4) | `src/settings/_api/updateUser_v1.ts:70` `if (name) newData = { ...newData, name }` — no length check | Port the template guard: reject `name.length > getProjectConfig().auth.nameMaxLength`. |
| O6 | `requestEmailChange` returns `auth.emailTaken` synchronously → authenticated account-enumeration oracle (rate-limited 5/window). Inline comment acknowledges the tradeoff. | Low (security) | DEFERRED-DECISION / OPEN (known) | 4/6 (#15) | 3/3 | `src/settings/_api/requestEmailChange_v1.ts:42-48` (+ `//?` comment :37-41) | Deliberate per the code comment but NOT recorded as an ADR. Either return a generic "confirmation sent if available" (match reset flow) or write a decision file accepting the oracle. See DEFERRED section. |
| O7 | Email-change confirmation auto-consumed on page mount (one-shot Redis token fired in `useEffect`, no user click) — link-prefetchers/AV detonation/webmail preview burn the token before the human clicks. | Medium (Low per wave2) | OPEN (known) | 1/6 (#12) | 2/3 (run-1, run-4 #10) | `src/settings/confirm-email/page.tsx:19-48` (`apiRequest('settings/confirmEmailChange')` in mount effect) | Require an explicit "Confirm email change" button click before calling the API. |
| O8 | Reset-password & email-change confirmation tokens travel in the URL **query string** (`?token=`) — recoverable from server/proxy/CDN logs + leaked via `Referer`. Sentry `beforeSend` strips only the Sentry leak, not access-log/Referer. | High (wave1) / Low (wave2 — accepted email-link design) | OPEN (known) | 1/6 (#3) | 1/3 (run-4 still-present, Low) | `src/reset-password/page.tsx:12` `params.get('token')`; `src/settings/confirm-email/page.tsx:14` `params.get('token')` | Deliver token in the URL fragment (`#token=`) + `replaceState`-strip, or exchange a short-lived `?code=` over POST. Wave-2 reframes as Low (standard email-link pattern); the framework-owned `auth.tokenLinkMode` helper (wave1 r4 FEAT) would close it for every consumer. |
| O9 | `WideSessionResponse` hand-cast on the bootstrap path — generated `system/session` response type models only `status:'success'`, forcing a Rule-21-forbidden hand-widening cast at the one endpoint every consumer touches first. | Medium (refactor / AI-drivability) | OPEN (known) | 5/6 (#25) | 3/3 | `src/_providers/SessionProvider.tsx:89-95` (`type WideSessionResponse`, `rawResponse as WideSessionResponse`) | Fix is generator-side: union every `apiRequest`/`syncRequest` response with the framework error envelope `{status:'error';errorCode:string}` (wave2 FI-1). Until then the bootstrap teaches the wrong pattern. |
| O10 | `LoginForm` can lock its submit button permanently on a blocked/slow redirect — success path schedules `setTimeout(...,1000)` → `location.href` but never restores `setLoading(false)`. | Low | OPEN (known) | 1/6 (#22) | 2/3 (run-1, run-4 #5) | `src/_components/LoginForm.tsx:137-147` | Navigate immediately or restore `setLoading(false)` before the timer. |
| O11 | Avatar `?v=` cache-buster appended to the uploaded data-URI (not just the display URL); server regex `/^data:(.+);base64,(.+)$/` captures it into base64 group 2, survives only via lenient `Buffer.from(...,'base64')`. Latent breakage if base64 decoding is ever tightened. | Low (Medium per wave1 r6) | OPEN (known) | 1/6 (#16) | 2/3 (run-1, run-4 #6) | `src/settings/page.tsx:159` `saveProfile(\`${result}?v=${Date.now()}\`)` vs `updateUser_v1.ts:32` regex | Append `?v=` only to the displayed URL; send the raw data-URI to `updateUser`. |
| O12 | Attacker-controlled string fields dereferenced without a `typeof` guard across reset/email/test handlers — `.trim()`/`.length` directly off `data.<field>`. Relies entirely on the validation stage; throws a `TypeError` if `validation.runtimeMode:'off'` or the route shape isn't enforced. | Low | OPEN (known) | 1/6 (#13) | 2/3 (run-1 ROOTSRC-04, run-4 verified mitigated) | `requestEmailChange_v1.ts:27` `data.newEmail.trim()`; `confirmReset_v1.ts:25` `data.token.trim()`, `:31` `password.length`; `confirmEmailChange_v1.ts:29` `data.token.trim()`; `testEmail_v1.ts:39` `data.email.trim()` | Add `typeof data.<field> !== 'string'` guards, or document the reliance on the validation stage. Realized risk is low under default `enforce` (see F10). |
| O13 | `SESS-RACE-01` — root `page.tsx` hard 1000ms timeout bounces a still-loading valid session to `/login` while `SessionProvider` is still retrying (backoff up to ~7.5s). Slow-but-valid first session fetch → spurious redirect to login. | Medium | NEW (confirmed real) | — | 1/3 (run-3) | `src/page.tsx:28-37` (1000ms `setTimeout` → `navigate(loginPageUrl)`) vs `SessionProvider.tsx:82-109` (maxAttempts=5, backoff to 4000ms) | The 1s guard defeats the backoff budget. Remove/raise the timeout past the backoff budget, or key the redirect off the same "gave up" signal (`sessionLoaded` only true after retries exhausted — which it already is at `SessionProvider.tsx:110`). Verify whether `sessionLoaded` already covers this before changing. |
| O14 | `PREF-01` — `updatePreferences_v1` overwrites the whole preferences JSON column with only the 2 allow-listed keys instead of merging — any other/future preference key is silently wiped. Works today only because the client always sends the full known set. | Low | NEW (confirmed real) | — | 1/3 (run-3) | `src/settings/_api/updatePreferences_v1.ts:27-33` (`sanitized` rebuilt from scratch, `data: { preferences: sanitized }`) | Read `dbUser.preferences`, spread the allow-listed values over it, write the merged object. Brittle to client/schema evolution. |
| O15 | Hardcoded English bypasses `useTranslator` (Rule 13) — `LoginForm` title/subtitle/button/`"Loading..."`, `reset-password` `'Loading...'` (x2), `Navbar` `DEFAULT_ITEMS` labels + mobile aria-labels, AND `main.tsx` `ErrorFallback` (which also MISUSES `enJson.common['404']` as the title for a generic crash → a render crash shows a "404"-labeled screen). | Medium (wave1 r2) / Low (wave2) | OPEN (known) | 1/6 (#20) | 3/3 (run-3 I18N-01 adds main.tsx, run-4 #8) | `LoginForm.tsx:14-18,240`; `reset-password/page.tsx:83,172`; `Navbar.tsx:64-71,183`; `main.tsx:254-257` | Route through `translate({key})`; in `ErrorFallback` use a generic error key, NOT `common['404']`. The main.tsx ErrorFallback mislabel is the sharpest part (run-3-only). |
| O16 | Non-token Tailwind colors (Rule 14) — `bg-primary ... text-white` on accent buttons should be `text-title-primary`, across many spots. (`Navbar` `bg-black`→`bg-overlay` was fixed.) | Medium (wave1 r2) / Low (wave2) | OPEN (known) | 1/6 (#21) | 3/3 | `ConfirmMenu.tsx:63`; `confirm-email/page.tsx:69,83`; `settings/page.tsx` (multiple `text-white` accent buttons); `LoginForm.tsx`; `reset-password/page.tsx` | Replace `text-white`→`text-title-primary` on accent surfaces. |
| O17 | God-component `settings/page.tsx` (~530 lines, single `Home`, 8 flows) — the O1 delete bug hid here. | Low (refactor) | OPEN (known) | 4/6 (#23) | 2/3 | `src/settings/page.tsx` | Split into `<ProfileSection>`/`<PasswordSection>`/`<SessionsSection>`/`<PreferencesSection>`/`<DangerZone>` + a `useSettingsApi` hook. |
| O18 | God-effect `socketInitializer.ts useSocket` (~190 lines) + 3 near-identical room-helper emit/once blocks. | Low (refactor) | OPEN (known) | 4/6 (#24) | 2/3 | `src/_sockets/socketInitializer.ts` | Extract `attachX(socket)` helpers + a single `socketRequest(event,payload,responseEvent)`. |

---

## DEFERRED-DECISION

| # | Finding | Severity | Status | Wave1 | Wave2 | Location | Note/Recommendation |
|---|---|---|---|---|---|---|---|
| O6 (dup) | `requestEmailChange` `emailTaken` enumeration oracle | Low | DEFERRED-DECISION | 4/6 (#15) | 3/3 | `requestEmailChange_v1.ts:42-48` | Acknowledged in-code as a deliberate tradeoff but no ADR exists. Needs a policy decision: accept (write `docs/decisions/`) or match the reset flow's always-success. Tracked also as O6 OPEN above. |

---

## FALSE-POSITIVE / STALE

| # | Finding | Severity | Status | Wave1 | Wave2 | Location | Note/Recommendation |
|---|---|---|---|---|---|---|---|
| FP1 | wave2 run-3 DRIFT-02 "shipped `signOutEverywhere` logs caller out of own device" | Medium | FALSE-POSITIVE (stale) | — | 1/3 | `packages/cli/assets/login/src/settings/_api/signOutEverywhere_v1.ts:23` | Asset already passes `user.token` as `exceptToken` (`revokeUserSessions(user.id, user.token)`), matching the consumer copy. The claim was stale at 302cbf1. (Recorded as FIXED F11.) |
| FP2 | wave1-corroborated SECURITY H-2 carried as "still-open pending recheck" by wave2 run-1 (Sentry masking) | High | FALSE-POSITIVE (stale) | 6/6 (#1) | run-1 top-level only | `src/_functions/sentry.ts:62-63` | run-1's top-level SECURITY.md explicitly says it did NOT re-trace `sentry.ts`; the actual file masks text+media. Resolved as FIXED F1. |
| FP3 | wave2 run-3 OFFLINE-01 "apiRequest offline-queue hang" attributed to root-src | Low | FALSE-POSITIVE (out-of-area) | — | 1/3 | `src/_sockets/{apiRequest,offlineQueue}.ts` are pure re-export shims | Any real queue bug lives in `@luckystack/core`, not root-src. Correctly self-flagged by run-3 as out-of-area; not counted against root-src. |

---

## Notable

- **The dominant root-src theme is twin-drift**: consumer `src/settings/_api/*` is the regressed branch and the shipped CLI asset (`packages/cli/assets/login/src/...`) is the corrected side (verified file-by-file). O2 (Redis-key formatter) and the delete half of O3 are the same root cause. Because `AI_PROJECT_INDEX`/Rule-12 point agents at the stale `src/` example FIRST, the structural fix is the `ai:check-template-drift` CI gate (wave1 r2 FEAT-1) + the wave2 FI-2 `ai:lint` invariant (flag `${PROJECT_NAME}-...` / `process.env.PROJECT_NAME` Redis-key literals in `_api`/`_sync`).
- **O1 (credentials self-delete) is the single highest-impact live defect** and has NO real-flow regression test (the handler `_v1.tests.ts` passes `password` directly, so it goes green while the UI is broken). wave2 FI-3 (`menuHandler.confirm` variant returning the typed value) is the enabling fix.
- No critical and no remote-unauth findings in this area, confirmed across all three wave-2 runs.
