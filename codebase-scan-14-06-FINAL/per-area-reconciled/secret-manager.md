# Reconciled area report: secret-manager

Reconciliation of wave-2 scans (runs 1, 3, 4 — run 2 was the input digest, ignored) against the
wave-1 6-scan MERGED baseline + LOW_ANALYSIS, all VERIFIED against the CURRENT working tree
(`packages/secret-manager/src/index.ts`, `index.test.ts`, `CLAUDE.md`, and the wired caller
`server/bootstrap/initSecrets.ts`).

## Verdict

`packages/secret-manager/src/index.ts` remains the cleanest, most defensively-engineered file in
the repo. **No Critical or High in any wave-2 run, and none found on re-verification.** The prior
6-run Medium/Low cohort is essentially all FIXED in the current tree (the rewrite that landed this
week closed them, each with a matching regression test). The remaining tail is: a few genuinely-NEW
Low correctness/robustness edges wave-1 missed (NEW, verified real), a Medium AI-context doc-drift
(CLAUDE.md), the long-standing fail-OPEN contract (confirmed deliberate, not a defect), the
"shadow-API" wiring gap (a quality observation, the root-theme #2 — DEFERRED to a wiring decision),
and a set of policy/by-design Lows correctly left untouched.

Wave-1 line numbers in the MERGED report (`:594-602`, `:219-231`, etc.) no longer exist — the six
prior runs audited an OLDER snapshot. Every "fixed" claim below is re-verified against current
line numbers.

## FIXED (wave-1 items, verified resolved in current tree)

| # | Finding | Severity | Wave1 | Wave2 | Location | Note/Recommendation |
|---|---------|----------|-------|-------|----------|---------------------|
| F1 | `envNames` allowlist not applied on dev file-reload path (scoping drift between ingest paths) | med | 3/6 | 3/3 | `index.ts:736-740` (reload) shares `splitPointers(...,config.envNames)` with `capturePointers` `:289` | FIXED. Shared `splitPointers` (`:265-279`) filters by `toNameFilter` FIRST on both paths. Regression test "envNames scoping on file-reload (SM-06 drift)" (index.test.ts:634-663). |
| F2 | Concurrent resolves race / TOCTOU on `pointerMap`/`cachedResolution`/`process.env` | med | 5/6 | 3/3 | `index.ts:498-510` (`doResolve` serializes via `resolveChain`), snapshot `:530` | FIXED for the resolve+apply critical section. Every resolve chains off `resolveChain`; `activePointerMap` snapshotted at `:530`. (Residual: the `pointerMap` MERGE on the reload path happens outside the serialized step — see O3/SM-N3, Low.) |
| F3 | Dev file-reload injects plain values unfiltered by `envNames` | med | 1/6 | 3/3 | `index.ts:736` (split), `:752-754` (inject) | FIXED. Plain values come from the same `envNames`-filtered `splitPointers`; injected only AFTER a successful `doResolve` (atomic). Test asserts the excluded plain value is not injected. |
| F4 | `refreshSecretManager` misses pointers added after a zero-pointer boot (sticky empty map) | med | 1/6 | 3/3 | `index.ts:524-528` | FIXED. Re-captures when `pointerMap === null || Object.keys(pointerMap).length === 0`. Test "refresh re-captures pointers added after an empty boot" (index.test.ts:695-711). |
| F5 | File-reload REPLACES pointer map, dropping inherited pointers | med | 1/6 | 3/3 | `index.ts:746` | FIXED. `pointerMap = { ...pointerMap, ...freshPointerMap }` (merge). Test "file-reload merges pointers". (Symmetric downside = O4/SM-03, Low.) |
| F6 | No size cap on resolve response body (OOM DoS) | med | 1/6 | 3/3 | `index.ts:330` cap, `:402-406` pre-check, `:336-355` streamed cap | FIXED. `MAX_RESOLVE_BODY_BYTES = 1 MiB` + Content-Length pre-check + streamed `readBodyCapped`. Test "response body size cap". |
| F7 | `onApplied`/`onResolveError` invoked without isolation; boot-vs-poll asymmetry | low | 4/6 | 3/3 | `index.ts:196-199` (`runHook`), `:208-214` (`runHookAsync`), used `:554`/`:569` | FIXED. Both hooks isolated; `onResolveError` runs in-catch and does not mask the original error (re-thrown `:562`). Tests "hook isolation". |
| F8 | Raw fetch `error` object logged (PII channel) | low/med | 3/6 | 3/3 | `index.ts:190-191` (`errorMessage`), used `:198,:212,:559,:608,:649,:686` | FIXED. `errorMessage(error)` at every warn site incl. the 3 background-timer catches (the LOW_ANALYSIS F9a stragglers are closed). |
| F9 | In-file `sleep` re-implemented instead of `@luckystack/core` | low | 4/6 | 3/3 | `index.ts:32` import, `:454` use | FIXED. `import { sleep, tryCatchSync } from '@luckystack/core'`. |
| F10 | `getCachedResolution` raw-secret footgun + wrong "deep copy" JSDoc | low | 3/6 | 3/3 | `index.ts:788-792` (`getCachedResolutionMeta`), `:760-765` JSDoc | FIXED. Values-free `getCachedResolutionMeta()` added; JSDoc reworded to "shallow copy (values are flat strings)". Test "getCachedResolutionMeta". |
| F11 | Non-ok response body left unconsumed | low | 1/6 | 3/3 | `index.ts:392` (`void response.body?.cancel()`), also `:404` | FIXED (body half). `response.statusText` still interpolated `:393` — accepted, no in-package injection sink (see WONT-FIX W3). |

## OPEN (real + still present in current tree)

| # | Finding | Severity | Status | Wave1 | Wave2 | Location | Note/Recommendation |
|---|---------|----------|--------|-------|-------|----------|---------------------|
| O1 | **NEW** — `pointerMap` is merged BEFORE the resolve it depends on, so a failed remote file-reload permanently pollutes the in-memory pointer map. A bad dev `.env.local` edit poisons every later `refreshSecretManager()`/poll for the life of the process (operator must restart). Dev-path + remote-mode only. | med | open (new) | 0/6 | 2/3 (r1 SECRETMANAGER-02, r3 SM-N3 frames it as a race) | `index.ts:746` (merge) then `:751` (`doResolve` may throw) | VERIFIED REAL. `pointerMap = { ...pointerMap, ...freshPointerMap }` (`:746`) is committed before `await doResolve(...,'file-reload')` (`:751`), which throws in remote mode on an unresolvable pointer (`applyResolved` `:467-472`). No rollback. The atomicity test (index.test.ts:826-844) asserts plain-value atomicity but NOT pointer-map rollback. Fix: build the merged map locally; commit to `pointerMap` only after `doResolve` succeeds (mirror the plain-value "apply after success" ordering). |
| O2 | **NEW** — `validateUrl` accepts userinfo (`user:pass@host`) in the secret-manager `url` and never strips/warns; it is concatenated into the endpoint at `:368` so `fetch` sends URL-embedded credentials — an unaudited 2nd auth channel that bypasses the single-bearer-token contract and can leak into logs/proxies. | low | open (new) | 0/6 | 1/3 (r3 SM-N4) | `index.ts:224-247` (`validateUrl` checks only scheme + loopback/http), `:368` (endpoint build) | VERIFIED REAL. `validateUrl` has no `parsed.username || parsed.password` check; `config.url` (userinfo intact) is concatenated at `:368`. Low likelihood (requires a misconfigured/malicious url) but undermines the package's secret-hygiene contract. Fix: `if (parsed.username \|\| parsed.password) throw` (or strip+warn). |
| O3 | **NEW** — file-reload `pointerMap` MERGE happens synchronously OUTSIDE the serialized `resolveChain` step, so under the dev watch+poll combo a queued resolve can observe a later reload's merged map (contradicts the in-code "strictly in order" guarantee). Dev-only, low blast radius. | low | open (new) | 0/6 | 1/3 (r3 SM-N3) | `index.ts:746` (sync mutation) vs `:530` (snapshot inside serialized `doResolveInner`) | VERIFIED REAL. The merge at `:746` is not part of the serialized critical section (only fetch+apply is). Distinct from O1 (which is the no-rollback bug); same line, different failure mode. Fix: pass `freshPointerMap` into `doResolve` and merge after acquiring the chain slot so capture+mutation are atomic. |
| O4 | **NEW** — `reloadSecretManagerFromFiles` never PRUNES a pointer dropped from the env files (merge-only), so a rotated-away/deleted pointer keeps being POSTed off-host on every reload/poll. Symmetric downside of the F5 merge fix. Dev-only, mildly surprising. | low | open (new) | 0/6 | 1/3 (r1 SECRETMANAGER-03) | `index.ts:746` | VERIFIED REAL. `{ ...pointerMap, ...freshPointerMap }` only unions; a removed file pointer survives. Fix: track file-sourced vs inherited pointers and replace only the file subset (`{ ...inherited, ...fresh }`), OR document that file-reload is additive (removal needs a restart). |
| O5 | **NEW** — token `fromFile` is re-read + re-validated on EVERY retry attempt (not just every resolve): `resolveToken` is called inside `fetchResolveOnce` (`:381`), which `fetchResolve` invokes once per attempt (`:449-457`). An empty/whitespace token throws a deterministic config error that is then retried `count` times with delays; a `"Bearer "` prefix warns once per attempt; a token file deleted mid-retry turns a transport retry into a hard token error. | low | open (new) | 0/6 | 2/3 (r1 SECRETMANAGER-04, r3 SM-N2) | `index.ts:381` (`resolveToken` in `fetchResolveOnce`), retried `:449-457` | VERIFIED REAL. Token resolution/validation lives in the per-attempt path. Fix: resolve+validate the token ONCE per `doResolveInner`/`fetchResolve` before the retry loop, so a token-config error fails fast and `validateToken`'s warn fires at most once per resolve. |
| O6 | **NEW** — re-scan of `process.env` after a zero-pointer boot can mis-classify an unrelated inherited value matching `^(.+)_V(\d+)$` (e.g. `RELEASE_2024_V2`) as a pointer and POST it off-host, since `envNames` defaults to allow-all. Widens the documented default-scan off-host window onto the refresh path. | low | open (new) | 0/6 | 1/3 (r3 SM-N5) | `index.ts:524-528` (re-capture on empty), `:254` (default allow-all) | VERIFIED REAL. The F4 fix (re-capture on empty map) re-runs `capturePointers` over all of `process.env` on every refresh after a zero-pointer boot. Related to wave-1 #17 (O8) but specific to the refresh widening. Fix: after first capture, only re-capture explicitly added/watched names, or require `envNames` when re-capture is enabled; at minimum document. |
| O7 | `pointerPattern.test()` runs against every env value with no ReDoS / length cap | low | open (known) | 4/6 | 3/3 | `index.ts:135` (default), `:275` (`splitPointers`) | STILL PRESENT but operator-controlled config (not user input); default `^(.+)_V(\d+)$` is linear-safe. Doc-only nicety. WONT-FIX class per LOW_ANALYSIS. |
| O8 | Default `envNames` scans entire inherited `process.env`, POSTs any pointer-shaped value off-host | low | open (known) | 1/6 | 3/3 | `index.ts:254` (`toNameFilter(undefined)`→`()=>true`), `:286-289` | STILL PRESENT, documented (JSDoc `:55-61`). Deliberate default-allow tradeoff; a boot-time self-announcing warn is the cheap win but adds a new default-boot side-effect → DEFERRED (see D2). |
| O9 | `isLoopbackHost` omits `127.0.0.0/8`, `::ffff:127.*`, `0.0.0.0`; dead `[::1]` branch | low | open (known) | 3/6 | 2/3 | `index.ts:218-222` | STILL PRESENT. Errs fail-CLOSED (over-strict — rejects real loopback under plain http). Usability nit, NOT a vuln. Report warns a careless widening could become an SSRF-style allow. WONT-FIX class. |
| O10 | `validateToken` Bearer-prefix is warn-only (doesn't strip); `"Bearer x"` → `Bearer Bearer x` → 401, silent stale fallback in hybrid | low | deferred-decision | 2/6 | 2/3 | `index.ts:299-303`, used `:381` | STILL PRESENT. "warn, don't mutate" is a deliberate documented contract; stripping vs throwing is a policy choice → DEFERRED (D3 / LOW_ANALYSIS #11). |
| O11 | Exported `reloadSecretManagerFromFiles` not `NODE_ENV`-gated (only its watcher caller is) | low | open (known) | 1/6 | 1/3 (r4) | `index.ts:708-710` (only short-circuits on missing config/`local`) | STILL PRESENT. The `envNames`-unfiltered worst case is fixed (F3); the residual is that a consumer/AI wiring this export to a prod surface (admin route, SIGHUP) lets a `.env.local` writer inject plain `process.env` values (bounded by `envNames` when set). Fix: hard-gate to non-prod, or document on the function that it mutates `process.env` regardless of `NODE_ENV`. |
| O12 | Module-global singletons = de-facto process singleton; 2nd `initSecretManager` clobbers `activeConfig` | low | open (known) | 3/6 | corroborated | `index.ts:153-173`, `:671` | STILL PRESENT, partially documented (`:151` "run once per process"). The optional MULTI_TENANCY cross-ref line + `ResolverState` refactor is a non-load-bearing nicety → DEFERRED (D4). |

## NEW (wave-2 found, verified real — quality/observation tier)

| # | Finding | Severity | Status | Wave1 | Wave2 | Location | Note/Recommendation |
|---|---------|----------|--------|-------|-------|----------|---------------------|
| N1 | Stale package `CLAUDE.md` Function Index omits exported `stopSecretManager` (`index.ts:800`) + `getCachedResolutionMeta` (`:788`); the `SecretManagerConfig` one-liner lists `{ url; token; source?; pointerPattern?; fetchImpl?; dev? }` and is missing ~9 keys (`envNames`, `allowInsecureHttp`, `timeoutMs`, `retries`, `resolvePath`, `headers`, `onApplied`, `onResolveError`, `pollIntervalMs`); the Config-keys table omits `pollIntervalMs`. | med | new | 0/6 | 1/3 (r1 SECRETMANAGER-01) | `packages/secret-manager/CLAUDE.md` (Function Index + `Type SecretManagerConfig` row + Config keys table) | VERIFIED REAL against current CLAUDE.md + exports. Directly defeats the "100% AI-driven after install" goal: an AI reading the post-install INDEX won't learn the prod rotation poll, `onApplied` rotation hook, `stopSecretManager` teardown, or `envNames` scoping exist, so it will hand-roll them. `.d.ts` is correct so a type-aware agent recovers, but the prose INDEX is the first surface (Rule 12). Fix: regenerate the Function Index + expand the type one-liner + add `pollIntervalMs` to Config keys; `npm run ai:capabilities` after. |

## DEFERRED-DECISION (real but intentionally deferred this session — policy/wiring/ADR)

| # | Finding | Severity | Status | Wave1 | Wave2 | Location | Note/Recommendation |
|---|---------|----------|--------|-------|-------|----------|---------------------|
| D1 | Almost the entire `SecretManagerConfig` surface is a "shadow API": the only wired prod caller `resolveSecretsIfConfigured` forwards only `{ url, token, source:'remote', dev }`; `SecretManagerBootConfig` (initSecrets.ts:18-25) exposes only `url`/`token`/`dev`. So `source:'hybrid'\|'local'`, `retries`, `timeoutMs`, `allowInsecureHttp`, `envNames`, `pointerPattern`, `resolvePath`, `headers`, `onApplied`, `onResolveError`, and the prod `pollIntervalMs` are unreachable through the wired path. `getCachedResolution(Meta)`, `refreshSecretManager`, `reloadSecretManagerFromFiles`, `stopSecretManager` have NO prod callers. `startRotationPoll` is therefore dead in prod. | med | deferred-decision (root-theme #2 wiring) | 0/6 | 1/3 (r3 SM-N1) | `server/bootstrap/initSecrets.ts:18-25, 48` | VERIFIED REAL. This is a wiring/contract decision, not an in-package bug — defer to the framework-wide "shadow-API" wiring bucket. Decision needed: widen `SecretManagerBootConfig` to pass through `source`/`timeoutMs`/`retries`/`pollIntervalMs`/`allowInsecureHttp`/`envNames` into `initSecretManager`, OR document that only `url`/`token`/`dev` are supported and mark the rest advanced-direct-call-only. Add a smoke test booting through `initSecrets.ts` with a non-remote source. |
| D2 | Default-allow env-scan self-announcing warn (boot-time `console.warn` naming pointer-shaped values when `envNames` is unset) — the cheap mitigation for O8. | low | deferred-decision | 1/6 | 3/3 | `index.ts:254`, `:286-289` | DEFERRED per LOW_ANALYSIS #17: adding a warn is a new side-effect on every default-config boot = a policy/default change. Pending a product/ADR decision. |
| D3 | `validateToken` Bearer-prefix: strip vs throw (the policy resolution of O10). | low | deferred-decision | 2/6 | 2/3 | `index.ts:299-303` | DEFERRED per LOW_ANALYSIS #11: "warn, don't mutate" is the documented contract; changing it (silently rewrite consumer config vs hard-fail boot) needs an ADR. |
| D4 | "single resolver per process; multi-tenant → ARCHITECTURE_MULTI_TENANCY.md" doc-line + optional `ResolverState` object (the O12 nicety). | low | deferred-decision | 3/6 | corroborated | `index.ts:151-166`, `:671` | DEFERRED per LOW_ANALYSIS #16: core idea already documented (`:151`); cross-ref line is a non-load-bearing nicety, `ResolverState` is a structural refactor (testability only). Rule 27 — not a drive-by edit. |

## FALSE-POSITIVE / CONFIRMED-NON-DEFECT (do NOT re-flag)

| # | Finding | Severity | Status | Wave1 | Wave2 | Location | Note |
|---|---------|----------|--------|-------|-------|----------|------|
| FP1 | fail-OPEN on package-absent / url-unset is a defect | n/a | false-positive (confirmed deliberate) | universal | 2/3 (r3 SM-C2, r4) | `server/bootstrap/initSecrets.ts:36-44` | CONFIRMED DELIBERATE (user-memory `feedback_secret_manager_failopen`). Correctly bounded: once `url` is set + package installed → `source:'remote'` is fail-CLOSED (unresolved pointer/unreachable server throws — `index.ts:467-472`). Only the not-configured case falls through (intended). NOT a defect. |
| FP2 | resolve-response validation "fails open on unrecognized shapes" (the framework-wide validateType theme) | n/a | false-positive (does not apply) | — | 1/3 (r3 SM-C3) | `index.ts:416-435` | CONFIRMED: response parsed as `unknown`, rejects missing/non-object `values` (`:417`), filters to requested pointers (`:428-431`), drops non-strings with a warn (`:432-435`); in remote a dropped/missing pointer THROWS (`:467-472`). Fail-CLOSED — the opposite of the validateType weakness elsewhere. |
| W3 | Drop `statusText` from the thrown error message | low | wont-fix (cosmetic) | 1/6 | corroborated | `index.ts:393` | Server-influenced but NO injection sink in-package (flows only to `onResolveError`/`console.warn`); dropping it removes operator signal. The resource half (`body?.cancel()`) is already done (F11). |
| W4 | `parseEnvFile` minimal hand-rolled parser (`KEY="a"b"` edge, no escapes/multiline) | low | wont-fix (by design) | 3/6 | corroborated | `index.ts:575-601` | Intentional dependency-free dev-only tradeoff, documented `:572-574`. Dev `.env` reload path only. |
| W5 | Token `readFileSync` synchronous on every resolve, no size cap | low | wont-fix (by design) | 2/6 | corroborated | `index.ts:311` | By design — read-at-resolve-time so a rotated token file is picked up (documented `:42-43`). Cost negligible for a normal token file. (Distinct from O5, which is the per-ATTEMPT re-read — a real Low.) |

## Notes

- **No NEW Critical/High.** The 6 NEW items (O1–O6) are all Low except O1 (Medium: the file-reload
  pointer-map no-rollback poisoning) and N1 (Medium: CLAUDE.md AI-context drift). All verified
  against current source with file:line evidence.
- **Wave-2 corroboration is thin on the NEW items** (mostly 1/3, O1 and O5 2/3) because the file is
  so clean that the three runs spent most effort confirming the prior cohort is fixed; the new tail
  is where they diverge. I verified each independently rather than relying on n/3.
- **No regression introduced by this week's rewrite** other than the *symmetric* O1/O3/O4/O6 edges
  that are direct consequences of the F2/F4/F5 fixes (serialization, empty-map re-capture, merge).
  These are the price of the fixes, not accidental regressions — each is Low except O1's Medium
  no-rollback poisoning, which is the one worth fixing.
- Build/lint/test state (per LOW_ANALYSIS): `npm run build` green, eslint clean, 52/52 tests pass.
