# Features & Capability Gaps — FINAL Combined Record (wave-1 ×6 + wave-2 ×3, reconciled to current code)

> **What this is.** The single authoritative, fully-deduplicated **Features & Capability Gaps** report for the whole LuckyStack repo, merging the wave-1 6-scan synthesis (`codebase-scan-14-06-MERGED/FEATURES.md`, items F1–F55) with the 18 wave-2 reconciled per-area reports (`per-area-reconciled/*.md`, runs `--1/--3/--4`), every row re-verified against the CURRENT working tree (branch `chore/package-split-prep`, HEAD `302cbf1` + ~248 uncommitted fixes). Two lenses are combined: **missing framework primitives / packages** (capabilities the 15–16-package split still leaves consumers to hand-roll) and **AI-drivability affordances** (places where docs / generated types / indexes / barrels silently lie to an AI author so it ships a no-op, a cast, or a hole while lint+build+test stay green — the "shadow-API" / silent-drift class).
>
> **STATUS legend.** Every row carries one: **NEW** (wave-2 found, wave-1 missed, verified real) · **OPEN** (real, still present, wave-1 + wave-2 corroborated) · **DEFERRED-DECISION** (real but blocked on a policy/ADR/contract call) · **FIXED** (closed in the current tree this week, recorded so it isn't re-flagged) · **FALSE-POSITIVE** (bogus — one-line reason). Sorted so actionable work is on top: **NEW + OPEN by severity**, then **DEFERRED-DECISION**, then **FIXED** (compact), then **FALSE-POSITIVE** (compact). Corroboration is `w1 n/6` (wave-1 runs) + `w2 n/3` (wave-2 runs).
>
> Scope note: this file keeps the **feature / capability-gap** framing. Pure bug- and security-severity findings that are not capability-shaped live in the per-area reports' OPEN/NEW tables and the security/bug rollups; where a single root cause is *both* a recurring bug class AND the justification for a missing primitive (e.g. the dual-transport drift → a shared pipeline + parity gate), it appears here once as the feature.

---

## Status count table

| STATUS | Count | Of which High | Med | Low/feature |
|---|---|---|---|---|
| **NEW** (wave-2, verified real) | 9 | 2 | 5 | 2 |
| **OPEN** (carried, still present) | 31 | 10 | 14 | 7 |
| **DEFERRED-DECISION** | 10 | 1 | 6 | 3 |
| **FIXED** (closed this week) | 9 | 4 | 4 | 1 |
| **FALSE-POSITIVE** | 7 | — | — | — |
| **Total distinct feature rows** | **66** | | | |

> Headline: **zero Critical feature-level gaps** (the two consensus Criticals — router wsProxy crash/SSRF and `/server.js` source-disclosure — are bugs and are now **FIXED** in-tree). The actionable feature surface is dominated by **two structural failure-modes that recur in nearly every area**: (1) **silent drift across hand-synced "must-match" surfaces** (api↔sync + socket↔HTTP transport twins, src⇄template⇄asset copies, root⇄template generator scripts, package CLAUDE.md↔code, barrel↔documented-exports, generated-types↔runtime) with no build gate; and (2) **shadow-APIs — exported/typed/documented/tested seams with ZERO production call-sites** (`preEmailSend` stop-signal, `flushErrorTrackers` on shutdown, the test-runner CSRF + extension layers, `posthogConfig.ts`, presence `getRoomPresence`, secret-manager's whole config surface, the docs-ui `renderCore` renderer). The single highest-leverage program is to **make divergence a build error**: a `luckystack doctor` static preflight + `ai:verify` end-to-end gate + per-route coverage/diagnostics artifacts + a contract-honesty lint rule turn the entire silent-drift + dead-wiring class structurally impossible. The top NEW items below are the two wave-2 shadow-API discoveries (docs-ui renderer broken against the real artifact shape; test-runner CSRF layer unwired) plus the email `preEmailSend` no-op.

---

## NEW — wave-2 found, wave-1 missed, verified real (actionable first)

**NF1 — docs-ui live renderer walks the WRONG artifact shape; the correct renderer is dead code** · High · w1 0/6 · w2 3/3 · NEW
Area: docs-ui (+ devkit emitter contract). The shipped `docsHtml.ts:339-342` iterates a NESTED `apis[page][name][version]` map, but the devkit emitter + committed artifact are FLAT `apis[page] = Entry[]` (`emitterArtifacts.ts:62,269,281`; `src/docs/apiDocs.generated.json:3`) → every real route renders as scrambled rows, syncs never render at all (`render()` reads only `data.apis`), and the CORRECT flat-array+sync-aware renderer `renderCore.ts` is imported only by its own test. A wrong-shaped fixture (`liveRenderCore.test.ts:156-164`) masks it green; dist ships the bug.
Fix: serialize `renderCore.ts`'s flat-array `renderDocsEndpoint`/`renderDocsSyncEntry` into the page (its documented `.toString()` strategy) OR rewrite `buildGroups` to `for (const [page, entries] of Object.entries(apis)) for (const entry of entries)`; replace the fictional nested test fixture with a real `apiDocs.generated.json` slice; add an `ai:lint` "exported symbol whose only importers are `*.test.ts`" rule. Pairs with **OF40** (typed `apiDocs.generated.json` contract + `schemaVersion`) which makes the shape mismatch a compile error. Ship-blocking for 0.2.0 docs (dev-only, `enabledInProd:false` → garbled dev docs + false-sense-of-delivered-fix, not an end-user breach).

**NF2 — test-runner CSRF-enforcement layer is dead surface (not exported, not in the default sweep)** · High · w1 0/6 · w2 3/3 · NEW
Area: test-runner. `csrfEnforcementCheck.ts` + `runCsrfEnforcementTests.ts` are fully implemented, typed, and tested but never exported from `index.ts` and never called by `runAllTests` (`runSweepLayers` runs only contract→auth→rate-limit→fuzz), and `package.json` exposes only the `.` barrel — a consumer cannot even deep-import it. Default `npm run test` gives **ZERO CSRF-enforcement coverage**; a consumer who disables/misconfigures CSRF still gets a fully green run, while the protection it verifies (`server/.../csrfMiddleware.ts`) is real.
Fix: export `runCsrfEnforcementCheck`/`runCsrfEnforcementTests`; add a 5th sweep layer in `runSweepLayers` gated on a session-bound CSRF token; bundle the in-check GET method-guard (test-runner #9) into the same change. Pairs with the "auto-mint a session + CSRF token in the sweep" feature all three runs converge on (and the thread-CSRF-header fix, test-runner #2, in **OF36**).

**NF3 — email `preEmailSend` stop-signal is IGNORED — documented suppression/abort seam is a silent no-op** · High · w1 0/6 · w2 2/3 · NEW
Area: email. `sendEmail` awaits `dispatchHook('preEmailSend', …)` and discards the returned `DispatchResult`; never checks `.stopped`; falls straight to `sender.send` (`sendEmail.ts:199-204`). README + CLAUDE.md present suppression/abort as LIVE (only `docs/hooks.md:135` admits the gap). A consumer/AI wiring a GDPR opt-out / bounce / unsubscribe suppression list ships a no-op — suppressed recipients still get mail and `sendEmail` returns `{ok:true}`.
Fix: check `pre.stopped` and short-circuit `{ok:false, reason: pre.signal.errorCode ?? 'email.suppressed'}` BEFORE `sender.send`; add an orchestration test asserting `sender.send` isn't called. Mirrors sibling sites that honor stop signals (loadSocket/httpHandler/login `_api`). Strongest single email finding.

**NF4 — devkit codegen hardcodes literal `src/`, silently dropping EVERY route on a non-`src` `srcDir` layout** · Medium · w1 0/6 · w2 1/3 · NEW
Area: devkit. `extractPagePath`/`extractSyncPagePath` (`typeMap/routeMeta.ts:20,52`) match `/src\/.../` and `return ''` otherwise; discovery is srcDir-agnostic so files ARE found, but `pagePath===''` makes the generator drop the entry (`if (!pagePath) continue`). A consumer setting the advertised `projectConfig.paths.srcDir = 'app'` gets ZERO API/sync types, no error. (Compounded by `normalizeImportPath` hardcoding `ROOT_DIR/src/_sockets` in two byte-duplicated files → wrong fallback-import base → trips the now-throwing identifier validator.)
Fix: anchor on `path.relative(getSrcDir(), file)` (as `loader.resolveApiRouteMetaFromPath` already does), or throw when discovery found a file but `extractPagePath` returns `''`. Directly defeats an advertised config key; the standout devkit NEW finding. Folds into **OF15** (`ai:verify`) as a non-`src`-layout conformance gate.

**NF5 — `posthogConfig.ts` is entirely dead (shadow-API): a documented PostHog tuning surface that ships as a no-op** · Medium · w1 0/6 · w2 2/3 · NEW
Area: error-tracking. `registerPostHogConfig`/`getPostHogConfig`/`PostHogConfig` are not barrel-exported, never imported, and `register.ts` builds `createPostHogAdapter({client})` with no `anonymousDistinctId`/`beforeSend`/`clientOptions`. An AI told "set a PostHog `beforeSend` redaction hook" reaches for `registerPostHogConfig({...})`, it type-checks, ships a no-op (events reach PostHog unredacted).
Fix: wire `register.ts` to read `getPostHogConfig()` + export from the barrel, OR delete the module + its doc claim. Sibling: **NF6** (the barrel omits 5 documented exports). Both are the error-tracking face of the repo-wide shadow-API + barrel-drift theme.

**NF6 — `@luckystack/error-tracking` barrel omits 5 exports its own CLAUDE.md Function Index documents** · Medium · w1 0/6 · w2 2/3 · NEW
Area: error-tracking. `flushErrorTrackers`, `appendErrorTracker`, `runWithErrorTrackerIdentity`/`Scope`+`set/getCurrentErrorTrackerIdentity`, `registerPreCaptureFilter`, `startSpanHandle` are documented in the package CLAUDE.md but live only in `@luckystack/core`; `index.ts` re-exports none. An AI following the package's own shutdown docs writes `import { flushErrorTrackers } from '@luckystack/error-tracking'` → TS2305. Directly amplifies **OF5**'s "wire flush-on-shutdown" path.
Fix: re-export from `@luckystack/core` via `adapter.ts` + `export * from './adapter'`, or correct CLAUDE.md to point at core. Generalizes to the dead-export/barrel-parity lint in **OF22**.

**NF7 — sync `flushPressure.maxBufferedBytes` (documented default 5 MiB) is NEVER read — shadow-config** · Medium · w1 0/6 · w2 1/3 · NEW
Area: sync. `flushPressure` resolves its threshold from the per-call arg + a hard-coded `DEFAULT_THRESHOLD_BYTES = 1 MiB`; `getProjectConfig().sync.flushPressure` is not imported in `streamEmitters.ts`. The configured 5 MiB default + `highWaterMarkChunks` (1000) are DEAD config — a consumer tuning `maxBufferedBytes` is silently ignored.
Fix: read `getProjectConfig().sync.flushPressure.maxBufferedBytes` (clamp by `highWaterMarkChunks`) as the fallback inside `flushPressure`, OR remove the config keys + docs. Folds into **OF47**'s unit-correctness fix (packets-named-as-bytes).

**NF8 — sync `redactToken`/`redactTokens` shadow-API + raw bearer-token leak to error-tracker** · Medium (sec-flavored) · w1 0/6 · w2 1/3 · NEW
Area: sync. The SYNC-17 redaction helpers are imported ONLY by their own test, while `clientFanout.ts:111` passes raw `targetToken: tempToken` into the `tryCatch` error-context (→ `captureException`) and `streamEmitters.ts:222-223` logs raw `{tokens: filtered}`; neither key is in `DEFAULT_REDACTED_LOG_KEYS`, so `sanitizeForLog` doesn't mask them. A mitigating control shipped specifically for these sites was never wired (key-name mismatch). Capability framing: the *redaction primitive exists but is not the convenient/default path* — same class as the security finding, listed here because the fix is "wire the shipped helper" (a drivability/seam gap). Un-gated tracker path; bounded by the `_client`-throw + tracker-registered precondition.
Fix: route the two call sites through `redactToken`/`redactTokens` (call-site redaction is the complete fix; `registerRedactedLogKeys` alone misses the legacy un-sanitized Sentry slot). The security-severity tracking lives in the sync area report (N2); recorded here as the shadow-helper drivability gap.

**NF9 — sync HTTP twin omits `postSyncAuthorize` + `preSocketMessage` hooks the socket handler fires (transport hook-parity drift)** · Medium · w1 0/6 · w2 1/3 · NEW
Area: sync. Audit/metrics subscribers on `postSyncAuthorize`, or a per-message gate on `preSocketMessage`, see only socket traffic and miss every HTTP-fallback sync; a `preSocketMessage` throttle/audit is bypassable by forcing the HTTP transport. Direct symptom of the **OF1** dual-transport hand-mirror.
Fix: dispatch `postSyncAuthorize` after the HTTP auth+receiver checks (parity with socket); decide+document whether `preSocketMessage` fires on HTTP; add a behavioural hook-dispatch-set parity test. Folds into the **OF1** shared-pipeline + parity-gate.

---

## OPEN — real, still present (wave-1 + wave-2 corroborated)

### High

**OF1 — Hand-synced dual-transport god-functions drift silently; no parity gate** · High · w1 6/6 · w2 3/3 · OPEN
Area: api, sync (core transport). The ~345–700-line socket-vs-HTTP handlers for both API and sync duplicate the auth→rate-limit→validate→execute→respond skeleton; CLAUDE.md mandates parity only in prose. Confirmed STILL-LIVE drifts (this week's fixes shared validation/lifecycle/envelope but rate-limit + auth + hook stages remain inline twins and ALREADY diverge): HTTP-method gate enforced only on HTTP (a `get*`/`list*` route mutating over the socket — api O4); loopback rate-limit skip on HTTP only + with spoofable `NODE_ENV`/`startsWith('127.')` (api O2/O8); HTTP omits `postSyncAuthorize`/`preSocketMessage` (sync N3 = NF9); `serverOutput` HTTP-envelope shape diverges from socket (sync O12); anonymous IP collapses to `'unknown'`/`'anonymous'` literals on HTTP only (api O9, sync O11).
Locations: `packages/api/src/handleApiRequest.ts` ↔ `handleHttpApiRequest.ts`; `packages/sync/src/handleSyncRequest.ts` ↔ `handleHttpSyncRequest.ts`; parity tests deliberately exclude rate-limit/method/logout/hook-set.
Fix: extract a transport-parameterized shared `runRequestPipeline`/`runSyncPipeline` (ordered gates: auth, rate-limit, method, validate, hooks, execute, respond) so both transports call one list; add an `ai:lint`/test-runner **parity gate** that fails when a gate/hook exists on one transport file but not its twin.

**OF2 — Runtime input-validator: terminal now fails CLOSED, but parser corner-cases + no coverage signal remain** · High · w1 6/6 · w2 3/3 · OPEN (core terminal FIXED — see FX2)
Area: core (consumed by api + sync). The wave-1 6/6 fail-OPEN terminal is **FIXED** (now fails closed, depth-capped, Record-value + proto-key checked, prod `enforce` default — recorded FX2). The RESIDUAL capability gap: there is still **no per-route coverage signal** for WHICH routes got an exact vs degraded validation contract, and the validator has verified parser corner-cases an AI can't see — `splitTopLevel` quote-blind (core N1, valid literal-union traffic rejected → route DoS), `Date` accepts any string (core N2), non-identifier quoted keys silently dropped (core N3), single-member union skipped (core O11), malformed `Record` back-door (core N4). Devkit's Zod emitter still degrades intersections/refs → `z.any()` (devkit O15/O16), disabling fuzz coverage with no signal.
Fix: emit `apiContracts.coverage.json` (per-route EXACT/FELL_BACK/DEGRADED + reason) + a `validated` column in `AI_PROJECT_INDEX`; make `splitTopLevel` quote-aware + add tuple/Date/quoted-key/single-member handling with tests; long-term emit a real schema from the generator instead of re-parsing type text. Pairs with **OF15/OF16/OF23**.

**OF3 — `@luckystack/jobs` — durable background jobs + scheduler/cron (missing primitive)** · High · w1 6/6 · w2 (carried) · OPEN
Area: framework-wide. No durable async-work primitive in the 15–16 packages; only low-level Redis `acquireLease`/`renew`/`release`. Deferred work (welcome-email, nightly cleanup, off-request upload encoding, webhook retry, reports) forces blocking the handler, a floating promise (the unhandled-rejection crash class seen across presence/core/server), or a hand-rolled BullMQ; `processUpload` encodes inline in-request.
Fix: ship `@luckystack/jobs` — file-based `src/_jobs/<name>_v1.ts` (`main({data,functions})` + optional `retry`/`schedule`), typed `enqueue` via route/version literals, Redis ZSET delay-queue + worker, exponential backoff, dead-letter, leader-elected cron via `acquireLease`, `preJobRun`/`postJobRun`/`jobFailed` hooks, devkit-emitted job typemap + jobs sweep. Foundation for **OF7, OF11, OF31**.

**OF4 — `@luckystack/storage` — pluggable file/blob storage adapter (missing abstraction)** · High · w1 6/6 · w2 (carried) · OPEN
Area: core (processUpload / serveAvatar). `processUpload` punts all persistence to a consumer `encodeAndSave` callback; `serveAvatar` is hard-wired to local disk. Every app re-rolls disk-vs-S3/GCS/R2 dispatch, signed-URLs, content-type + size validation, path-traversal safety, read-serving — and "serve files safely" is a recurring easy-to-get-wrong surface (the `/server.js` disclosure + avatar traversal hardening + serveFile substring-denylist all prove it).
Fix: ship `@luckystack/storage` — `registerStorage(adapter)` + `put/get/delete/signedUrl/serve`; built-in `LocalDiskStorage` (lifting serveAvatar's traversal-guard) + optional `S3Storage`/`R2Storage` (lazy peer, mirroring email's resend/nodemailer gating); wire into `processUpload`; tenant key-prefixing via the existing formatter; `maxBytes` + `allowedContentTypes` for safe-by-default.

**OF5 — `flushErrorTrackers()` never wired into server shutdown — telemetry data-loss every redeploy (dead-wiring)** · High · w1 2/6 · w2 3/3 · OPEN
Area: error-tracking, server. The ALS per-request identity (wave-1 #1 flagship) is now genuinely **FIXED** on the adapter path (recorded FX5). The remaining high-impact dead-wiring: `flushErrorTrackers()` has ZERO call-sites in `packages/server/src` (the only SIGINT/SIGTERM handlers are dev-only inside `initDevTools` and `process.exit(0)` immediately); posthog-node batches in memory and the prior accessor was removed, so flush is the ONLY drain path. Same root as the lost graceful shutdown (server O18: `createServer` returns `{httpServer,ioServer,listen}` with no `stop()`, no prod drain, no post-listen `httpServer.on('error')`).
Fix: add `stop()` to `RunningLuckyStackServer` (await `ioServer.close()` + Redis pub/sub `.quit()` + `httpServer.close()` + `await flushErrorTrackers()` + PostHog `shutdown()`), persistent `httpServer.on('error')`, wire prod SIGTERM→`stop()` via a `preServerStop` hook. Owner: server cluster. Generalize: wire reliability seams in the shared request-lifecycle/boot module.

**OF6 — `luckystack doctor` — AI-autonomous static wiring/posture preflight (no server-start)** · High · w1 4/6 · w2 (carried) · OPEN
Area: framework-wide / cli. The only correctness check (`verifyBootstrap`) runs at server-boot — a developer action (Rule 8) the AI can't trigger. Installed-but-unregistered packages (login without `registerSessionProvider` → `auth.login:true` routes silently PUBLIC), missing env/overlay-imports, typed routes resolving to unregistered runtime keys, errorCodes without locale keys, degraded capability-detection on Node < 20.6 — all give a green build + broken app with no signal. The shadow-API cluster (NF1–NF9, presence/test-runner/secret-manager dead surfaces) is exactly what a doctor would surface.
Fix: ship `npx luckystack doctor --json` (static, no server-start, AI-autonomous like lint/build): cross-check installed manifests × `config.ts` + `.env_template` + overlay-imports + generated route-keys + barrel-vs-CLAUDE.md exports + package-CLAUDE.md hooks/config-vs-code → `{severity, package, missing:'register'|'env'|'overlay-import'|'peer'|'export'|'hook', fix}`. Step 0 of the verify runbook. Builds on **OF12**'s manifest.

**OF7 — `@luckystack/webhooks-in` — typed inbound webhook receiver (verify + idempotency)** · High · w1 6/6 · w2 (carried) · OPEN
Area: http / framework-wide. Inbound webhooks are security-critical copy-paste: the framework ships a ~90-line hand-rolled GitLab example (read raw body, HMAC, constant-time compare, parse provider headers, timestamp window, replay dedupe). Getting the compare or raw-body capture wrong is a silent auth-bypass; every Stripe/GitHub/Twilio integration re-copies it.
Fix: ship `@luckystack/webhooks-in` — `src/_webhooks/<provider>_v1.ts` with `verify = hmacVerifier({header,secretEnv,algo,skewMs})` presets (stripe/github/gitlab/svix) + `main({event,raw,functions})`; framework auto-registers the origin-exempt prefix, captures raw body pre-cap, runs constant-time HMAC + timestamp window, idempotency-dedupes per event-id via Redis SETNX, returns 401 before `main`. Pairs with **OF3**.

**OF8 — No errorCode + i18n key catalog; guaranteed wrong-guess on every error/translation path** · High · w1 4/6 · w2 (carried, multiple drivers) · OPEN
Area: core / i18n / framework-wide. An AI writing `{status:'error', errorCode}` or a `useTranslator` call has zero contract for which codes/keys exist or how to register one. Locale files appear in NO generated index, runbook, or MCP tool. ~10 framework codes ship untranslated; the missing-key fallback renders the raw `some.code` to users (Rule 13 violation lint/build/test all pass). Confirmed live: `common.500` referenced but absent from template locale JSON (login A19); `common.404` misused as a generic-crash title (root-src O15); LoginForm hardcoded English strings (cli O9, root-src O15). The cli `check-i18n` "used-key" heuristic is regex-only and mis-reports helper-accessed + underscore/hyphen-namespace keys as "unused", inviting an LLM to delete live keys (cli O8/O12).
Fix: generate `docs/AI_ERROR_CODES.md` / `AI_I18N_INDEX.md` (codes from all `_api`/`_sync` + framework packages + per-locale "translated?" flags + params) via `npm run ai:errorcodes`/`ai:i18n`; missing-locale-key column in `AI_PROJECT_INDEX`; MCP `find_error_code`/`find_i18n_key`; explicit "add errorCode + JSX strings to every locale" runbook step.

**OF9 — No consolidated config-surface / security-defaults contract; default-insecure footguns are tribal** · High · w1 3/6 · w2 (carried) · OPEN
Area: core / server / framework-wide. ~100 `registerProjectConfig` keys live as one comma-list in CLAUDE.md prose; defaults + security implications are scattered across package CLAUDE.md + `SEC-NN`/`//!` code comments. Confirmed-live default-insecure picks an AI would choose when asked to harden: `healthHash:'plain'` (secret-fingerprint leak — core O9, server O2, the `@bootUuid` salt dead on the caller), permissive sync receiver defaults (sync O1, core D1), leftmost-XFF trust under `trustProxy` (core O3), `onStoreError:'memory'` fail-open (core O6), `X-XSS-Protection:'1; mode=block'` (server O10), unsigned double-submit CSRF echo (server O16), implicit `acceptBearerInCookieMode` (core O10), `requestTimeoutMs` config no transport enforces (api O10).
Fix: generate `docs/AI_CONFIG_REFERENCE.md` (key/type/default/security-note/recommended-prod-value/ADR) + `docs/AI_SECURITY_DEFAULTS.md` from `ProjectConfig` + `DEFAULT_PROJECT_CONFIG` + structured `//! security:` annotations; MCP `get_config_key`/`security_defaults`; `create-luckystack-app` prints the insecure-default subset post-install. Pairs with **OF20** (boot-time `verifySecurityPosture`).

**OF10 — Silent paired-file / src⇄template / asset drift has no authoring-time guard (the dominant theme)** · High · w1 4/6 · w2 3/3 · OPEN
Area: cli / scaffolder / create-app / root-src. The single most-corroborated structural class across wave-2. Confirmed-live drifts: credentials self-delete broken in BOTH the asset page and template/consumer page (UI sends `{confirmation:'DELETE'}`, server requires a password — cli O1, root-src O1, scaffolder N1, login LOGIN-01-gdpr); `ErrorPage` stack-leak fixed in framework `src/` but NOT the template copy (scaffolder N2); consumer `src/settings/_api/*` regressed behind the hardened CLI asset (root-src O2/O3 — hand-built `process.env.PROJECT_NAME` Redis keys, `id`-vs-`handle` shape); `template/scripts/generateAiCapabilities.mjs` drifted from root (missing `Tests` column — scaffolder N5); `ls-np/` 115-file stale-overlay artifact (scaffolder N6); 5 dead template vars documented as substituted (scaffolder K17); README references a non-existent `prisma:migrate:dev` script (scaffolder N7); CI `test:e2e` undefined + wrong marker (scaffolder N8); asset↔template `updateUser_v1` exempted indefinitely via `ASSET_AHEAD_OF_TEMPLATE` (cli O4). `// KEEP IN SYNC` markers unenforced; `pruneOptionalPackages` is ~220 lines of literal `[find,replace]` pairs that must byte-match the template (scaffolder K1).
Fix: a `luckystack verify-assets` / `ai:check-template-drift` CI gate (diff every dual-existing file, fail on divergence) + a `checkTemplateSync.mjs` for the root⇄template script pairs + an `ai:lint` invariant flagging `process.env.PROJECT_NAME` Redis-key literals in `_api`/`_sync`; promote `assetParity` to blocking; replace literal-pair pruning with sentinel markers; post-inject `tsc --noEmit` on `luckystack add`. Best: a single-source generator emitting both template + asset copy.

**OF11 — `@luckystack/audit` — tamper-evident actor-action ledger from lifecycle hooks** · High → Medium · w1 6/6 · w2 (carried) · OPEN
Area: framework-wide / hooks. The hook-bus already emits exactly the audit-worthy events (`sessionCreated/Revoked`, `apiAuthRejected`, `rateLimitExceeded`, `csrfMismatch`, `corsRejected`, `onUploadComplete`, `postApiExecute`) and the comments say "audit logs subscribe via registerHook" — but nothing consumes them into a durable, queryable "who did what when". The confirmed OAuth-link/account events are exactly what it should capture. Compliance/IR gap for the explicit multi-tenant SaaS target.
Fix: ship `@luckystack/audit` — `registerAuditSink(sink)` (Prisma `AuditEvent` default + append-only file/SIEM) auto-subscribing the security hooks, writing `{actorId, action, target, ip, requestId, before?, after?, ts}` via `sanitizeForLog`, hash-chained per row; `functions.audit.record(...)` for business events. Pairs with **OF3** (async flush) + **OF4**.

**OF13 — Generated framework-error-envelope missing from per-route response types (forces Rule-21 casts)** · High · w1 3/6 · w2 3/3 · OPEN
Area: core / devkit. `apiRequest`/`syncRequest` return `{status:'error', errorCode}` at runtime (rate-limit, transport, auth) but the generator models only the handler-declared success branch, so every consumer handling a failed call hand-widens with `as` — exactly the cast Rule 21 forbids, on the framework's most-used surface. Confirmed live: `WideSessionResponse` on the bootstrap `system/session` path (root-src O9). An AI following Rule 21 gets stuck.
Fix: have the devkit emitter fold the framework-error branch into each per-route inferred union so callers narrow on `status` without a cast; remove the `Wide*` widenings.

**OF17 — Dist-consumer typed-call parity is unproven (the user's recorded open blocker)** · High · w1 1/6 · w2 (carried) · OPEN
Area: core / create-app / packaging. The whole "install package → AI implements with full typing" premise assumes a freshly-installed consumer gets the same zero-cast typed `apiRequest`/`syncRequest` as an in-repo dev. The generated module-augmentation is verified against in-repo `src` paths, NOT the published `@luckystack/core/client` `.d.ts`; no end-to-end test exists. `apiRequest` is also uncallable (`name` typed `never`) until `apiTypes.generated.ts` exists → cryptic compile error for a fresh consumer.
Fix: a packaging smoke-test (in `create-luckystack-app` smoke or a devkit fixture) that scaffolds + installs from local tarballs + runs `generateArtifacts` + asserts `apiRequest({name,version,data})` type-checks without `any` against DIST types; target the augmentation at the published declarations; add the **OF51** degraded `name:string`-with-breadcrumb overload. The user's memory flags this as THE scaffold blocker.

**OF18 — `node:async_hooks` dragged into the client login bundle (build-breaking shim leak)** · High · w1 1/6 · w2 1/3 · OPEN
Area: root shared / create-app. The repo-root `shared/tryCatch.ts` re-exports the **capturing** core variant, dragging `node:async_hooks` (`AsyncLocalStorage` at module top-level via `sentrySetup`→`errorTrackerRegistry`) into the framework's own client login bundle; vite aliases only `@luckystack/*/client`, NOT bare `shared/*`. CONCRETE PROOF (root-server F1): the built `dist/assets/client-D1shg7jL.js` entry contains both the `node:async_hooks` externalization stub AND a top-level `new AsyncLocalStorage` → the repo login/register client throws at module-eval. Compounded: the shipped template `shared/tryCatch.ts` does NOT capture, but the shipped CLAUDE.md says the server path auto-captures to Sentry → a consumer AI believes handler errors are reported when they're dropped.
Fix: mirror the template's non-capturing client-safe body in the repo `shared/tryCatch.ts` OR break the static `tryCatch`→`sentrySetup` edge in core (lazy `import()` in catch) OR add a `shared/tryCatch` client alias; reconcile CLAUDE.md. Scope-capped HIGH (not CRIT): shipped template is clean — repo-internal drift only.

### Medium

**OF19 — Declarative per-route receiver authorization (sync) — replace permissive global flags** · Medium · w1 4/6 · w2 3/3 · OPEN
Area: sync. Receiver/room authorization is two global flags (`allowClientReceiverAll`, `requireRoomMembership`, permissive-by-default — HTTP enforcement now FIXED, sync F1) plus an opt-in `preSyncAuthorize` hook — invisible to an AI scaffolding a route. A stock install lets a client broadcast cluster-wide or target any room on a `login:false` route fully unauthenticated (sync O1).
Fix: `export const receiver = { allow: 'joined-rooms' | 'all' | (ctx) => boolean }` per route, default-closed, honored by BOTH transports; the generator emits a typed per-route policy. (The default-flip itself is **DD-A**.)

**OF20 — Boot-time security-posture self-check / `verifySecurityPosture()`** · Medium · w1 3/6 · w2 (carried) · OPEN
Area: server / core. Many footguns only bite in prod on default config: CSRF-exempt custom POST routes (server O4), `trustProxy:true` without a proxy hop, dev-surfaces failing OPEN on non-prod hosts (docs-ui auto-mount prod gate is env-only + the `LUCKYSTACK_DOCS_UI_DISABLE_AUTOMOUNT` opt-out is GONE — docs-ui #7), `/_health` leaking secret fingerprints unauthenticated (server O2/O17, core O9), origin-less WS handshake accepted (server O11), deploy.config invariants validated only at codegen never at boot (root-server RS-02). An AI can't infer "behind a proxy"/"no TLS" from config alone.
Fix: add `verifySecurityPosture()` to `verifyBootstrap` warning on these at boot, plus a shared `assertDevSurfaceExposure({routePath, enabledInProd, authorize, bindHost})` so every dev-only package mounts loopback-only by default; call `validateResolvedConfig` in the boot path; surface the same checks statically in **OF6** doctor.

**OF21 — Devkit/test-runner test-coverage map (`ai:coverage`) — distinguish real test from stub** · Medium · w1 2/6 · w2 (carried, multi-area) · OPEN
Area: devkit / test-runner. The only coverage signal is a binary `tested: fileExists(route.tests.ts)`; it can't tell a real test from the scaffold stub, so a placeholder reads "tested: yes" (devkit O7/O19, test-runner #15). Confirmed bugs (the credentials-delete UI break) live in exactly the per-route-untested-flow routes. The auto-sweep also never walks sync (test-runner #7), the contract layer passes any error envelope (test-runner #6), and skipped/xpass don't move exit totals (test-runner #11) — so the headline X/Y overstates health.
Fix: emit `docs/coverage.generated.json` per route `{kind,transport,auth,mutating,sweep:{applicable}, perRouteTest:'none'|'placeholder'|'real', gapReasons:[]}`; source `AI_PROJECT_INDEX`'s `tested` column as `none/stub/real`; MCP `coverage_gaps`/`route_coverage`; make `scaffold:test` coverage-aware.

**OF22 — Contract-honesty lint-rule (`@wired-by`/`@guarantee`/dead-export) for unwired-guarantee surfaces** · Medium · w1 2/6 · w2 3/3 (every shadow-API NEW) · OPEN
Area: framework-wide. Docs/types/comments promise behavior the body doesn't deliver and are test-backed in isolation, so the usual "does the test pass?" counter-check also fails. Wave-2 produced a *flood* of confirmed instances this rule would catch: `preEmailSend` no-op (NF3), `posthogConfig.ts` dead (NF5), error-tracking barrel-omits-5 (NF6), test-runner CSRF + extension-runtime unwired (NF2, test-runner #3), docs-ui `renderCore` shadow (NF1), presence `getRoomPresence`/`getLastActivity`/`listActivityEvents` shadow (presence #12), secret-manager whole config surface unwired (DD-H), server `timingSafeStringEqual` dead-but-wired (server O3), api/sync CLAUDE.md documenting 5 phantom hooks/helpers/config keys (api O1), `socketLeaveRoom` misnomer (presence #7), `startSpanHandle`/`registerTestFixture` no prod caller.
Fix: add an `ai:lint` rule (in `@luckystack/core/eslint`) flagging (a) `@wired-by <symbol>`/`@guarantee` JSDoc whose symbol has no real caller, (b) any exported symbol whose only importers are `*.test.ts`, (c) any hook/config key named in a package CLAUDE.md not dispatched/read in that package, (d) barrel-vs-CLAUDE.md-Function-Index export drift; degrade aspirational comments to imperative. The structural counter to the entire NEW-row shadow-API cluster.

**OF23 — Contract-trust toolchain — machine-readable type-diagnostics + shim parity** · Medium · w1 2/6 · w2 (carried) · OPEN
Area: devkit. The AI-driven model trusts all generated types equally, but the generator silently emits unreliable types: extractors falling back to `{}`/`{status:string}` (devkit O7/O19), `__RUNTIME_UNRESOLVED__` markers, `z.any()` fallbacks (devkit O15/O16). The `functions.{redis,sentry}.default` shim-key mismatch is now **FIXED** (root-server #18 removed). No way to ask "which routes have a trustworthy contract?" before building.
Fix: emit `apiTypeDiagnostics.generated.json` per route `{resolved, fellBackToDefault, unresolvedSymbols, zodFallback}`; add a `luckystack validate-types` gate (regen in tempdir, fail on markers/fallbacks/diff); keep a shim parity-check in `generateTypeMapFile()`. Overlaps **OF15/OF16/OF21**.

**OF24 — Doc-contract lint (`ai:doccheck`) — type-check the CLAUDE.md/ARCHITECTURE code snippets** · Medium → High · w1 2/6 · w2 (carried) · OPEN
Area: docs / framework-wide. The always-read CLAUDE.md + package docs ship code examples the runtime/codegen REJECT or that drift from code: `export const method` (codegen reads only `httpMethod`); positional `upsertSyncEventCallback(name,cb)` (silent no-op); `syncRequest({name,data,receiver})` missing required `version`; the api/sync CLAUDE.md documenting 5 phantom hooks/helpers/config keys (api O1); `http-proxy.md` actively teaching the REMOVED insecure XFP behavior (router O8); the `registerRoomNameFormatter` docstring claiming a wiring that doesn't exist (presence #2); secret-manager CLAUDE.md Function Index omitting 9 config keys + 2 exports (secret-manager N1). An AI copies them verbatim and ships a no-op / re-introduces a hole.
Fix: ship `@luckystack/doc-contract-lint` (`ai:doccheck`): extract fenced ts blocks from CLAUDE.md/ARCHITECTURE/package docs, wrap in a synthetic module importing the real `@luckystack/core/client` + generated maps, type-check via the devkit `ts.Program`, map diagnostics back to `<docfile>:<line>`, exit 1 on error; fix the `method`→`httpMethod` + phantom-hook + router-proxy-doc snippets now.

**OF25 — Manifest-driven static allowlist + build-time served-root check (server source disclosure)** · Medium · w1 2/6 · w2 3/3 · OPEN (the `/server.js` disclosure itself FIXED — see FX9)
Area: server / scripts. The disclosure is now **FIXED** in-tree (denylist regex, FX9) BUT the underlying model is still a hand-maintained substring/basename DENYLIST (allow-by-default) that drifts from the framework route-regex (root-server #2, server F9 residual), the compiled `dist/server.js` is still emitted into the served `dist/`, and BOTH guard fixes are uncommitted. One stray `dist/` artifact re-opens disclosure.
Fix: `@luckystack/server` owns a serve-by-manifest helper reading the Vite build-manifest (anything not a declared client output 404s by construction) + a post-build assertion (in `bundleServer.mjs`/doctor) failing the build if the server bundle or any `*.map` lands in the served dir; build the server bundle OUTSIDE `dist/`.

**OF26 — Router edge-trust + forwarding-prelude + upstream timeout (shared HTTP+WS proxy primitive)** · Medium · w1 2/6 · w2 3/3 · OPEN (WS crash/SSRF/spoof FIXED — see FX-router)
Area: router. The WS-proxy CRITICALs + XFF/XFP spoof are now **FIXED** (centralized in `proxyUtils.ts`, FX-router). Residual capability gaps: the WS+HTTP **upstream leg has NO timeout** (router O1, NEW HIGH within router — half-open pair accumulation, unauth-reachable on a stalled `system` backend, 3/3 wave-2), `x-forwarded-host` reflected verbatim (host-poisoning seam, router O15), no parity test pinning the now-shared guards (router O21).
Fix: one `forwardingPrelude`/`wsUpgradeGate` helper: set `timeout` on `transport.request` from `routing.upstreamTimeoutMs` (504 + destroy), origin-form guard, trustworthy forwarding-header assembly, `Host` validation against `routing.allowedHosts`, optional upstream-host allowlist; add a both-proxy guard-parity test. The deny-gate is **DD-B**, the body-cap **DD-C**.

**OF27 — OAuth verification-policy slot + typed custom-provider authoring kit** · Medium · w1 4/6 · w2 3/3 · OPEN (takeover-class FIXED — see FX-login)
Area: login. The unverified-OAuth-email account-takeover under `unified` is now **FIXED** (FX-login). Residual: verified-email "trust" semantics still have no single typed contract / per-route policy slot; `register.ts` now captures config + registers providers at MODULE LOAD (LOGIN-01 regression — stale `callbackBase`); a single-provider merge-by-name append is still missing (the consumer overlay re-implements the whole registry).
Fix: one `auth.oauthEmailVerification: 'require'|'trust'|'per-provider'` slot (default `'require'` under unified) closing the class generically; a `defineOAuthProvider` factory + the now-exported `asOAuthUserData`/`OAuthUserData`; defer all `register.ts` config/env reads to provider factories / first-request assembler. Pairs with **OF50**.

**OF28 — Generated typed shapes for non-route extension surfaces (hooks/adapters/providers/config)** · Medium · w1 2/6 · w2 (carried) · OPEN
Area: framework-wide / devkit. Routes are reliably AI-driveable because their shapes are generated; the 40+ hooks, the `SessionProvider`/`UserAdapter`/`EmailSender`/`RateLimitStrategy` slots, and the `registerProjectConfig` bag exist only as scattered markdown tables. An AI writing a hook-handler or custom adapter reconstructs the shape from prose and guesses wrong (the LOGIN-02 raw-`srem` adapter bypass, the email custom-adapter `attachments`/`headers` drop, the F39/F46 prisma-accessor casts are all symptoms).
Fix: `scaffold:hook`/`scaffold:adapter` generators emitting typed skeletons from the installed package declarations + a generated `AI_EXTENSION_INDEX.md` + MCP `get_hook`/`get_adapter`. Builds on **OF12**'s manifest.

**OF29 — `@luckystack/rbac` / `registerPermissions` — uniform permission registry** · Medium · w1 1/6 · w2 (carried) · OPEN
Area: login / framework-wide. RBAC exists in two incompatible forms: `auth.additional[]` predicates on routes AND hand-written `switch(path)` middleware in `page.tsx`. No registry to answer "who may X"; an AI adding an admin route guesses which mechanism a sibling used. Authorization is the highest security-severity AI-error class (Rule 19).
Fix: thin `@luckystack/rbac` (or core `registerPermissions`/`definePermissionSet`) that both `additional[]` and the middleware route through (`can(user, permission)`); per-route "required permission" column in `AI_PROJECT_INDEX`.

**OF30 — `graph_status` / freshness + correctness signals for MCP tools** · Medium · w1 2/6 · w2 1-2/3 · OPEN (prototype-key + numeric-id bugs FIXED — see FX-mcp)
Area: mcp. Prototype-chain keys, numeric `get_decision` over-match, and `resolveNodeId` raw-vs-normalized are now **FIXED** (FX-mcp). Residual: no staleness signal (uncommitted `src/` edits serve a stale graph, no `generatedAt`, mcp #8); `grepLines` truncates at 60 with no flag (mcp #2 — an agent concludes a helper doesn't exist, defeating Rule-12); `god_nodes` schema advertises `max(100)` but producer caps at 25 (mcp #5); `loadGraph` `JSON.parse` outside try/catch (mcp #1, NEW — a merge-conflicted graph crashes the handler); basename-collision + slug-substring return bare `null` with no "ambiguous" signal (mcp #3/#7); no schema↔producer contract test.
Fix: emit `generatedAt` + a `graph_status` tool `{version,nodeCount,edgeCount,generatedAt,stale}`; truncation flag on `grepLines`; align the `god_nodes` cap; wrap `JSON.parse`; distinguish not-found from ambiguous; add a generator→`GraphSchema.safeParse` fixture test.

**OF31 — Outbound webhooks (`@luckystack/webhooks` outbound) — signed + retried delivery** · Medium · w1 1/6 · w2 (carried) · OPEN
Area: framework-wide. `ARCHITECTURE_HTTP.md` covers inbound but there's zero support for outbound delivery (`order.created`, `user.deleted` to other systems with signed payloads, per-endpoint subscriptions, retries, delivery-log). Every API-product consumer re-implements signing/retry/backoff/replay.
Fix: `registerWebhookEvent(...)` + `WebhookEndpoint` model + `webhooks.emit(event, payload)` enqueuing a signed delivery per matching endpoint (built on **OF3**); `HMAC-SHA256(secret, timestamp + '.' + rawBody)` in `X-LuckyStack-Signature` matching the inbound-verify shape; exponential backoff + jitter, dead-letter, per-endpoint delivery-log + manual replay; ships `verifyWebhookSignature` so **OF7** becomes a one-liner.

**OF32 — Request-scoped telemetry/identity context as a first-class seam (legacy Sentry path)** · Medium · w1 1/6 · w2 1/3 · OPEN (adapter path FIXED — see FX5)
Area: error-tracking. The ALS identity is bound on the ADAPTER path (FX5). Residual: a consumer using only the legacy `initializeSentry()` slot has a process-global `setUser` scope that `propagateIdentity` overwrites per request (cross-request user bleed, error-tracking O3), and `sentrySetup.captureException` forwards RAW context as Sentry `extra` bypassing `sanitizeForLog` (error-tracking O2). The api/sync handlers own the request boundary; an adapter can't bind its own scope.
Fix: make `initializeSentry()` register `createSentryAdapter()` instead of the legacy `initSharedSentry` slot — one change closes O2 (adapter scrubs context), O3 (adapter reads ALS per-event), and the N5 double-capture. Pair with an adapter-conformance test-kit (`@luckystack/error-tracking/testing`) enforcing identity isolation under concurrency. Worth an ADR.

**OF33 — Token-link hygiene helper + privacy-default observability preset** · Medium · w1 1/6 · w2 (carried) · OPEN (consumer Sentry masking FIXED — see FX-misc)
Area: login / error-tracking / template. The consumer `src/_functions/sentry.ts` masking is now **FIXED**. Residual: reset/email-change tokens still travel in the query-string (logs/Referer leak — root-src O8, login F19/F33) while OAuth deliberately uses the fragment; email-change confirm auto-fires on page mount (prefetch detonation — root-src O7). Each hand-rolled per page.
Fix: one `auth.tokenLinkMode: 'fragment'|'code'` helper that both mints the link AND does the page-side capture (fragment-read + `replaceState`-strip, or `?code=`→POST exchange); require an explicit confirm-button click; an `initObservability()` preset with privacy-on defaults.

**OF34 — Typed failure taxonomies + retriable + idempotency helpers (email/secret-manager)** · Medium · w1 2/6 · w2 2/3 · OPEN
Area: email / secret-manager. `EmailResult.reason` is an open `string` with a fixed undocumented vocabulary (email #17); `resolveSender` silently falls through to default/ConsoleSender on an explicitly-requested-but-unregistered adapter (email O4 — a reset-mail can go via the wrong sender while the caller sees `{ok:true}`); there's NO send timeout (email O8 — a hung SMTP blocks an unauth forgot-password handler); `EmailMessage.attachments`+`headers` are typed+promised but silently dropped by `toProviderPayload` (email O2); secret-manager's `onResolveError` gets `unknown`.
Fix: export `EmailFailureReason` (`| (string & {})`) + `isRetriableEmailFailure(reason)` + a first-class `idempotencyKey` + `sendTimeoutMs` on `SendEmailInput`; forward attachments/headers in `toProviderPayload` (or `@deprecated` them); warn/strict-mode on an explicit-miss adapter fallback; a discriminated `SecretResolveError {phase, kind}`. (The explicit-miss fall-through policy is **DD-F**; the new-feature hardening is **DD-I**.)

**OF35 — Security-token primitives: hashed-token-at-rest + atomic session-slot + single-use consume** · Medium · w1 1/6 · w2 3/3 · OPEN
Area: login. Password-reset, email-change, and OAuth-state have three subtly-different Redis single-use-token implementations (login F36); reset/email-change tokens are stored UN-hashed at-rest while OAuth state hashes its nonce (login F19, 3/3); `rejectNew` (session cap) is a racy read-then-write TOCTOU (login F4, 3/3); the session token is stored plaintext inside the Redis VALUE (login M9); the Redis session write is non-atomic SET-then-EXPIRE (login M7 — an immortal TTL-less session on a crash between).
Fix: one `tokenStore` primitive (`mint() -> {token, key}`, `consume(token)`) hashing before storage + invalidating prior tokens on re-issue + asserting the MULTI `del` leg; an optional `reserveSlot(userId, cap): Promise<boolean>` on `SessionAdapter` (Redis INCR/Lua) making `rejectNew` a hard cap; atomic `set(key,val,'EX',ttl)`; strip `token` from the persisted value. One `consumeSingleUseToken(key)` in core used by all three consumers. All three runs converge on these.

**OF36 — Schema-aware fuzzing + sync auto-sweep + CSRF-threading + machine-readable run-report** · Medium · w1 2/6 · w2 3/3 · OPEN
Area: test-runner. The auto-sweep walks only `apiMethodMap`, never sync (test-runner #7 — the most authz-sensitive surface, exactly the F1/O1 room-membership class, is the one untested); authenticated sweeps omit the CSRF header the server enforces (test-runner #2 — and the mutating-method partition was removed, so fuzz now hits authed DELETE/PUT with junk on token-mode); the contract layer counts any error envelope as PASS (test-runner #6); `__proto__` fuzz payload serializes to `{}` (test-runner #17, inert); the extension/fixture registry is unwired (test-runner #3); output is console-only.
Fix: extend the contract/auth/csrf sweep over a `syncMethodMap`; thread a session-bound CSRF token (auto-mint a session); assert `success` on valid input; pre-serialize the `__proto__` payload; wire (or honestly document) the fixture registry; emit a JSON/JUnit run-report for agent consumption.

**OF37 — First-class room-presence roster/snapshot sync event + cross-instance activity store** · Medium · w1 2/6 · w2 1/3 · OPEN
Area: presence. Presence is delta-only (`userAfk`/`userBack`/`userLeft`), so a late joiner has no built-in initial roster; `getRoomPresence` is a pull API the consumer must hand-wire AND it's a shadow-API (no prod caller — presence #12) AND it reads a local-only `lastActivityBySocket` so multi-instance remote peers always show `afk:false` (presence #3, the cross-instance hole the shadow-status hid); per-token AFK rollup is missing (false-AFK in normal multi-tab use, presence #4/#5).
Fix: a framework-shipped typed `requestRoomPresence → RoomPresenceEntry[]` snapshot+delta sync event (gated by `socketActivityBroadcaster`), wired into the room-join flow; store last-activity in a cross-instance Redis key (or return tri-state `afk: boolean|'unknown'` for remote sockets); aggregate AFK per token. Pairs with **OF49**.

**OF40 — Typed `apiDocs.generated.json` contract package + `schemaVersion`** · Medium · w1 2/6 · w2 2/3 · OPEN
Area: docs-ui / devkit. The docs-UI renderer best-effort-coerces an untyped artifact (`data.apis ? data.apis : data`), duplicates `DocsApiEntry`/`DocsSyncEntry`, accepts a legacy bare-map with no `schemaVersion` handshake, and re-derives the entry key in two places. **This independence is the direct root cause of NF1** (each side declared the shape independently → the nested-vs-flat mismatch).
Fix: ship a typed `@luckystack/api-docs-contract` (types + `schemaVersion` + `parseApiDocs(json): Result`) consumed by BOTH the devkit emitter and docs-ui; stamp a `schemaVersion`; export one `docsEntryKey(...)`. A shared versioned parser makes NF1's mismatch a compile error.

**OF41 — `--db=sqlite` scaffolds an invalid Prisma schema (advertised option fails on first run)** · Medium · w1 1/6 · w2 (carried) · OPEN
Area: create-app. `--db=sqlite` is an advertised, validated `PROVIDER_OPTIONS` entry, but the template `schema.prisma` uses enums + `Json?` which the SQLite connector rejects, so the auto-run `prisma generate` fails on the very first scaffold.
Fix: add a SQLite schema transform (enum→`String @default(...)`, `Json?`→`String?`) gated on `dbProvider==='sqlite'`, OR drop `sqlite` from `PROVIDER_OPTIONS` until supported; add a per-DB `prisma generate` smoke test.

**OF42 — Mutator/integration test coverage for the scaffolder + asset/template combos** · Medium · w1 4/6 · w2 3/3 · OPEN
Area: scaffolder / create-app. `index.test.ts` exercises only pure helpers; `copyTree`/`pruneOptionalPackages`/`editScaffoldFile`/`injectOptionalDeps`/`wireAiBrowserTooling`/framework-docs copy run only via the full smoke (scaffolder K14) — so an AI gets a green unit run while a `--auth=none`/`--no-i18n`/`--no-presence` scaffold is broken (the **OF10** prune-drift class). The credentials-delete + ErrorPage template HIGHs (NF1-class) shipped through 6 audit rounds precisely because nothing compiled the injected/copied output.
Fix: a temp-dir integration test scaffolding each major choice combination + asserting the result builds + `ai:lint`-clean; a CI matrix over `--auth=none`/`--no-presence`/`--no-i18n`/`--db=*`; post-inject `tsc --noEmit` on `luckystack add`.

### Low / forward-looking

**OF12 — Package Capability Manifest + MCP package-surface tools (installed + offline catalog)** · Medium→Low · w1 1/6 · w2 (carried) · OPEN
Area: mcp / devkit / framework-wide. Every `@luckystack/*` surface (exports, hooks `{mutable,stopCapable,payloadType}`, registries, env-vars, config keys, self-wire entrypoints, peerDeps) lives only as lossy prose; MCP serves only the consumer's own artifacts; for an uninstalled package no local detail exists. The foundation `luckystack doctor` (OF6), the env/config catalogs (OF8/OF9), and `scaffold:hook` (OF28) all need.
Fix: generate per-package `luckystack.manifest.json` from the devkit TS-program pass; aggregate in the consumer repo at install. MCP `get_package(name)`/`find_hook`/`find_export`/`package_env` + a bundled offline catalog so pre-install questions resolve.

**OF15 — `ai:verify` end-to-end "AI-add actually wired up" gate + drift gate** · Medium→Low · w1 3/6 · w2 (carried) · OPEN
Area: framework-wide / devkit / CI. Runbooks chain `generateArtifacts → lint && build → test`, but no command proves the end-to-end contract held (route generated + typed without casts + validates at runtime + round-trips). `lint`/`build` pass even when the prod validator fell open (OF2) or the typed call silently degraded (OF16) or codegen dropped every route on a non-`src` layout (NF4). CI lints/builds against an UNCOMMITTED gitignored `apiTypes.generated.ts` (only non-empty checked).
Fix: ship `npm run ai:verify [<route>]`: regenerate → assert route in maps → assert a synthetic typed `apiRequest` has no `any` against DIST types → assert `validateType` doesn't fall open on the declared input → run the route's sweep; add a CI drift gate (regenerate all indexes to a tempdir + `git diff --exit-code`); a non-`src`-layout conformance check (NF4).

**OF16 — Type/Zod extraction degrades silently to placeholders (generator-fidelity)** · Medium→Low · w1 3/6 · w2 (carried) · OPEN
Area: devkit. `extractors.ts` returns default `{}` input / `{status:string}` output on any catch or missing source file (devkit O7/O19); `zodEmitter` emits `z.any()` for intersections/cross-file refs (devkit O16). An AI trusts the generated contract and can't know a route got a placeholder → its runtime fuzz/validation gate is OFF.
Fix: emit a per-route coverage map (`EXACT`/`FELL_BACK_TO_DEFAULT`/`DEGRADED_TO_ANY` + reason) + a marker comment in the generated TS; adopt a loud `__RUNTIME_UNRESOLVED__`-style marker; surface via doctor + MCP; optional strict-mode failing CI on degraded-to-default for non-trivial inputs. (Output-shape decision = **DD-G**.)

**OF43 — Phased boot/registration API (`bootstrapLuckyStack({phases})`)** · Low · w1 2/6 · w2 1/3 · OPEN
Area: server / core. Boot order is import-order-by-comment; no machine-checkable boot-sequence contract. Confirmed live: `config.ts` captures `EMAIL_FROM`/`DNS`/`EXTERNAL_ORIGINS` at module-eval BEFORE `resolveSecretsIfConfigured` (root-server RS-06); the boot order is "correct but tribal" (root-server #21). Pairs with **OF6/OF20**.

**OF45 — `tryCatch({capture:false})` / benign-error classifier** · Low · w1 1/6 · w2 (carried) · OPEN
Area: core. `tryCatch` sends every caught error to `captureException`, flooding the tracker with expected control-flow errors; callers can't opt out. Fix: optional `{capture?: false}` param or a registered benign-error classifier.

**OF46 — Typed project-Prisma accessor + `pingDatabase()` (avoid the Rule-21 cast)** · Low · w1 1/6 · w2 2/3 · OPEN
Area: core / framework-wide. Handlers + health probe + `notifications.ts` reach for a cast (`prisma as unknown as PrismaPingShape` server O9, the notifications stacked `as`/`as unknown` root-server #4). No typed accessor → the safe path isn't the convenient one. Fix: a typed provider-aware `pingDatabase()` + project-Prisma accessor generated alongside the client.

**OF47 — Cross-instance backpressure `flushPressureTo(tokens|rooms)` + byte-accurate threshold** · Low · w1 2/6 · w2 3/3 · OPEN
Area: sync. `flushPressure` is a packet-COUNT approximation under a byte-named `thresholdBytes` API (sync O5/N5), samples only the local room (cross-instance recipients invisible — sync O13), and for `streamTo` samples the wrong room. Plus the **NF7** shadow-config `maxBufferedBytes` + the **OF49** unbounded `chunkCounters` Map leak. Fix: rename `thresholdPackets` or sum real bytes; read the configured default; sample a rotating subset; `flushPressureTo(tokens|rooms)`; document the cross-instance limitation.

**OF48 — Tiered idle states (idle/away/offline) as a registerable preset** · Low · w1 1/6 · w2 (carried) · OPEN
Area: presence. Presence is binary present/AFK; a `registerActivityTier` preset is a low-priority registerable extension.

**OF49 — Bounded/self-evicting TTL presence/stream-state store primitive** · Low · w1 1/6 · w2 1/3 · OPEN
Area: presence / core / sync. `clientSwitchedTab`/`tempDisconnectedSockets`/`disconnectTimers`/`lastActivityBySocket`/sync `chunkCounters`/event `lastFired` are module-level Maps/Sets cleaned only by specific lifecycle paths (the `clientSwitchedTab` leak was a confirmed-now-fixed bug; sync `chunkCounters` grows unbounded — sync N4). A shared TTL-bounded store primitive in core makes them structurally leak-proof. Pairs with **OF37**.

**OF50 — Single-provider `registerOAuthProvider` merge-by-name append** · Low · w1 1/6 · w2 (carried) · OPEN
Area: login. Provider registration is all-or-nothing; the overlay re-implements the whole registry today and clobbers framework wiring. Fix: a `registerOAuthProvider(one)` that merges-by-name; delete the consumer overlay. Pairs with **OF27** + LOGIN-01.

**OF51 — Generation-state-aware `apiRequest`/`syncRequest` fallback (`name: never` → `name: string`)** · Low · w1 1/6 · w2 (carried) · OPEN
Area: core. Before `generateArtifacts` runs, the empty `ApiTypeMap` collapses `ApiFullName` to `never`, giving a fresh consumer/AI a cryptic compile error (overlaps OF17). Fix: a degraded overload guarded by `[keyof ApiTypeMap] extends [never]` accepting `name:string;version:string` with a breadcrumb pointing at `npm run generateArtifacts`.

**OF52 — Shared `_lib/scan.mjs` for the generator scripts** · Low · w1 2/6 · w2 (carried) · OPEN
Area: scripts / framework-tooling. `safe`/`walkFiles`/`extractImports`/`extractExports`/brace-scanner are duplicated across 6-7 `ai:*` generators AND duplicated again into `template/scripts/` (~12 copies for one fix); the regex export extractors miss valid TS forms and mis-count `additional[]` (cli O8, scaffolder K16, devkit O13). Fix: one pure-Node zero-dep `scripts/_lib/scan.mjs` imported by all generators (+ shipped in template); reuse `generateGraph.mjs`'s `ts.Program` pass; CI diff-check on the source/template pairs. Direct contributor to **OF10**.

**OF53 — npm-publish provenance + CI publish job + committed generated artifacts** · Low · w1 1/6 · w2 (carried) · OPEN
Area: CI / scripts. `npm publish` runs with no `--provenance`, no integrity/audit gate, no CI publish job — 16 public packages ship from a dev laptop; all three MCP servers + `@luckystack/mcp` wired `@latest` (unpinned supply chain — scaffolder K4). Fix: a tag-triggered GH Actions publish using OIDC + `npm publish --provenance`, built from clean `npm ci` + `npm audit --omit=dev`; commit the generated artifacts + a CI regen gate (also satisfies OF15's drift gate); pin the `@latest` entries.

**OF54 — Misc smaller primitives (backlog cluster)** · Low · w1 3/6 · w2 (carried) · OPEN
Area: framework-wide. A cluster of lower-priority primitives: typed `EmailModule` contract (`@luckystack/email/contract`) so the login lazy-import stays typed (email cross-pkg #15, root-server #16), a non-capturing `tryCatch` variant for scripts/client (overlaps OF18), a tenant-scoped `clearRateLimits(prefix)` replacing the fleet-wide `clearAllRateLimits` (core O8), routing the boot-UUID key through `formatKey` for multi-project Redis safety, `getResolvedPointerNames()` values-free secret-manager accessor (secret-manager F10), `updatePreferences` merge-not-overwrite primitive (root-src O14). None blocking; implement opportunistically alongside the Medium/High items they compose with.

---

## DEFERRED-DECISION — real, blocked on a policy/ADR/contract call

**DD-A — Flip the permissive sync receiver defaults for 0.2.0** · Medium · w1 5-6/6 · w2 3/3 · DEFERRED-DECISION
`allowClientReceiverAll:true` / `requireRoomMembership:false` are deliberate 0.2.0 posture (sync O1, core D1). The secure values exist but are opt-in; flipping them is a cross-package policy call (defaults in core, enforcement in sync). Decision: flip + ship the scaffold `config.ts` with secure values, OR emit a boot/dev warn when both permissive. The headline 0.2.0 hardening item.

**DD-B — Router edge deny-gate (`proxyRequestGate`) on both transports** · Medium · w1 refutes-#3/#11 · w2 3/3 · DEFERRED-DECISION
No edge deny mechanism exists on either proxy — only observer hooks; WS dispatches nothing (router DD2). A socket-first deployment has no way to refuse a banned IP / enforce maintenance mode / edge auth. The wave-1 "gate fails open" findings were FALSE-POSITIVE (the gate never existed). Decision: add a fail-CLOSED `proxyRequestGate` with a typed stop contract on both paths, or document the absence.

**DD-C — Router request-body size cap (capability removed)** · Medium · w1 (regression) · w2 1/3 · DEFERRED-DECISION
The edge body cap was removed this week (router DD1); an unbounded upload now streams straight to the backend (backends still defend). Decision: re-introduce a config-driven edge 413, or DOCUMENT that body limiting is intentionally delegated to the backend.

**DD-D — Rate-limit fail-OPEN default on Redis outage** · Medium · w1 1/6 · w2 (carried) · DEFERRED-DECISION
`store:'memory'` + `onStoreError:'memory'` default → per-process counting multiplies the effective global limit by instance count under outage (api D1, core O6). Documented availability tradeoff. Decision: keep `'memory'` default or default `'deny'` when `store:'redis'` AND prod; or boot-warn that `onStoreError` is Redis-mode-only.

**DD-E — `requestEmailChange` `emailTaken` enumeration oracle** · Low · w1 4/6 · w2 3/3 · DEFERRED-DECISION
Authenticated, 5/window rate-limited; the inline comment documents the tradeoff (login A12, root-src O6). No ADR exists. Decision: accept (write `docs/decisions/`) or match the reset flow's always-success.

**DD-F — Email `resolveSender` explicit-adapter silent fall-through** · Medium · w1 5/6 · w2 2/3 · DEFERRED-DECISION
Fixing **OF34**'s O4 changes the documented "send via fallback rather than drop" policy (email DD1). Product call on whether an explicit-adapter miss should fail/warn vs the best-effort `adapterHint`.

**DD-G — Strict-codegen gate / `apiTypeDiagnostics.generated.json` output-shape** · Medium · w1 — · w2 3/3 · DEFERRED-DECISION
Emitting a per-route coverage/marker (`__RUNTIME_UNRESOLVED__`, `.strict()` on Zod) changes generated OUTPUT SHAPE and flips test-runner fuzz pass/fail (devkit D1/D2). A deliberate marker-contract decision underpinning **OF15/OF16/OF21/OF23**.

**DD-H — Secret-manager config surface widening (whole shadow-API)** · Medium · w1 — · w2 1/3 · DEFERRED-DECISION
The wired prod caller forwards only `{url,token,source:'remote',dev}`; `source:'hybrid'|'local'`, `retries`, `timeoutMs`, `envNames`, `pollIntervalMs`, `onApplied`/`onResolveError`, `refreshSecretManager`/`reloadSecretManagerFromFiles`/`stopSecretManager` have NO prod path (secret-manager D1 — `startRotationPoll` is dead in prod). Decision: widen `SecretManagerBootConfig`, OR document only `url`/`token`/`dev` are supported. (The fail-OPEN seam itself is intentional — FALSE-POSITIVE XP7.)

**DD-I — Email render-error / CRLF / send-timeout hardening as new features** · Low-Med · w1 WONT-FIX(phantom) · w2 — · DEFERRED-DECISION
CRLF sanitization (email O9), send-timeout (email O8 = OF34), ConsoleSender token redaction (email O7), CTA-URL/accent validation (email O3) are NEW features relative to the simplified tree (wave-1 audited a phantom more-elaborate file). The decision is whether to ADD the hardening, not whether the gap exists.

**DD-J — `env.ts` eager `export const env` (import-time side effect)** · Low · w1 3/6 · w2 (carried) · DEFERRED-DECISION
Converting the eager `export const env = bootstrapEnv()` to lazy `getEnv()` is a public-API change for every `import { env }` consumer (core D2). Idempotent + cached today. Needs an ADR.

---

## FIXED — closed in the current tree this week (compact; recorded so they are NOT re-flagged)

**FX1 — `apiRequest` offline-queue `onDrop` hang** · High · w1 6/6 · `apiRequest.ts:439-446` — onDrop wired with the `suppressOnDrop` window mirroring `syncRequest`.
**FX2 — `validateType` terminal fail-OPEN + unbounded recursion** · High · w1 6/6 · `runtimeTypeValidation.ts:363-373,184-189` — terminal fails CLOSED, depth-capped (64), Record-value + proto-key checked, `safeValidateType` wrapper, prod `enforce`. (Residual parser corners → **OF2**.)
**FX5 — Per-request error-tracker identity (ALS) bound in prod (adapter path)** · High · w1 6/6 · all four request handlers open `runWithErrorTrackerIdentityScope` pre-await; PostHog/Datadog/Sentry adapters read the getter. (Legacy-Sentry-slot residual → **OF32**.)
**FX9 — `/server.js` + `*.map` source-disclosure** · High (was CRIT) · w1 4/6 · `staticRoutes.ts:14,43-47` denylist regex 404s before any `serveFile`; sourcemaps default off. (Uncommitted + allow-by-default residual → **OF25**.)
**FX-router — WS-proxy crash (listener-less RST) + SSRF/host-pin + XFF/XFP spoof + req/res abort leak** · Critical/High · w1 6/6 · `wsProxy.ts`/`httpProxy.ts`/`proxyUtils.ts` centralize the guards; `wsProxy.test.ts`/`redisHealthStore.test.ts` regressions present. (Upstream-timeout + deny-gate residual → **OF26**/**DD-B**.)
**FX-sync — HTTP `requireRoomMembership` enforcement + client-only-route auth + validation-mode typo + array-guard + `'all'` branch** · High/Med · w1 6/6 · both transports now fail-closed (sync F1-F5).
**FX-login — Unverified-OAuth-email takeover under `unified` + raw-claims `extraSessionFields` + framework-key clobber + Microsoft UPN + backslash open-redirect** · High · w1 4/6 · fail-closed cross-provider guard + per-provider `emailVerifiedKey` (login F1-F12). (Verification-policy slot residual → **OF27**.)
**FX-mcp — Prototype-chain key lookups + numeric `get_decision` over-match + `resolveNodeId` normalization + `find_route` row guard + empty-query dump + path-containment guard** · High/Med · w1 1-6/6 · `Object.hasOwn` guards + padded-prefix numeric ids + normalized fallback (mcp 13-20). (Staleness/truncation residual → **OF30**.)
**FX-misc — devkit root-sync route-key divergence (`'root'`→`'system'`) + supervisor missing-`'error'`-crash + `functions.{redis,sentry}.default` dead exports removed + consumer Sentry replay masking** · High/Low · w1 2-3/6 · devkit F1-F3, root-server #18, root-src F1.

---

## FALSE-POSITIVE — bogus (one-line reason each)

**XP1 — "`validateType` still fails OPEN"** — stale read of a pre-fix snapshot; current terminal + unresolved branches fail CLOSED (api FP1, devkit FP2). Recorded FX2.
**XP2 — Router `proxyRequestGate` deny-gate "never applied to WS" / "fails open on throw"** — the gate the baseline assumed NEVER existed (grep=0); the real gap is the absent deny capability, tracked as **DD-B** (router FP1/FP2).
**XP3 — Server "`registerErrorFormatter` is a shadow API, never consulted"** — stale-scope grep; `applyErrorFormatter` IS called in api/sync handlers + `errorBuilders` (server FP1).
**XP4 — Socket transport "no HTTP method = HIGH transport-asymmetry vuln"** — method is an HTTP/CSRF-semantics control, not an authz boundary; the socket is independently origin-gated + auth-checked per message (core FP2). The real GET-named-mutation hazard is folded into **OF1**.
**XP5 — Email `sanitizeHeaderMap`/`withSendTimeout`/`redactUrlSecrets`/`safeColor`/`dispatchEarlyDrop` phantom-code findings** — none of those helpers ever existed in git history; wave-1 audited a hallucinated `sendEmail.ts` (email FP1-FP8). Genuine residuals re-characterized as **OF34**/**DD-I**.
**XP6 — docs-ui CSP `nonce` mismatch / `ensureCsrfToken` race / `enabledInProd` boot-warning** — the constructs no longer exist in the rewritten package (grep=0) (docs-ui X1-X5).
**XP7 — Secret-manager / docs-ui fail-OPEN-on-absence "is a defect"** — confirmed DELIBERATE per user-memory (`feedback_secret_manager_failopen`); once `url` is set the remote path is fail-CLOSED; the docs-ui authorize gate is fail-closed (secret-manager FP1/FP2, root-server RS-07, core FP6). Recorded so it's never "fixed" into a fail-closed regression.

---

## Cross-area ownership notes for the orchestrator

- **The two HIGH NEW shadow-API discoveries (NF1 docs-ui renderer, NF2 test-runner CSRF) + NF3 (email preEmailSend) are the standout actionable items** — all three are fully-built-but-unreachable seams a green test suite hides. They are the empirical case for **OF22** (contract-honesty / dead-export lint) and **OF6** (doctor).
- **`OF5` (flush-on-shutdown) + `OF18` (async_hooks leak)** are both rooted in `@luckystack/server` + the repo `shared/` and are the two highest-leverage in-tree wiring fixes before the 0.2.0 publish.
- **The 0.2.0 hardening bucket** = **DD-A** (sync receiver defaults) + **OF9/OF20** (security-defaults surface + boot posture) + **OF25** (commit the served-root guards, move the bundle out of `dist/`).
- **The single highest-leverage program across the whole report** is the drift+dead-wiring gate family: **OF6** (doctor) + **OF10/OF42** (template-drift + scaffold integration tests) + **OF15/OF21/OF23** (`ai:verify` + coverage + type-diagnostics) + **OF22** (contract-honesty lint) + **OF40** (typed docs contract). Together they convert the entire NEW-row shadow-API cluster and the OF1/OF10 silent-drift class from "caught by a lucky red test or a human audit" into "build error".
