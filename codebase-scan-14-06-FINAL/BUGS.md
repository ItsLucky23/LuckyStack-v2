# Bugs & Defects — FINAL Authoritative Report (wave-1 6 scans + wave-2 3 scans, reconciled to current code)

Branch `chore/package-split-prep`, HEAD `302cbf1` + ~248 uncommitted working-tree changes (this week's remediation pass). Reconciliation date 2026-06-15.

This is the single authoritative Bugs & Defects record. It merges **wave-1** (`codebase-scan-14-06-MERGED/BUGS.md`, 6-scan merge: C1–C3, H1–H33, M1–M54, L1–L36) with **wave-2** (3 independent re-scans) via the 18 reconciled per-area docs in `per-area-reconciled/*.md`. Every kept row was re-verified by opening the cited file at the cited line in the CURRENT tree — wave-2 "fixed/present" claims were not trusted on faith.

Scope is correctness/security defects: crashes, hangs, leaks, races, fail-open / silent-skip, transport-asymmetry twin-drift, codegen wrongness, and "shadow-API" wiring gaps (typed/documented but never wired — marked 🔌).

**STATUS legend.** FIXED = verified resolved in current tree · OPEN = real, still present (wave-1 known) · NEW = wave-2 found, wave-1 missed, verified real · DEFERRED-DECISION = real but pending a policy/ADR/contract call · FALSE-POSITIVE = does not hold against current code. Corroboration shown as wave-1 (n/6) + wave-2 (n/3).

## Status count table

| Status | Count | CRITICAL | HIGH | MEDIUM | LOW |
|---|---|---|---|---|---|
| **NEW** | 14 | 0 | 4 | 7 | 3 |
| **OPEN** | 78 | 0 | 9 | 33 | 36 |
| **DEFERRED-DECISION** | 13 | 0 | 0 | 6 | 7 |
| **FIXED** | 70 | 3 | 16 | 23 | 28 |
| **FALSE-POSITIVE** | 18 | 0 | — | — | — |
| **Total** | **193** | 3 | 29 | 69 | 74 |

> Plus one **dev-only docs-ui CRITICAL** (renderer reads the wrong artifact shape) — recorded under the Cross-area note, not in the CRITICAL column above (which is reserved for the cross-repo wave-1 CRITICALs C1–C3, all FIXED).

**Headline actionable items (NEW + OPEN, top of the list):**
- **B1 (NEW HIGH)** router HTTP + WS proxy upstream leg has **no timeout** → half-open pair accumulation, unauth-reachable on the WS path (3/3).
- **B2 (NEW HIGH)** `serveFile` unguarded `decodeURIComponent` on `/assets/*` → `URIError` → unhandled rejection → **process crash** (unauth single-request DoS).
- **B3 (NEW HIGH)** `getParams` request-stream `'error'` → unhandled rejection → worker crash on client RST mid-body (same crash CLASS as B2, distinct path).
- **B4 (OPEN HIGH)** credentials user can **NEVER self-delete** (UI never sends the password the server requires) — GDPR break, shipped in asset + template + consumer `src/`.
- **B5 (NEW HIGH)** test-runner CSRF-enforcement layer fully built but **never wired** → default `npm run test` gives ZERO CSRF coverage.
- **B6 / B7 (NEW HIGH)** email `preEmailSend` stop-signal ignored (suppression seam is a no-op); api `packages/api/CLAUDE.md` documents a hook+helper+config surface absent from code.
- The three wave-1 CRITICALs (router WS listener-less crash, `GET /server.js` source disclosure, WS-proxy SSRF) are all **FIXED** in the current tree.

---

## NEW — wave-2 found, wave-1 missed, verified real (fix these first)

| ID | Sev | W1 | W2 | Area · Location | Finding · Recommendation |
|---|---|---|---|---|---|
| B1 | HIGH | — | 3/3 | router · `wsProxy.ts:96-119`, `httpProxy.ts:96-119` | **Upstream leg has NO timeout.** `transport.request({...})` passes no `timeout`; grep `setTimeout`/`timeout` = 0 on both proxies. A backend that accepts TCP but never answers leaves the upstream request firing none of `upgrade`/`response`/`error`; for WS only client disconnect reaps it → a connected client pins both sockets indefinitely (resource-exhaustion DoS, unauth on the WS path via a stalled `system` backend). Fix: set `timeout` from a `routing.upstreamTimeoutMs` knob; 504+destroy on timeout; WS handshake watchdog. |
| B2 | HIGH | — | 1/3 | root-server · `server/prod/serveFile.ts:54`, `staticRoutes.ts:50-58`, `httpHandler.ts:306`, `createServer.ts:194-195` | **Unguarded `decodeURIComponent(url)` → process crash.** `GET /assets/%ZZ` reaches `serveFile` raw (the `/assets/*` branch skips `KNOWN_STATIC_FILE_REGEX`); decode throws `URIError`; `handleHttpRequest` has no try/catch; dispatch is `void handleHttpRequest(...)` with no `.catch`; no `process.on('unhandledRejection')` anywhere → Node terminates. Unauth single-request DoS. Fix: `tryCatchSync(() => decodeURIComponent(url))` → 400; add `.catch` on the voided dispatch as a framework backstop. |
| B3 | HIGH | — | 1/3 | core · `getParams.ts:111-113`; caller `server/httpHandler.ts:190` + `createServer.ts:195` (`void`) | **`getParams` request-stream `'error'` → unhandled rejection → worker crash.** `req.on('error', e => reject(e))`; caller has no top-level try/catch and is fired as `void`. A client RST/aborted mid-body on POST/PUT/DELETE crashes the process. Core-safe fix: resolve `null` (the parser's "no usable body" signal) on `'error'`; add `.catch` on the dispatch (server-side). Same class as B2. |
| B5 | HIGH | — | 3/3 | test-runner · `index.ts:1-69` (no csrf export), `runAllTests.ts:88-133`, `csrfEnforcementCheck.ts:34` | **CSRF-enforcement layer is dead surface** — written, typed, tested, but never exported from `index.ts` and never added to `runSweepLayers`. Default `npm run test` gives ZERO CSRF coverage; a consumer who disables/misconfigures CSRF still gets a green run, and the package barrel won't even let them deep-import it. Fix: export `runCsrfEnforcementTests` + add as a 5th sweep layer gated on a valid session; bundle B-tr7 (method guard) in. |
| B6 | HIGH | — | 2/3 | email · `sendEmail.ts:199-204`; `core/hooks/registry.ts:44-68` | **`preEmailSend` stop-signal IGNORED.** `sendEmail` awaits the hook, discards the returned `DispatchResult`, never checks `.stopped`, falls through to `sender.send`. README+CLAUDE.md advertise suppression/abort as live (only `docs/hooks.md:135` admits the gap). A GDPR opt-out / unsubscribe / bounce suppression list ships as a silent no-op; suppressed recipients still get mail. Fix: check `pre.stopped` and short-circuit with a typed `{ok:false}` before `sender.send`. |
| B7 | HIGH | — | 3/3 | api · `packages/api/CLAUDE.md` vs `packages/api/src/*` | 🔌 **Shadow-API: the package CLAUDE.md documents a hook+helper+config surface absent from code** — `apiAuthRejected` hook, `preSocketMessage` hook, `applyGlobalIpRateLimit` helper, `rateLimiting.identity`, `rateLimiting.skipLoopbackInDev` all grep to ZERO matches in `packages/api/src`. An AI wiring abuse-detection against `apiAuthRejected` ships a no-op; a consumer sets `skipLoopbackInDev`/`identity` (both dead). Fix: implement on both transports OR strike the phantom entries; add an `ai:lint` rule that every hook/config named in a package CLAUDE.md is dispatched/read in that package. |
| B8 | MED | — | 1/3 | sync · `_shared/streamEmitters.ts:59,245-248` | 🔌 **Shadow-config: `sync.flushPressure.maxBufferedBytes` (documented 5 MiB) is never read.** `flushPressure` resolves its threshold from the per-call arg + a hard-coded `DEFAULT_THRESHOLD_BYTES = 1 MiB`; no `getProjectConfig` import. Tuning `maxBufferedBytes`/`highWaterMarkChunks` is silently ignored. Folds into M-sync2. |
| B9 | MED | — | 1/3 | sync · `_shared/redactToken.ts:17-27` (dead); `clientFanout.ts:111`, `streamEmitters.ts:222-223` | 🔌 **Dead `redactToken`/`redactTokens` + raw bearer-token leak.** The SYNC-17 helpers are imported only by their own test; `clientFanout.ts:111` passes raw `targetToken: tempToken` into the `tryCatch` error-context (→ `captureException`) and `streamEmitters.ts` logs raw `{ tokens: filtered }`. Neither key is in `DEFAULT_REDACTED_LOG_KEYS` (exact-match), so `sanitizeForLog` doesn't mask them; the legacy Sentry slot forwards context unsanitized. Raw replayable tokens reach Sentry/PostHog/Datadog when a `_client` throws + a tracker is registered (un-gated). Fix: route both call sites through `redactToken`/`redactTokens`. |
| B10 | MED | — | 1/3 | sync · socket fires `preSocketMessage`@`handleSyncRequest.ts:220` + `postSyncAuthorize`@`:521`; HTTP fires neither | **Twin drift: HTTP sync handler omits `postSyncAuthorize` + `preSocketMessage` hooks.** A `preSocketMessage` throttle/audit is bypassed by forcing the HTTP transport; `postSyncAuthorize` audit logs under-count. Root cause is the sync hand-mirror. Fix: dispatch `postSyncAuthorize` on the HTTP path; decide+document `preSocketMessage`; add a hook-dispatch parity test. |
| B11 | MED | — | 2/3 | sync · `_shared/streamEmitters.ts:12-19` | **`chunkCounters` module-level Map grows unbounded.** Keyed `${routeName}|${recipient}` (recipient = room OR token); `bumpChunkIndex` only `set`s, no delete/clear; the "cleared on receiver-room teardown" comment is false. Process-lifetime leak on a long-lived streaming server (LLM tokens, collab). Fix: move the counter into the per-request closure of `buildSyncStreamEmitters`, or drop it. |
| B12 | MED | — | 1/3 | server · `apiRoute.ts:26-31` vs `syncRoute.ts:63-69` | **REGRESSION — SSE error/abort hardening ported to `syncRoute` but NOT `apiRoute` (twin drift).** apiRoute registers only `req.on('close')`; syncRoute registers close+error+aborted on req + error on res. A write `'error'` on the api ServerResponse has no listener → Node throws uncaught → worker crash. (HIGH not CRIT: `sendSseEvent` guards `writableEnded` + attacker can't force a write-error on demand.) Fix: extract `bindSseStreamLifecycle(req,res,markClosed)` into `sse.ts`, call from both; add an ai:lint parity check. |
| B13 | MED | — | 1/3 | mcp · `artifacts.ts:70` | **`loadGraph` `JSON.parse` is outside any try/catch.** A torn/merge-conflicted (`<<<<<<<`) `docs/ai-graph.json` throws `SyntaxError` straight through the catch-less `blast_radius`/`who_imports`/`god_nodes`/`who_calls` handlers — bypassing the "missing/corrupt → friendly `npm run ai:graph` message" contract for the most likely corruption mode. Wave-1 wrongly judged this unreachable (WONT-FIX). Fix: `try { JSON.parse } catch { return null }` before `safeParse` (the `if (!graph)` path already renders the right message). |
| B14 | MED | — | 1/3 | test-runner · `contractCheck.ts:85-114` | **Contract layer treats any well-formed error envelope as PASS.** `runContractCheck` returns `pass` for `{status:'error',errorCode}` on valid input, so a route that rejects ALL input (broken validation / always-throwing guard) passes; the headline X/Y overstates health. Fix: when `inputFor` produced a non-empty sample, assert `status:'success'`, or surface error-envelope passes as a distinct "soft pass" category. (Contract decision — tightening flips currently-green runs.) |

Additional verified-NEW items sit beside their sibling known-issues in the OPEN tables below: server O7 (credentials-login IP rate-limit disarmed when `defaultApiLimit:false`, MED) / O3 (dead-but-wired `timingSafeStringEqual`, MED); test-runner M-tr5 (Layer-5 session leak, MED); root-server RS-02 (deploy.config validated only at codegen, MED); root-src B-rs SESS-RACE-01 (1s redirect defeats backoff, MED) / PREF-01 (preferences overwrite-not-merge, LOW); core M-core1–M-core3 + the LOW N-cluster; presence M-pres1 (cross-instance activity map, MED); secret-manager M-sm1/M-sm2 (no-rollback poison MED + CLAUDE.md drift MED); login M-login1/M-login2 (two THIS-week regressions, MED each); scaffolder M-scaf1–M-scaf7.

---

## OPEN — HIGH (real, still present)

| ID | W1 | W2 | Area · Location | Finding · Recommendation |
|---|---|---|---|---|
| B4 | 4/6 | 3/3 | cli + login + root-src + scaffolder · `settings/page.tsx` (sends only `{confirmation:'DELETE'}`) vs `deleteAccount_v1.ts:29-33` (requires `data.password`) | **Credentials user can NEVER self-delete (GDPR break).** Server enters `if (dbUser?.password)` for every credentials account → `data.password` undefined → always `login.wrongPassword`. `menuHandler.confirm({input:'DELETE'})` resolves a boolean and discards the typed text. OAuth accounts delete fine (masks it). Present in the shipped asset, the create-app template, AND the consumer `src/`. No real-flow regression test (the `_v1.tests.ts` passes `password` directly → green while UI is broken). Fix: collect+send the password for credentials accounts (extend `ConfirmMenu`/`menuHandler.confirm` to return the typed value); add a UI-flow regression test; mirror to all three copies. Fails-closed → HIGH not CRIT. |
| B15 | — | 2/3 (HTTP) | api · `handleApiRequest.ts:141-143`, `handleHttpApiRequest.ts:318-320` | **Per-route rate-limit bucket keyed on the RAW SESSION TOKEN** — contradicts CLAUDE.md "keyed on validated `user.id` … never the raw token". `key = ${keyPrefix}:${token}:api:${name}`; the token lands in Redis key names AND the dev warn log `{ key }`. The wave-1 "never contains the token" positive is REFUTED. Fix: key on `user.id` when authenticated (else resolved IP); add a test asserting no key contains the token. |
| B16 | — | 3/3 | api · `handleHttpApiRequest.ts:359-362` | 🔌 **HTTP loopback rate-limit skip is spoofable + ignores its own config knob.** `requesterIsLoopback = NODE_ENV !== 'production' && requesterIp.startsWith('127.')` — reads neither `rateLimiting.skipLoopbackInDev` (default false) nor `isLoopbackIp`. In any non-prod deploy a 127.x-looking IP defeats the cross-route `:api:all` cap; the documented config knob is an unconditional no-op. Fix: gate on `skipLoopbackInDev && isLoopbackIp(resolvedIp)`. |
| B17 | 1/6 | 3/3 | api · `handleHttpApiRequest.ts:393-407` (gate) vs `handleApiRequest.ts` (none) | **HTTP-method gate enforced only on the HTTP transport** — a GET/read-only (or `get*`/`list*`-named) route is fully callable + mutating over the websocket; docs claim "both transports execute the same sequence". Pairs with GET-route CSRF/origin-exemption. Fix: enforce method on the socket OR loudly document `httpMethod` as HTTP-only. |
| B18 | 4/6 | 3/3 | core · `resolveClientIp.ts:85-91`; wired `handleApiRequest.ts:613` | **`trustProxy` XFF resolution trusts the LEFTMOST (client-controlled) hop** → per-IP rate-limit evasion / audit-IP spoofing. Only on the opt-in `trustProxy:true` posture (default false is the safe gate → wave-2 settled MED/HIGH; HIGH on the opt-in path). Router (FIXED) no longer compounds it. Fix: resolve from the RIGHT skipping `trustedProxyHopCount`; prefer `X-Real-IP`; document overwrite-not-append. |
| B19 | 4/6 (#5) | 1/3 | core · `extractTokenFromRequest.ts:30-34`; socket twin `extractToken.ts` | **Cookie-mode sessions silently accept `Authorization: Bearer` / socket `handshake.auth.token` as a fallback** → CSRF model bypass for a stolen token (replays over Bearer, dodging the cookie-bound CSRF defense). Fix: make the fallback opt-in (`http.acceptBearerInCookieMode`, default false). |
| B20 | 3/6 (#2) | 3/3 | presence + server + sync · presence `peerNotifier.ts:45`/`activitySampler.ts:78` (format) vs `server/loadSocket.ts:226,414,418`, `sync/handleSyncRequest.ts:722`, `handleHttpSyncRequest.ts:619` (RAW) | **Room-name formatter one-sided across the whole framework.** Presence broadcast+snapshot apply `formatRoomName`; every JOIN + the sync fan-out use RAW codes. A non-identity `registerRoomNameFormatter` (the documented multi-tenant primitive) silently makes `userAfk`/`userBack`/`userLeft` reach 0 peers, `getRoomPresence` return empty, AND two tenants on the same raw code receive each other's sync broadcasts — with NO error. In-package half now consistent (F14); remaining fix is symmetric at the server+sync join sites (cross-package → DEFERRED), but the asymmetry is a live HIGH. |
| B21 | 2/6 (#13) | 3/3 | error-tracking + server · `createServer.ts:70-71,194-206`; `errorTrackerRegistry.ts:175-186` | 🔌 **`flushErrorTrackers()` never wired into server shutdown** → PostHog batch + buffered events lost on every redeploy. `createServer` lost graceful `stop()` entirely (also drops Redis pub/sub `.quit()` + socket drain + a persistent post-listen `httpServer.on('error')`); the only signal handlers are dev-only `process.exit(0)`. `grep flushErrorTrackers packages/server/src` = 0. Primitives complete — the gap is a `preServerStop`/prod-SIGTERM subscriber in `@luckystack/server`. Fix: add `stop()` awaiting `ioServer.close()` + Redis quit + `httpServer.close()` + `flushErrorTrackers()`; wire prod signals. |
| B22 | 6/6 (#1) | 3/3 | login · `login.ts:316`→`:532`; `authLockout.ts:84-89` | **Full password-policy validation runs on the LOGIN path AND policy-failure trips the lockout counter (amplified).** `validateCredentialsShape` runs `validatePassword` for the login branch before the login/register split; `NON_COUNTING_REASONS` omits `login.password*`. Combined: an attacker locks ANY victim out by POSTing policy-violating passwords for their email — no real-password guess needed — and the policy is leaked to unauth probers. Fix: cheap shape-checks only on login, full policy on register; collapse policy fails to `login.wrongPassword`; add `login.password*` to `NON_COUNTING_REASONS`. |
| B23 | 2/6 (#1) | 1/3 | server · `healthRoutes.ts:80-89`; default `projectConfig.ts:703-706` | **Unauthenticated `/_health` publishes unsalted `sha256(secret)` fingerprints (+ `bootUuid` + `envKey`) by default.** `healthHash.mode` ships `'plain'`; the `@bootUuid` salt mitigation is DEAD on this caller (route calls `computeSynchronizedEnvHashes()` with NO bootUuid → collapses to plain). `verifyBootstrap` only WARNs. Fix: pass bootUuid into the hash; don't co-disclose raw bootUuid; default `'hmac'` when synchronized keys non-empty (or gate behind a router-probe token). |
| B24 | 1/6 | 2/3 | root-src · `listSessions_v1.ts`, `revokeSession_v1.ts`, `deleteAccount_v1.ts` (all `process.env.PROJECT_NAME ?? 'luckystack'` + template literals) | **Consumer session handlers hand-build Redis keys, bypassing `formatKey`/`sessionKeyFor`/`activeUsersKeyFor`.** Byte-identical under default config; silently divergent when `session.projectName` is set OR a `registerRedisKeyFormatter` (multi-tenant) is installed → `listSessions` empty, `revokeSession` can't resolve, `deleteAccount` leaves a stale activeUsers set. It's the example AI clones first (Rule 12). Fix: import the key helpers from `@luckystack/login` (the CLI asset already does); delete the local `PROJECT_NAME`. |
| B25 | 1/6 (#6) | 2/3 | root-src · consumer `src/settings/_api/*` vs `packages/cli/assets/login/src/settings/_api/*` | **Consumer `src/settings/_api/*` drifted behind the hardened CLI asset (consumer is the regressed branch).** `deleteAccount_v1` reaches past `getUserAdapter()` to `prisma.user.delete`, hardcodes the activeUsers key, skips pre/postAccountDelete hooks + avatar unlink; `listSessions`/`revokeSession` use 64-char `id` while the asset uses 16-char `handle`. No cross-tree parity gate covers `src/`. Fix: re-sync `src/` from the asset; add `ai:check-template-drift` CI. |

---

## OPEN / NEW — MEDIUM (grouped by area)

### api
| ID | Status | W1 | W2 | Location | Finding · Recommendation |
|---|---|---|---|---|---|
| M-api1 | OPEN | 5/6 (#2) | 3/3 | `handleApiRequest.ts:452-459` | **`system/logout` socket shortcut bypasses the respond-hook chain, not rate-limited, always reports `success:true`** (hand-built `socket.emit`; `performLogout` result discarded; spammable). Fix: route through `emitApiResult`, reflect real status, apply a per-route bucket. |
| M-api2 | OPEN | 2/6 (#8) | 2/3 | `handleApiRequest.ts:495`, `handleHttpApiRequest.ts:274-309` | **Failed-auth requests consume no rate-limit bucket on either transport** (auth precedes rate-limit, early-returns); undocumented. Fix: increment the global-IP bucket on the auth-fail path, OR document. |
| M-api3 | OPEN | 6/6 (#5) | 3/3 | `handleApiRequest.ts:117-211` vs `handleHttpApiRequest.ts:311-391`; `transportParity.test.ts:22-24` | **Rate-limit + auth hand-duplicated; parity test deliberately excludes rate-limit/method/logout parity — and they ALREADY diverge** (socket `resolveClientIp` no loopback skip; HTTP raw `requesterIp` + loopback skip). Fix: extract `applyApiRateLimits({transport})`, resolve IP once, extend the parity test. |
| M-api4 | OPEN | 5/6 (#4) | 2/3 | `handleHttpApiRequest.ts:318,363` | **HTTP buckets fall back to literal `'unknown'`/`'anonymous'`, collapsing distinct callers into one bucket** (per-IP → global limiter; socket diverges). Fix: resolve IP via `resolveClientIp` on HTTP; consider fail-closed deny on the sentinel. |
| M-api5 | OPEN | — | 1/3 | `handleHttpApiRequest.ts:221` | **HTTP `data` shape guard does not reject ARRAY payloads** (`typeof [] === 'object'`); socket rejects it → a `relaxed` HTTP route crashes where the socket 400'd. Fix: `|| Array.isArray(data)`. |
| M-api6 | OPEN | — | 1/3 | `handleApiRequest.ts:213-238`, `handleHttpApiRequest.ts:476-492` | 🔌 **`api.requestTimeoutMs` config has no enforcement on either transport** (grep=0); a hung handler never times out server-side. Fix: race `main()` against a timeout that aborts + resolves `api.requestTimeout`. |

### sync
| ID | Status | W1 | W2 | Location | Finding · Recommendation |
|---|---|---|---|---|---|
| M-sync1 | OPEN | 1/6 (#5) | 3/3 | `core/cancelRegistry.ts:29-30`; client `syncRequest.ts:532` | **Abort registry keyed on client-controlled `cb` (`socketId:cb`), unconditional `set`.** Default `cb=${name}/${version}` → overlapping same-route requests overwrite/cross-delete the controller; `syncCancel` aborts the wrong request → LLM "stop generating" breaks. Fix: per-request unique id (include `responseIndex`), or a `Set` per `cb`. |
| M-sync2 | OPEN | 6/6 | 3/3 | `_shared/streamEmitters.ts:38-46,59-64,227-261` | **`flushPressure` measures the wrong sockets / packet-COUNT not bytes / local-only.** Samples the original `receiver` room while `streamTo` emits to token rooms (recipients never measured); `writeBuffer.length × AVG_PACKET_BYTES=1024` under a byte-named API; ignores Redis-remote recipients + sockets past #32. Real backpressure hole on LLM streaming. Folds in B8 (dead config) + the byte-named-packets lie. Fix: `flushPressureTo(tokens)`, rename/sum bytes, rotating sample, document. |
| M-sync3 | OPEN | 1/6 | 3/3 | `handleSyncRequest.ts:408-530` | **Rate-limit gated behind auth on both transports; no per-socket message guard.** Worst-case bounded (garbage token = one uncounted Redis `getRaw`/msg); realistic Low–Medium. Fix: connection-level/`preSocketMessage` rate guard or a cheap pre-auth IP bucket. |
| M-sync4 | OPEN | 1/6 (#4) | 3/3 | `handleSyncRequest.ts:494`, HTTP `:390` | **Consumer `preSyncAuthorize`/`postSyncAuthorize` (DB/tenant lookups) run BEFORE `applySyncRateLimits`** → unbounded per-message work on a `login:false` route. Coupled to M-sync3. Fix: move rate-limit ahead (keep built-in receiver check first) or document. |
| M-sync5 | OPEN | 1/6 (#1) + 4/6 | 3/3 | `handleSyncRequest.ts` fanout `~:833`, `preSyncRecipient` `~:633` | **Full `serverOutput` broadcast verbatim when no `_client` exists; `preSyncRecipient` can SKIP but not REDACT and passes `recipientUserId:null`.** Easy PII leak by omitting a `_client`; the auth hook can't see identity. Fix: optional redaction/override return + opt-in `resolveRecipientUser`. |
| M-sync6 | OPEN | 1/6 (#3) | 3/3 | `handleSyncRequest.ts:808` loop / `:852` ack | **Per-recipient `_client` runs SERIALLY; originator ack waits for the whole fanout** → large `receiver:'all'` can trip client `requestTimeoutMs` (30000), false 504. Fix: ack as soon as `_server` completes + fanout dispatched, or bounded concurrency. |
| M-sync7 | OPEN | 1/6 (#2) | 3/3 | `handleHttpSyncRequest.ts:97,133-136` vs socket `resolveClientIp` | **HTTP sync rate-limit identity collapses anonymous callers into one bucket** + pre-resolved `requesterIp` + non-prod loopback skip the socket lacks. Fix: resolve own IP, use as anon identity, align loopback or document with a parity test. |

### error-tracking / core (capture path)
| ID | Status | W1 | W2 | Location | Finding · Recommendation |
|---|---|---|---|---|---|
| M-et1 | OPEN | 1/6 | 3/3 | `core/sentrySetup.ts:42-66` | **Legacy `sentrySetup.captureException/Message` forward RAW context as Sentry `extra`** — bypass `sanitizeForLog` (only the fan-out sanitizes; `builtinBeforeSend` touches only `request.cookies`). The most-common Sentry config (DSN, no adapter) hits this slot; falsifies the "raw token never reaches Sentry" comment. Fix: `sanitizeForLog(context)` once at the top of both fns. |
| M-et2 | OPEN | (folded #1) | 1/3 | `autoInstrumentation.ts:76-80`; `sentrySetup.ts:42-48` | **Legacy Sentry identity is a per-request process-global scope** → cross-request user bleed on the `initializeSentry()`-only path (no `withIsolationScope` anywhere). The ALS fix covers only the adapter path. Fix: route `initializeSentry()` through `createSentryAdapter`, OR attach `getCurrentErrorTrackerIdentity()` per event. (One fix closes M-et1, M-et2, and the mixed-mode double-capture.) |
| M-et3 | OPEN | 4/6 (#12) | 3/3 | `redactedLogKeys.ts:10-18,30-32` | **Default redacted-key set omits `csrftoken`/`apikey`/`secret`/`accesstoken`/`refreshtoken`; exact-match only** (`token` ≠ `csrftoken`). Session carries a live `csrfToken`. Deferred (redaction can hide debug fields). Fix: add the keys + suffix matching. |
| M-et4 | OPEN | 1/6 (#5) | 3/3 | `posthog.ts:81,84`; `datadog.ts:128,135` | **`error.message`/`error.stack` forwarded to PostHog & Datadog bypass redaction** (key-based only); an interpolated secret reaches the tracker. Sentry protected SDK-side. Fix: central value-level scrub in `captureExceptionAcrossTrackers`. |
| M-et5 | OPEN | 3/6 (#7) | 3/3 | `errorTrackerRegistry.ts:133-141` | 🔌 **`registerPreCaptureFilter` payload is context-only** — the documented "drop noisy errors by type" use case is impossible (filter never sees `{ error }`). Fix: pass `{ error, context }`. |
| M-et6 | NEW | — | 2/3 | `posthogConfig.ts:33-38`; `register.ts:50-57` | 🔌 **`posthogConfig.ts` is ENTIRELY DEAD** — not barrel-exported, never imported; the zero-config adapter ships no `beforeSend`. An AI told "set a PostHog beforeSend redaction hook" reaches for `registerPostHogConfig` → type-checks → no-op. Fix: wire `getPostHogConfig()` + export, OR delete + correct the doc claim. |
| M-et7 | NEW | — | 2/3 | `index.ts:1-57` vs error-tracking CLAUDE.md | 🔌 **`@luckystack/error-tracking` barrel omits 5 exports its own CLAUDE.md documents** (`flushErrorTrackers`, `appendErrorTracker`, `runWithErrorTrackerIdentity*`, `registerPreCaptureFilter`, `startSpanHandle` — they live in core). Following the shutdown docs → TS2305. Amplifies B21. Fix: re-export from core via `adapter.ts`, or fix CLAUDE.md. |

### core (validator + infra)
| ID | Status | W1 | W2 | Location | Finding · Recommendation |
|---|---|---|---|---|---|
| M-core1 | NEW | — | 1/3 | `runtimeTypeValidation.ts:19-45` | **`splitTopLevel` is quote-BLIND** — splits inside a string literal with `|`/`&`, before the literal recognizer. A literal-bearing union (`"a|b" | "c"`) is mis-parsed → valid traffic REJECTED (route DoS, fail-closed). Not tested. Fix: quote-aware `inString` + escape. |
| M-core2 | NEW | — | 2/3 | `runtimeTypeValidation.ts:144` | **`validateType` accepts ANY string for a `Date`-typed field**; handlers doing `new Date(x).toISOString()` get `RangeError`. Fix: `!Number.isNaN(Date.parse(v))`. |
| M-core3 | NEW | — | 1/3 | `runtimeTypeValidation.ts:69,98` | **Object-field parser silently DROPS non-identifier quoted keys** (`'foo-bar': string`) → a required hyphenated key is never enforced AND a present one is treated as unknown → whole object REJECTED. Fix: broaden the quoted-key capture + surface `unvalidatable field`. |
| M-core4 | OPEN | 1/6 (#16) | 2/3 | `rateLimiter.ts:258-278` | **Redis `clearAllRateLimits` SCAN+DEL is project-rooted (not tenant-rooted), publicly exported with no guard** → one call wipes every tenant's counters. Fix: gate behind `NODE_ENV!=='production'` or require a namespace; document fleet-wide. |
| M-core5 | OPEN | 1/6 (#7) | 2/3 | `rateLimiter.ts:60,111,348` | **In-memory rate-limit store unbounded** — key-varying flood grows the Map between cleanup ticks → GC pressure/OOM. Fix: cap/LRU; document memory-mode single-instance/dev. |
| M-core6 | OPEN | 5/6 (#6) | 2/3 | `rateLimiter.ts:228-235` | **Default rate limiter fails OPEN** — `'deny'` honored only inside `isRedisMode()`, so memory mode ignores it. Consistent with the deliberate framework stance — flag. Fix: honor `'deny'` regardless of store, or boot-warn. (= DD2 policy half.) |
| M-core7 | OPEN | 1/6 (#17) | 2/3 | `checkOrigin.ts:68-71` | **Origin resolver-function mode receives the RAW `Origin`/`Referer`** while array-mode normalizes both sides → a `startsWith('https://app.com')` resolver is bypassable. Fix: pass the normalized origin, or export the normalizer + document. |
| M-core8 | OPEN | 1/6 (#20) | 2/3 | `apiRequest.ts:613-619` | **`apiRequest` interceptor window un-timed + abort-unwired** (timeout/abort armed AFTER the interceptor await) → un-counted latency + ignored abort for `abortable:false`. Fix: wire abort + a guard timer before the await. |
| M-core9 | OPEN | 2/6 (#22) | 1/3 | `apiRequest.ts:500-521` | **`apiRequest` lost-response listener + promise leak when timeout disabled** (`timeoutMs:false` + non-abortable + server restart → forever-pending). Fix: a `socket.on('disconnect')` one-shot that settles in-flight non-abortable requests. |
| M-core10 | OPEN | 3/6 (#8) | 2/3 | `getParams.ts:57-66` | **`getParams` body parser has no already-responded latch** → a second buffered chunk re-trips 413 → `ERR_HTTP_HEADERS_SENT` inside the event handler (another uncaught path, pairs with B3). Fix: `let responded=false`/`if (headersSent) return`. |
| M-core11 | OPEN | 3/6 (#23) | carried | `react/TranslationProvider.tsx:92-97` | **`translate()` builds `new RegExp(param.key)` unescaped + `$`-bearing value as the replacement string** → mis-substitution / `SyntaxError` / `$&`/`$1` corruption on the localized-error render path. Ships in the published client bundle (latent). Server twin = M-rs1. Fix: escape the key or `split/join` + replacer function. |
| M-core12 | OPEN | 2/6 (#21) | carried | `react/Middleware.tsx:99-103` | **`Middleware` denial with no `redirect` calls `navigate(-1)`** → deep-linked user lands on a blank page; consecutive denials chain. Fix: navigate to `loginRedirectUrl`. |

### login
| ID | Status | W1 | W2 | Location | Finding · Recommendation |
|---|---|---|---|---|---|
| M-login1 | NEW | — | 1/3 | `register.ts:54-58,65,110` | **REGRESSION — `register.ts` captures `getProjectConfig()`+`env()` at MODULE LOAD then `registerOAuthProviders()` at import** → stale/empty `callbackBase`, ignores post-import `auth.credentials`. The file header itself warns the overlay runs after this import. Fix: defer reads into provider factories. |
| M-login2 | NEW | — | 1/3 | `logout.ts:34` | **REGRESSION — `logout` bypasses `SessionAdapter` with raw `redisClient.srem`** → on a non-Redis adapter the skip-path active-set is never cleaned (sign-out-everywhere gap). Fix: drop the raw `srem`; call `getSessionAdapter().untrackActive`. |
| M-login3 | OPEN | — | 1/3 | `requestEmailChange_v1.ts:26-65` | **Email-change initiated with NO current-password re-auth** → a session-hijacker rotates the email (confirm link to the NEW address); on confirm all sessions revoked → real owner locked out. Fix: require+verify current password for credentials accounts. |
| M-login4 | OPEN | 4/6 | 3/3 | `session.ts:92-94`→`:141/:152` | **TOCTOU on `rejectNew` concurrent-session cap** (count then non-atomic persist). Fix: atomic reserve (Lua/SETNX) or document soft-cap. |
| M-login5 | OPEN | 1/6 | 2/3 | `session.ts:264-317` | **`getSession` extends TTL on EVERY read** (sliding-expiration amplification; side-effects in a getter) → a stolen token survives as long as anything touches it. Fix: `getSession(token,{refresh=true})` or a dedicated `touchSession`. |
| M-login6 | OPEN | — | 1/3 | `login.ts:396,490`; `session.ts:141,414-436` | **Session token stored in PLAINTEXT inside the Redis session VALUE** (redundant with the key) and surfaced by `getAllSessions()` — any admin/log carries a live credential (UI safe via `sessionHandle()`; the primitive is not). Fix: strip `token` before `setRaw`/in `getAllSessions`. |
| M-login7 | OPEN | — | 1/3 | `sessionAdapter.ts:90-94` | **Non-atomic SET-then-EXPIRE** → a crash between leaves an immortal TTL-less session. Fix: `redis.set(key,value,'EX',ttl)`. |
| M-login8 | OPEN | 3/6 | 2/3 | `login.ts:61` | **Dummy bcrypt hash hard-pinned to cost 10** → reopens enumeration timing channel when `bcryptRounds`>10 (inline comment argues early-return dominates → borderline DEFERRED, but the delta is real on rounds 12+). Fix: derive the dummy hash lazily at configured rounds. |
| M-login9 | OPEN | 1/6 | 1/3 | `login.ts:321` | **Register path skips the per-account brute-force lockout** → unbounded per-account `resolveUserByEmail` via register. Fix: apply the lockout check on the register email-exists lookup. |
| M-login10 | OPEN | — | 1/3 | `userAdapter.ts:98-99` | **Unified-account resolution non-deterministic on duplicate-email rows** (`findFirst` no `orderBy`) → same login resolves to different rows AND the cross-provider link guard flips non-deterministically. Fix: `orderBy:{createdAt:'asc'}` + warn; document `unified` needs unique email. |
| M-login11 | OPEN | 1/6 | 1/3 | `login.ts:347,364` | **Register-time TOCTOU duplicate accounts** (`resolveUserByEmail`→`create` not atomic; no `email @unique`). Fix: require `email @unique` for `unified`; treat unique-violation as re-resolve-and-link. |
| M-login12 | OPEN | 1/6 | 0/3 | `passwordReset.ts:31-36`; `emailChange.ts:28-34` | **No invalidation of prior reset/email-change tokens on issue** → multiple live redeemable links. Fix: one active token per user, delete prior on issue. |
| M-login13 | OPEN | 1/6 | 0/3 | `login.ts:496,855` | **Avatar resolution `existsSync(${id}.webp)` sync FS on the login hot path** (blocking; couples session to local disk). Fix: `fs.promises.stat` or an avatar adapter. |
| M-login14 | OPEN | 1/6 | 2/3 | `session.ts:56,133,313` | **Raw session token logged via the non-redacting default logger** (trace paths use `slice(0,8)`); leak depends on the core logger running `sanitizeForLog` (cross-area). Fix: route context through `sanitizeForLog`, or `slice(0,8)` consistently. |
| M-login15 | OPEN | — | 1/3 ea | `confirmEmailChange_v1.ts:50-59`, `deleteAccount_v1.ts:59-62`, `signOutEverywhere_v1.ts:20` | **Consumer-route correctness cluster (each low):** confirmEmailChange collision re-check non-atomic + `credentials`-scoped (misses OAuth rows under `unified`); deleteAccount wipes sessions + raw `redis.del` BEFORE the `adapter.delete` that can throw; signOutEverywhere revokes the caller's own session. Fix per the per-area report. |

### secret-manager
| ID | Status | W1 | W2 | Location | Finding · Recommendation |
|---|---|---|---|---|---|
| M-sm1 | NEW | — | 2/3 | `index.ts:746` then `:751` | **`pointerMap` merged BEFORE the resolve it depends on, no rollback** → a failed remote file-reload permanently pollutes the pointer map (a bad dev `.env.local` poisons every later refresh/poll until restart). Dev+remote only. Fix: build the merged map locally; commit only after `doResolve` succeeds. |
| M-sm2 | NEW | — | 1/3 | `packages/secret-manager/CLAUDE.md` | **Stale package CLAUDE.md Function Index** omits `stopSecretManager`+`getCachedResolutionMeta`; the `SecretManagerConfig` one-liner misses ~9 keys; Config-keys table omits `pollIntervalMs`. Defeats the "100% AI-driven after install" goal. Fix: regenerate the Index + expand the type + `npm run ai:capabilities`. |

### server / root-server
| ID | Status | W1 | W2 | Location | Finding · Recommendation |
|---|---|---|---|---|---|
| M-srv1 | NEW | — | 1/3 | `authApiRoute.ts:88` | **Credentials-login IP rate-limit skipped entirely when `rateLimiting.defaultApiLimit === false`** — disabling the generic limit disarms login IP-throttle (spraying one attempt across many accounts from one IP faces no IP cap). Fix: fall back to `rateLimiting.auth.{maxAttempts,windowMs}` for the IP key. |
| M-srv2 | NEW | — | 1/3 | `timingSafeEqual.ts`; `csrfMiddleware.ts:57,84`; `testResetRoute.ts:26` | **`timingSafeStringEqual` is dead-but-wired** — a constant-time helper + full test added this week, imported NOWHERE in prod; CSRF compares still use `===`. Misleading surface. Fix: route the three compares through it, OR delete helper+test. |
| M-srv3 | OPEN | 5/6 (#3) | 2/3 | `csrfMiddleware.ts:34-37` | **State-changing custom routes (`registerCustomRoute` POST) + `/auth/callback` fall outside the CSRF gate** (matches only `/api/`,`/sync/`,`/auth/api/`); rely solely on the origin gate, nothing warns at registration. Fix: extend CSRF to non-exempt custom POST with opt-out, OR expose `ctx.enforceCsrf` + loud doc. |
| M-srv4 | OPEN | 1/6 (#11) | 3/3 | `loadSocket.ts:366-368` vs join `:227`/leave `:286` | **`updateLocation` persists session WITHOUT `sanitizeSessionRoomKeys`** + mutates the cached session in place (aliasing) + lost-update race vs presence's writes outside the same lock. Fix: one `persistSessionMutation` routed through all three. |
| M-srv5 | OPEN | 1/6 (#39) | 3/3 | `capabilities.ts:27-42` | 🔌 **Optional-package capability detection silently degrades on Node < 20.6** — `localRequire.resolve` THROWS `ERR_PACKAGE_PATH_NOT_EXPORTED` on the import-only exports maps → every package reported ABSENT, bare `catch {return false}`, no diagnostic. Ties to the known stray node-20.5.0 box. Fix: loud one-time warn; distinguish not-found from other resolver errors. |
| M-srv6 | OPEN | 1/6 (#40) | 1/3 | `runtimeMapsLoader.ts:80` | 🔌 **`loadGeneratedMaps` callback typed `Promise<unknown>`; a mis-shaped module coerces to `{}`** → every route silently `notFound`, no compile error. Fix: export+type `GeneratedRuntimeMapsModule`. |
| M-srv7 | OPEN | 6/6 | 3/3 | `serveFile.ts:67,69-89` vs `staticRoutes.ts:6,14` | **`serveFile` static guard is a substring/basename denylist (allow-by-default), not an allow-list** — one stray `dist/` artifact re-opens disclosure; the stale comment claims a non-existent zod pre-filter. (The `/server.js`+`*.map` CRIT itself is FIXED.) Fix: invert to a Vite-build-manifest allow-list; build the server bundle OUTSIDE `dist/`. |
| M-srv8 | OPEN | 6/6 | 3/3 | `server/hooks/notifications.ts:40-42,80-82` | **`notifications.ts` reads the User row via stacked inline `as`/`as unknown` Prisma casts (Rule 21), duplicated in two functions; `tryCatch` swallows a schema-rename failure silently.** Fix: typed client + a single `loadNotifiableUser` helper. |
| M-rs1 | OPEN | 3/6 | 3/3 | `server/utils/responseNormalizer.ts:87-88` | **Unescaped `new RegExp(\`{{${param.key}}}\`)` + `$`-sequences in the replacement value** in the localized-error normalizer (server twin of M-core11). Fix: literal-key `replaceAll` + function replacer; shared `escapeRegExp`. |
| M-rs2 | OPEN | 1/6 | 3/3 | `server/auth/login.ts:1`, `extractToken*.ts`, `server/hooks/*` | **Consumer shims import framework by `../../packages/*/src` source paths instead of published `@luckystack/*` names** (twin drift; breaks the post-install model — two conventions in one tree). Fix: standardize on published names + a re-export breadcrumb. |
| M-rs3 | NEW | — | 1/3 | `presetLoader.ts:140-152`; `deploy.config.ts:14` | **deploy.config shared-resource/fallback-cycle invariants validated ONLY at build-time codegen, never at server boot** despite the comment claiming "enforced by presetLoader". Editing `deploy.config.ts` + `npm run prod` without re-running `generateArtifacts` boots an unvalidated topology. Fix: `validateResolvedConfig(loadResolvedConfig())` in the boot path. |

### devkit
| ID | Status | W1 | W2 | Location | Finding · Recommendation |
|---|---|---|---|---|---|
| M-dk1 | NEW | — | 1/3 | `typeMap/routeMeta.ts:20,52` | **Codegen hardcodes literal `src/`, silently dropping EVERY route on a non-`src` `srcDir` layout.** Discovery is srcDir-agnostic (files found) but `extractPagePath` returns `''` → the generator drops the entry. `srcDir='app'` (an advertised key) gets ZERO API/sync types, no error. Highest-value new devkit finding. Fix: anchor on `path.relative(getSrcDir(), file)`, or throw on the `''` case. |
| M-dk2 | NEW | — | 1/3 | `extractors.ts:18-19` + `tsProgram.ts:145-146` | **`normalizeImportPath` hardcodes `ROOT_DIR/src/_sockets`** (wrong import base on a non-`src` layout) AND is byte-duplicated → trips the now-throwing `validateGeneratedTypeIdentifiers` → whole generation fails. Fix: resolve `fromDir` from `path.dirname(getGeneratedSocketTypesPath())`; consolidate. |
| M-dk3 | OPEN | 6/6 (#1) | 3/3 | `templateInjector.ts:656-731` | **Template-injector PAIRED-twin still pure regex source-mutation** — `[^}]*` stops at the first inner `}`, orphaning the outer brace of nested `clientInput` types → uncompilable TS written to the user's client file; returns `true` and the pair upserts anyway. (DELETE twin migrated; this + the server-inject splice were not.) Fix: TS AST; re-parse before writing; leave untouched + log on failure. |
| M-dk4 | OPEN | 1/6 (#30) | 1/3 | `templateInjector.ts:692-694` + `hotReload.ts:249-251` | **Failed paired-client rewrite swallowed; the pair upserts anyway** → server registered with types the client never migrated to, no error. Fix: check the return; log + skip/roll-back on `false`. |
| M-dk5 | OPEN | 2/6 (#7) | 3/3 | `hotReload.ts:211,503-505,285` | **Async chokidar listeners + `void task()` have no `.catch`** → under Node-15+ default, a watcher-path throw crashes the dev server, defeating supervisor backoff. Fix: wrap each listener + `scheduleReload` task. |
| M-dk6 | OPEN | 1/6 (#13) | 3/3 | `loader.ts:138-151` vs `discovery.ts:36` | **Dev loader walks `node_modules`/dotfolders that discovery skips** (dev/build divergence + hang vector on `src/node_modules`/vendored pkg/large `.cache`). Fix: share the discovery skip rule. |

### docs-ui (see Cross-area CRITICAL note)
| ID | Status | W1 | W2 | Location | Finding · Recommendation |
|---|---|---|---|---|---|
| M-du1 | OPEN | 3/6 | 3/3 | `index.ts:107`; `register.ts:19` | **Auto-mount prod gate is env-only** (`NODE_ENV==='production' && !enabledInProd`) and fails OPEN on staging/preview/unset hosts; auto-mounts unconditionally with no `authorize` hook, and the documented `LUCKYSTACK_DOCS_UI_DISABLE_AUTOMOUNT` opt-out NO LONGER EXISTS. Fix: fail-closed for non-loopback binds unless explicitly enabled; add a required `authorize` hook when `enabledInProd`. |
| M-du2 | OPEN | 2/6 | 2/3 | `index.ts:127-131` | **JSON 404 discloses the absolute filesystem path** via `{ expectedAt: docsPath }`; real exposure materializes via M-du1 on staging. Fix: gate behind `NODE_ENV!=='production'` or strip to relative. |

### scaffolder
| ID | Status | W1 | W2 | Location | Finding · Recommendation |
|---|---|---|---|---|---|
| M-scaf1 | NEW | — | 1/3 | `src/index.ts:1543` | **Framework-docs copy substitutes the `{{…}}` example tokens INSIDE the very docs that document the token mechanism** (`AI_QUICK_INDEX.md`, `ARCHITECTURE_EXTENSION_POINTS.md`) → corrupts the AI-facing copied docs. Fix: copy framework docs WITHOUT substitution. |
| M-scaf2 | NEW | — | 2/3 | `src/index.ts:1635-1638` (catch), `1430-1433` (guard) | **No partial-scaffold cleanup** — a prune/copy throw leaves a half-written dir; the retry hits the `existsSync` guard and refuses → manual `rm`. Refutes baseline prose claiming cleanup is "hardened and tested". Fix: track fresh-create; `rmSync(targetDir)` in the catch only if freshly created. |
| M-scaf3 | NEW | — | 3/3 | `template/scripts/generateAiCapabilities.mjs` vs root | **Template `generateAiCapabilities.mjs` drifted from root** — missing the `Tests` column + `hasTestFile` (42 diff lines), no `KEEP IN SYNC` marker, no parity guard → scaffolded consumers never see the per-route Tests indicator Rule 12 tells the AI to read. Fix: re-sync + marker + `checkTemplateSync.mjs`. |
| M-scaf4 | NEW | — | 2/3 | `create-luckystack-app/ls-np/` (115 files) | **`ls-np/` is a 115-file committed scaffold-output artifact carrying STALE overlay files** the `template/` dropped — repo bloat + a second canonical-looking template surface that drifts. Fix: delete + `.gitignore`. |
| M-scaf5 | NEW | — | 1/3 | `src/index.ts:1612`; `README.md:13`; `package.json` | **Next-steps + README tell SQL users to run `npm run prisma:migrate:dev` — a script that does not exist** → 3 of 4 DB choices get a broken first command. Fix: add the script. |
| M-scaf6 | NEW | — | 1/3 | `template/.github/workflows/ci.yml:55-56` | **CI calls `npm run test:e2e` (undefined) gated on `@vercel-labs/agent-browser` (never installed)** — guard always false; if it passed it would crash. Fix: add `"test:e2e":"playwright test"`; fix the guard marker. |
| M-scaf7 | NEW | — | 1/3 | `template/_dot_env_dot_local_template:26`; `README.md:94-95`; `prisma/schema.prisma:3` | **Overlay-doc twin drift** — docs breadcrumb to `luckystack/login/oauthProviders.ts`+`userAdapter.ts` that no longer exist (wiring moved into `@luckystack/login/register`). Fix: update breadcrumbs. |

### email
| ID | Status | W1 | W2 | Location | Finding · Recommendation |
|---|---|---|---|---|---|
| M-email1 | OPEN | — | 3/3 | `adapters/providerPayload.ts:17-29`; contract `core/emailRegistry.ts:48-61` | 🔌 **`EmailMessage.attachments` + `headers` typed + JSDoc-promised as adapter-forwarded, but `toProviderPayload` silently drops them** (a test pins the drop). A consumer sets `attachments`/`headers['List-Unsubscribe']`, type accepts it, send returns `{ok:true}`, data never reaches Resend/nodemailer (both support these). Fix: forward them, OR `@deprecated`/remove. |
| M-email2 | OPEN | 5/6 (#4) | 2/3 | `sendEmail.ts:64-76` | **`resolveSender` silently falls through when an EXPLICITLY-requested `adapter` is unregistered** → routes through default (or `ConsoleSender` in a misconfigured prod box) while the caller sees `{ok:true}` — confused-deputy mis-delivery for security mail (login passes `adapterHint:'transactional'`). Also DEFERRED (explicit-vs-hint policy). Fix: distinguish explicit `adapter` (warn/`{ok:false}`) from best-effort `adapterHint`. |
| M-email3 | OPEN | 2/6 (#9/#10) | 2/3 | `renderEmailLayout.ts:52-53` | **`renderEmailLayout` interpolates `ctaUrl` raw into `href="…"` and `accent` raw into `background:…`** (every text field IS escaped). Built-in URLs safe today; a consumer/AI passing user-derived values gets attribute-breakout / `javascript:` link on a framework-shipped "safe layout". Fix: `new URL()` scheme-allowlist + escape for href; color-regex allowlist for accent. |

### presence
| ID | Status | W1 | W2 | Location | Finding · Recommendation |
|---|---|---|---|---|---|
| M-pres1 | NEW | — | 1/3 | `activitySampler.ts:19,82-89` | **`getRoomPresence` walks cross-instance RemoteSockets but reads a local-only `lastActivityBySocket` Map** → for any socket on another instance `afk:false`/`lastActivity:undefined`; the doc sells it as adapter-aware. A multi-instance late joiner shows every remote peer present-and-active. Fix: cross-instance activity store, or tri-state `afk:'unknown'` for remote sockets + fix the comment. |

### test-runner
| ID | Status | W1 | W2 | Location | Finding · Recommendation |
|---|---|---|---|---|---|
| M-tr1 | OPEN | 2/6+3/6 | 3/3 | `runAllTests.ts:73-80`; `contractCheck.ts:46`, `rateLimitCheck.ts:36`, `fuzzCheck.ts:42` vs `customTests.ts:319` | **Authenticated sweep layers omit the CSRF header the server enforces, AND the mutating-method partition was removed.** Cookie-mode+CSRF-on: contract/fuzz false-PASS (`auth.csrfMismatch` counts as covered), rate-limit false-FAILs; token-mode/CSRF-off: the fuzz layer now hits every authenticated DELETE/PUT with junk (mutation risk on body-ignoring routes). Pairs with B5. Fix: thread the session CSRF token into the auth builders; restore a mutating-method gate. |
| M-tr2 | OPEN | partial | 3/3 | `runRegisteredLayers.ts:8-10`, `:67`; `runAllTests.ts:175-191` | 🔌 **Entire `runRegisteredLayers` extension runtime is unwired AND its docstring contradicts the docs** (claims "invoked after the built-in sweeps by `runAllTests`"; `docs/extension-hooks.md:87` says manual-invoke only). `registerTestLayer/Fixture/Reporter` register into a registry nothing iterates. Non-negotiable fix: correct the lying docstring, OR wire it. |
| M-tr3 | OPEN | 3/6 (#6) | 3/3 | `rateLimitCheck.ts:55-58,76-95` | **Rate-limit drain cancels each body without inspecting it** → if drains are rejected pre-limiter (CSRF 403/auth.required/400, likely given M-tr1) the bucket is never incremented and the final probe blames the limiter. Fix: sample drain envelopes; classify `skipped`/inconclusive when the limiter was never reached. |
| M-tr4 | OPEN | 3/6 (#3) | 2/3 | `runAllTests.ts:144-148` | **Rate-limit layer leaks the shared per-IP bucket between endpoints** (`resetBetweenEndpoints` defaults false, never set; one `clearAllRateLimits()`) → a saturated neighbour makes a route look enforced (false pass) or throws off the N-drain. Fix: reset the IP bucket between layers, or require `defaultIpLimit:false` + fail loudly. |
| M-tr5 | NEW | — | 1/3 | `customTests.ts:248-282,460-493` | **Layer-5 minted sessions have NO automatic cleanup** — `ctx.session.login` mints a real Redis session; the per-case loop closes watchers in a guaranteed path but never deletes `state.token`. A case that throws/forgets `logout` leaves a valid session until TTL → accumulation on dev/CI Redis. Fix: best-effort `deleteSession(state.token)` in the guaranteed path. |
| M-tr6 | OPEN | — | 1/3 | `walkEndpoints.ts:5-24` | **Auto-sweep covers only API endpoints, never sync routes** (no `syncMethodMap` walk) → the most authz-sensitive surface (sync room-membership over HTTP/SSE) gets NO auto coverage; documented roadmap gap. Fix: add a sync walk running auth-enforcement + contract. (DEFERRED feature.) |
| M-tr7 | OPEN | — | 1/3 | `csrfEnforcementCheck.ts:34-95` | **`csrfEnforcementCheck` has no method guard** — the GET-exemption lives only in the unwired wrapper, so a direct caller on a GET route hits "CSRF not enforced" (false fail). Bundle with B5. Fix: return `skipped` for non-state-changing methods inside the check. |

---

## OPEN / NEW — LOW (compact, grouped)

| Area | Location refs | Finding (one line each) |
|---|---|---|
| api | `responseEnvelope.ts:86-93`; `:153`; `:1-2`; `backpressure.ts:29-31`; `responseEnvelope.ts:72-84`; test `:57` | Success envelope spreads raw handler result (e.g. a Prisma row w/ passwordHash to the wire); `rateLimitExceeded` `scope` mislabeled; file-level `eslint-disable` on both handlers; engine.io backpressure no-op-on-rename; returned `{status:'error',message}` not redacted (vs thrown path); orphaned `@luckystack/login` mock; socket-only paths untested. |
| sync | `handleSyncRequest.ts:782`; `handleHttpSyncRequest.ts:~702`; `clientFanout.ts:104` + N6/N7/N8 | `ignoreSelf` no-op for anon/HTTP (echo); HTTP success envelope spreads `serverOutput` (leaks reserved keys) + differs in shape from socket; raw `tempToken` into `_client` params; two divergent disconnect/abort mechanisms; `attachSyncReceiver` forwards reserved keys into the `stream` payload; stream hooks fire on no-op emit (over-count). |
| core | N4/N5/N6/N7/N8/N9/N10/N11; O18/O19/O20/O21 | `parseRecordParts`-null fail-open back-door; `deepMerge` drops legit `constructor`/`prototype` keys; in-memory limiter `windowMs=60_000` default shadows config; 413 branch doesn't drain the body; offline-queue eviction fires `onDrop` synchronously (unwrapped hook); proxy `Reflect.get` receiver; `dispatchHook` catch body unguarded logger/tracker; `checkOrigin` empty-port same-origin reject; redis stray-prefix net partial (by-design); `clientHookBus`/`apiInterceptors` raw try/catch; `waitForSocket` busy-poll + unbounded `responseIndex`; `getCookieValue` raw-on-decode-throw (lenient). |
| error-tracking | O7/O8/O9/O11/O13; N3/N4/N5 | Fan-out `setUser/recordMetric/flush` swallow throws bare; `beforeSend` w/o `forwarded:true` silently dropped; `startSpanHandle` per-call event no duration (zero prod callers); `SpanResult<T>=T` no-op alias; Sentry/Datadog adapters have no `flush()`; CLAUDE.md claims `postLogin` auto-wired (only `postLogout`); Datadog span-before-counter; mixed-mode double-captures to Sentry. |
| login | F19/F14/F20/F22/F25/F36; run1 items | Reset/email tokens stored un-hashed (raw token as key); unbounded `listAll` SCAN; bcrypt 72-byte truncation unenforced; attacker email to `loginFailed`/lockout key (PII); duplicated `consume*` blocks; Microsoft avatar inlined unbounded base64 into the session; `consume*` don't assert the DEL leg; forgotPassword logs raw email+`found` (enum oracle in logs). |
| secret-manager | O2/O3/O4/O5/O6 | `validateUrl` accepts userinfo (URL-embedded creds); file-reload merge outside the serialized step; merge-only never PRUNES a dropped pointer; token `fromFile` re-read+re-validated per RETRY attempt; zero-pointer re-scan can mis-classify `^(.+)_V(\d+)$` env values off-host. Dev/remote. |
| server / root-server | O9–O17; RS-04/RS-06/RS-09; #3/#5/#10–#16/#20/#22/#23 | `pingPrisma` cast (silent 503 on rename); legacy `X-XSS-Protection`; Socket.io handshake accepts origin-less; `/assets/*` matched via `includes`; testReset URL from `Host`; `serveWithRewrittenUrl` mutates shared `req.url`; dead `\|\| '/'` operand; unsigned double-submit CSRF echo; unauth un-rate-limited `/readyz`+`/_health` DB/Redis amplification; 403 denylist returns 200 (no `writeHead`); committed `console.log` on the prod static hot path; dev REPL exposes session enumeration+tokens; `startsWith(rootFolder)` no trailing-sep; SVG inline no CSP; sync `readFileSync` in async locale reload; `loadProdMaps` swallows errors; build-fallback 200 string; config captured pre-secret-resolve; `@ts-expect-error` email signature drift; blanket `eslint-disable`. |
| devkit | O3–O7; O9/O13–O21 | Orphaned shadow `emitter.ts`; dashboard template tests absolute path; ESM cachebust leaks a module/save; loader follows symlinks no cycle guard; stale-program → `{}` all-reject type; `$`-injection in server-inject splice; regex import-extraction misses multi-line/type-only; `calculateRelativePath` fallback first-`src/`; Zod no `.strict()`; intersections → `z.any()`; prod supervisor no signal handlers; non-literal `auth.login` extracts public silently; `getSourceFile` miss → DEFAULT type silent; blanket eslint-disable; force-exit 1500ms magic; god-functions/dup utils. |
| mcp | 2/3/4; 5–12 | `grepLines` truncates at 60 silently; `resolveNodeId` basename collision → bare `null`; `GraphSchema` no referential-integrity check; `god_nodes` schema `max(100)` vs producer cap 25; `readDocFile` guard lexical (no realpath/case); `get_decision` slug unanchored substring; no graph staleness signal; `projectRoot` memoizes string not promise; boot catch no `process.exit`; `who_calls` no dedupe; library-import boots the stdio server (by-design). |
| docs-ui | #5/#6/#9–#15 | Try-it-out runner URL omits `api/` prefix → 404; non-array `meta.tags` crashes the page; raw `brandColor`/`fontFamily` in `<style>` (config-trust); raw method as CSS class; `logoUrl` no scheme allow-list; legacy bare-map accepted with no `schemaVersion`; per-keystroke full re-render + listener re-bind; non-JSON error body mislabel; three divergent `escapeHtml` copies. |
| scaffolder | K6–K21 | `copyTree` follows symlinks; i18n boolean asymmetry; repeated unguarded pkg-json `JSON.parse`/rewrites; all-`@latest` MCP pins; `set -e` pre-commit hard-blocks; `pickFromList` non-TTY silent default; `editScaffoldFile` silent no-op on absent target; dep injectors swallow absent pkg.json; 5 dead template vars documented as substituted; Prisma `LANGUAGE` enum untouched by `--no-i18n`; template `text-white` (Rule 14) the scaffold's own ai:lint flags; `npm ci` w/o lockfile; `.env` gitignored vs "commit this file"; zero unit coverage on mutators. |
| presence | #2/#4/#5/#6/#9/#10/#11/#14/#12 | Doc dead-pointer for `registerRoomNameFormatter`; per-socket AFK double-fire (multi-tab); per-token AFK rollup missing (false-AFK); multi-tab guard probes RAW token room (latent, keyed to B20); `informRoomPeersLeft` fires no pre/postPresenceUpdate hook; stale dedup doc; `LocationProvider` reads `globalThis.location.search` lagging router; sampler runs with AFK disabled; `getRoomPresence`/`getLastActivity` shadow-API (no prod caller). |
| root-src | O4/O5/O10/O12/O14/O15/O16/O17/O18 | `LoginForm` gates success on `!response.status` not `===true`; `src/updateUser_v1` omits `nameMaxLength`; login button can lock permanently on slow redirect; attacker string fields deref without `typeof` guard; PREF-01 preferences overwrite-not-merge; hardcoded English bypasses `useTranslator` (incl. `main.tsx` ErrorFallback mislabeling `common['404']`); non-token `text-white` (Rule 14); god-component `settings/page.tsx`; god-effect `socketInitializer`. |
| test-runner | #10–#21 | `walkEndpoints` casts any string to `HttpMethod`; `computeTotals` ignores `skipped`/`xpassed` in exit totals; `apiMetaMap` hand-mirrored no conformance guard; duplicated probe scaffolding; totals/reporter hand-maintained term-by-term; `customTests` silently skips empty exports (stub illusion); `as never` cast; fuzz `__proto__` payload inert (serializes to `{}`); `JUNK_PAYLOADS` mutable module-level; auth-enforcement hinges on undocumented check-ordering; `session.login` mints a session with no Prisma user row; rate-limit check encodes one limiter model. |

---

## DEFERRED-DECISION (real, pending policy/ADR/contract)

| ID | Sev | W1 | W2 | Area · Location | Decision needed |
|---|---|---|---|---|---|
| DD1 | MED | 5-6/6 | 3/3 | core (defaults) + sync (enforcement) · `projectConfig.ts:773-774`; `_shared/receiverAuth.ts` | **Default-permissive sync receiver auth** (`allowClientReceiverAll:true`, `requireRoomMembership:false`) — any client (unauth on a `login:false` route) may broadcast `receiver:'all'` (cluster-wide amplifier) or target any unjoined room. The helper is correct + wired; secure values exist but are opt-in. Flip defaults for 0.2.0 OR boot/dev-warn + ship the scaffold `config.ts` with secure values commented. The headline 0.2.0 hardening item. |
| DD2 | MED | 1/6 | — | core · `rateLimiter.ts`; `rateLimiting.onStoreError` | Rate-limit fail-OPEN by default on Redis outage (`'memory'` per-process multiplies the global limit). Keep `'memory'` or default `'deny'` when `store:'redis'` && prod. (= M-core6 policy half.) |
| DD3 | MED | 1/3 | — | router · `httpProxy.ts:191` | **Edge request-body size cap REMOVED** (no `maxRequestBodyBytes`) — the LB no longer caps oversized bodies; backends still defend. Re-introduce an edge cap OR document delegation to the backend. |
| DD4 | MED | refutes #3/#11 | 3/3 | router · `wsProxy.ts` (no hooks); `httpProxy.ts:88-94` | **No edge deny-gate on either transport** — only observer hooks; WS dispatches nothing. A socket-first deployment can't refuse a banned IP / enforce maintenance / edge auth. Add a fail-CLOSED `proxyRequestGate` on BOTH paths, or document the absence. |
| DD5 | MED | 1/6 | (carried) | secret-manager · `server/bootstrap/initSecrets.ts` | Almost the entire `SecretManagerConfig` is shadow-API: the wired caller forwards only `{url,token,source:'remote',dev}` (rotation poll/`retries`/`envNames`/etc. unreachable). Widen `SecretManagerBootConfig` OR document `url`/`token`/`dev`-only. |
| DD6 | MED | 5/6 | 1/3 | docs-ui · `docsHtml.ts:335-395` | `render()` decomposed in-place but NOT re-architected to event-delegation / typed code — the real fix is wiring renderCore (= the docs-ui CRITICAL below). Bundled into that rewire decision. |
| DD7 | LOW | various | various | login / root-src · `requestEmailChange_v1.ts:42-48` | `requestEmailChange` `emailTaken` enumeration oracle (authenticated, rate-limited, acknowledged in-code, no ADR); account-lockout-as-DoS-lever (opt-in); whether `updatePasswordHash` should revoke sessions at the primitive. Write the ADRs or accept. |
| DD8 | LOW | — | various | devkit `extractors.ts`/`zodEmitter.ts`; email `sendEmail.ts`; test-runner `customTests.ts`/`runAllTests.ts` | Output-shape/contract changes deferred: devkit `getSourceFile` miss → loud sentinel vs silent `{}`; Zod `.strict()` (flips fuzz pass/fail); email CRLF-sanitize / send-timeout / ConsoleSender token-redaction / CTA-URL validation (new features vs the simplified tree); template render throw→typed-failure; test-runner `skipped`-variant for empty custom-test files; `computeTotals` xpass→red strict mode. |
| DD9 | LOW | 3/6 | (carried) | core · `env.ts:108` | `export const env = bootstrapEnv()` runs `loadDotenv` at module import — converting to lazy `getEnv()` is a public-API change (idempotent + cached, low impact). ADR. |
| DD10 | LOW | — | 1/3 | core · `projectConfig.ts:673-677`; `rateLimiter.ts` | 🔌 `rateLimiting.auth` lockout slot declared in core config but unread by the core limiter (implemented in `@luckystack/login`). Confirm login consumes it + document the core slot as no-op-without-login, or boot-warn. |
| DD11 | LOW | 1/6 | confirms | root-server · `server/bootstrap/initSecrets.ts:38-44` | Secret-manager seam deliberately fail-OPEN on package absence (URL set but package missing → boot continues). Confirmed intentional (user memory). Add a security-docs note so a future reviewer doesn't "fix" it. |
| DD12 | LOW | 2/6 | 2/3 | email · `sendEmail.ts:24-25,56` | Unsalted recipient hash (HMAC needs a secret source + key-rotation decision); `adapterHint` typed Internal but documented as consumer API (publish-vs-internal). |
| DD13 | LOW | various | various | login `passwordReset.ts`; presence `lifecycle.ts` | `updatePasswordHash` doesn't revoke sessions at the primitive (shipped routes do); presence multi-tab second-disconnect reason precedence; `intentionalDisconnect` userAfk fan-out gate change. Behavior/contract changes. |

---

## FIXED — verified resolved in current tree (compact)

**The three wave-1 CRITICALs are all FIXED:**

| ID | W1 | Area · Location | Evidence |
|---|---|---|---|
| C1 | 6/6 | router · `wsProxy.ts:59-64` | WS proxy `onClientGone` on `'error'`+`'close'` at the top before any IO; regression test "survives a client RST mid-handshake". |
| C2 | 4/6 | server + root-server · `staticRoutes.ts:14,43-47`; `serveFile.ts:70-71` | `GET /server.js` + `*.map` source disclosure: `SERVE_DENYLIST_REGEX` 404s both BEFORE any serveFile; sourcemaps default OFF. (Caveat: guards are uncommitted; bundle still emitted into `dist/` — allow-by-default model OPEN as M-srv7.) |
| C3 | 3/6 | router · `wsProxy.ts:70-73,86-89`; `proxyUtils.ts:64-78` | WS-proxy SSRF: `isOriginFormTarget` 400s non-origin-form before URL build; `isHostPinned` 502s after; tests cover absolute-form + protocol-relative. |

**Wave-1 HIGHs now FIXED (verified):**

| W1 ID | Area · Location | Evidence (residual noted) |
|---|---|---|
| H1 (ET-02 ALS) | error-tracking · all 4 handlers + 3 adapters | Per-request identity bound in prod; all adapters read the getter; Datadog ALS too. **Residual: legacy `initializeSentry()` path (M-et2).** |
| H3 (apiRequest onDrop hang) | core · `apiRequest.ts:439-446` | `onDrop` wired with `suppressOnDrop`; `cleanupAbortController` on drop. |
| H4/H29/H30 (validateType fail-OPEN/recursion/Record/proto) | core · `runtimeTypeValidation.ts:184,262-296,363-387` | Terminal fails CLOSED; depth 64; Record value+key validated; proto-keys rejected; `safeValidateType` wrapper. **Residual: quote-blind splitter (M-core1), Date-any-string (M-core2), single-member union skip.** |
| H5 (ActiveSession token/handle) | cli · `settings/page.tsx:36-40` | Asset declares `handle`; `listSessions_v1` returns `handle`. |
| H6/H7 (CLI pnpm substring / unguarded JSON.parse) | cli · `lib/project.ts:230-243,57-71` | PM-detection removed; `readPackageJson` guards. (New residual: always-npm ignores yarn/pnpm/bun.) |
| H8/H9/H15/H16 (presence/supervisor/hotReload unguarded async) | presence `lifecycle.ts`; devkit `supervisor.ts` | Bodies wrapped in `tryCatch`; supervisor `'error'` listener + backoff; 30s blocking `spawnSync` gone. |
| H11/H33 (unverified OAuth email; overlay clobber) | login · `login.ts:570,846`; `oauthProviders.ts` | Fail-closed cross-provider link guard; `emailVerifiedKey` set; UPN fallback gated; raw claims to extraSessionFields + key strip. |
| H12/H17 (devkit regex-rewrite; root-sync route-key) | devkit · `loader.ts`, `routeMeta.ts`, `templateInjector.ts` | DELETE-twin uses replacer functions; root syncs canonical `system/<name>`. **Residual: PAIRED twin + server-inject splice (M-dk3).** |
| H19 (serveAvatar stream crash) | core · `serveAvatars.ts` | `stream.pipeline` with an error sink. |
| H20/H21 (XFF spoof) | router · `proxyUtils.ts:113-122` | Router strips inbound `x-forwarded-*`/`x-real-ip`, sets its own. **Residual: backend leftmost-XFF on the opt-in path (B18).** |
| H22 (router req/res abort leak) | router · `httpProxy.ts:184-189`; `wsProxy.ts:155-158` | abort on req error/aborted + res error; non-101 `'response'` 502s. **Residual: upstream-leg no-timeout (B1).** |
| H23/H24 (deleteAccount no-op/broken) | cli · `deleteAccount_v1.ts:52-62` | Adapter resolved + fails loud if `!adapter.delete`. **Residual: UI passwordless-delete (B4) + null-hash gate (O2).** |
| H25 (Sentry replay PII) | root-src · `sentry.ts:62-63` | `maskAllText:true`+`blockAllMedia:true`+`beforeSend` strips token/code. |
| H26/H27 (token in query; ErrorPage stack leak) | root-src · `page.tsx:13-26`; `ErrorPage.tsx:37` | Root `?token=` removed; `ErrorPage` gates stack behind DEV. **NOTE: the TEMPLATE ErrorPage is NOT gated (scaffolder N2) + the template deleteAccount is broken (scaffolder N1) — drift, OPEN.** |
| H28/H32 (translate regex-injection; async_hooks in client) | core `TranslationProvider` (residual M-core11); root-server `shared/tryCatch.ts` (OPEN) | async_hooks: the shipped TEMPLATE `shared/tryCatch.ts` is clean; the repo's OWN copy still drags `node:async_hooks` into the client bundle → repo-internal drift, OPEN HIGH-leaning. |
| H31 (sqlite invalid Prisma schema) | scaffolder | Not re-flagged by wave-2; treated resolved. |

**Wave-1 MEDIUM/LOW now FIXED (per-area verified, compact):** M13 (`SessionProvider` raw JSON.parse + stale socket); M14-class (playground unauth stream + arbitrary-recipient email — `login:true` + `MAX_TARGETS` + dev-gate); M15 (`MiddlewareResult` union includes `undefined`); M40 (avatar `?v=` display double-append); presence F1–F14 (all wave-1 presence highs + snapshot-formatter); secret-manager F1–F11 (the prior Med/Low cohort + regression tests); email F1–F4; cli F2–F17; devkit F4–F11; mcp 13–20 (proto-chain keys, `get_decision` numeric, `resolveNodeId`, `find_route` guard, empty-query, version read, casts removed, path-containment guard); test-runner 22–35 (`reauthenticate` path gone, `schemaSampleInput` coverage, Layer-5 parse+timeout, `requiresLogin` guard, symlink-loop guard, stream-watcher leak, invalid-token probe, `resetServerState` timeout, webhook SSRF warn, god-function decomposition, xpass reporting); server F1–F10 (trustProxy auth throttle, IP-resolution dedup, `sanitizeForLog` depth+cycle, requestId-at-top, SSE sync hardening, `withSessionLock` defuse, HEAD CSRF, source-disclosure block); root-src F1–F11; root-server 1/8/17/18.

---

## FALSE-POSITIVE (compact — one-line reason each is bogus)

| Area | Claim | Why bogus |
|---|---|---|
| email | wave-1 #1/#2/#5/#6/#9/#10/#13 (sanitizeHeaderMap, withSendTimeout, redactUrlSecrets, safeColor/safeCtaUrl, dispatchEarlyDrop) | **Phantom code** — those helpers never existed in git history; wave-1 audited a hallucinated `sendEmail.ts`/`console.ts`. Real residuals re-tracked as O3/O7/O8/O9. |
| router | `proxyRequestGate` deny-gate never applied to WS / fails OPEN on throw (#3/#11) | The gate the baseline assumed **never existed** (grep=0); real gap = no deny capability → DD4, not a bug. |
| server | `registerErrorFormatter` is a shadow API never consulted | Stale-scope read — relocated to core; `applyErrorFormatter` IS called from api/sync handlers; the scan only grepped the server package. |
| api / devkit | `validateType` "still fails OPEN" | Stale read of a pre-fix version; terminal + unresolved branches fail CLOSED now. |
| core | `apiRequest`/`validateType`/`projectConfig` god-functions; socket-method = HIGH vuln; `getCookieValue` raw-on-bad-encoding; redis stray-prefix tenant bug; `serveAvatar` oracle; `acquireLease` ambiguity | Quality observations / by-design / out-of-area, not defects (socket-method is an HTTP-semantics control, not authz; the route is origin-gated + per-message auth). |
| login | "unified links unverified OAuth email" still HIGH; "stale ActiveSession breaks build"; template `updateUser` arbitrary theme/language | All refuted: cross-provider guard present; handle/token aligned; theme/language are Prisma DB enums rejected at the write. |
| cli | template `updateUser` persist-arbitrary-theme/language; `LANGUAGE_RE` session poisoning; avatar decompression-bomb | DB-enum-blocked; blast radius is the attacker's OWN session; the avatar policy is core `processUpload`'s. |
| secret-manager | fail-OPEN on package-absent/url-unset = defect; resolve-response "fails open" | **Confirmed deliberate** (user memory) + bounded (remote mode IS fail-closed); response parsing is fail-CLOSED. |
| devkit | `validateDeploy` ANSI "missing ESC byte"; `validateType` "fails OPEN at the resolver boundary" | The ESC byte (0x1b) IS present (`od -c` confirms); the resolver returns `__RUNTIME_UNRESOLVED__` → core maps it to a CLOSED error. |
| mcp | shebang on a "pure library entry"; `createLuckystackMcpServer` ~200-line factory | No separate library entry exists (single file = bin = exports); the factory was renamed/flattened away. |
| docs-ui | self-minted CSP `nonce` mismatch; `ensureCsrfToken` race; `enabledInProd`-without-`authorize` boot warning; `pageSet` overcount; `meta.tags` `[object Object]` | All cite constructs no longer in current code (grep=0) or are XSS-safe (`escapeHtml(String(t))`). |
| presence | snapshot reads raw room | Was a REAL bug, already fixed this session (F14) → FIXED, not a false positive. |
| root-src | shipped `signOutEverywhere` logs caller out of own device; Sentry masking "still open"; apiRequest offline-queue hang attributed to root-src | Asset already passes `exceptToken`; `sentry.ts` masks text+media; the queue bug lives in core (root-src files are re-export shims). |
| root-server | Sentry replay PII leak (`replaysOnErrorSampleRate=1` + unmasked text) | The only consumer sets `maskAllText:true`+`blockAllMedia:true`+`beforeSend`; the chain does not exist. |
| scaffolder | wave-1 H5 "stale ActiveSession interface in template" | The template interface already matches `listSessions_v1`'s return shape; no drift. |
| test-runner | CSRF check can mutate on a CSRF-off server; flaky `/_test/reset` wall-of-skips; minted-session try/finally leak; `runRateLimitTests` cookie `split('=')` | The describing paths no longer exist (layer unwired so never invoked; `resetBetweenEndpoints` never set; sessions now per-case; the fragile split is gone). The session-leak concern survives as M-tr5. |

---

## Cross-area CRITICAL note — docs-ui (publish-blocker for 0.2.0, but dev-only)

`per-area-reconciled/docs-ui.md` carries a **NEW verified CRITICAL** — the package's sole function is broken — recorded here for completeness. Its blast radius is **dev-only** (`enabledInProd:false` default), so it is a broken/garbled-dev-docs + publish-blocker, **not** an end-user or security breach. It is NOT in the count table's CRITICAL column (reserved for C1–C3) but it is the top docs-ui OPEN-NEW item.

- **The shipped renderer reads the WRONG artifact shape (CRITICAL, 3/3).** `docsHtml.ts:339-342` iterates a NESTED `apis[page][name][version]` map, but the emitter + committed `apiDocs.generated.json` are FLAT `apis[page] = Entry[]` (`emitterArtifacts.ts:62,269,281`). Every real route renders as scrambled rows; method falls back to POST. Syncs are never rendered (`render()` reads only `data.apis`). The correct renderer (`renderCore.ts`, flat-array + sync-aware) is DEAD code imported only by its test, and a wrong-shaped nested test fixture keeps CI green over the broken path. Dist ships the bug. **Fix:** serialize `renderCore.ts`'s flat-array `renderDocsEndpoint` into the page (its documented `.toString()` strategy) OR rewrite `buildGroups` to iterate the array; add an `ai:lint` "exported-but-only-tests-import-it" rule. The related HIGHs (syncs never rendered, renderCore shadow code, fictional-fixture green CI) all fold into the same rewire.
