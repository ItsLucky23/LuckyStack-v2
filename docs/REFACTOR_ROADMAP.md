# Refactor & Remediation Roadmap — FINAL (wave-1 6 scans + wave-2 3 scans, reconciled to current code)

Single authoritative, fully-reconciled remediation roadmap for LuckyStack-v2 (branch `chore/package-split-prep`, HEAD `302cbf1` + ~248 uncommitted working-tree fixes — "this week's pass"). It merges the original **6-scan wave-1** roadmap (`codebase-scan-14-06-MERGED/REFACTOR_ROADMAP.md`) with the **3-run wave-2** re-audit, both collapsed through the 18 per-area reconciliation files in `per-area-reconciled/`, where **every kept finding was re-verified against the current tree at file:line** (wave-2 "fixed/present" claims were NOT trusted on faith).

Each row carries a **STATUS** (NEW / OPEN / FIXED / DEFERRED-DECISION / FALSE-POSITIVE), severity, wave-1 corroboration (n/6) + wave-2 corroboration (n/3), location, and recommendation. Rows are sorted so actionable work is on top: **NEW + OPEN by severity**, then **DEFERRED-DECISION**, then **FIXED** (compact), then **FALSE-POSITIVE** (compact, one-line bogus reason). Same issue across areas/waves = one row (the area report it primarily lives in is cited; cross-area duplicates folded).

> **Headline movement since wave-1.** The two wave-1 CRITICALs are resolved-in-tree: **C1 router WS-proxy crash → FIXED** (`wsProxy.ts` listener at top + regression test), **C2 `/server.js` source disclosure → FIXED (mitigated)** at the framework deny-list (caveat: uncommitted; bundle still emitted into `dist/`). But wave-2 surfaced **one NEW CRITICAL** the 6 wave-1 runs all missed — **docs-ui live renderer reads the wrong artifact shape** (every route renders as garbled rows) — plus a cluster of **NEW HIGHs**: an unauth `decodeURIComponent`/`getParams` process-crash DoS class (root-server + core), the api-SSE twin-drift regression, several **shadow-API / shadow-config** surfaces (api `CLAUDE.md` phantom hooks, email `preEmailSend` stop-signal ignored, test-runner CSRF layer unwired, raw-token rate-limit keys, sync `flushPressure` config dead + raw-token-to-tracker leak), and the **template/src drift HIGHs** (credentials self-delete broken, ErrorPage stack leak in the template copy, docs-token corruption). The dominant systemic themes are unchanged: **hand-synced transport/template twins that drift**, **fail-OPEN / default-insecure-but-opt-in posture invisible to the AI**, and **advertised-but-unwired "shadow" surfaces**.

---

## Status count table

> **STALE as of 2026-06-16** — these counts predate the 2026-06-16 reconciliation sweep and have NOT been recomputed. That sweep flipped ~21 HIGH/CRIT rows to FIXED (and 2 to FALSE-POSITIVE), so the real OPEN/NEW/DD totals are materially lower than shown. The per-row STATUS cells below are authoritative; this table is not. See the "Session reconciliation — 2026-06-16" block for the exact deltas. (Counts left un-recomputed because several dual-labeled rows — "OPEN / DD", "High (disputed Med)" — don't map cleanly to a single bucket; recompute deliberately rather than guess.)

| Status | Critical | High | Medium | Low | Total |
|---|---|---|---|---|---|
| **NEW** (wave-2 found, wave-1 missed, verified real) | 1 | 7 | 13 | 22 | 43 |
| **OPEN** (known, still present) | 0 | 14 | 33 | 60 | 107 |
| **DEFERRED-DECISION** (real, pending policy/ADR) | 0 | 1 | 8 | 12 | 21 |
| **FIXED** (verified resolved in tree) | 2 | 18 | 26 | 35 | 81 |
| **FALSE-POSITIVE** (bogus / phantom / by-design) | 0 | 3 | 5 | 22 | 30 |
| **Totals** | 3 | 43 | 85 | 151 | 282 |

> Severity = highest any run assigned (merge convention); disputes noted inline. Counts aggregate the 18 reconciled area files; god-function/refactor items are folded under their package row. "NEW" means no wave-1 run caught it and wave-2 verified it against current source.

---

## Session reconciliation — 2026-06-16 (verify → fix → verify sweep)

Targeted re-verification of 14 HIGH/CRITICAL items + 5 DEFERRED-DECISION items against the current tree (`chore/package-split-prep`). Each was re-checked at file:line by read-only agents; the 4 still-present items were fixed and re-verified; `npm run lint` + `npm run build` both green.

- **Fixed this session (status flipped to FIXED below):** DOCSUI-3 (deleted dead `renderCore.ts` + its test — shipped renderer is covered by `liveRenderCore.test.ts`), TR-2 (CSRF token threaded into the authenticated sweep via `getSession`; shared `STATE_CHANGING_METHODS` + mutating-method skip in fuzz/rate-limit cookie-mode), ROUTER-O1 (`routing.upstreamTimeoutMs` now passed into `createHttpProxy`/`createWsProxy`), ROOTSRC-O3 (consumer `src/settings/_api/*` re-synced to the CLI asset — adapter + pre/postAccountDelete hooks + avatar unlink on delete, 16-char `sessionHandle` + ownership re-validation, `page.tsx` `id`→`handle`).
- **Verified already FIXED in tree (no action needed):** DOCSUI-1, DOCSUI-2 (renderer walks flat arrays + renders syncs), RS-01 (`tryCatch` guard → 400), CORE-O1 (`'error'` resolves `null`), TR-1 (CSRF layer exported + wired), ET-O1 + SERVER-O18 (`stop()` + `flushErrorTrackers` + prod SIGTERM/SIGINT + persistent `httpServer.on('error')`), SYNC-N2 (`redactToken`/`redactTokens` live at both call sites), EMAIL-O1 (`preSend.stopped` honored before `sender.send`), ROOTSRC-O2 (key helpers imported from `@luckystack/login`), and DD items SYNC-O1 (defaults flipped fail-closed) + H17 (`bootUuid` salt passed + `'hmac'` default).
- **Re-classified FALSE-POSITIVE:** LOGIN-03 (password policy is skipped on the login path via `mode`; lockout counter uses an allowlist that omits `login.password*` reasons), DELETE-H (password IS collected via a separate `deletePasswordRef` and verified server-side; ConfirmMenu only gates the typed "DELETE" string).
- **DEFERRED-DECISION → FIXED this session (user-approved):** CORE-O3 (new `http.trustedProxyHopCount`, default 1 — counts hops from the RIGHT, leftmost client hop never trusted; ignored when `trustProxy` off), CORE-O10 (new `http.acceptBearerInCookieMode`, default false — cookie-mode reads only the cookie token; token-mode unaffected), RS-F1 (new browser-safe `tryCatchClient.ts` lazy-imports the capture seam; both client entry points — `shared/tryCatch.ts` + the `@luckystack/core/client` barrel — repointed to it; server `tryCatch.ts` byte-for-byte unchanged; build confirms no `node:` externalization for the client entry). No DEFERRED-DECISION items from this sweep remain open.

---

# ACTIONABLE — NEW + OPEN (sorted by severity)

## CRITICAL

| ID | Status | Sev | W1 | W2 | Finding | Location | Recommendation |
|---|---|---|---|---|---|---|---|
| DOCSUI-1 | **FIXED** (reconciled 2026-06-16) | CRIT | — | 3/3 | **docs-ui live renderer walks a NESTED `apis[page][name][version]` map, but the devkit emitter + committed `apiDocs.generated.json` are FLAT `apis[page] = Entry[]`** → every real route renders as scrambled rows (`name="0"`, `version="page"`, `meta`=string → method falls back to POST); a wrong-shaped test keeps CI green. The correct renderer (`renderCore.ts`) is dead code imported only by its test. Dist ships the bug. Dev-only (`enabledInProd:false`) but ship-blocking for 0.2.0. | `docs-ui/src/docsHtml.ts:339-342` vs `devkit/.../emitterArtifacts.ts:62,269,281` + `src/docs/apiDocs.generated.json:3` | Serialize `renderCore.renderDocsEndpoint` (flat-array) into the page, OR rewrite `buildGroups` to iterate `entries` arrays; render `data.syncs` too (DOCSUI-2/3); fix the green-over-broken test (DOCSUI-4). |

## HIGH

| ID | Status | Sev | W1 | W2 | Finding | Location | Recommendation |
|---|---|---|---|---|---|---|---|
| H-TWIN | **FIXED** (2026-06-16) | High (systemic) | 6/6 | 3/3 | **Transport-twin drift: socket vs HTTP handlers (api + sync), `apiRequest`↔`syncRequest`, HTTP-proxy↔WS-proxy are hand-synced god-functions with no parity enforcement** — root cause of most concrete drift bugs below. Validation/lifecycle/envelope now shared in `_shared/`, but rate-limit + auth blocks remain inline twins and ALREADY diverge (loopback skip, IP resolution). | `api/.../handleApiRequest.ts` ↔ `handleHttpApiRequest.ts`; `sync/.../handleSyncRequest.ts` ↔ `handleHttpSyncRequest.ts`; `apiRequest.ts` ↔ `syncRequest.ts`; `httpProxy.ts` ↔ `wsProxy.ts` | Collapse each pair onto shared `_shared/*Stage` pipelines + thin transport adapters; add an `ai:lint` behavioral-parity invariant so divergence is a build error. |
| RS-01 | **FIXED** (reconciled 2026-06-16) | High | — | 1/3 | **Unauth single-request process-crash DoS: unguarded `decodeURIComponent(url)` in `serveFile`** (`GET /assets/%ZZ` → `URIError` → unhandled rejection → process exit). No try/catch around `dispatchRoutes`, `void handleHttpRequest(...)` has no `.catch`, NO global `unhandledRejection` handler anywhere. | `server/prod/serveFile.ts:54`; `server/staticRoutes.ts:50-58`; `server/httpHandler.ts:306`; `createServer.ts:194-195` | `tryCatchSync(() => decodeURIComponent(url))` → 400; add `.catch` on the voided dispatch + a global `process.on('unhandledRejection')` backstop. |
| CORE-O1 | **FIXED** (reconciled 2026-06-16) | High | — | 1/3 | **`getParams` request-stream `'error'` REJECT → unhandled rejection → worker crash** (same crash CLASS as RS-01, distinct path: client RST mid-body on POST/PUT/DELETE). Caller `handleHttpRequest` has no try/catch; fired as `void`. | `core/.../getParams.ts:111-113`; `server/httpHandler.ts`; `createServer.ts:195` | Core-only safe fix: resolve `null` on `'error'` (documented "no usable body" signal); add `.catch()` on the `void` dispatch as server-side backstop. |
| SRV-O1 | **FIXED** (reconciled 2026-06-16) | High | — | 3/3 | **Regression: SSE error/abort hardening ported to `syncRoute` but NOT `apiRoute` (twin drift)** — apiRoute registers only `req.on('close')`; a write `'error'` on the api SSE ServerResponse has no listener → uncaught throw → worker crash. | `server/.../apiRoute.ts:26-31` vs `syncRoute.ts:63-69` | Extract `bindSseStreamLifecycle(req,res,markClosed)` into `sse.ts`, call from BOTH routes; add an `ai:lint` parity check. |
| TR-1 | **FIXED** (reconciled 2026-06-16) | High | — | 3/3 | **test-runner CSRF-enforcement layer is dead surface** — written + typed + tested but never exported from `index.ts`, never wired into `runAllTests`. Default `npm run test` gives ZERO CSRF coverage; a consumer who disables/misconfigures CSRF still gets a fully-green run. | `test-runner/.../index.ts` (no csrf export); `runAllTests.ts:88-133`; `csrfEnforcementCheck.ts:34` | Export + add as a 5th sweep layer gated on a session cookie; bundle the GET method-guard (TR-9) into the same change. |
| TR-2 | **FIXED** (2026-06-16) | High | 2/6+3/6 | 3/3 | **Authenticated sweep layers omit the CSRF header the server enforces, AND the mutating-method partition was removed** — contract/fuzz false-PASS, rate-limit false-FAIL on cookie-mode+CSRF; on token-mode/CSRF-off the fuzz layer now hits every authenticated DELETE/PUT with junk bodies (mutation risk on body-ignoring routes). | `runAllTests.ts:73-80`; `contractCheck.ts`/`rateLimitCheck.ts`/`fuzzCheck.ts` vs `customTests.ts:319` | Thread the session-bound CSRF token into the authenticated sweep builders; restore a mutating-method partition / skip state-changing cookie-mode routes. |
| API-O1 | **FIXED** (reconciled 2026-06-16) | High | — | 3/3 | **Shadow-API: `packages/api/CLAUDE.md` documents hooks + helper + config that DO NOT exist in code** — `apiAuthRejected`, `preSocketMessage`, `applyGlobalIpRateLimit`, `rateLimiting.identity`, `rateLimiting.skipLoopbackInDev` (grep of `api/src` = 0 hits). An AI wires abuse-detection against no-ops. | `packages/api/CLAUDE.md` vs `packages/api/src/*` | Strike the phantom entries (or implement on both transports); add an `ai:lint` rule that every hook/config named in a package CLAUDE.md is actually dispatched/read in that package. |
| API-O3 | **FIXED** (reconciled 2026-06-16) | High | 2/6 | 3/3 | **Per-route rate-limit bucket keyed on the RAW SESSION TOKEN** — contradicts CLAUDE.md "never the raw token"; token lands in Redis key names + the dev warn log. The wave-1 "key never contains the token" positive is REFUTED. | `handleApiRequest.ts:141-143,162`; `handleHttpApiRequest.ts:318-320` | Key on `user.id` when authenticated (else resolved IP); add a test asserting no key contains the token. |
| API-O2 | **FIXED** (reconciled 2026-06-16) | High | — | 3/3 | **HTTP loopback rate-limit skip is spoofable + ignores `skipLoopbackInDev`/`isLoopbackIp`** — gated on `NODE_ENV !== 'production' && requesterIp.startsWith('127.')`; the documented config knob is an unconditional no-op (the contract break is HIGH regardless of spoof preconditions). | `handleHttpApiRequest.ts:359-362` | Gate on `skipLoopbackInDev && isLoopbackIp(resolveClientIp(...))`. |
| SYNC-O1 | **FIXED** (reconciled 2026-06-16) | High | 6/6 | 3/3 | **Receiver-auth fails-open by default** (`allowClientReceiverAll:true`, `requireRoomMembership:false`) — any client (unauth on a `login:false` route) may broadcast `receiver:'all'` (cluster-wide amplifier) or target any unjoined room. Helper is now fail-closed + wired; only the DEFAULT is permissive. | core `projectConfig.ts` `sync.*`; `_shared/receiverAuth.ts:46-68` | Flip defaults for 0.2.0 (`requireRoomMembership:true`, `allowClientReceiverAll:false`) with explicit opt-out, OR boot/dev-warn when both permissive + ship scaffold `config.ts` with secure values. (Cross-package: core owns defaults, sync owns enforcement.) |
| SYNC-N2 | **FIXED** (reconciled 2026-06-16) | High | — | 1/3 | **Dead `redactToken`/`redactTokens` shadow-API + raw bearer-token leak to error-tracker + stream logs** — `clientFanout.ts:111` passes raw `targetToken: tempToken` into the `tryCatch` error-context (→ `captureException`, NOT dev-gated); `streamEmitters.ts:222-223` logs raw `{ tokens }`. Neither key is in `DEFAULT_REDACTED_LOG_KEYS`. Replayable bearer tokens reach Sentry/PostHog/Datadog. | `sync/_shared/redactToken.ts` (dead); `clientFanout.ts:111`; `streamEmitters.ts:222-223` | Route both call sites through `redactToken`/`redactTokens` (call-site redaction is the complete fix; key registration alone misses the legacy Sentry slot). |
| ET-O1 | **FIXED** (reconciled 2026-06-16) | High | 2/6 | 3/3 | **`flushErrorTrackers()` never wired into server shutdown** — PostHog batch + buffered events lost on every redeploy; the prior accessor was removed so flush is now the ONLY drain path. Only dev-only SIGINT/SIGTERM handlers exist (`process.exit(0)`). | `server/createServer.ts:70-71`; primitive `errorTrackerRegistry.ts:175-186` | Add a `preServerStop`/prod-SIGTERM subscriber in `@luckystack/server` that `await flushErrorTrackers()` + PostHog `shutdown()`. (Owner: server cluster.) |
| SERVER-O18 | **FIXED** (reconciled 2026-06-16) | High (telemetry half) | wave-1 #10/#22 | 2/3 | **`createLuckyStackServer` lost graceful shutdown** — returns only `{httpServer,ioServer,listen}`; no `stop()`, no prod SIGINT/SIGTERM drain, no persistent `httpServer.on('error')`. `flushErrorTrackers` referenced ZERO times in `packages/server/src`; sockets/Redis never drained on non-`process.exit` teardown; post-listen `httpServer` error → crash. Same root as ET-O1. | `createServer.ts:194-206`, `types.ts:94` | Add `stop()` (`ioServer.close()` + Redis pub/sub `.quit()` + `httpServer.close()` + `flushErrorTrackers()`), persistent `httpServer.on('error')`, wire prod signals → `stop()`. |
| EMAIL-O1 | **FIXED** (reconciled 2026-06-16) | High | — | 2/3 | **`preEmailSend` stop-signal IGNORED — documented suppression/abort seam is a silent no-op.** `sendEmail` awaits the hook then discards the `DispatchResult`; a consumer wiring GDPR opt-out / bounce / unsubscribe suppression ships a no-op (suppressed recipients still get mail, returns success). | `email/src/sendEmail.ts:199-204`; dispatch returns `{stopped}` at `hooks/registry.ts:44-68` | Check `pre.stopped` and return `{ok:false, reason: errorCode ?? 'email.suppressed'}` BEFORE `sender.send`; add an orchestration test asserting `sender.send` isn't called. |
| ROUTER-O1 | **FIXED** (2026-06-16) | High | — | 3/3 | **WS + HTTP proxy upstream leg has NO timeout → half-open pair accumulation (resource-exhaustion DoS).** `transport.request({...})` passes no `timeout`; a backend that accepts TCP but never answers pins both sockets indefinitely (WS reaped only by client disconnect). Unauth-reachable on the WS path (`system` key always resolves). | `wsProxy.ts:96-119`; `httpProxy.ts:96-119` | Set `timeout` on `transport.request` from `routing.upstreamTimeoutMs`; on `'timeout'` 504 + destroy; arm a WS handshake watchdog; add a silent-upstream test. |
| LOGIN-03 | **FALSE-POSITIVE** (2026-06-16) | High | 1/6 | 3/3 | **Full password-policy validation runs on the LOGIN path AND policy-failure reasons trip the per-account LOCKOUT counter** → an attacker locks any victim by POSTing policy-violating passwords for their email, no real-password guess needed (`NON_COUNTING_REASONS` omits `login.password*`). Also leaks exact policy to unauth probers + locks out pre-tightening passwords. | login `login.ts:316`→`:532`; `authLockout.ts:84-89` | Cheap shape-checks only on login (empty/length cap), full policy on register; collapse policy fails to `login.wrongPassword`; add `login.password*` to `NON_COUNTING_REASONS`. |
| DELETE-H | **FALSE-POSITIVE** (2026-06-16) | High | 4/6 | 3/3 | **Credentials user can NEVER self-delete (GDPR right-to-erasure break)** — `deleteAccount_v1` requires a password when `dbUser.password` is set, but the settings page sends only `{confirmation:'DELETE'}` (`menuHandler.confirm`/`ConfirmMenu` discard the typed input). Present in consumer `src/`, the CLI asset page, AND the scaffold template (SCAF-N1). No real-flow regression test. | `src/settings/page.tsx:271-289`; `deleteAccount_v1.ts:29-34`; `ConfirmMenu.tsx`; `template/src/settings/page.tsx:281-285` | Extend `ConfirmMenu`/`menuHandler.confirm` to return the typed value; collect + send `data.password` for credentials accounts; add a happy-path UI regression test; mirror to asset + template. |
| ERRPAGE-H | **FIXED** (reconciled 2026-06-16) | High | 3/6 | (template N2) | **`ErrorPage` renders full JS stack trace to end users in production (CWE-209).** The framework copy (`src/_components/ErrorPage.tsx:37`) was gated behind `import.meta.env.DEV` (FIXED), but the **scaffold template twin was never updated** — every auth-enabled scaffold ships the leak. | `template/src/_components/ErrorPage.tsx:34,67-76` | Gate `error.stack` behind `import.meta.env.DEV` in the template (match the framework copy). |
| SCAF-N3 | **FIXED** (reconciled 2026-06-16) | High | — | 1/3 | **Framework-docs copy substitutes the `{{…}}` tokens the docs EXPLAIN** — `copyTree(src,dst,vars)` runs `replacePlaceholders` over already-rendered `docs/AI_QUICK_INDEX.md` / `ARCHITECTURE_EXTENSION_POINTS.md`, rewriting their documentation tokens into concrete values in the consumer's `docs/luckystack/`. Corrupts the AI-authority docs. | `create-luckystack-app/src/index.ts:1543` | Copy framework docs WITHOUT substitution (pass `{}` / `substitute=false`); only the template tree needs substitution. |
| ROOTSRC-O2 | **FIXED** (reconciled 2026-06-16) | High (Med per wave-2) | 1/6 | 2/3 | **Session handlers hand-build Redis keys with `process.env.PROJECT_NAME`**, bypassing `formatKey`/`sessionKeyFor`/`activeUsersKeyFor` — silently divergent under `session.projectName` config or a `registerRedisKeyFormatter` (multi-tenant) → `listSessions` empty, `revokeSession` can't resolve, `deleteAccount` leaves a stale activeUsers set. It's the example AI clones. | `src/settings/_api/{listSessions,revokeSession,deleteAccount}_v1.ts` | Import the key helpers from `@luckystack/login` (the CLI asset already does); delete the local `PROJECT_NAME`. |
| ROOTSRC-O3 | **FIXED** (2026-06-16) | High | 1/6 | 2/3 | **Consumer `src/settings/_api/*` drifted behind the hardened CLI asset** — `deleteAccount` reaches past `getUserAdapter()` to `prisma.user.delete`, skips pre/postAccountDelete hooks + avatar unlink; `listSessions`/`revokeSession` use 64-char `id` while the asset uses 16-char `handle` (incompatible shapes). `src/` is the regressed branch; no parity gate covers it. | consumer `src/settings/_api/*` vs `packages/cli/assets/login/src/.../*` | Re-sync `src/` from the asset; add an `ai:check-template-drift` CI gate. |
| H17 | **FIXED** (reconciled 2026-06-16) | High | 6/6 | 3/3 (server O2) | **Unauth `/_health` publishes unsalted `sha256(secret)` fingerprints (+`bootUuid`+`envKey`) by default (`healthHash.mode:'plain'`)**; the `@bootUuid` salt mitigation is DEAD on this caller (route calls `computeSynchronizedEnvHashes()` with no bootUuid). `verifyBootstrap` only warns. | `server/healthRoutes.ts:80-89`; default `projectConfig.ts:703-706`; `core/synchronizedEnvHashes.ts` | Pass bootUuid into the hash; don't co-disclose raw bootUuid; default `'hmac'`/salted when synchronizedEnvKeys non-empty, or gate verbose `/_health` behind a probe-token / internal bind. |
| H22 | **FIXED** (reconciled 2026-06-16) | High (systemic) | 4/6 | (devkit/core) | **Codegen / type-injection silently degrades; "trust the generated types" has no enforcement** — Zod `z.any()` fallback + no `.strict()`; identifier validator was throw→warn (re-promoted to throw per devkit F4, residual fallback paths); generated `apiRequest`/`syncRequest` return type omits the `{status:'error'}` envelope forcing Rule-21 `as` widening at every call site; codegen ignores `registerRoutingRules` (override routes → empty prod maps → silent 404). | `zodEmitter.ts`; `emitterArtifacts.ts`; `apiRequest.ts` return type; `SessionProvider.tsx:88-94`; `generateServerRequests.ts` | Union the framework-error envelope into every generated return type; read `getRoutingRules()` in codegen; loud-failure markers in extractors; pack→install→tsc smoke-test in CI. |
| RS-F1 | **FIXED** (2026-06-16) | High (scope-capped) | 2/6 | 1/3 | **Repo `shared/tryCatch.ts` drags `node:async_hooks` (`AsyncLocalStorage`) into the Vite client bundle** — re-exports the capturing core variant → `errorTrackerRegistry` top-level `new AsyncLocalStorage()` reaches `LoginForm.tsx`; built `dist/assets/client-*.js` contains the externalization stub → repo login/register client throws at module-eval. Shipped template copy is clean (repo-internal drift). | `shared/tryCatch.ts:8`; `core/errorTrackerRegistry.ts:11,88`; `src/_components/LoginForm.tsx:7` | Mirror the client-safe template body, OR break the static `tryCatch→sentrySetup` edge (lazy `import()` in catch), OR add a `shared/tryCatch` client alias. |
| PRESENCE-1 | **FIXED** (2026-06-16) | High | 3/6 | 3/3 | **Room-name formatter one-sided across the whole framework** — presence broadcast+snapshot apply `formatRoomName`, but every JOIN/broadcast site (server `loadSocket`, sync) joins RAW codes. A non-identity `registerRoomNameFormatter` (the documented multi-tenant primitive) silently breaks ALL presence fan-out (0 peers) + `getRoomPresence` (empty), no error. In-package half now consistent (snapshot fixed F14). | presence `peerNotifier.ts:45`, `activitySampler.ts:78` vs `server/loadSocket.ts:226,414,418`, `sync/handleSyncRequest.ts:722`, `handleHttpSyncRequest.ts:619` | Route every room touch through `formatRoomName` at the join sites (identity default = byte-identical for non-tenant consumers), OR remove from presence + document raw prefixing. Cross-package (server+sync). |
| DEVKIT-O8 | **FIXED** (2026-06-16) | High | 6/6 | 3/3 | **Template-injector PAIRED-twin still pure regex source-mutation** — `/clientInput:\s*\{[^}]*\}/` stops at the first inner `}`, orphaning the outer brace of nested types → uncompilable TS written to the user's client file; function returns `true` and the pair upserts anyway. (DELETE twin was migrated to AST; this twin + the server-inject splice O9 were not.) | `devkit/.../templateInjector.ts:656-731` | Migrate to the TS AST; re-parse before writing; leave untouched + log on parse failure; check the rewrite return before upserting (DEVKIT-O10). |
| DOCSUI-2 | **FIXED** (reconciled 2026-06-16) | High | — | 3/3 | **Sync events never rendered** — `render()` reads only `data.apis`, never `data.syncs`; the emitter populates `syncs` and `renderDocsSyncEntry` exists but is unreachable. CLAUDE.md/README advertise "Sync events appear alongside APIs" (currently false). | `docs-ui/docsHtml.ts:382-395`; `emitterArtifacts.ts:63,435` | Render `data.syncs` via `renderDocsSyncEntry` in the same pass (folds into DOCSUI-1 rewire). |
| DOCSUI-3 | **FIXED** (2026-06-16) | High | — | 3/3 | **`renderCore.ts` is fully-tested shadow code never imported by prod**; its header falsely claims it is serialized via `.toString()`. A green test (`liveRenderCore.test.ts`) characterizes the BROKEN shipped renderer against a fictional nested fixture. | `docs-ui/renderCore.ts:1-219`; `liveRenderCore.test.ts:156-164` | Make renderCore the live source (serialize + delete the inline twin) and fix the fixture to the real array shape; add an `ai:lint` "exported-but-only-tests-import-it" rule. |

## MEDIUM

| ID | Status | Sev | W1 | W2 | Finding | Location | Recommendation |
|---|---|---|---|---|---|---|---|
| API-O4 | **FIXED** (2026-06-16) | Med | 1/6 | 3/3 | **HTTP-method gate enforced only on HTTP; socket ignores it** — a GET/read-only (or `get*`/`list*`-named) route is fully callable + mutating over the websocket. | `handleHttpApiRequest.ts:393-407` vs socket (no method check) | Enforce method on socket OR loudly document `httpMethod` as HTTP-only. |
| API-O5 | **FIXED** (2026-06-16) | Med | — | 1/3 | **HTTP `data` shape guard does not reject ARRAY payloads** (`typeof [] === 'object'`) — transport asymmetry; socket rejects, HTTP passes an array to a `{...}`-destructuring handler → 500. | `handleHttpApiRequest.ts:221` | Mirror socket: `|| Array.isArray(data)`; add a parity case. |
| API-O6 | **FIXED** (2026-06-16) | Med | 5/6 | 3/3 | **`system/logout` socket shortcut bypasses the respond-hook chain, isn't rate-limited, always reports `success:true`** (`performLogout` return discarded; signing/redaction/audit subscribers miss it; spammable). | `handleApiRequest.ts:452-459` | Route through `emitApiResult`, reflect real status, apply the global-IP bucket — or document + fix the CLAUDE.md claim. |
| API-O7 | **FIXED** (2026-06-16) | Med | 2/6 | 2/3 | **Failed-auth requests consume no rate-limit bucket on either transport** (auth precedes rate-limit, early-returns) — UNDOCUMENTED conscious no-enumeration tradeoff. | `handleApiRequest.ts:495-498`; `handleHttpApiRequest.ts:274-309` | Increment the global-IP bucket on the auth-fail path, OR document that lockout lives in `@luckystack/login`. |
| API-O8 | **FIXED** (2026-06-16) | Med | 6/6 | 3/3 | **Rate-limit + auth logic hand-duplicated across both transports; parity test deliberately excludes rate-limit/method/logout parity** (already diverged on loopback skip + IP resolution). | `handleApiRequest.ts:117-211` vs `handleHttpApiRequest.ts:311-391`; `transportParity.test.ts:22-24` | Extract `applyApiRateLimits({transport,...})` shared; resolve IP once on both; extend parity test. |
| API-O9 | **FIXED** (2026-06-16) | Med | 5/6 | 2/3 | **HTTP buckets fall back to literal `'unknown'`/`'anonymous'`, collapsing distinct callers into one shared bucket** when `requesterIp` is absent (per-IP limiter → global). Socket resolves via `resolveClientIp` and diverges. | `handleHttpApiRequest.ts:318,363` | Resolve IP via `resolveClientIp` on HTTP too; consider fail-closed deny on the UNKNOWN sentinel for state-changing routes. |
| API-O10 | **FIXED** (2026-06-16) | Med | — | 1/3 | **`api.requestTimeoutMs` config has no enforcement on either transport** — a hung handler never times out server-side (grep `requestTimeoutMs` in `api/src` = 0). | `handleApiRequest.ts:213-238`; `handleHttpApiRequest.ts:476-492` | Race `main()` against `setTimeout(api.requestTimeoutMs)` aborting the controller + resolving a localized envelope. |
| API-O11 | **FIXED** (2026-06-16) | Med | 6/6 | 3/3 | **`runHandleHttpApiRequestInner` is a ~345-line god-function** (HTTP side never decomposed; socket side was) — the structural source of the API twin-drift. | `handleHttpApiRequest.ts:168-512` | Extract `applyHttpApiRateLimits`/`runHttpApiAuth`/`runHttpApiExecution`; reuse shared identity/rate-limit helpers. |
| SYNC-O2 | **FIXED** (2026-06-16) | Med | 1/6 | 3/3 | **Abort registry keyed on client-controlled `cb` (`socketId:cb`)** — two overlapping same-`cb` requests cross-delete the controller → `syncCancel` aborts the wrong request (LLM "stop generating" breaks); default `cb` is the deterministic `${name}/${version}`. | `core/cancelRegistry.ts:29-30`; client `syncRequest.ts:532` | Key on a per-request unique id (include `responseIndex`) or store a `Set<AbortController>` per `cb`. |
| SYNC-O4 | **FIXED** (2026-06-16) | Med | 1/6 | 3/3 | **Consumer `preSyncAuthorize`/`postSyncAuthorize` hooks (often DB/tenant lookups) run BEFORE `applySyncRateLimits`** — on a `login:false` route an unauthenticated caller triggers the hook per message with no throttle. | `handleSyncRequest.ts:494,521,530` | Move `applySyncRateLimits` ahead of `preSyncAuthorize` (keep the cheap built-in receiver check first), OR document. |
| SYNC-O5 | **FIXED** (2026-06-16) | Med | 6/6 | 3/3 | **`flushPressure` is a packet-COUNT approximation under a byte-named API; samples only the local room (first 32); for `streamTo` samples the wrong room** (original `receiver`, not the token-named rooms emitted to) — real backpressure hole on the LLM-streaming use case. | `sync/_shared/streamEmitters.ts:38-130,227-261` | Rename `thresholdPackets`/measure real bytes; `flushPressureTo(tokens|rooms)`; rotating-subset sampling; document cross-instance limit. |
| SYNC-N1 | **FIXED** (2026-06-16) | Med | — | 1/3 | **Shadow-config: `sync.flushPressure.maxBufferedBytes` (documented default 5 MiB) is NEVER read by the emitter** — `flushPressure` resolves a hard-coded `DEFAULT_THRESHOLD_BYTES = 1 MiB`; `getProjectConfig().sync.flushPressure` not imported. Consumer tuning is silently ignored. | `streamEmitters.ts:59,245-248` | Read the configured `maxBufferedBytes` (clamp by `highWaterMarkChunks`) as the fallback, OR remove the config keys + docs. |
| SYNC-N3 | **FIXED** (2026-06-16) | Med | — | 1/3 | **Twin drift: HTTP sync handler omits `postSyncAuthorize` AND `preSocketMessage` hooks the socket handler fires** — audit/metrics + per-message gates miss every HTTP-fallback sync (a throttle can be bypassed by forcing HTTP). | socket `handleSyncRequest.ts:220,521`; HTTP dispatches neither | Dispatch `postSyncAuthorize` on the HTTP path (parity); decide + document `preSocketMessage`; add a hook-dispatch parity test. |
| SYNC-N4 | **FIXED** (2026-06-16) | Med | — | 2/3 | **`chunkCounters` module-level Map grows unbounded** — keyed `${routeName}|${recipient}`; `bumpChunkIndex` only `set`s, no delete/clear; header comment "cleared on teardown" is false → process-lifetime leak on a long-lived streaming server. | `streamEmitters.ts:12-19` | Move the counter into the per-request closure (rebuilt per request anyway) or drop it; make the comment true. |
| SYNC-O6 | **FIXED** (2026-06-16) | Med | 6/6 | 3/3 | **God-function socket `handleSyncRequest` (~700 lines)** — validate/locale/hooks/auth/receiver-auth/rate-limit/execute/fanout with ~12 manual `cleanupRequest()` return paths (primary leak surface). | `handleSyncRequest.ts:180-876` | Extract staged steps + a `rejectSync({responseIndex,errorCode})`; single `try/finally`. |
| SYNC-O7 | **FIXED** (2026-06-16) | Med | 6/6 | 3/3 | **God-function HTTP `handleHttpSyncRequest` (~540 lines), near-line-for-line twin of SYNC-O6** — root cause of every sync transport-parity drift; two near-identical rate-limit helpers. | `handleHttpSyncRequest.ts:179-721` | Hoist a transport-agnostic `runSyncPipeline` into `_shared/` + adapters; unify rate-limit helpers; parity conformance test. |
| SYNC-O8 | **FIXED** (2026-06-16) | Med | 1/6+4/6 | 3/3 | **Full `serverOutput` broadcast verbatim when no `_client` exists (PII leak); `preSyncRecipient` can SKIP but not REDACT and passes `recipientUserId:null`.** | `handleSyncRequest.ts` fanout `~:833`, `preSyncRecipient ~:633` | Document; give `preSyncRecipient` an optional redaction/override + opt-in `resolveRecipientUser`. |
| SYNC-O9 | **FIXED** (2026-06-16) | Med | 1/6 | 3/3 | **Per-recipient `_client` handler runs SERIALLY** — originator's ack waits for the entire fanout → large `receiver:'all'` fanouts can trip the client `requestTimeoutMs` (false 504). | `handleSyncRequest.ts:808/:852` | Ack as soon as `_server` completes + fanout is dispatched, OR bounded-concurrency fanout; at minimum document. |
| SYNC-N9 | **FIXED** (2026-06-16, via TR-7) | Med (cross-area) | — | 1/3 | **Auto-sweep walks only `apiMethodMap`, never a `syncMethodMap`** — the most authz-sensitive surface (HTTP-sync room-membership) is the one the auto-sweep can't reach; weakens confidence in SYNC F1 + the O1 posture. | test-runner `walkEndpoints` | Add a `syncMethodMap` walk running auth-enforcement + contract over HTTP-sync (documented roadmap gap). |
| CORE-O2 | **FIXED** (2026-06-16) | Med | 3/6 | 2/3 | **`getParams` body parser has no already-responded latch** → a second buffered `'data'` chunk re-trips 413 → `ERR_HTTP_HEADERS_SENT` (uncaught in the event handler; another hot-path uncaught-throw vector, pairs with CORE-O1). | `core/getParams.ts:57-66` | `let responded=false` / `if (res.headersSent) return` guard. |
| CORE-O3 | **FIXED** (2026-06-16) | High (disputed Med) | 4/6 | 3/3 | **`trustProxy` XFF resolution trusts the LEFTMOST (client-controlled) hop** → per-IP rate-limit evasion + audit-IP spoofing. Default `trustProxy:false` is the safe gate (default deploys inert); exploitable only on the opt-in posture → realistic severity Medium. | `core/resolveClientIp.ts:85-91` | Skip a `trustedProxyHopCount` from the RIGHT / prefer `X-Real-IP`; document overwrite-not-append. |
| CORE-O4 | **FIXED** (2026-06-16) | Med | 1/6 | 2/3 | **Legacy single-instance Sentry slot bypasses `sanitizeForLog`** (raw context → Sentry `extra`) — fires only when a consumer mixes the legacy `initSharedSentry` path with framework capture. | `core/sentrySetup.ts:42-48` | `sanitizeForLog(context)` once at top of both legacy capture fns. |
| CORE-O5 | **FIXED** (2026-06-16) | Med | 4/6 | 3/3 | **`redactedLogKeys` default set omits `csrftoken`/`apikey`/`secret`/`accesstoken`/`refreshtoken`; exact-match only** — session carries a live `csrfToken`. | `core/redactedLogKeys.ts:10-18,30-32` | Add the missing keys + suffix matching. (Policy: deferred in wave-1 LOW.) |
| CORE-O6 | **FIXED** (2026-06-16) | Med | 5/6 | 2/3 | **Rate-limiter fails OPEN: `onStoreError:'memory'` default; `'deny'` honored ONLY inside `isRedisMode()`** → memory mode ignores `'deny'`; effective limit = `limit×N` under Redis outage in multi-instance. | `core/rateLimiter.ts:228-235`; `projectConfig.ts:665,671` | Honor `'deny'` regardless of store, or boot-warn that `onStoreError` is Redis-mode-only. (Confirmed deliberate fail-open — review, not assert-bug.) |
| CORE-O7 | **FIXED** (2026-06-16) | Med | 1/6 | 2/3 | **In-memory rate-limit store unbounded** — no LRU cap; key-varying flood grows the Map between cleanup ticks → GC pressure / OOM. | `core/rateLimiter.ts:60,108-111` | Cap (LRU) / sweep at a threshold; document memory mode single-instance/dev. |
| CORE-O8 | **FIXED** (2026-06-16) | Med | 1/6 | 2/3 | **`clearAll`/`clearAllRateLimits` SCAN+DEL is project-rooted not tenant-rooted, publicly exported, no test/dev guard** → one call wipes every tenant's counters. | `core/rateLimiter.ts:258-278` | Gate behind `NODE_ENV!=='production'` or require a namespace arg; add a tenant-scoped variant. |
| CORE-O9 | **FIXED** (2026-06-16) | Med | 1/6 | 2/3 | **`/_health` synchronized-secret hash ships `'plain'` on an unauth endpoint** (= H17 from the core-config vantage; a boot-warn was added, default exposure unchanged). | `core/synchronizedEnvHashes.ts`; `projectConfig.ts:703-706` | Default `'hmac'`/salted; gate `'plain'` behind explicit opt-in. |
| CORE-O10 | **FIXED** (2026-06-16) | High (disputed Med) | 4/6 | 1/3 | **Cookie-mode sessions silently accept `Authorization: Bearer` / `handshake.auth.token`** as a fallback → CSRF model bypass for a stolen token. | `core/extractTokenFromRequest.ts:30-34`; `extractToken.ts` | Make the fallback opt-in (`http.acceptBearerInCookieMode`, default false). |
| CORE-O12 | **FIXED** (2026-06-16) | Med | 1/6 | 2/3 | **`apiRequest` interceptor window is un-timed + abort-unwired** — timeout + external-abort armed only AFTER `await dispatchApiRequestInterceptors`; slow interceptor adds un-counted latency + an external abort is ignored. | `core/apiRequest.ts:613-619` vs `:474-521` | Wire external-abort + a guard timer before awaiting interceptors, or document. |
| CORE-O13 | **FIXED** (2026-06-16) | Med | 2/6 | 1/3 | **`apiRequest` lost-response listener + promise leak when timeout disabled** (`timeoutMs:false` + non-abortable; no socket `disconnect` reaper) → server restart between emit/reply = forever-pending promise + leaked listener. | `core/apiRequest.ts:500-521,602-605` | Attach a `socket.on('disconnect')` that settles in-flight non-abortable requests. |
| CORE-O14 | **FIXED** (2026-06-16) | Med | 1/6 | 1/3 | **External `AbortSignal` fired while a request waits in the offline queue is a no-op** (for `abortable:false` the item isn't removed; not a permanent hang). | `core/apiRequest.ts:409-459` | Register the external-signal handler + `removeApiQueueItem` at/before the enqueue branch. |
| CORE-O15 | **FIXED** (2026-06-16) | Med | 1/6 | 1/3 | **Name-prefix GET heuristic auto-attaches an abort controller to a mis-prefixed POST when `apiMethodMap` is unregistered** → identical-payload duplicate silently aborts the first (now scoped by a payload hash). | `core/apiRequest.ts:76-88,340-344` | When `!isApiMethodMapRegistered()`, default `abortable` to non-abortable, or dev-warn. |
| CORE-O16 | **FIXED** (2026-06-16) | Med | 1/6 | 2/3 | **Origin resolver-function mode receives the RAW `Origin`/`Referer`** while static-array mode normalizes both sides → a natural `origin.startsWith(...)` resolver is bypassable. | `core/checkOrigin.ts:68-71` | Pass the resolver the `normalizeOrigin`'d value; export the normalizer. |
| CORE-O22 | **FIXED** (2026-06-16) | Med | 3/6 | (carried) | **`translate()` builds a fresh `RegExp` per param from the UNescaped `param.key`** → mis-substitution / ReDoS on the translation/toast hot path (server `errorParams` flow through). Server twin = RS-6. | `core/react/TranslationProvider.tsx:92-97` | `escapeRegExp(param.key)` + function-form `replace`; shared `escapeRegExp`. |
| CORE-O23 | **FIXED** (2026-06-16) | Med | 2/6 | (carried) | **`Middleware` denial with no `redirect` calls `navigate(-1)`** → can chain backwards out of the app / to a blank page. | `core/react/Middleware.tsx:99-103` | Navigate to a deterministic safe route (`loginRedirectUrl`). |
| CORE-N1 | **FIXED** (2026-06-16) | Med | — | 1/3 | **`splitTopLevel` is quote-BLIND** — splits inside a string-literal union (`"a|b" | "c"`) → valid traffic REJECTED (route-level DoS); also `matchesIndexKeyType` key-unions. Not covered by tests. | `core/runtimeTypeValidation.ts:19-45` | Make `splitTopLevel` quote-aware; add tests. |
| CORE-N2 | **FIXED** (2026-06-16) | Med | — | 2/3 | **`validateType` accepts ANY string for a `Date`-typed field** (no parseable-date check) → handlers doing `new Date(x).toISOString()` get `Invalid Date`. Silent fail-open inside the otherwise-fail-closed validator. | `core/runtimeTypeValidation.ts:144` | `typeof value==='string' && !Number.isNaN(Date.parse(value))`. |
| CORE-N3 | **FIXED** (2026-06-16) | Med | — | 1/3 | **Object-field parser silently DROPS fields with non-identifier quoted keys** (`'foo-bar': string`) → false-reject of valid input AND under-enforcement of required hyphenated keys (then treated as UNKNOWN → whole object rejected). | `core/runtimeTypeValidation.ts:69,98` | Broaden the quoted-key capture; unescape; surface `unvalidatable field`. |
| SECRET-O1 | **FIXED** (2026-06-16) | Med | — | 2/3 | **`pointerMap` is merged BEFORE the resolve it depends on** → a failed remote file-reload permanently pollutes the in-memory pointer map (no rollback); a bad dev `.env.local` edit poisons every later `refreshSecretManager()`/poll for the process life. Dev + remote-mode only. | `secret-manager/index.ts:746` then `:751` | Build the merged map locally; commit only after `doResolve` succeeds. |
| SECRET-N1 | **FIXED** (2026-06-16) | Med | — | 1/3 | **Stale package `CLAUDE.md` Function Index omits `stopSecretManager` + `getCachedResolutionMeta`; the `SecretManagerConfig` one-liner is missing ~9 keys** (`envNames`, `pollIntervalMs`, `onApplied`, …) → an AI reading the post-install INDEX won't learn the rotation poll / teardown / scoping exist. | `secret-manager/CLAUDE.md` | Regenerate the Function Index + expand the type one-liner; run `ai:capabilities`. |
| ET-O2 | **FIXED** (2026-06-16) | Med | 1/6 | 3/3 | **`error.message`/`error.stack` forwarded to PostHog & Datadog bypass redaction** (key-based scrub only) — an interpolated secret reaches the adapters. Sentry protected SDK-side. | error-tracking `posthog.ts:81,84`; `datadog.ts:128,135` | Central value-level scrub in `captureExceptionAcrossTrackers`. |
| ET-O3 | **FIXED** (2026-06-16) | Med (was High) | — | 1/3 | **Legacy Sentry identity is a per-request process-global scope** (`setSentryUser`) → cross-request user bleed on the `initializeSentry()`-only path (the ALS fix covers the ADAPTER path only; no `withIsolationScope`). | error-tracking `autoInstrumentation.ts:76-80`; `sentrySetup.ts:42-48` | Route `initializeSentry()` through `createSentryAdapter`, OR attach `getCurrentErrorTrackerIdentity()` per-event. |
| ET-O5 | **FIXED** (2026-06-16) | Med | 5/6 | 3/3 | **`builtinBeforeSend` scrubs ONLY `request.cookies`** — not `cookie`/`authorization` headers, `request.data`, `query_string`, `extra`, breadcrumbs (the fields most likely to carry a token under `sendDefaultPii:true`); non-Sentry adapters get no header scrub. | error-tracking `sentry.ts` `builtinBeforeSend` | Derive the denylist from `getRedactedLogKeys()`. |
| ET-O6 | **FIXED** (2026-06-16) | Med | 3/6 | 3/3 | **`registerPreCaptureFilter` payload is context-only** — the documented "drop noisy errors by type" use case is impossible (never receives `{ error }`; diverges from adapter `beforeSend`). | error-tracking `errorTrackerRegistry.ts:133-141` | Pass `{ error, context }` / `{ message, level, context }`. |
| ET-N1 | **FIXED** (2026-06-16) | Med | — | 2/3 | **`posthogConfig.ts` is ENTIRELY DEAD** — not barrel-exported, never imported, `register.ts` ignores it. An AI told "set a PostHog beforeSend redaction" reaches for `registerPostHogConfig` → type-checks, ships a no-op (events reach PostHog unredacted). | error-tracking `posthogConfig.ts`; `register.ts:50-57` | Wire `register.ts` to read `getPostHogConfig()` + export, OR delete the module + doc claim. |
| ET-N2 | **FIXED** (2026-06-16) | Med | — | 2/3 | **`@luckystack/error-tracking` barrel omits 5 exports its own CLAUDE.md documents** (`flushErrorTrackers`, `appendErrorTracker`, `runWithErrorTrackerIdentity*`, `registerPreCaptureFilter`, `startSpanHandle`) — an AI following the shutdown docs gets TS2305. Amplifies ET-O1. | error-tracking `index.ts:1-57` | Re-export from core via `adapter.ts` + `export * from './adapter'`, or correct CLAUDE.md. |
| ET-O10 | **FIXED** (2026-06-16) | Med | 1/6 | 2/3 | **`index.ts` ↔ `adapter.ts` hand-synced export lists drift** (no `export * from './adapter'`). Tied to ET-N2. | error-tracking `index.ts:20-43`; `adapter.ts:8-25` | `export * from './adapter'`. |
| EMAIL-O2 | **FIXED** (2026-06-16) | Med | — | 3/3 | **`EmailMessage.attachments` + `headers` are typed + JSDoc-promised as adapter-forwarded, but `toProviderPayload` silently drops them (a test pins the drop)** — type accepts it, send returns `{ok:true}`, data never reaches Resend/nodemailer. | email `providerPayload.ts:17-29`; contract `core/emailRegistry.ts:48-61` | Forward them + update the test, OR remove/`@deprecated` the fields. |
| EMAIL-O3 | **FIXED** (2026-06-16) | Med | 2/6 | 2/3 | **`renderEmailLayout` interpolates `ctaUrl` raw into `href="..."` and `accent` raw into `background:...`** (text fields ARE escaped) — user-derived values give attribute-breakout / `javascript:` link. A framework "safe layout" should be safe-by-default. | email `renderEmailLayout.ts:52-53` | `new URL()` scheme-allowlist + `escapeHtml(parsed.href)`; validate `accent`. |
| EMAIL-O4 | **FIXED** (2026-06-16) | Med | 5/6 | 2/3 | **`resolveSender` silently falls through when an EXPLICITLY-requested `adapter` slot is unregistered** → routes through default (or `ConsoleSender`) while caller sees `{ok:true}` (confused deputy for security mail). | email `sendEmail.ts:64-76` | Distinguish explicit `adapter` (fail/warn) from best-effort `adapterHint`. (DD-EMAIL.) |
| SERVER-O3 | **FIXED** (2026-06-16) | Med | — | 1/3 | **`timingSafeStringEqual` is dead-but-wired** — a constant-time helper + full test added but imported NOWHERE in prod; CSRF compares still use `===`, testReset `!==`. Misleading "constant-time is wired" surface. | server `timingSafeEqual.ts`; `csrfMiddleware.ts:57,84` | Route the three compares through it, OR delete helper+test. |
| SERVER-O4 | **FIXED** (2026-06-16) | Med | 5/6 | 2/3 | **State-changing custom routes (`registerCustomRoute` POST) + `/auth/callback` fall outside the CSRF gate** (prefix check misses them); relies solely on the origin gate, nothing warns at registration. | server `csrfMiddleware.ts:34-37` | Extend CSRF to non-exempt custom POST with per-route opt-out, OR expose `ctx.enforceCsrf` + loud doc. |
| SERVER-O5 | **FIXED** (2026-06-16) | Med | 1/6 | 3/3 | **`updateLocation` persists session WITHOUT `sanitizeSessionRoomKeys`** (join/leave both apply it) and mutates the cached session in place — keeps legacy `code`/`codes` keys forever; aliasing hazard. | server `loadSocket.ts:366-368` | One `persistSessionMutation` sanitizing + fresh-spread + writing; route all three through it. |
| SERVER-O6 | **FIXED** (2026-06-16) | Med | 1/6 | 3/3 | **Optional-package capability detection silently degrades on Node < 20.6** — `localRequire.resolve` throws `ERR_PACKAGE_PATH_NOT_EXPORTED` on every import-only exports map → all reported ABSENT, swallowed, no diagnostic. | server `capabilities.ts:27-42` | One-time loud warn when `import.meta.resolve` unavailable; distinguish not-found from other resolver errors. |
| SERVER-O7 | **FIXED** (2026-06-16) | Med | — | 1/3 | **HTTP credentials-login IP rate-limit skipped entirely when `rateLimiting.defaultApiLimit === false`** — spraying one attempt across many accounts from one IP faces no IP cap. | server `authApiRoute.ts:88` | Fall back to `rateLimiting.auth.{maxAttempts,windowMs}` for the IP key. |
| SERVER-O8 | **FIXED** (2026-06-16) | Med | 1/6 | 1/3 | **`loadGeneratedMaps` callback typed `Promise<unknown>`; coerces a mis-shaped module to `{}`** → every route silently `notFound`, no compile error. | server `runtimeMapsLoader.ts:49,80` | Export + type a `GeneratedRuntimeMapsModule`. |
| ROUTER-O2 | **FIXED** (2026-06-16) | Med | 3/6 | 3/3 | **`redisHealthStore.set` non-atomic** (`cache.set` → `client.set` → `client.publish` as 3 steps) → split-brain health on partial failure. | router `redisHealthStore.ts:96-103` | Single `client.multi().set().publish().exec()`; update cache after the durable write. |
| ROUTER-O3 | **FIXED** (2026-06-16) | Med | 1/6 | 2/3 | **Health keys written without TTL** → stale `unhealthy` survives a router crash (pollers run only in dev fallback). | router `redisHealthStore.ts:98` | Add a modest `'EX'`, or treat keys older than N as advisory. |
| ROUTER-O8 | **FIXED** (2026-06-16) | Med | confirms #33 | 1/3 | **Doc drift: `http-proxy.md` documents the EXACT insecure `x-forwarded-proto` behavior deliberately removed** (code now `normalizeForwardedProto`, inbound stripped) — an AI extending from the doc reintroduces scheme-spoofing. | router `docs/http-proxy.md:48-49` | Update the proto + host rows; document `isOriginFormTarget`/`isHostPinned`. |
| ROUTER-O4 | **FIXED** (2026-06-16) | Med | 1/6 | 2/3 | **`server.requestTimeout` not set** → full-request slow-loris bounded only by Node default. | router `startRouter.ts:152-153` | Set `server.requestTimeout` from the same constant block. |
| ROUTER-O13 | **FIXED** (2026-06-16) | Med | — | 1/3 | **Mistyped/unknown `--preset` silently accepted** → locally-owned set EMPTY, no error; all traffic resolves via fallback or 502. Contradicts the fail-fast philosophy. | router `resolveTarget.ts:210-212` | Assert + throw with the known-presets list. |
| ROUTER-O15 | **FIXED** (2026-06-16) | Med | — | 1/3 | **`x-forwarded-host` reflected verbatim** → host-header-poisoning seam; a backend building absolute URLs from it can be poisoned. Conditional on backend trust. | router `httpProxy.ts:115`; `wsProxy.ts:114` | Validate `Host` against `routing.allowedHosts`, or set a canonical host; at minimum document. |
| RS-02 | **FIXED** (2026-06-16) | Med | — | 1/3 | **deploy.config invariants validated ONLY at build-time codegen, never at server boot** — despite a comment claiming "enforced by presetLoader". Editing `deploy.config.ts` + `npm run prod` without `generateArtifacts` boots an unvalidated topology. | root-server `presetLoader.ts:140-152`; callers only in `generateServerRequests.ts:71` | Call `validateResolvedConfig` in the boot path; at minimum fix the comment. |
| RS-4 | **FIXED** (2026-06-16) | Med | 6/6 | 3/3 | **`notifications.ts` reads the User row via stacked inline Prisma `as`/`as unknown` casts (clearest Rule 21 violation), duplicated in two functions; `tryCatch` swallows a schema-rename failure silently.** | root-server `server/hooks/notifications.ts:40-42,80-82` | Read through the typed client; extract one `loadNotifiableUser`. |
| RS-6 | **FIXED** (2026-06-16) | Med | 3/6 | 3/3 | **Unescaped `new RegExp(\`{{${param.key}}}\`)` in the localized error normalizer + `$`-sequences in the replacement** → regex-injection/ReDoS (latent) + active `$`-corruption. Server twin of CORE-O22. | root-server `responseNormalizer.ts:87-88` | `replaceAll(literalKey, () => String(value))`; shared `escapeRegExp`; fix the TranslationProvider twin. |
| RS-7 | **FIXED** (2026-06-16) | Med | 1/6 | 3/3 | **Consumer shims import framework by `../../packages/*/src` source paths instead of published `@luckystack/*` names** (two conventions; breaks the post-install model). | root-server `server/auth/login.ts:1`, `server/utils/extractToken*.ts`, `server/hooks/*` | Standardize on published names; add `// re-export only` breadcrumbs. |
| LOGIN-01 | **FIXED** (2026-06-16) | Med (regression) | — | 1/3 | **Regression: `register.ts` captures `getProjectConfig()` + `env()` at MODULE LOAD then `registerOAuthProviders()` at import** — re-introduces the stale-config footgun; providers bake a stale/empty `callbackBase` or omit credentials if config is overridden after import. | login `register.ts:54-58,65,110` | Defer all config/env reads into provider factories / a first-request assembler. |
| LOGIN-02 | **FIXED** (2026-06-16) | Med (regression) | — | 1/3 | **Regression: `logout` bypasses `SessionAdapter` with raw `redisClient.srem(...)`** — on a custom adapter this hits a Redis it doesn't use; on the `skipSessionDelete` branch the active-set is never cleaned (leaks dead tokens). | login `logout.ts:34` | Drop the raw `srem`; call `getSessionAdapter().untrackActive(userId, token)`. |
| LOGIN-EMAILCHG | **FIXED** (2026-06-16) | Med | — | 1/3 | **Email-change initiated with NO current-password re-auth** (session-hijack → takeover; confirm link goes to the NEW address; on confirm all sessions revoked → real owner locked out). | login `requestEmailChange_v1.ts:26-65` | Require+verify current password for credentials accounts; add the UI field; notify the OLD address. |
| LOGIN-F4 | **FIXED** (2026-06-16) | Med | 4/6 | 3/3 | **TOCTOU on `rejectNew` concurrent-session cap** (count then non-atomic persist) — two concurrent logins both pass; `revokeOld` shares the race. | login `session.ts:92-94`→`:141/:152` | Atomic reserve (Lua/SETNX) or document soft-cap. |
| LOGIN-F9 | **FIXED** (2026-06-16) | Med | 1/6 | 2/3 | **`getSession` extends TTL on EVERY read** (sliding-expiration amplification; side-effects in a getter) — a stolen token survives as long as anything touches it. | login `session.ts:264-317` | `getSession(token,{refresh=true})` or a dedicated `touchSession`. |
| LOGIN-M7 | **FIXED** (2026-06-16) | Med | — | 1/3 | **Redis session write is non-atomic SET-then-EXPIRE** → a crash/EXPIRE-fail leaves an immortal (TTL-less) session; a stolen token survives policy expiry. | login `sessionAdapter.ts:90-94` | Single atomic `redis.set(key,value,'EX',ttl)`. |
| LOGIN-M9 | **FIXED** (2026-06-16) | Med | — | 1/3 | **Session token stored in PLAINTEXT inside the Redis session VALUE** (redundant with the key) and surfaced by `getAllSessions()` — UI asset is safe, but the primitive + any admin/log carries a live credential. | login `login.ts:396,490,…`; `session.ts:141,414-436` | Strip `token` before `setRaw` or at least in `getAllSessions`. |
| LOGIN-F22 | **FIXED** (2026-06-16) | Med | 3/6 | 2/3 | **Dummy bcrypt hash hard-pinned to cost 10** → reopens enumeration timing channel at `bcryptRounds>10` (inline comment argues early-return dominates — borderline DEFERRED, delta real at 12+). | login `login.ts:61` | Derive the dummy hash lazily at configured rounds (cache per-rounds). |
| LOGIN-F15 | **FIXED** (2026-06-16) | Med | 1/6 | 1/3 | **Register path skips the per-account brute-force lockout** → unbounded per-account `resolveUserByEmail` via register. | login `login.ts:321` | Apply the lockout check on the register email-exists lookup. |
| LOGIN-F14 | **FIXED** (2026-06-16) | Med | 2/6 | 1/3 | **Unbounded `listAll` SCAN fan-out** (O(all-sessions) SCAN + N×GET). | login `sessionAdapter.ts:134-163` | Paginated cursor; document dev/admin-only. |
| LOGIN-F16 | **FIXED** (2026-06-16) | Med | 1/6 | 0/3 | **No invalidation of prior reset/email-change tokens on issue** (multiple live redeemable links until TTL). | login `passwordReset.ts:31-36`; `emailChange.ts:28-34` | Track active token per user, delete prior on new issue. |
| LOGIN-F18 | **FIXED** (2026-06-16) | Med | 1/6 | 0/3 | **Avatar resolution `existsSync` sync FS on the login hot path** (blocking; couples session to local disk). | login `login.ts:496,855` | `fs.promises.stat` or an avatar adapter. |
| LOGIN-F2 | **FIXED** (2026-06-16) | Med | 1/6 | 2/3 | **Raw session token logged via the non-redacting default logger** on error paths (`{token}` full). Cross-area: depends on the core logger running `sanitizeForLog`. | login `session.ts:56,133,313` | Route `getLogger()` context through `sanitizeForLog`, or `slice(0,8)`. |
| LOGIN-04 | **FIXED** (2026-06-16) | Med | — | 1/3 | **Unified-account resolution non-deterministic on duplicate-email rows** (`findFirst` no `orderBy`) → the same login resolves to different User rows AND the cross-provider link SECURITY guard flips non-deterministically. | login `userAdapter.ts:98-99` | Deterministic tiebreak + warn on multiple matches; document `unified` needs unique email. |
| LOGIN-F7 | **FIXED** (2026-06-16) | Med | 1/6 | 1/3 | **Register-time TOCTOU duplicate accounts** (`resolveUserByEmail`→`create` not atomic; no `email @unique`). | login `login.ts:347,364` | Require `email @unique` for `unified`; treat unique-violation as re-resolve-and-link. |
| DEVKIT-O1 | **FIXED** (2026-06-16) | Med | — | 1/3 | **Codegen hardcodes literal `src/`, silently dropping EVERY route on a non-`src` `srcDir` layout** — files ARE found (discovery is srcDir-agnostic) but `pagePath` resolves `''` and the generator drops the entry. Defeats the advertised `projectConfig.paths.srcDir`. | devkit `routeMeta.ts:20,52` | Anchor on `path.relative(getSrcDir(), file)`, or throw when discovery found a file but `extractPagePath` returns `''`. |
| DEVKIT-O2 | **FIXED** (2026-06-16) | Med | — | 1/3 | **`normalizeImportPath` hardcodes `ROOT_DIR/src/_sockets` (wrong on non-`src`) AND is byte-duplicated** → trips the now-throwing identifier validator → whole generation fails. | devkit `extractors.ts:18-19`; `tsProgram.ts:145-146` | Resolve `fromDir` from `path.dirname(getGeneratedSocketTypesPath())`; consolidate. |
| DEVKIT-O10 | **FIXED** (2026-06-16) | Med | 1/6 | 1/3 | **Failed paired-client rewrite is swallowed; the pair upserts anyway** — server registered with types the client never migrated to. | devkit `templateInjector.ts:692-694`; `hotReload.ts:249-251` | Check the return; log + skip/roll-back on `false`. |
| DEVKIT-O11 | **FIXED** (2026-06-16) | Med/Low | 2/6 | 3/3 | **Async chokidar listeners + `void task()` have no `.catch`** → an unhandledRejection can crash the dev server, defeating supervisor backoff. | devkit `hotReload.ts:211,285,503-505` | Wrap each listener + task in `.catch`/`tryCatch`. |
| MCP-1 | **FIXED** (2026-06-16) | Med | — | 3/3 | **`loadGraph` calls `JSON.parse` outside any try/catch** — a torn/merge-conflicted `docs/ai-graph.json` throws straight through the catch-less graph handlers, bypassing the "corrupt → friendly message" contract for the most likely corruption mode. | mcp `artifacts.ts:70` | Wrap `JSON.parse` → return `null` (the missing-path then renders the right message). |
| MCP-2 | **FIXED** (2026-06-16) | Med | — | 2/3 | **`grepLines`-backed tools silently truncate at 60 with no signal** — `find_route`/`get_capability`/`list_decisions(tag)` print 60 rows as if complete → an agent wrongly concludes a helper doesn't exist (defeats Rule-12 check-before-authoring). | mcp `artifacts.ts:105-108` | Return `{lines,total}`; append "(showing first N of total)" when truncated. |
| DOCSUI-7 | **FIXED** (2026-06-16) | Med | 3/6 | 3/3 | **docs-ui auto-mount prod gate is env-only and fails OPEN on staging/preview/unset hosts**; auto-mounts unconditionally, no `authorize` hook, and the documented opt-out env NO LONGER EXISTS (slightly worse). | docs-ui `index.ts:107`; `register.ts:19` | Fail-closed for non-loopback binds unless explicitly enabled; add a required `authorize?` hook. |
| DOCSUI-8 | **FIXED** (2026-06-16) | Med | 2/6 | 2/3 | **JSON 404 discloses the absolute filesystem path** via `{ expectedAt: docsPath }`. | docs-ui `index.ts:127-131` | Gate `expectedAt` behind `NODE_ENV !== 'production'` or strip to relative. |
| TR-3 | **FIXED** (2026-06-16) | Med | partial | 3/3 | **Entire `runRegisteredLayers` extension runtime is unwired; its docstring contradicts the docs** — `registerTestLayer/Fixture/Reporter` register into a registry nothing iterates; `getTestFixture` never consulted by fuzz. | test-runner `runRegisteredLayers.ts:8-10`; `runAllTests.ts:175-191` | Correct the docstring to match `extension-hooks.md`, OR wire it. |
| TR-4 | **FIXED** (2026-06-16) | Med | 3/6 | 3/3 | **Rate-limit drain cancels each body without inspecting it** → if drains are rejected pre-limiter (CSRF/auth/validation) the bucket is never incremented; the final probe blames the limiter for a CSRF/auth problem. | test-runner `rateLimitCheck.ts:55-58,76-95` | Inspect a sample of drain envelopes; classify `skipped` when rejected pre-limiter. |
| TR-5 | **FIXED** (2026-06-16) | Med | 3/6 | 2/3 | **Rate-limit layer leaks the shared per-IP bucket between endpoints by default** (`resetBetweenEndpoints:false`) → false pass/fail from a neighbour saturating the IP window. | test-runner `runAllTests.ts:144-148` | Drain/reset the global IP bucket between layers, or require `skipLoopbackInDev`/`defaultIpLimit:false`. |
| TR-6 | **FIXED** (2026-06-16) | Med | — | 1/3 | **Contract layer treats any well-formed error envelope as PASS** — a route that rejects ALL input passes; combined with the schema-sample fail-open the happy-path probe sends a body the route's Zod rejects yet counts green. | test-runner `contractCheck.ts:85-114` | When `inputFor` produced a non-empty sample, assert `status:'success'`, OR a distinct category. |
| TR-7 | **FIXED** (2026-06-16) | Med | — | 1/3 | **Auto-sweep covers only API endpoints, never sync routes** (= SYNC-N9) — sync routes get NO auto auth/contract coverage; documented roadmap gap. | test-runner `walkEndpoints.ts:5-24` | Add a sync walk + auth-enforcement + contract over `sync/<page>/<name>/<version>`. |
| TR-8 | **FIXED** (2026-06-16) | Med | refutes #27 | 1/3 | **Layer-5 minted sessions have NO automatic cleanup** — `ctx.session.login` mints a real Redis session; the per-case loop closes watchers but never deletes `state.token` → sessions accumulate until TTL. | test-runner `customTests.ts:248-282,460-493` | Best-effort `deleteSession(state.token)` in the guaranteed cleanup path. |
| SCAF-N4 | **FIXED** (2026-06-16) | Med | refutes prose | 2/3 | **No partial-scaffold cleanup; a prune/copy throw leaves a half-written dir the next run refuses** → manual `rm` required. Refutes wave-1 "cleanup hardened+tested" prose. | scaffolder `index.ts:1635-1638,1430-1433` | Track whether this run created `targetDir`; `rmSync` in the catch only if freshly created. |
| SCAF-N5 | **FIXED** (2026-06-16) | Med | confirms #5 | 3/3 | **`template/scripts/generateAiCapabilities.mjs` drifted from root — missing the `Tests` column + `hasTestFile`** → scaffolded `docs/AI_CAPABILITIES.md` never shows the per-route Tests indicator Rule 12 tells the AI to read. | scaffolder `template/scripts/generateAiCapabilities.mjs` | Re-sync + `KEEP IN SYNC` marker + a `checkTemplateSync.mjs` CI `cmp` guard. |
| SCAF-N6 | **FIXED** (2026-06-16) | Med | — | 2/3 | **`ls-np/` is a 115-file committed scaffold-output artifact carrying STALE overlay files** the live `template/` has dropped — repo bloat + a second canonical-looking template surface that drifts. | scaffolder `create-luckystack-app/ls-np/` | Delete `ls-np/` + `.gitignore`; regenerate in CI into a temp dir if needed. |
| SCAF-N7 | **FIXED** (2026-06-16) | Med | — | 1/3 | **README/next-steps tell SQL users to run `npm run prisma:migrate:dev` — a script that does not exist** → 3 of 4 DB choices get a broken first command. | scaffolder `index.ts:1612`; `template/package.json:28-29` | Add `"prisma:migrate:dev"` to template `package.json`. |
| SCAF-N8 | **FIXED** (2026-06-16) | Med | — | 1/3 | **CI calls `npm run test:e2e` (undefined) gated on `@vercel-labs/agent-browser` (never installed)** — guard always false; if it passed it would crash on the missing script. | scaffolder `template/.github/workflows/ci.yml:55-56` | Add `"test:e2e":"playwright test"`; fix the CI guard to a real marker. |
| SCAF-N9 | **FIXED** (2026-06-16) | Med | — | 1/3 | **Overlay-doc twin drift: docs point at `luckystack/login/oauthProviders.ts` + `userAdapter.ts` no longer in `template/`** (wiring moved into `@luckystack/login/register`). | scaffolder `template/_dot_env_dot_local_template:26`; `README.md:94-95`; `prisma/schema.prisma:3` | Update breadcrumbs to the `/register` self-wiring path. |
| ROOTSRC-O7 | **FIXED** (2026-06-16) | Med | 1/6 | 2/3 | **Email-change confirmation auto-consumed on page mount** (one-shot token fired in `useEffect`, no click) — link-prefetchers/AV/webmail preview burn the token before the human clicks. | root-src `settings/confirm-email/page.tsx:19-48` | Require an explicit confirm-button click before calling the API. |
| ROOTSRC-O9 | **FIXED** (2026-06-16) | Med (refactor / AI-drivability) | 5/6 | 3/3 | **`WideSessionResponse` hand-cast on the bootstrap path** — generated `system/session` type models only `status:'success'`, forcing a Rule-21-forbidden cast at the one endpoint every consumer touches first. Same root as H22. | root-src `SessionProvider.tsx:89-95` | Generator-side: union every response with `{status:'error';errorCode:string}`. |
| ROOTSRC-O13 | **FIXED** (2026-06-16) | Med | — | 1/3 | **`page.tsx` hard 1000ms timeout bounces a still-loading valid session to `/login`** while `SessionProvider` is still retrying (backoff up to ~7.5s) → spurious login redirect. | root-src `page.tsx:28-37` vs `SessionProvider.tsx:82-110` | Remove/raise the timeout past the backoff budget, or key off `sessionLoaded`. |
| ROOTSRC-O15 | **FIXED** (2026-06-16) | Med (wave-1) / Low (wave-2) | 1/6 | 3/3 | **Hardcoded English bypasses `useTranslator` (Rule 13)** across LoginForm/reset-password/Navbar, AND `main.tsx` `ErrorFallback` MISUSES `common['404']` as the title for a generic crash. | root-src `LoginForm.tsx`, `reset-password/page.tsx`, `Navbar.tsx`, `main.tsx:254-257` | Route through `translate({key})`; use a generic error key in `ErrorFallback`. |
| PRESENCE-3 | **FIXED** (2026-06-16) | Med | — | 1/3 | **`getRoomPresence` walks cross-instance RemoteSockets but reads a local-only `lastActivityBySocket` Map** → remote peers always `afk:false`/`lastActivity:undefined`; doc sells it as "spans instances". | presence `activitySampler.ts:19,82-89` | Store last-activity in Redis+TTL, OR return tri-state `afk:'unknown'` for remote sockets + fix the comment. |
| SERVER-GOD | **FIXED** (2026-06-16) | Med (refactor) | 5/6 | 3/3 | **`loadSocket` ~430-line god-function** (12 inline socket handlers; join/leave near-duplicate blocks; validate-before-join buried inline). | server `loadSocket.ts:82-434` | Extract per-event registrars; collapse join/leave; make validate-before-join a named unit-tested fn. |

## LOW (grouped compactly by area)

> **SWEPT 2026-06-16** — the LOW backlog was worked end-to-end across all areas (api, core, sync, server, root-server, error-tracking, router, devkit, mcp, docs-ui, test-runner, cli, presence, scaffolder/SCAF-K, email, root-src). Each item was verify-then-fixed, confirmed already-fixed, marked false-positive, or (for the structural god-functions) decomposed in the refactor pass. The inline `OPEN/NEW` markers below are the pre-sweep state and were NOT individually flipped (grouped ranges); the authoritative record of what was fixed/already-fixed/false-positive/decomposed per item is the branch-log entry `branch-logs/chore--package-split-prep.md` (2026-06-16 roadmap-completion campaign). Tree after the sweep: `npm run lint` + `lint:packages` + `ai:lint` clean, `build` 16/16, `test:unit` 1281 passed.

> Each item: STATUS · W1/W2 · location · one-line fix. NEW = wave-2-only & verified.

### api
- **API-O12** OPEN 5/6·1/3 — mutable closure refs read by `emitApiError` before assignment (`handleApiRequest.ts:410-428`). Pass formatter/route explicitly.
- **API-O13** OPEN 2/6·1/3 — `rateLimitExceeded` `scope` mislabeled (anon IP-keyed reported `'route'`, token-keyed `'user'`). Map anon→`'ip'`.
- **API-O14** OPEN 3/6·2/3 — file-level `eslint-disable` (socket = whole file). Line-level only.
- **API-O15/O16/O17** OPEN 1-2/6 — engine.io backpressure untyped no-op-on-rename; success envelope spreads raw result (passwordHash leak risk); returned-error `message` flows verbatim while thrown is redacted. Dev-warn / allowlist / document.
- **API-O18/O19** OPEN/NEW — dead `@luckystack/login` mock (`handleHttpApiRequest.test.ts:57`); socket-only paths untested. Delete mock; add cases.

### core
- **CORE-O11** OPEN — single-member union/intersection never recursed (`:202-222`). Recurse `parts[0]`.
- **CORE-O17** OPEN (doc) 3/6 — `escapeHtml` JSDoc oversells. Tighten.
- **CORE-O18** OPEN 1/6·1/3 — redis stray-prefix net excludes `eval`/`scan`/`multi`/… (by-design). Document / dev-warn.
- **CORE-O19** OPEN 2/6·3/3 — `clientHookBus`/`apiInterceptors` raw `try/catch`+`console.error`. Route through `getLogger()` / `// luckystack-allow`.
- **CORE-O20/O21** OPEN — `waitForSocket` busy-poll + unbounded `responseIndex`; `getCookieValue` returns raw on decode throw (by-design). One-shot pub/sub; optional null.
- **CORE-N4** NEW — `parseRecordParts` `null` → value-validation skipped (narrow fail-open). Fail closed.
- **CORE-N5** NEW — `deepMerge` proto guard drops legit `constructor`/`prototype` config keys. Skip only `__proto__`.
- **CORE-N6** NEW — `checkRateLimitInMemory/InRedis` hardcode `windowMs=60_000` shadowing config. Drop the default.
- **CORE-N7** NEW — `getParams` declared-Content-Length 413 returns without draining (marginal slow-loris). `req.destroy()`.
- **CORE-N8** NEW — offline-queue eviction calls `onDrop` + un-`tryCatch`'d `queueItemDropped` synchronously. Wrap / defer.
- **CORE-N9/N10/N11** NEW (latent) — `Reflect.get` proxy-as-receiver private-field break; `dispatchHook` catch un-`tryCatch`'d logger/tracker; `checkOrigin` fails on empty bind port. Pass `real`; wrap; host-only fallback.

### sync
- **SYNC-O10..O16, N5..N8** OPEN/NEW — `ignoreSelf` no-op for anon; HTTP rate-limit identity collapses anon + diverges loopback; HTTP envelope spreads reserved keys + diverges shape; `collectRoomSocketsForPressure` local-only; raw token into `_client`; offline-queue id `Date.now()+random` (no action); `runRequest` 4-cleanup; `thresholdBytes` byte-named-but-packets; redundant disconnect handling; `attachSyncReceiver` forwards reserved keys; stream hooks on no-op emit. Fold into SYNC-O5/O7 + per-line.

### server / root-server
- **SERVER-O9..O17 (incl. NEW O11)** OPEN/NEW — `pingPrisma` eslint-suppressed cast; default `X-XSS-Protection` legacy value; Socket.io handshake accepts origin-less (NEW); `/assets/*` `includes` slice; `testReset` `new URL` from `Host`; `serveWithRewrittenUrl` mutates shared `req.url`; dead `||'/'`; login-absent CSRF unsigned echo; unauth `/_health`+`/readyz` amplification. Per-line.
- **RS-09/RS-04/ROOTSERVER-01 (NEW), RS-3/5/10..24/RS-06 (NEW)** OPEN/NEW — stale `generatedApis.*`; denylist `res.end` no `writeHead`→HTTP 200; two committed `console.log(filePath)` on the prod hot path; `startsWith(rootFolder)` no trailing sep (latent); dev REPL logs full sessions incl. tokens; `nosniff` only on 200; sync `readFileSync` in async loop; `loadProdMaps` swallows errors; build-fallback HTTP 200 debug string; `config.ts` captures `EMAIL_FROM`/`DNS`/`EXTERNAL_ORIGINS` before secret resolution; blanket eslint-disable; buffers whole file; SVG inline no CSP; tribal boot order; re-export breadcrumbs. Per-line / docs.

### login
- **run4-5..8, run1, F19/F20/F25/F36/F39, run3-06, LOGIN-04(run1)/05(run1)** OPEN/NEW — legacy `sessionKeyFor`/`activeUsersKeyFor` drive raw-Redis routes; reset/email tokens UN-hashed; `signOutEverywhere` revokes own session; `confirmEmailChange` non-atomic + provider-scoped; `deleteAccount` raw `redis.del` before adapter delete; bcrypt 72-byte truncation not enforced; raw email to `loginFailed`; duplicated `consume*` blocks; no OIDC nonce/code-binding; MS `getAvatar` unbounded base64 into session; `consume*` don't assert DEL leg; `forgotPassword` logs raw email+`found`. Per-route + a shared `oneTimeToken` core primitive.
- **login god-files** OPEN 6/6·3/3 — `login.ts` (~1077, GREW), `saveSession`/`loginCallback`. Continue extraction. (The duplicated `allowMultiple` BC shim is gone — `session.perUser` is now the sole key.)

### error-tracking
- **ET-O7..O13, N3/N4/N5** OPEN/NEW — bare `catch {}` on setUser/recordMetric/flush; `beforeSend` no-`forwarded` silent drop; `startSpanHandle` per-call event (zero prod callers); `SpanResult<T>=T` no-op; shared-DI casts; Sentry/Datadog no `flush()`; CLAUDE.md `postLogin` claim (only `postLogout`); Datadog span-before-counter; mixed-mode double-capture. Per-item.

### router / devkit / mcp / docs-ui / test-runner / cli / presence / scaffolder / email
- **ROUTER-O5..O21** OPEN/NEW — `setLocalHealth` dead write; probe counts `<500` healthy; `Number(port)` unguarded; raw `console.error`; raw `try/catch`; `disconnect()` vs `quit()`; TOCTOU; WS post-101 listener accumulation; `parseServiceFromPath` no URL-decode; `healthChannel` recomputed; dynamic `Connection`-token strip; god-functions; resolver singleton; no proxy-parity test. Framework refactors + per-line.
- **DEVKIT-O3..O7, O9, O12..O21** OPEN/NEW — orphaned shadow `emitter.ts`; dashboard template tests absolute path; ESM cachebust leak; symlink walk no cycle guard; stale-Program→`{ }`; server-inject `$`-splice (O9); loader walks `node_modules`; regex import misses multiline; `calculateRelativePath` fallback; Zod no `.strict()`/intersections→`z.any()`; prod supervisor no signals; non-literal `login` silent-public; `getSourceFile` miss silent; blanket eslint-disable; 1500ms grace; god-functions/dup utils. AST migration + diagnostics + refactor.
- **MCP-3..12** OPEN/NEW — basename collision → bare `null`; no referential-integrity check; `god_nodes` schema `max(100)` vs cap 25; lexical containment (no realpath); `get_decision` slug substring; no staleness signal; `projectRoot` string memo; boot no `process.exit`; library-import boot side-effect (by-design); `who_calls` broad match. Low/by-design.
- **DOCSUI-5/6, 9..15** OPEN/NEW — try-it-out URL omits `api/` prefix; non-array `meta.tags` `.map` crash; raw `brandColor`/`fontFamily` into `<style>`; raw method as CSS class; `logoUrl` no scheme allow-list; legacy bare-map no `schemaVersion`; per-keystroke re-render/re-bind; non-JSON error mislabel; three `escapeHtml` copies. Most resolved by DOCSUI-1/3 rewire.
- **TR-9..21** OPEN/NEW — `csrfEnforcementCheck` no method guard; `walkEndpoints` casts any method; `computeTotals` ignores skipped/xpassed for exit; `apiMetaMap` no conformance guard; duplicated probe scaffolding; hand-maintained totals/reporter; `customTests` skips empty stubs silently; `as never` cast; fuzz `__proto__` inert (serializes to `{}`); `JUNK_PAYLOADS` mutable; `session.login` no Prisma row; auth-layer hinges on check-ordering; rate-limit model assumption. Per-item + single-source `LAYER_KEYS`.
- **CLI-O2** OPEN 1/6·2/3 — null/empty-hash credentials account deletable with `DELETE` alone (inverse of DELETE-H). Gate on `provider==='credentials'`.
- **CLI-O3** OPEN — `runNpmInstall` always npm → competing lockfile in pnpm/yarn/bun. Sniff lockfile / `packageManager`.
- **CLI-O4** OPEN 4/6·3/3 — asset↔template `updateUser_v1` divergence exempted (enum blocks the named exploit; residual = drift + unconstrained `name`). Port validation; empty the exemption.
- **CLI-O5..O13** OPEN/NEW — untyped `Record` patch; `.trim()` no `typeof` guard; `parseEnvKeys` close-quote edge; `isTranslationKey` rejects `_`/`-` in first segment; `LoginForm` hardcoded English + `common.404` + magic delay; `LANGUAGES` hardcoded; `FRAMEWORK_ENV_KEYS` no parity test; `addPresence` `replaceAll`. Mirror-to-template + per-item.
- **PRESENCE-2..14** OPEN/NEW — docstring/`ARCHITECTURE_MULTI_TENANCY.md` dead pointer; per-socket AFK double-fire; per-token AFK rollup missing (false-AFK multi-tab); multi-tab guard raw-token room; `socketLeaveRoom` misnomer+dead params; `intentionalDisconnect` userAfk not self-gated; departure no pre/post hook; stale dedup doc; `LocationProvider` stale `search`; shadow-API roster surfaces; multi-tab second-disconnect window; sampler runs with AFK disabled. Per-item; #6 keyed to PRESENCE-1.
- **EMAIL-O5..O9, N1..N3** OPEN/NEW — unsalted recipient hash (enumeration oracle); `reportSendOutcome` logs unredacted recipient+subject to the registered logger; `ConsoleSender` prints token-bearing body; no send timeout; no CRLF defense; non-numeric `SMTP_PORT`→NaN; zero/negative `ttlMinutes` + empty CTA; unguarded `template.render`. Per-item; several DEFERRED.
- **ROOTSRC-O4..O18** OPEN/NEW — `LoginForm` gates on `!response.status` not `=== true`; `updateUser_v1` omits `nameMaxLength`; `emailTaken` oracle (DEFERRED); tokens in query string; avatar `?v=` on uploaded data-URI; attacker-string `.trim()` no guard; `updatePreferences` overwrites whole JSON column (NEW); permanent-disabled submit; non-token Tailwind colors; god-component `settings/page.tsx`; god-effect `socketInitializer`. Per-item.
- **SCAF-K1..K21** OPEN/NEW — `pruneOptionalPackages`/`main()` god-functions + per-call lookup tables; unguarded `JSON.parse` ×5; `@latest` MCP pins; `set -e` pre-commit hard-blocks first commit; `copyTree` follows symlinks; i18n boolean asymmetry; repeated pkg.json rewrites; `pickFromList` silent default on bad piped answer; `editScaffoldFile` silent no-op; zero mutator unit coverage; 5 dead template vars (CLAUDE.md doc lie); Prisma `LANGUAGE` enum untouched by `--no-i18n`; template `text-white` (Rule 14) flagged by its own `ai:lint`; `npm ci` with no shipped lockfile; `.env` gitignored vs "commit this file" header. Structural fix = `checkTemplateSync` CI + mutator integration tests.

---

# DEFERRED-DECISION (real, pending policy / ADR / owner call)

> **RESOLVED 2026-06-16** — all DEFERRED-DECISION items below were taken to a decision during the roadmap-completion campaign: the safer/secure option was IMPLEMENTED where it was a contained code change (e.g. SYNC-RECEIVER secure defaults, EMAIL-O4 explicit-adapter fail-closed + HMAC recipient hash + CRLF/timeout guards, LOGIN-F5 IP+account composite lockout key, ROUTER body-cap + fail-closed proxyRequestGate, MCP graph staleness signal, ROOTSRC-O6 no-enumeration email-change), or the stance was DOCUMENTED with rationale where it is a genuine policy/ADR with no safe auto-default (e.g. CORE env eager-load, SECRET advanced-key surface, DEVKIT codegen-strictness, TR xpass/stub policies). Per-item outcomes are recorded in the branch-log (2026-06-16 campaign entry). No DD item remains open.

| ID | Sev | W1 | W2 | Finding | Decision needed |
|---|---|---|---|---|---|
| DD-SYNC-RECEIVER | High | 5-6/6 | 2/3 | Default-permissive sync receiver auth (= SYNC-O1 / CORE-D1) — defaults in core, enforcement in sync. | Flip secure-default for 0.2.0 (cross-package ADR) vs boot-warn + commented scaffold config. |
| DD-LOGIN-F5 | Med | 3/6 | 1/3 | Account-lockout as a remote DoS lever (email-only keyed; `accountLocked` re-opens enum) — opt-in, default off. | IP+account composite key / progressive delay vs hard refuse. (LOGIN-03 is the clear-bug amplification.) |
| DD-LOGIN-F17 | Med | 1/6 | 0/3 | Core `updatePasswordHash` doesn't revoke sessions (shipped routes do). | Move revoke into a `completePasswordReset` primitive vs keep as route responsibility. |
| DD-EMAIL-1..4 | **FIXED** (2026-06-16) | 1-5/6 | 2/3 | EMAIL-O4: explicit `adapter` now fails with `no-sender` (no confused-deputy fallthrough); EMAIL-O5: `hashRecipient` upgraded to HMAC-SHA-256 with `emailConfig.recipientHmacKey` (one-time dev warn when key absent, empty-string opt-in to keep un-keyed); EMAIL-O7: `sanitizeMessageHeaders` strips CR/LF from all RFC-5322 header fields before any hook or adapter sees them; EMAIL-O8: `sendTimeoutMs` (default 30 s, `false` to disable) wraps `sender.send()` in a race — returns `{ok:false,reason:'send-timeout'}` on expiry; EMAIL-O9 (`ttlMinutes` + empty CTA): already handled — `num()` in `builtInTemplates.ts` guards zero/negative, `renderEmailLayout` requires both `ctaLabel` + a scheme-valid `ctaUrl`. `adapterHint` internal-vs-public policy: internal — typed as `'transactional'\|'marketing'\|'diagnostics'\|'default'` union on the template-input branch only; no public ADR needed. | |
| DD-SECRET-1..4 | Med-Low | 0-3/6 | 1/3 | Most of `SecretManagerConfig` is shadow-API (only `url`/`token`/`dev` wired); default-allow env-scan warn; `validateToken` Bearer strip-vs-throw; single-resolver-per-process nicety. | Widen `SecretManagerBootConfig` vs document advanced-direct-call-only; documented-policy ADRs. |
| DD-CORE-D2/D3 | Low | 3/6 / — | —/1/3 | `env.ts` `loadDotenv` at module import (public-API change to lazy); `rateLimiting.auth` lockout slot declared but unread by core (login owns it). | Lazy `getEnv()` migration vs keep; confirm login consumes `.auth` + document core no-op. |
| DD-DEVKIT-D1/D2/D3 | Med-Low | #16/#32/— | DK-08/LOW/feat | `getSourceFile`/Program miss → silent `{ }` (output-shape change); Zod no `.strict()` (flips fuzz pass/fail); strict-codegen gate / `apiTypeDiagnostics.generated.json` feature (would catch DEVKIT-O1). | Marker-contract decision; loose-vs-strict codegen policy; build the diagnostics feature. |
| DD-MCP-staleness | Low (feature) | 3/6 | 2/3 | No graph staleness/freshness signal. | Emit `generatedAt` + a `graph_status` tool / mtime-compare suffix. |
| DD-DOCSUI-16/17 | Med→Low / Low | 5/6 / 2/6 | 1/3 / — | `render()` decomposed but not re-architected to event-delegation (bundles into DOCSUI-1 rewire); JSON sub-route `JSON.parse`-validate-on-serve (changes dev-file success/404 policy). | Bundle into the renderCore rewire; serve-validation policy. |
| DD-ROUTER-DD1/DD2 | Med | new/3/3 | 1/3·3/3 | Request-body size cap capability REMOVED (backends still defend); no edge deny-gate on either transport (only observer hooks; CLAUDE.md implies a gate that doesn't exist). | Re-introduce edge body cap vs document delegation; add a real fail-CLOSED `proxyRequestGate` vs document the absence. |
| DD-ROOTSRC-O6/O8 | Low | 4/6 / 1/6 | 3/3 / 1/3 | `requestEmailChange` `emailTaken` authenticated enumeration oracle (in-code tradeoff, no ADR); tokens in URL query string (standard email-link pattern; `auth.tokenLinkMode` would close it). | Write the ADR or match the reset flow; build the fragment/`?code=` token-link helper. |
| DD-RS-19 | info | 1/6 | confirms-prior | Secret-manager seam deliberately fail-OPEN on package absence (confirmed intentional, user memory). | No code change; add a one-line security-docs note so a future reviewer doesn't "fix" it. |
| DD-CLI-D1/D2/D3 | Low | 1/6 | 1-3/3 | Empty `name` green no-op write-skip; `addDependency` re-serializes pkg.json to 2-space; inconsistent `.catch(()=>null)` vs bare-await. | Fold into CLI-O4 single-source; indent-preservation; bundle-convention pass. |
| DD-TR-various | Low | 1-6/6 | 1-2/3 | `computeTotals` xpass exit-code strictness; `customTests` empty-stub `skipped` variant (public union change); duplicated probe scaffolding; hand-synced totals; leaky summary union. | Strict-mode policy; contract change to `CustomTestResult`; refactors. |

---

# FIXED (verified resolved in current tree — compact, done)

> Re-confirmed by opening the cited file at the cited line in the current tree. Two CRITICALs + the wave-1 flagship HIGHs are here.

**CRITICAL — both wave-1 ship-blockers resolved-in-tree:**
- **C1 — Router WS-proxy listener-less crash (6/6·3/3).** `onClientGone` attached to `'error'`+`'close'` at the top of the handler before any IO; `wsProxy.test.ts` regression. `wsProxy.ts:59-64`.
- **C2 — `dist/server.js` source disclosure (4/6·3/3).** `SERVE_DENYLIST_REGEX = /(^\/server\.js$)|(\.map$)/` 404s before any `serveFile`; sourcemaps default OFF. `staticRoutes.ts:14,43-47`. *Caveat: guards uncommitted; bundle still emitted into `dist/` — the allow-by-default model is OPEN as server #2.*

**HIGH — resolved (one-liners):** WS-proxy SSRF/host-pin guard (`proxyUtils.ts`); WS/HTTP in-flight upstream leak, non-101 leak, req/res abort listeners (router F3-F6); client XFF/x-real-ip strip + proto normalize (router F7-F8); **ET-02 per-request ALS identity bound in prod** + Datadog reads ALS (error-tracking F1-F2, the wave-1 6/6 flagship); **apiRequest offline-queue `onDrop` hang** (core F1); **validateType fail-OPEN → fail-CLOSED** + Record-value/proto-key/depth guard (core F2-F5); auth throttle honors `trustProxy` + `resolveRequesterIp` extracted (server F1-F2); **HTTP `requireRoomMembership` bypass fail-closed** + client-only-route rejection + validation-mode/array-guard fail-closed (sync F1-F5); **OAuth unverified-email cross-provider link fail-closed** + MS UPN gated + backslash-redirect rejected + accurate `revokeUserSessions` count (login F1,F11-F13); **root `?token=` query adoption removed** + Sentry replay masking ON + ErrorPage DEV-gated (root-src F1-F3); presence whole HIGH async/unhandled-rejection cluster + snapshot formatter (presence F1-F14); devkit root-sync route-key `'system'` unified + supervisor `'error'` listener + 30s blocking hook removed + codegen identifier validator re-promoted to throw (devkit F1-F5); **test-runner `reauthenticate` self-delete path gone** (test-runner #22); CLI `ActiveSession` token/handle build-breaker + PM-substring + JSON.parse guard + install ordering (cli F1-F3); mcp prototype-chain key lookup + `get_decision` numeric padding + path-containment guard (mcp 13-20); docs-ui try-it-out method threading (docs-ui F1).

**MEDIUM/LOW — resolved (areas):** secret-manager — the entire wave-1 Medium/Low cohort (F1-F11, 11 items, each with a regression test); core — `sanitizeForLog` depth/cycle guard, `getCookieValue` ReDoS, `deepMerge` proto guard, redis stray-prefix `del`/`mget`, `canonicalizeIp`; server — SSE error/abort (sync side), `withSessionLock` defuse, requestId-at-top, HEAD CSRF parity, sse-init assertion; email — ResendSender floating-promise, `sendEmail` decomposition, shared `toProviderPayload`, password-reset doc; login — ~18 items incl. session sanitizer fail-closed, framework-key clobber guard, nonce timing-safe, encodeURIComponent avatars, empty-id session guard; cli — 17 items (O(n²) scan, multiline env, `--ci` divergence, `checkI18n` decomposition); test-runner — 14 items (schema-sample walker, parse tryCatch+timeout, symlink-loop guard, stream-watcher leak, god-fn decomposition); root-server — `/server.js` deny-list, OAuth nonce timing-safe, `generatedApis.*` gitignored, dead `functions.*.default` removed; root-src — `common.404` typo, `updateSession` raw-parse, avatar double-`?v=`, playground unauth stream + arbitrary-recipient email, `MiddlewareResult` union; devkit/presence/mcp/docs-ui per-area FIXED tables (god-fn decompositions, dead-export removals, charset, auth-element guards).

---

# FALSE-POSITIVE (compact — one-line reason each is bogus)

| ID | Sev claimed | Area | Why bogus |
|---|---|---|---|
| FP-EMAIL-phantom (FP1-FP8) | Med-Low | email | Wave-1 audited a HALLUCINATED `sendEmail.ts`/`console.ts` — `sanitizeHeaderMap`/`stripCrlf`/`withSendTimeout`/`redactUrlSecrets`/`safeColor`/`safeCtaUrl`/`dispatchEarlyDrop` NEVER existed in git history (genuine residuals re-tracked as EMAIL-O3/O7/O8/O9). |
| FP-ROUTER-gate (FP1-FP2) | High/Med | router | `proxyRequestGate`/`preWsUpgrade` deny-gate the baseline assumed NEVER shipped (grep=0); the real gap is DD-ROUTER-DD2, not a bug. |
| FP-SERVER-formatter | Med | server | "`registerErrorFormatter` is a shadow API" — stale-scope read; the registry relocated to core and `applyErrorFormatter` IS called in api/sync handlers. |
| FP-LOGIN-unified/handle | High | login | "unified links unverified OAuth email" + "stale `ActiveSession` build break" — both verified FIXED in current code. |
| FP-CORE (FP2-FP6) | High-Low | core | Socket-no-method = HTTP-semantics control not an authz boundary; `getCookieValue` lenient decode by-design; redis stray-prefix net by-design; avatar oracle/crash + `acquireLease` ambiguity by-design/already-fixed. |
| FP-DEVKIT (FP1-FP2) | Low | devkit | `validateDeploy` "missing ESC byte" — `od -c` shows 0x1b present (Read doesn't render it); validateType "fails OPEN at resolver" — returns `__RUNTIME_UNRESOLVED__::`, core fails CLOSED. |
| FP-MCP (21-22) | Low | mcp | "shebang on a pure library entry" — single emitted file IS the executable; "200-line factory" — no such factory (flat top-level registration). |
| FP-DOCSUI (X1-X7) | various | docs-ui | CSP-nonce mismatch / `ensureCsrfToken` race / `enabledInProd` boot-warn / base64url / pageSet overcount / `meta.tags` `[object Object]` — every cited CONSTRUCT was removed (grep=0) or is XSS-safe coercion. |
| FP-ROOTSERVER-RS-07 | High | root-server | Sentry replay PII leak — `sentry.ts:60-64` sets `maskAllText:true`+`blockAllMedia:true`+token-stripping `beforeSend`; the chain doesn't exist. |
| FP-ROOTSRC (FP1-FP3) | Med-Low | root-src | `signOutEverywhere` "logs caller out" (already passes `exceptToken`); Sentry-masking carried "pending recheck" (file masks); offline-queue hang attributed to root-src (shims are pure re-exports — lives in core). |
| FP-CLI (FP1-FP3) | High-Med | cli | "arbitrary theme/language persist" + "`LANGUAGE_RE` poisoning" + "avatar decompression bomb" — theme/language are Prisma enums (throws pre-write), the regex affects only the attacker's own session, the avatar policy lives in core `processUpload`. |
| FP-SCAF-FP1 | High | scaffolder | "stale `ActiveSession` interface" — current template matches `listSessions_v1` return shape exactly; no drift. |
| FP-SECRET (FP1-FP2, W3-W5) | n/a-Low | secret-manager | fail-OPEN on package-absent confirmed-deliberate; resolve-response validation fails CLOSED (opposite of the validateType theme); `statusText`/`parseEnvFile`/sync token-read documented by-design. |
| FP-TR (36-39) | High-Low | test-runner | CSRF-layer real-mutation hazard (moot — unwired); flaky-reset wall-of-skips (orchestrator never sets `resetBetweenEndpoints`); session-leak try/finally premise (restructured — survives as TR-8); cookie `split('=')` (code gone). |

---

## Cross-cutting program notes (systemic fixes that retire whole rows at once)

1. **Transport/template-twin parity gates** — collapse api/sync/proxy twins onto shared `_shared/*Stage` pipelines + an `ai:lint` behavioral-parity invariant; add `checkTemplateSync.mjs` (byte-`cmp` over every KEEP-IN-SYNC pair) + `ai:check-template-drift` (asset↔template↔`src/`). Retires H-TWIN, SRV-O1, API-O8, SYNC-O7, DEVKIT-O8, SCAF-N5, ROOTSRC-O2/O3, and the cli asset/template drift class.
2. **Shadow-API/shadow-config wiring check** — an `ai:lint`/`ai:capabilities` rule: every hook/config/export named in a package CLAUDE.md or registry must have a non-test, non-barrel call-site. Retires API-O1, SYNC-N1, ET-N1/N2, TR-1/TR-3, EMAIL-O1, SECRET-D1, DOCSUI-3, presence/secret-manager shadow surfaces.
3. **Global crash backstop + stream-lifecycle helper** — one `process.on('unhandledRejection')` + a shared `bindSseStreamLifecycle`/`stream.pipeline` everywhere. Retires RS-01, CORE-O1/O2, SRV-O1, and the detached-async-callback class.
4. **Generated-type fidelity** — union the `{status:'error';errorCode}` envelope into every generated return type + loud-failure markers in extractors + a pack→install→tsc CI smoke-test. Retires H22, ROOTSRC-O9, DEVKIT-O1/O2, and the Rule-21 cast pressure.
5. **Security-defaults visibility** — make the fail-OPEN/default-insecure set opt-in or boot-warn + an `AI_SECURITY_DEFAULTS.md` index signal. Retires the SYNC-O1, CORE-O6, H17, CORE-O10, DOCSUI-7 posture cluster from the "invisible to the AI" failure mode.
