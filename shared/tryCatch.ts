//? Default-re-export of the canonical `tryCatch` from @luckystack/core.
//? See `shared/sleep.ts` for the rationale behind the `as default` alias.
export { tryCatch as default } from '@luckystack/core';
