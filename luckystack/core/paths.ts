//? Override the framework's default file layout. Empty here = use defaults
//? (`src/`, `server/`, `shared/`, `uploads/`, `public/`, `server/functions/`).
//?
//? Example: if your project keeps source under `app/src` and uploads under
//? `storage/uploads`, register the overrides like this:
//?
//?   import { registerProjectConfig } from '@luckystack/core';
//?   registerProjectConfig({
//?     paths: {
//?       srcDir: 'app/src',
//?       uploadsDir: 'storage/uploads',
//?     },
//?   });

export {};
